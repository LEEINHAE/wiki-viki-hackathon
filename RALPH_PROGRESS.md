# P0 진행 기록

## 현재 재개 작업 — 반복 57 (2026-09-22)

- **W2 완료**: 사용자가 실제 실패 XLSX와 오류 화면을 제공하여 재현 자료 대기가 해소됐다. 첨부 파일 자체는 수정하거나 저장소에 복사하지 않았다.
- 이번 완료 단위: 해당 파일의 업로드 실패 원인 재현 → 최소 수정 → 같은 파일의 AI 생성·격리 DB 저장·브라우저 성공 확인. 기존 전송 검사·32개 제한·원자적 저장·모달 입력 보존을 유지한다.
- 시작 상태: git 작업 트리 깨끗함, 적용 AGENTS 없음. 변경 전 기본 test **136 통과·DB 22 생략**, check **오류 0·경고 0**, lint/build 통과. 기존 실패 없음.
- 첨부 파일은 15,402바이트, 1개 시트·42개 데이터 행·4열이며 현재 파서가 3,066자로 추출하고 로컬 검사를 통과한다. 이후 실제 AI 호출을 단계별로 계측한다. 원문·키·연결 정보는 로그에 출력하지 않는다. 증거: `/tmp/wiki-xlsx-case-20260922/`.
- 실제 원인 재현: 의미 검사 통과 후 `gpt-5-mini` 주제 계획 63.035초, 본문 4개 묶음 중 3개 완료·마지막 요청이 남은 26.963초에 취소됐다. 계획과 본문이 공유하는 90초 제한 때문에 SDK `APIUserAbortError`가 발생했고 기존 API는 이를 일반 500으로 숨겼다. 파일 파싱 문제나 콘텐츠 차단이 아니다.
- 수정: 위키파이어의 `gpt-5-mini` 및 날짜 버전에만 주제 계획·본문 생성 `reasoning.effort: low`를 명시한다. 다른 모델·콘텐츠 검사·32개 제한·90초 제한·일괄 저장은 유지한다. SDK 시간 초과는 HTTP 504와 저장하지 않았다는 재시도 안내로 구분한다. [OpenAI reasoning 문서](https://developers.openai.com/api/docs/guides/reasoning)와 설치 SDK 지원을 확인하고 실제 모델 호출로 검증했다.
- 회귀 재현: 변경 전 새 단위 검사 **4 실패·1 통과**(기본/명시/날짜 모델의 생성 설정, 시간 초과 500 대신 504 기대). 수정 후 관련 단위와 실제 PostgreSQL 검사 **50 통과·생략 0**. 기존 문서·초안 스냅샷 보존, 세 묶음 완료 뒤 마지막 묶음 취소 시 새 초안 0, 동일 XLSX 재시도 시 32개 저장을 확인했다.
- 실제 외부 AI 검증: 같은 원본 파일의 생성이 수정 후 **32개 성공**(계획 34.218초 + 최장 본문 15.202초). 별도 실제 Chrome 업로드도 **201·32개 초안·48.587초**에 완료하고 격리 DB의 제목/개수/검사 상태와 일치했다. 모바일 라이트·데스크톱 다크 화면을 직접 확인하고 마지막 초안으로 키보드 이동해 저장 본문과 같음을 검증했다. 페이지 오류 0. 키 없는 같은 파일은 외부 호출 추가 없이 원문을 보존한 기본 초안 1개와 검사 생략을 기록했다. 운영 DB·배포에는 접근하지 않았다.
- 주요 파일: `src/lib/server/openai.js`, `src/routes/api/wikify/+server.js`, `tests/wikify-governance.test.js`, `tests/wikify-timeout-db.test.js`, `tests/browser/xlsx-upload.mjs`, README.
- 최종 전체 `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 URL> bun run test`: **665 통과·실패 0·생략 0**. 기본 `bun run test`: **141 통과·DB 23 생략**. `bun run check`: **오류 0·경고 0**, `bun run lint`·`bun run build`·`git diff --check` 통과. 변경 전 통과하던 검사에 새 회귀 없음 (`final-*.log`).
- `node tests/browser/xlsx-upload.mjs`: **13개 묶음 통과**, 360/768/1440px × 라이트/다크의 빈 상태·로딩·정상·오류·모달 재열기·키보드·32개 결과 탐색, 새 504 안내/선택 파일 보존과 재시도 검증. 이 자동 브라우저 회귀는 AI 모의 응답이며 외부 HTTP 0·페이지 오류 0이다. 앞의 `live-results.json`은 첨부 원본과 실제 외부 AI·실제 DB·실제 브라우저 검증으로 별도 구분한다.
- **남은 필수 검증·다음 반복 작업 없음**: 마지막으로 남았던 특정 Excel 사례도 해결되어 이번 대상인 P0 및 W2를 완료했다. GOAL 체크박스와 본문은 이번에 변경하지 않았다. 운영 배포·원격 DB 변경·Node.js 22에서의 별도 실행은 수행하지 않았으며, 검증은 로컬 Node.js 26과 기존 Vercel Node.js 22 대상 빌드에서 수행했다. P1/P2는 확장하지 않는다.
- 최종 문서 반영 후 lint·diff 검사도 통과했다. 격리 테스트 스키마 잔여 0개를 확인하고 이번에 시작한 PostgreSQL 및 Vite/Chrome을 모두 종료했다.

## 범위와 시작 상태

- 기준: `GOAL.md` §12 P0 및 관련 §6, §7, §8, §11.3, §13. P1/P2는 이번 실행 범위 밖이다.
- 적용되는 `AGENTS.md`와 기존 진행 기록은 없었다. 시작 시 사용자 변경은 `GOAL.md`뿐이며 본문과 체크박스를 보존한다.
- 문서의 완료 설명과 코드가 다르다. `drafts/+page.server.js`는 게시·별칭·상태를 개별 저장하고, `wiki.js`는 슬러그 충돌 시 덮어쓴다. AI 통합·버전 보호 구현 및 `RUN_MERGE_DB_TESTS` 통합 테스트는 현행 파일에서 확인하지 못했다. 기존 완료 표시를 검증 증거로 사용하지 않는다.
- 우선순위: 사용자 지정 보안·외부 전송 → 원자성·충돌 → 복구 → 입력 보존 → 공통 UI. 첫 작업은 원문 검사 실패 후에도 `structureDocuments`가 실행되는 업로드 경로를 차단한다.

## 검증 가능한 작업 목록

| 작업                                                                                                | 연결된 완료 기준            | 상태 | 완료에 필요한 증거                                                                   |
| --------------------------------------------------------------------------------------------------- | --------------------------- | ---- | ------------------------------------------------------------------------------------ |
| S1. 업로드 원문·파일명 검사 실패 시 외부 호출 및 저장 중단, 키 없는 기본 초안과 검사 생략 표시 유지 | P0 전송 차단, §11.3         | 완료 | 정규식 차단 시 AI 0회, 의미 검사 실패 시 생성 0회, 오류·정상·키 없는 흐름 회귀 검사  |
| S2. 검색어·근거 문서에 공통 전송 검사 적용 및 실패/키 없음 회귀 확인                                | P0 전송 차단, §11.3         | 완료 | 민감 검색어·근거 제외 시 실제 전송 입력 검증, 일반 검색 유지                         |
| S3a. 초안 수정·게시의 제목·본문·별칭 검사와 수정 흐름                                               | P0 검사 일관성, §11.3       | 완료 | 외부 전송 차단, 별칭 수정, 실제 DB의 blocked/review 상태·게시 금지/허용              |
| S3b. 직접 편집의 제목·본문·별칭·편집 요약 검사와 저장 실패 입력 보존                                | P0 검사 일관성, §11.3       | 완료 | 실제 DB의 차단 전후 데이터 동일성, 외부 전송 차단, 생략 안내·브라우저 입력 보존      |
| S3c. 되돌리기의 검사 대상·차단/생략 처리 통일                                                       | P0 검사 일관성, §11.3       | 완료 | 실제 DB에서 리비전 소속·차단 시 문서/이력 보존, 안전한 되돌리기·생략 안내            |
| S3d. 토론의 노출 필드 검사와 실패 입력 보존                                                         | P0 검사 일관성, §11.3       | 완료 | 현행 토론 등록의 전송 차단·거부/생략 안내·데이터 및 입력 보존                        |
| S4. 시드 파일 처리의 외부 전송 전 공통 검사                                                         | P0 전송 차단, §11.3         | 완료 | 차단 원문/파일명 전송 0회, 키 없는 기본 초안·검사 생략, 파일별 실패 격리             |
| S5. 사용자 요청에 따른 일반 업무 자료 검사 완화                                                     | 사용자 우선 지시·§6 수정    | 완료 | 연락처/인사/대외비 허용, 고위험 인증/식별값 차단, 업로드→저장→게시                   |
| W1. 파일 하나의 독립적인 의미·업무 절차마다 별도 초안 생성                                          | 사용자 우선 지시·§4         | 완료 | 주제 계획과 생성 결과 일치, 복수 초안 연결·일괄 저장·개별 검토/게시, 실패 원자성     |
| D0. 기본 시드 재실행의 기존 문서·이력 보존                                                          | P0 데이터 보존 원칙         | 완료 | 수정한 기본 문서·별칭·이력 유지, 재실행·중간 실패·동시 실행의 실제 DB 검증           |
| D0b. 일반 데모 시드의 기존 활성 문서·별칭 보존                                                      | P0 데이터 보존 원칙         | 완료 | 실제 CLI의 재실행·충돌·중간 실패·동시 실행에서 사용자 편집과 관계 유지               |
| D1. 초안 새 게시의 원자성·중복/별칭 충돌·버전 보호                                                  | P0 게시 보호 및 보존 기준   | 완료 | 격리 PostgreSQL에서 동시 게시·재시도·중간 실패 롤백                                  |
| D2. 문서에 명시된 AI 통합 기능과 버전/상충 이력의 실제 상태 확인 및 미충족 기능 보완                | P0 기존 통합 보존           | 완료 | 후보→생성→검토→적용, 상충 이력·DB 원자성·동시 적용                                   |
| D2a. AI 통합 선행: 초안 수정·삭제의 검토 버전·입력 보존                                             | P0 초안 동시 변경 보호, §5  | 완료 | 화면 이후 변경·게시·삭제 및 AI 검사 중 경합 거부, BIGINT·확인·실패 입력 보존         |
| D2b. AI 통합 후보·생성·초안 저장·원안 검토·버리기                                                   | P0 기존 통합 보존, §5·§11.2 | 완료 | 전송 차단·한도·실패 보존·실제 DB 경합·실제 AI 가상 예제·브라우저                     |
| D2c. 통합 최종 편집·명시적 적용·상충 역사                                                           | P0 기존 통합 보존, §5·§11.2 | 완료 | 원자적 적용·별칭 보존·동시 적용·상충 상세·이전 리비전·브라우저                       |
| D2d. 통합·되돌리기 검사에서 내부 식별자의 연락처 오탐 회귀 수정                                     | P0 통합 보호·검사 일관성    | 완료 | 식별자 모양과 무관한 통합·원래 이력 보존, 실제 민감 텍스트 차단, DB 원자성           |
| D3a. 직접 편집 제목/별칭 충돌·별칭 제거·동시 저장과 입력 보존                                       | P0 편집 보호                | 완료 | 실제 DB 원자성·독립 연결 동시성, 기존 URL/별칭 ID/이력 보존, 브라우저 비교 후 재저장 |
| D3b. 이력 화면의 검토 버전에 따른 되돌리기·중복 요청 보호                                           | P0 동시 변경 보호           | 완료 | 화면을 연 뒤 변경/삭제된 문서의 오래된 되돌리기 거부, 최신 이력 재검토               |
| D4a. 보존형 마이그레이션 적용 이력·순차 실행·실패 복구                                              | P0 삭제 복구 선행, §11.1    | 완료 | 기존 데이터 보존, 실제 DB의 전체 롤백·동시 실행·중단 후 재시도                       |
| D4b. 문서 휴지통 이동·조회 제외·관계 보존과 복구                                                    | P0 삭제 복구, §8            | 완료 | 보존형 스키마 변경, 삭제/복구 화면, 검색 제외, 관계 유지와 복구 충돌                 |
| E1. 직접 편집의 현재 입력 미리보기                                                                  | P0 미리보기                 | 완료 | 게시 렌더링과 일치, 반응형 화면 확인                                                 |
| E2. 초안 수정의 현재 입력 미리보기                                                                  | P0 미리보기                 | 완료 | 실제 현재 입력·이미지 표시·새 게시/통합 후 일치, 두 테마/주요 크기                   |
| E3. 직접 편집 저장 실패·이동·새로고침의 입력 복구                                                   | P0 입력 보존                | 완료 | 충돌/실패 시 입력 유지, 보관·삭제 정책, 복구 확인                                    |
| E4. 초안 수정·최종 통합 편집의 실패·이동·새로고침 복구                                              | P0 입력 보존                | 완료 | 원래 버전·대상/통합안·확인 초기화, 실패/보호/탭 분리, 실제 DB·브라우저               |
| U1. 공통 토큰·버튼·상태·고딕체 계층 적용                                                            | P0 UI 통일, §7.1            | 완료 | 주요 7개 화면, 라이트/다크, 360/768/1440px                                           |
| U2. 탐색·홈 작업 진입과 모바일 메뉴 정리                                                            | P0 UI 통일, §7.2            | 완료 | 주요 경로·키보드·빈/오류 상태                                                        |
| U3. 문서 목차·번호·표·각주·관련 문서 읽기 흐름                                                      | P0 문서 읽기, §7.3          | 완료 | 모바일/데스크톱·키보드·200% 확대                                                     |

후속 반복에서는 위 목록의 상태와 새 증거만 갱신한다. 큰 작업은 착수 시 독립 검증 가능한 수직 단위로 추가 분할한다.

- E2a. 공통 미리보기의 반응형 초점 보존 — **완료** (§7.5/§13). 직접 편집·초안·최종 통합의 미리보기 DOM을 유지하고 제거되는 탭의 초점을 연결한다. 양방향 화면 폭 전환·실제 접근성 속성·입력/게시 일치를 검증했다.

- E2b. 초안 이미지 미리보기와 게시 표시 일치 — **완료** (§12 P0). 현재 입력의 이미지 표시 선택, 입력 변경 시 해제, 실제 로딩/실패·재시도와 게시·통합 후 일치를 검증했다. 기본 자동 요청 없음과 원문/URL·DB 보존을 유지한다.

- U3a. 문서 읽기 화면과 주변 테마 통일 — **완료** (최신 사용자 요청·§7.1/§7.3). 읽기/미리보기의 별도 차가운 팔레트를 공통 브랜드 토큰으로 통일하고 본문·목차·보조 패널의 라이트/다크·반응형 가독성과 기존 동작을 실제 브라우저로 확인했다. U1/U3 전체 개편으로 확장하지 않는다.

- U3b. 긴 문서의 현재 문단 목차와 모바일 본문 복귀 — **완료** (§7.3/§13). 기존 문단 주소·번호·미리보기 목차를 유지하고, 데스크톱 고정 목차·읽는 위치 표시·모바일 접기/본문 복귀를 실제 브라우저에서 검증한다.

- U3c. 각주의 원문 복귀와 반복 참조 — **완료** (§7.3/§12). 실제 각주 참조마다 고유 위치를 만들고 기존 각주 주소·번호·미리보기 접두사를 보존하며 기본 링크/키보드로 복귀한다. 게시·편집/통합 미리보기·초안 목록의 실제 브라우저와 격리 DB 일치 검사를 통과했다.

- U3d. 표의 가로 스크롤 안내와 키보드 접근 — **완료** (§7.3/§12). 게시·편집·초안/통합 미리보기의 동일 표 구조와 안내/초점, 실제 키보드·터치·JavaScript 없는 스크롤·입력/DB 보존을 검증했다.

- U3e. 관련 문서·역링크의 실제 연결 이유 표시 — **완료** (§7.3/§12). 기존 자동/명시적 연결 계산과 주소를 유지하면서 연결 방향·이유를 읽기 화면에서 구분하고 모바일에서도 탐색한다.

- U1a. 직접 편집·초안 업무 화면의 고딕체와 제목 계층 — **완료** (§7.1). 일반 편집·초안 목록/상세의 작업 제목·보조 안내 계층을 통일하고, 문서 미리보기와 저장·복구 흐름을 보존한다.

- U1b. 검색·토론·이력 화면의 글자 계층 — **완료** (§7.1). U1a의 고딕체/제목 기준을 공유하고 검색 설명·토론 주제·이력 차이/상충 기록의 가독성과 기존 작업 흐름을 확인한다.

- U1c. 공통 버튼의 주/보조/위험 구분과 대비·초점·비활성 상태 — **완료** (§7.1/§7.9). 브랜드 팔레트와 실제 저장·검토·모달 동작을 보존하면서 모든 공통 버튼 상태를 확인한다.

- U1d. 공통 안내·오류·완료·처리 상태 — **완료** (§7.1/§7.9). 실제 처리 결과·다음 행동·검사 생략을 보존하면서 공통 컴포넌트와 대비/읽기 순서를 적용한다.

- U1e. 홈 업무 패널·휴지통·자동 연결의 글자 계층 — **완료** (§7.1). 로고/주 소개를 보존하고 실제 지표·목록·제목·보조 안내의 공통 고딕체와 대비를 검증한다.

- U1f. 공통 패널·입력 토큰과 상태 대비 — **완료** (§7.1/§7.9). 주요 업무 패널의 여백/모서리와 입력 면·테두리·초점·읽기 전용/비활성 스타일을 공유하고 기존 작성/검색/모달 동작을 검증한다.

- U1g. 공통 UI 적용 누락과 최종 감사 — **완료** (§7.1/§12). 빈 화면 컴포넌트·토론/이력 보조 버튼·페이지 오류 복구 안내를 공통 체계에 맞추고 주요 7개 화면을 현재 코드와 실제 브라우저로 재검증한다.

- U2a. 모바일 메뉴 접기/펼치기와 검색 우선 배치 — **완료** (§7.2/§7.9). 키보드·기본 HTML·현재 경로·모달·작성 중 입력 보호를 유지하며 좁은 화면의 메뉴를 접는다.

- U2b. 주요 화면의 검색·문서 탐색·새 작성 진입 정리 — **완료** (§7.2). 기존 URL과 작성/파일 전환 흐름을 활용해 전역 진입점을 구성하고 메뉴 길이·키보드·반응형을 검증한다. 홈 정보 재배치는 별도 단위다.

- U2c. 홈의 검색·실제 문서 진입·사용 안내 우선 배치 — **완료** (§7.2). 통계/소개를 뒤로 옮기고 실제 데이터와 명확한 시작 안내로 첫 작업을 연결한다. 기존 활동/지도 보기·URL·빈/오류 상태를 유지한다.

- U2d. 활동·지도 조회 실패 표시와 탐색 완료 대조 — **완료** (§7.2/§13). DB 조회 실패를 빈 목록/0개로 읽게 하는 기존 분기를 실제로 재현하고, 재조회·보기 URL·다음 행동을 보완한다.

- L1. 문서 자동 연결·전체 검사 — **완료** (최신 사용자 요청·§3). 제목·등록 별칭을 별도 승인 없이 연결하고 읽기·관련 문서·역링크·지도에 공통 규칙을 적용한다. `/links`에서 최신 전체 연결을 재계산하며 원문·리비전·검토 버전을 보존한다. 신규 DB/브라우저 검사, 기존 검토·미리보기 화면 회귀 및 최종 전체 검사를 통과했다.

- W3. 위키파이어 세부 주제 분할 — **완료** (최신 사용자 요청·§4/§7.6). 큰 업무 단위로 묶던 분할 지침과 8개 생성 제한을 수정했다. 독립적으로 검색할 세부 작업·판단 기준·점검 항목별 최대 32개 초안을 계획하고, 8개씩 최대 4개 요청으로 생성한 뒤 전체 검증·원자 저장한다. 개수/제목 일치·누락/중복/중간 실패·모달 결과·키 없는 기본 흐름과 합성 자료의 실제 AI 분할을 검증했다. R1의 한 번에 8개 선택 게시와 기존 문서는 유지한다.

- R1. 목록에서 본문·검사 상태 확인 및 선택 초안 일괄 게시 — **완료** (사용자 우선 요청·§5/§7.6). 기존 상세 수정·AI 통합·복구·URL을 유지하고, 선택한 검토 버전만 항목별 원자적으로 게시한다. 일부 실패/충돌·중복/동시 요청·재시도·AI 없음과 실제 브라우저의 목록/선택/결과를 검증했다. 별도의 검토 상태나 일괄 통합/삭제는 추가하지 않는다.

- W2. Excel 업로드 오류·모달 상태 보존 — **완료** (사용자 우선 지시·§4). 반복 57에서 제공된 실제 XLSX로 주제 계획·본문 생성의 공통 시간 초과를 재현·수정했다. 실제 AI/브라우저/격리 DB의 32개 초안 생성·검토 진입과 시간 초과 후 원자성·입력 보존·재시도를 검증했다.

- E3a. 이전 직접 편집 방문의 늦은 저장·삭제 응답 보호 — **완료** (P0 입력 보존). 다른 문서 이동·같은 문서 재진입·편집기 종료 후 응답이 현재 입력/저장 상태를 바꾸지 않게 하고 원래 서버 작업·복구본을 보존한다.

- D3c. 기존 주소와 다른 제목·별칭 표기의 새 이름 충돌 보호 — **완료** (P0 편집 보호). 직접 편집/게시/통합에서 같은 저장 표기의 새 선점을 막고 기존 중복 이름·공백·주소·이력을 보존한다. 시드/복구/기존 제목의 후속 완료 대조는 D3d/e/f와 D4c에 기록했다.
- D3d. 기본/데모 시드의 기존 표시 이름 충돌 보호 — **완료** (P0 편집 보호의 최소 선행 보완). 실제 CLI/격리 DB에서 주소가 다른 기존 제목/별칭과 겹치는 예시의 전체 생략, 기존 자료·이력·휴지통 예약 및 동시 선점 보존을 검증했다.
- D4c. 복구 시 기존 이름과 실제 주소 보존 — **완료** (P0 삭제·복구 보호). 사용하지 않는 제목 계산 URL 때문에 옛 문서 복구가 거부되는 문제를 수정하고, 기존 중복 이름·관계 보존과 실제 주소 충돌·재시도를 DB/브라우저에서 검증했다.
- D3e. 프로토타입 예시 적재의 이름·주소 충돌 및 동시 생성 보호 — **완료** (§8 예시 보존·P0 저장 보호). 실제 Bun/Node CLI·격리 DB에서 충돌 예시 전체 생략·변경한 참조 초안 보존·동시 쓰기·전체 롤백/재시도·안전한 실패 출력을 검증했다.
- D3f. 기존 제목의 미사용 URL과 본문 저장·되돌리기 보존 — **완료** (P0 편집 보호). 기존 제목/실제 URL을 유지하는 편집만 허용하고 새 제목/실제 별칭 URL 충돌은 계속 막는다. 현재 코드의 전체 실제 DB·브라우저 대조를 마쳐 **P0 직접 편집 보호 및 삭제/복구 두 기준을 완료**했다.

## 반복 1 — 업로드 전송 차단 (2026-09-21)

### 변경 전 검사

- `bun run test`: 15/15 통과.
- `bun run check`: 오류 0, 경고 0.
- `bun run build`: 앱 컴파일 및 Vercel 패키징 통과.
- `bun run lint`: 기존 6개 파일의 포맷 실패 (`scripts/migrate.js`, `scripts/seed.js`, `src/routes/discussion/[slug]/+page.server.js`, `src/routes/discussion/[slug]/+page.svelte`, `src/routes/edit/[slug]/+page.svelte`, `src/routes/history/[slug]/+page.svelte`). 관련 없는 포맷은 수정하지 않는다.
- 실행 환경: Bun 1.2.13, Node.js 26.7.0. 배포 대상 Node.js 22와 구분한다.

### 변경과 결정

- 정규식 차단은 의미 검사 전 중단하고, 의미 검사 거부·유효하지 않은 응답은 초안 생성 전 중단한다. 파일명도 AI 입력이므로 함께 검사한다.
- 차단 요청은 422 안내로 반환한다. 원문을 DB에 새로 저장하지 않으며, 사용자 로컬 파일과 기존 DB 데이터는 보존한다. 검사 응답이 잘못되거나 미완료면 503으로 재시도를 안내한다. 원문 조각이나 모델의 거부 사유를 오류 응답에 그대로 노출하지 않는다.
- `src/lib/server/governance.js`: 공통 전송 전 검사와 안전한 차단 오류. `src/lib/server/openai.js`: 함수 직접 호출에도 로컬 검사, 의미 검사 응답의 타입·완료 여부 검증, 모델이 반환한 `skipped` 무시, 저장 비활성화·45초 제한·자동 재시도 0회.
- `src/routes/api/wikify/+server.js`: 파일명+원문 검사 후에만 의미 검사/생성을 수행하고 실패 시 DB 접근 전 종료. 성공 응답에 실제 `semanticSkipped`를 전달한다.
- `src/lib/components/WikifyForm.svelte`: 의미 검사 생략을 경고 문구와 `role="status"`로 표시. 기존 기본 초안과 수동 검토 진입 유지.
- `tests/wikify-governance.test.js`, `tests/fixtures/wikify-server.js`: 실제 파일 추출기·API 핸들러·OpenAI SDK를 실행하고 HTTP 전송/DB 경계만 격리한 회귀 검사. `.env`를 읽지 않고 테스트용 키만 사용한다. 의존성·스키마 변경 없음.

### 변경 후 검증

- `bun run test`: **23/23 통과** (기존 15 + 신규 8). 기밀·인사·사번·이메일·연락처·주민번호·파일명 차단 시 SDK 전송 0회/DB 접근 0회. 의미 거부 시 검사 1회/생성 0회/DB 0회. 잘못된 JSON·타입·상충 결과·미완료 응답 및 429에서도 후속 처리 중단. 정상 생성과 키 없는 로컬 초안·검사 생략 상태 확인.
- `bun run check`: 오류 0, 경고 0.
- `bun run build`: 앱 컴파일 및 Vercel 패키징 통과. 로그: `/tmp/wiki-viki-s1-20260921/build.log`.
- `bun run lint`: 변경 전과 동일한 6개 파일만 실패. 변경/추가한 7개 파일의 `bunx --no-install prettier --check ...`와 `git diff --check` 통과. 이번 변경으로 남은 자동 검사 실패 없음.
- 실제 Chrome + 프로덕션 preview: `/wikify`에서 360/768/1440px × 라이트/다크 6조합 통과. 빈 상태 버튼 비활성, 실제 서버의 파일명 차단 422, 선택 파일 보존·재시도, 키보드 Enter 제출·검토 링크 초점, 로딩 중 중복 제출 제한, 성공/검사 생략 문구, 페이지 가로 넘침 없음 확인. 로딩·성공 화면은 API 모의 응답이며 실제 DB 저장 성공으로 해석하지 않는다. 360px 다크 오류/성공과 1440px 라이트 성공 스크린샷을 직접 확인했다.
- 브라우저 실행: `DATABASE_URL='' OPENAI_API_KEY='' bun run preview --host 127.0.0.1 --port 4174` 후 `node /tmp/wiki-viki-s1-20260921/browser.mjs`. 환경에 이미 있는 Playwright/Chrome 사용, 저장소 의존성 추가 없음. 결과·스크린샷은 같은 임시 폴더에 보관한다.
- 공유 모달도 `node /tmp/wiki-viki-s1-20260921/modal.mjs`로 360/1440px에서 검사 생략 경고·키보드 제출·Escape 닫기를 확인했다 (성공 API 모의 응답).

### 검증 중 문제와 해결

- 변경 전 신규 차단 회귀 테스트에서 후속 외부 요청 시도를 재현했다. 초기 HTTP 모의 응답에 SDK가 요구하는 `object: 'response'`가 빠져 정상 응답이 해석되지 않아 보충했다.
- 테스트와 `check` 병행 시 Vite 감시기가 재생성된 tsconfig를 감지해 격리 DB 상태를 초기화했다. 테스트 전용 서버의 파일 감시를 꺼서 해결했고 두 명령의 병행 실행도 통과했다.
- preview 실행 중 재빌드해 이전 해시의 JS가 사라지면서 preview가 종료됐다. 프로세스의 ENOENT 종료를 확인한 뒤 새 빌드로 재시작했고 브라우저 6조합을 다시 통과했다. 앱 변경으로 발생한 회귀가 아닌 검증 절차 문제다.

### 남은 검증과 다음 반복

- 실제 OpenAI 호출, 실제 DB 저장·원자성, Node.js 22 실행, 배포는 이번에 검증하지 않았다. 이 작업의 전송 차단은 실제 SDK 전송 경계에서 검증했으며, DB 모의 응답을 원자성·제약 검증으로 계산하지 않는다. 운영 DB 변경·푸시·배포 없음.
- 전체 P0와 §12의 전송 차단 체크 항목은 아직 미완료다. 검색/게시/되돌리기 등 다른 경로, 전체 화면·200% 확대, 데이터 복구 검증은 남아 있다. `GOAL.md`의 사용자 변경과 체크박스는 수정하지 않았다.
- **다음 반복의 구체 작업: S2 — `/api/answer`의 검색어·근거 문서에 공통 전송 전 검사를 적용하고, 민감 검색어 차단·민감 근거 제외·안전한 근거만 전송·키 없음/AI 실패 시 일반 검색 유지 회귀 테스트를 추가한다.**
- S2는 실제 키나 DB 없이 격리된 서버 테스트로 시작할 수 있다. D1 이후 DB 원자성 검증에는 사용자 자료와 분리된 PostgreSQL 환경이 필요하다.

## 반복 2 — 검색 설명의 전송 검사 (2026-09-21)

- 이전 반복은 코드·회귀 테스트·브라우저 검증을 완료한 진행으로 분류한다. 목표 파일·요구사항·README·설정·git 상태와 적용 AGENTS 부재를 재확인했고, S1 변경을 보존한다.
- 시작 검사: 테스트 23/23, check 오류/경고 0. lint는 이전과 같은 6개 파일만 실패한다.
- 검색 설명에 이미 있는 민감 근거 제외는 보존한다. 현재 검색어 검사는 DB 조회 뒤여서 DB 장애 시 차단 상태 대신 재시도가 안내된다. S2는 공통 전송 검사와 DB 조회 전 차단, 콘텐츠 차단/일시 장애 구분, 안전한 근거·실제 출처만 사용하는 회귀 검증으로 한정한다. 일반 검색의 사용 범위나 P1의 요청형 AI 생성은 바꾸지 않는다.

### 변경과 검증 결과

- `src/routes/api/answer/+server.js`: S1의 `assertSafeForAI`를 DB 조회 전에 적용한다. 콘텐츠 차단은 422/`blocked`, 일시 장애는 기존 503/`unavailable`로 구분한다. 응답에는 검색어나 민감 원문을 되돌리지 않는다.
- `src/lib/server/openai.js`: `answerQuestion` 직접 호출에도 검색어 검사를 먼저 적용한다. 기존 제목·전체 본문 검사와 민감 문서 제외를 유지하고, 안전한 근거가 하나도 없으면 콘텐츠 차단을 반환한다. 검사 전에 원문을 잘라 민감 내용을 놓치지 않는지 확인했다.
- `src/lib/components/AnswerCard.svelte`: 콘텐츠 차단 제목과 원문 탐색 안내를 표시한다. 차단 상태에는 동일 요청의 재시도 버튼을 표시하지 않고, 키 없음·일시 장애의 재시도는 유지한다. 일반 검색 결과·필터·출처 링크는 유지한다.
- `tests/answer-governance.test.js`, `tests/fixtures/answer-server.js`: 실제 핸들러·검색 SQL 호출·SDK를 실행하며 DB와 HTTP 경계만 격리했다. 신규 8개 중 변경 전 4개는 차단 상태/순서에서 실패했고, 기존 근거 제외·출처 검증 등 4개는 이미 통과했다. 수정 후 8개 모두 통과했다.
- 변경 전/후 `bun run build`: 모두 앱 컴파일 및 Vercel 패키징 통과. `bun run test`: **31/31 통과**. `bun run check`: 오류 0, 경고 0. `bun run lint`: 기존 6개 파일만 실패하며 새 실패 없음. 이번 변경 파일의 Prettier 검사와 `git diff --check` 통과.
- 브라우저: `node /tmp/wiki-viki-s2-20260921/browser.mjs`로 실제 SvelteKit 앱/Chrome과 격리 DB fixture를 사용했다. 360/768/1440px × 라이트/다크에서 실제 API의 차단 422·키 없음 안내, 차단 재시도 미표시, 일반 결과 유지, 키보드 재시도·출처 초점, 가로 넘침 없음 확인. 빈 결과에서는 빈 상태를 표시하고 AI 카드를 만들지 않음을 확인했다. 로딩·정상 AI 답변은 HTTP 모의 응답이며 외부 HTTP는 차단했다.
- 360px 다크 차단, 1440px 라이트 차단, 768px 라이트 정상 답변 스크린샷을 직접 확인했다. 결과 JSON·이미지·빌드 로그는 `/tmp/wiki-viki-s2-20260921/`에 있다. 브라우저와 검증 서버는 종료했다.

### 남은 범위와 다음 반복

- 실제 OpenAI 답변 품질·실제 DB 검색 품질/제약·배포·Node.js 22 실행·전체 화면 200% 확대는 이번 검증 범위 밖이며 완료로 계산하지 않는다. DB 쿼리 fixture는 DB 통합 검증을 대신하지 않는다. 사용자 데이터·환경 파일·요구사항 체크박스는 변경하지 않았다.
- 전체 P0는 미완료다. **다음 작업은 S3의 첫 수직 단위: 초안 수정·게시에서 제목·본문·저장된 별칭을 공통으로 검사하고, 차단된 필드가 의미 검사나 게시로 넘어가지 않도록 서버·안내·회귀 테스트를 완료한다.** 직접 편집·토론·되돌리기는 이어지는 S3 작업으로 남긴다. 현재 초안 게시 원자성 문제는 D1에 유지하며 이 검사 작업만으로 해결됐다고 표시하지 않는다.

## 반복 3 — 초안 수정·게시의 콘텐츠 검사 (2026-09-21)

- 목표 파일·요구사항·README·설정·진행 기록·git 상태·AGENTS 부재를 재확인했다. 이전 반복은 구현/검증을 완료한 진행이다. 시작 검사 31/31 통과, check 오류/경고 0, lint 기존 6개 실패. 사용자 GOAL 변경과 S1/S2 변경 보존.
- S3를 초안 수정/게시(S3a)와 나머지 경로(S3b)로 나눴다. 차단된 저장 별칭을 고칠 수 있도록 줄 단위 별칭 입력을 최소 선행 UI로 포함한다. 쉼표가 있는 기존 별칭은 분리하지 않으며, 이전 폼처럼 별칭 필드가 없으면 기존 값을 보존한다.
- 의미 검사 장애에서도 수정 내용은 `blocked` 초안으로 저장한다. 검사 미완료/생략/거부를 구분하고 게시 시 다시 검사한다. 직접 편집·되돌리기·토론 및 D1의 원자성/동시성 문제는 이번 작업으로 완료 처리하지 않는다.
- 로컬 PostgreSQL 17 실행 파일을 확인했다. `/tmp/wiki-viki-s3a-20260921/`에 별도 클러스터를 만들고 루프백의 테스트 DB만 사용한다. 테스트마다 고유 스키마를 생성/정리하며 실제 사용자 DB·환경 파일은 사용하지 않는다.

### 변경과 결과

- `src/lib/server/document-governance.js`: 제목·본문·별칭의 공통 검사, 필드별 차단 사유, 잘못된 별칭 구조 차단, 의미 검사 미완료/생략 구분. `openai.js`는 의미 검사 지시와 입력 자료를 분리했다.
- `src/routes/drafts/+page.server.js`: 수정과 게시 모두 공통 검사를 적용한다. 차단/검사 장애에서도 수정 초안을 보존하고, 게시 시에는 문서·리비전·별칭 생성 전에 중단한다. 성공 게시에도 최신 검사 결과를 보존한다.
- `src/routes/drafts/+page.svelte`: 별칭 수정 입력, 필드 수정 링크, 최신 게시 검사 실패 즉시 표시/게시 버튼 제한, 검사 생략/장애 안내. 기본 폼의 선택 초안 ID도 유지한다.
- `tests/document-governance.test.js`: 키가 없어도 민감 필드는 차단하며 정상 수동 검토와 의미 검사 생략을 구분한다. `tests/draft-governance-db.test.js`와 `tests/fixtures/governance-database.js`: 실제 PostgreSQL에 저장·조회하며 9개 시나리오를 검사한다. `README.md`에 실행 조건을 추가했다. 의존성·스키마 변경 없음.
- 기본 `bun run test`: **33개 통과, DB 검증 1개 묶음 생략**. `GOVERNANCE_TEST_DATABASE_URL`을 격리된 로컬 DB에 설정하고 `RUN_GOVERNANCE_DB_TESTS=1 bun run test`: **43/43 통과, 생략 0** (9개 DB 시나리오와 상위 테스트 포함). 실제 DB에서 차단 시 문서/리비전/별칭 0개, 수정 내용·기존 별칭 보존, 쉼표 포함 별칭 수정 후 게시·리비전·별칭 연결을 확인했다. 이 결과는 동시성·원자성 검증을 포함하지 않는다.
- 변경 전/후 `bun run build`: 앱 컴파일·Vercel 패키징 통과. `bun run check`: 오류/경고 0. `bun run lint`: 기존 6개 파일만 실패. 이번 변경 파일의 Prettier 검사·`git diff --check` 통과.
- 실제 Chrome/SvelteKit/격리 PostgreSQL: `node /tmp/wiki-viki-s3a-20260921/browser.mjs`. 360/768/1440px × 라이트/다크에서 게시 차단→별칭 수정→저장→키 없는 수동 게시→문서 이동·리비전 생성 확인. 저장 중 게시 제한, 키보드 저장/게시, 가로 넘침 없음, 빈 초안 목록, 의미 검사 장애 중 입력 보존도 확인했다. JavaScript 없는 기본 폼에서도 게시 실패 뒤 같은 초안의 입력란을 유지했다. AI 호출은 SDK HTTP 경계에서 모의 처리했다.
- 차단·정상·장애 화면 스크린샷 직접 확인. 결과 JSON·이미지·빌드 로그는 `/tmp/wiki-viki-s3a-20260921/`에 보관. 테스트 후 잔여 `governance_` 스키마 0개를 SQL로 확인했다.

### 확인한 문제와 처리

- `postgres`와 Neon의 JSON 매개변수 직렬화 차이 때문에 초기 DB 테스트에서 JSON 문자열이 다시 JSON 문자열로 저장됐다. 실제 Neon의 문자열 전달 방식에 맞춰 테스트 연결의 JSON serializer만 조정했으며, 이후 실제 JSONB 배열/객체 저장을 확인했다. 운영 DB 드라이버는 변경하지 않았다.
- 기본 폼의 `?/publish`가 `open` 값을 제거해 실패 응답 뒤 선택 초안이 사라졌다. 수정/게시 action에 `open`을 유지했고 JS 없는 브라우저 검증으로 확인했다. JS 검증은 hydration이 완료된 뒤 수행했다.

### 미검증과 다음 작업

- 실제 OpenAI 판단 품질, Neon HTTP 전송, 배포, Node.js 22 실행은 미검증이다. 게시 원자성·중복/별칭 충돌·동시 변경 보호는 D1에서 수행해야 한다. 이번에는 실제 DB 단일 요청의 콘텐츠 검사·저장·차단을 검증했다.
- 검증용 클러스터는 `/tmp/wiki-viki-s3a-20260921/postgres`에 보관하고 종료한다. 후속 DB 검사 시 `pg_ctl`로 이 격리 클러스터를 다시 시작하거나 새 로컬 테스트 DB를 준비한다. 연결 정보는 `GOVERNANCE_TEST_DATABASE_URL`, 활성화는 `RUN_GOVERNANCE_DB_TESTS=1`로 지정한다. 테스트는 루프백과 `wiki_viki_test_` DB 이름만 허용한다.
- **다음 반복: S3b의 첫 단위인 직접 편집의 제목·본문·별칭(및 변경 요약) 검사를 통일하고, 차단/의미 검사 장애 시 입력 보존과 키 없는 수동 편집 안내를 검증한다.** 되돌리기와 토론 검사는 그 뒤의 별도 단위로 남긴다. 전체 P0는 미완료이며 `GOAL.md` 체크박스는 변경하지 않았다.

## 반복 4 — 직접 편집의 검사와 실패 입력 보존 (2026-09-21)

- 목표 파일·요구사항·README·설정·진행 기록·git 상태·AGENTS 부재를 재확인했다. 이전 반복은 구현/검증 완료로 이어받고 사용자 GOAL 및 기존 변경을 보존했다. S3b를 직접 편집, 되돌리기(S3c), 토론(S3d)으로 나눴다.
- 시작 검사: 기본 테스트 33개 통과/DB 묶음 1개 생략, check 오류/경고 0, build 통과, lint 기존 6개 실패. 기존 직접 편집은 본문만 검사했고 의미 검사 없이 저장했다. 새 회귀 시나리오 7개 모두 변경 전 실패해 우회 저장·생략 안내 누락·내부 DB 오류 노출을 재현했다.

### 변경과 결정

- `src/lib/server/document-governance.js`: 공통 검사에 공개 편집 요약을 추가했다. 초안 호출에는 빈 요약을 기본값으로 사용한다.
- `src/routes/edit/[slug]/+page.server.js`: 제목·본문·별칭·편집 요약을 DB 변경 전에 검사한다. 로컬 차단은 AI 호출 없이 400, 의미 거부는 400, 검사 장애는 503을 반환한다. 실패 시 공백을 포함한 제출값을 돌려주며 실제 저장값만 정규화한다. DB 읽기/쓰기 오류는 내부 메시지를 노출하지 않는다.
- `src/routes/edit/[slug]/+page.svelte`: 생략 안내, 차단 이유와 필드 수정 링크, 저장 중 입력·중복 제출·삭제 제한, 네트워크 오류 시 폼 유지. JavaScript 없는 기본 폼도 유지한다. DB 조회 실패 시 빈 문서를 덮어쓰지 않도록 편집/저장 입력을 비활성화한다. 기존 디자인·쉼표 별칭 입력 규칙은 유지하며 변경한 편집 화면의 기존 포맷 실패를 함께 해소했다.
- `tests/edit-governance-db.test.js`: 격리 PostgreSQL에서 8개 시나리오를 추가했다. 기존 문서/이력/별칭의 동일성, 유효하지 않은 입력, 정상/차단/장애/키 없음, 실제 DB trigger로 주입한 쓰기 실패와 테이블 이름 변경으로 주입한 읽기 실패를 검사한다. `tests/document-governance.test.js`에도 요약 차단을 추가했다. README의 DB 검사 범위를 갱신했다.
- 저장 원자성·별칭 충돌/제거·동시성은 D3에 남긴다. DB 중간 실패는 일부 저장됐을 수 있으므로 오류 안내도 저장되지 않았다고 단정하지 않고 현재 문서 상태 확인을 요청한다. 이동·새로고침 복구(E3)는 미완료다.

### 검증 결과와 해결한 문제

- `RUN_GOVERNANCE_DB_TESTS=1 bun run test` (격리 로컬 DB 연결 지정): **52/52 통과, 생략 0**. 로컬 차단 시 SDK 전송 0회, 의미 거부/장애 시 1회 후 저장 중단, 문서·리비전·별칭 보존을 확인했다. 기본 `bun run test`: **33개 통과, DB 묶음 2개 생략**.
- `bun run check`: 오류/경고 0. `bun run build`: 앱 컴파일·Vercel 패키징 통과. `bun run lint`: **기존 5개 파일만 실패** (`scripts/migrate.js`, `scripts/seed.js`, 토론 server/page, 이력 page). 이번 변경으로 남은 검사 실패 없음. 변경 파일 Prettier 및 `git diff --check` 통과.
- 테스트 초기에는 FormData 전송이 LF를 CRLF로 변환해 입력 보존 비교가 실패했다. 테스트 입력을 실제 HTTP 전송 줄바꿈에 맞춰 비교했고, 브라우저에서도 여러 줄·앞뒤 공백 보존을 별도로 확인했다. 검사 기준이나 앱의 본문을 바꾸지 않았다.
- `node /tmp/wiki-viki-s3b-20260921/browser.mjs`: 실제 Chrome/SvelteKit/격리 PostgreSQL, 360/768/1440px × 라이트/다크 6조합 통과. 차단→필드 수정→키 없는 저장→문서 이동/리비전 추가, 키보드 제출·오류 필드 링크 초점, 로딩 제한·가로 넘침 없음 확인. 빈 신규 문서, JS 없는 실패 폼, 네트워크 중단·AI 429·DB 쓰기/읽기 오류도 입력/안내와 함께 확인했다.
- 360px 다크 차단, 1440px 라이트 로딩, 768px 의미 검사 장애 화면을 직접 확인했다. 스크롤 위치에서 캡처한 전체 페이지 이미지의 고정 헤더 위치와 실제 화면 동작은 구분했다. 로그·결과 JSON·이미지는 `/tmp/wiki-viki-s3b-20260921/`에 보관한다. DB 검증 후 잔여 테스트 스키마 0개를 확인했고 서버·브라우저·격리 DB를 종료한다.

### 미검증과 다음 작업

- 실제 OpenAI 판단 품질·Neon HTTP 연결·Node.js 22·배포는 미검증이다. SDK 전송 경계는 모의 응답으로, DB 저장/조회/오류는 실제 PostgreSQL로 확인했다. 운영 DB·비밀 값·원격 저장소는 변경하지 않았다. 전체 P0와 GOAL 체크박스는 미완료 상태를 유지한다.
- **다음 반복: S3c — 이력 되돌리기에 공통 콘텐츠 검사를 적용한다. 리비전의 문서 소속을 유지하면서 제목·복원 본문·저장된 별칭·요약을 검사하고, 거부/장애 시 문서·리비전이 바뀌지 않는지와 검사 생략 안내를 실제 DB/브라우저로 검증한다.** 현재 `history/[slug]/+page.server.js`에는 리비전 소속 조건만 있으며 콘텐츠 검사는 없다.
- 재개 환경: `/tmp/wiki-viki-s3a-20260921/postgres` 격리 클러스터를 루프백 55437에서 다시 시작하거나 별도 로컬 테스트 DB를 준비한다. 앞선 README의 플래그/DB 이름 제한을 유지한다.

## 반복 5 — 이력 되돌리기의 콘텐츠 검사 (2026-09-21)

- 이전 반복은 코드·실제 DB·브라우저 검증을 완료한 진행이다. 목표 파일·요구사항·README·설정·git 상태와 AGENTS 부재를 다시 확인했다. 시작 검사: 기본 테스트 33개 통과/DB 묶음 2개 생략, check 오류/경고 0, build 통과, lint 기존 5개 실패. 사용자 GOAL 및 이전 변경은 보존했다.
- 범위는 S3c 한 단위다. 본문 중심 되돌리기, 현재 제목·별칭과 기존 URL, 익명 편집자 `Operator-A`, 새 리비전 생성과 기존 이력 보존을 유지한다. 전체 상태 복원이나 새 비교 UI는 추가하지 않는다.

### 변경과 검증

- `src/routes/history/[slug]/+page.server.js`: 리비전의 문서 소속 확인 후 복원 본문·현재 제목·저장된 별칭·새 되돌림 요약을 공통 검사한다. 로컬 차단은 외부 전송 0회/400, 의미 거부는 400, 의미 검사 장애는 503으로 저장 전에 중단한다. 잘못된 ID는 DB 조회 전에 400으로 처리하고 유효한 BIGINT ID를 문자열로 보존한다. 읽기/쓰기 오류는 내부 정보를 노출하지 않는다.
- `src/routes/history/[slug]/+page.svelte`: 검사 생략·차단·장애 안내와 원문 차이/편집 링크, 처리 중 모든 되돌리기 버튼 제한, 네트워크 오류 안내, 빈 이력 상태를 추가했다. 기본 HTML 폼의 `diff`도 유지해 실패 뒤 확인 중이던 차이가 사라지지 않는다. 수정한 이력 화면의 기존 포맷 오류를 해소했다.
- `tests/history-governance-db.test.js`: 실제 PostgreSQL에서 7개 시나리오(의미 거부/장애 2개 하위 검사 포함). 변경 전에는 새 테스트 10개(상위 묶음 포함)가 모두 실패했다. 변경 후 제목/본문/별칭 차단, 잘못된 ID·타 문서 리비전·없는 문서, 의미 검사 결과, 키 없는 수동 되돌림, 기존 문서·리비전·별칭·토론 관계 보존이 통과했다. README에 이력 DB 검증 범위를 추가했다.
- 리비전 INSERT에 실제 DB trigger 오류를 주입했을 때 기존 `saveDocument` CTE의 본문 UPDATE도 함께 롤백되는 것을 확인했다. 이는 단일 되돌림의 중간 실패 원자성 증거이며 동시 수정·중복 요청 보호의 증거는 아니다.
- `RUN_GOVERNANCE_DB_TESTS=1 bun run test` (격리 로컬 DB 연결 지정): **62/62 통과, 생략 0**. 기본 `bun run test`: **33개 통과, DB 묶음 3개 생략**. `bun run check`: 오류/경고 0. `bun run build`: 앱 컴파일·Vercel 패키징 통과.
- `bun run lint`: **기존 4개 파일만 실패** (`scripts/migrate.js`, `scripts/seed.js`, 토론 server/page). 변경 파일 Prettier 및 `git diff --check` 통과. 이번 변경으로 남은 자동 검사 실패 없음.
- 브라우저: `node /tmp/wiki-viki-s3c-20260921/browser.mjs`, 실제 Chrome/SvelteKit/격리 PostgreSQL. 360/768/1440px × 라이트/다크에서 차단→다른 안전한 리비전 되돌림→본문 복원/새 리비전 생성, 선택한 차이 유지, 키보드 제출·수정 링크 초점, 로딩 중 전체 버튼 제한, 가로 넘침 없음 확인. JS 없는 폼, 네트워크 중단, 의미 검사 429 후 정상 재시도, DB 리비전 쓰기 실패, 빈 이력, DB 읽기 오류도 확인했다. SDK HTTP만 모의 응답으로 처리했다.
- 360px 다크 차단·1440px 라이트 로딩·768px 검사 장애 화면을 직접 확인했다. 로그·결과 JSON·이미지는 `/tmp/wiki-viki-s3c-20260921/`에 보관한다. DB 검증 후 잔여 테스트 스키마 0개 확인 및 서버·브라우저·격리 DB 종료.

### 실패 조사와 남은 범위

- 첫 브라우저 실행은 로딩 문구 대기 중 탐색 완료를 기다리며 시간 초과됐다. 진단 실행에 POST 헤더·탐색·페이지 오류 기록을 추가했다. Vite 재최적화와 초기 재탐색을 관찰했고, 후속 실행은 6조합 모두 `x-sveltekit-action: true` 제출 2회씩, 페이지 오류 0개로 통과했다. 기본 폼도 별도 검증했다. 최초 실행의 정확한 타이밍 원인은 확정하지 않았으며 앱 수정으로 해결했다고 주장하지 않는다.
- 실제 OpenAI 판단 품질, Neon HTTP 연결, Node.js 22, 배포는 미검증이다. 동시 편집/되돌림, 검사 도중 문서·별칭 변경/삭제, 다중 탭 중복 요청은 아직 보호·검증하지 않았다. D3에서 직접 편집과 같은 저장 경계를 사용하는 되돌리기도 함께 다뤄야 한다. 전체 P0는 미완료이며 GOAL 체크박스는 변경하지 않았다.
- **다음 반복: S3d — 토론 등록의 주제·본문을 공통 로컬/의미 검사로 통일하고, 차단·장애 시 작성값 보존과 키 없는 수동 등록 안내를 실제 DB/브라우저로 검증한다.** 현행 토론은 주제+본문 정규식 검사만 있으며 의미 검사와 DB 오류 처리가 없다. 현행 스키마에는 답글 기능이 없으므로 P0에 새 답글 모델을 추가하지 않는다.
- 재개에는 앞선 격리 PostgreSQL 클러스터를 루프백 55437에서 시작하면 된다. 사용자 데이터·실제 키·운영 DB·원격 저장소·배포는 이번 반복에서 사용하거나 변경하지 않았다.

## 반복 6 — 토론 등록의 검사와 작성 내용 보존 (2026-09-21)

- 이전 반복은 구현·실제 DB·브라우저 증거를 남긴 진행이다. 목표 파일·요구사항·README·설정·git 상태와 AGENTS 부재를 재확인했다. 시작 검사: 기본 테스트 33개 통과/DB 묶음 3개 생략, check 오류/경고 0, build 통과, lint 기존 4개 실패. 사용자 GOAL과 이전 변경을 보존했다.
- S3d는 기존 토론 등록 한 단위다. 새로 저장할 주제·의견과 익명 이름을 검사하며, 변경하지 않는 부모 문서 원문·별칭은 AI에 전송하지 않는다. 답글·계정·새 저장 모델은 추가하지 않는다.

### 변경과 검증 결과

- `src/lib/server/document-governance.js`: 기존 검사 내부를 공유하는 `inspectDiscussionContent`를 추가해 주제/의견의 필드별 사유와 의미 검사 거부·장애·생략을 구분한다. 기존 문서 검사 인터페이스와 별칭 검증은 유지했다.
- `src/routes/discussion/[slug]/+page.server.js`: 제출값은 그대로 보존하고 저장값만 기존 방식으로 trim한다. 대상 문서가 있을 때만 주제·의견을 검사한 뒤 저장한다. 로컬 차단/의미 거부는 400, 의미 검사 장애는 503이며 저장하지 않는다. DB 오류는 내부 정보를 숨긴다. 기본 POST 뒤 조회 실패나 대상 삭제가 발생해도 작성값이 있는 폼을 표시한다. 일반 GET의 없는 문서는 기존 404를 유지한다.
- `src/routes/discussion/[slug]/+page.svelte`: 검사 생략·차단·장애 안내, 오류 필드 수정 링크, 처리 중 입력/중복 제출 제한, 네트워크 오류 때 폼 유지, 성공 후 주제·의견 초기화와 익명 이름 유지. 조회 실패와 빈 목록을 구분하고 조회 실패 시 등록을 제한하되 작성값은 복사할 수 있게 둔다. 기존 토론과 관계·URL을 보존했다.
- `tests/discussion-governance-db.test.js`: 9개 실제 DB 시나리오와 의미 거부/장애 하위 검사 2개. 변경 전 신규 테스트 12개(상위 묶음 포함)가 모두 실패했고 변경 후 통과했다. 로컬 차단 시 SDK 전송 0회, 거부/장애 시 기존 DB 상태 동일, 별칭 URL 등록의 올바른 문서 연결, 오류 시 공백·줄바꿈 포함 입력 보존, 실제 DB trigger 오류를 확인했다. 검사 응답 대기 중 문서를 삭제해도 FK가 고립된 토론 생성을 막고 실패 입력이 반환되는지 검증했다.
- `tests/document-governance.test.js`에 토론 필드/키 없는 검사 2개를 추가하고 README의 DB 검증 범위를 갱신했다. `RUN_GOVERNANCE_DB_TESTS=1 bun run test` (격리 DB 연결 지정): **76/76 통과, 생략 0**. 기본 `bun run test`: **35개 통과, DB 묶음 4개 생략**.
- `bun run check`: 오류/경고 0. `bun run build`: 앱 컴파일·Vercel 패키징 통과. `bun run lint`: **기존 `scripts/migrate.js`, `scripts/seed.js` 2개만 실패**. 변경한 토론 server/page의 기존 포맷 오류도 해소했다. 변경 파일 Prettier 및 `git diff --check` 통과, 이번 변경으로 남은 자동 검사 실패 없음.
- `node /tmp/wiki-viki-s3d-20260921/browser.mjs`: 실제 Chrome/SvelteKit/격리 PostgreSQL에서 360/768/1440px × 라이트/다크 통과. 빈 토론, 차단→필드 수정→키 없는 등록, 실제 DB 1개 저장·목록 반영·폼 초기화, 키보드 제출/수정 링크 초점, 로딩 제한, 가로 넘침 없음 확인. 네트워크 중단·AI 429 후 정상 재시도·DB 쓰기 오류도 작성값을 유지했다. JS 없는 기본 폼에서 조회 실패 및 검사 중 문서 삭제 후에도 입력을 확인했다.
- 360px 다크 차단·1440px 라이트 성공·768px 의미 검사 장애·기본 폼 DB 조회 오류 화면을 직접 확인했다. 증거는 `/tmp/wiki-viki-s3d-20260921/`에 보관한다. 잔여 테스트 스키마 0개 확인, 브라우저·서버·격리 DB 종료.

### 검증 중 문제와 다음 작업

- 첫 브라우저 실행은 자동 검사와 병행 중 개발용 동적 모듈을 가져오지 못해 초기화 대기에서 실패했다. 다음 실행은 6개 JS 조합 통과 후 기본 폼의 클릭 안정성 대기에서 멈췄다. 앱 동작 오류로 단정하지 않고 요청/페이지 오류를 기록하고, 브라우저 Vite 캐시를 전용 임시 경로로 분리했다. 활성 탭과 실제 테마 초기화를 확인하고 기본 폼을 키보드로 제출한 최종 실행은 전체 시나리오가 통과했다. 향후 브라우저와 자동 검사는 생성 파일/캐시를 공유하지 않게 실행한다.
- 외부 호출 위치를 추가 점검하며 `scripts/seed.js`가 `structure(text, file)` 실행 뒤에야 별도 `blocked(text)`를 적용하는 것을 확인했다. 시드도 원본 파일을 외부 AI에 전송하므로 P0 전송 차단을 완료 선언할 수 없다. 보안 우선 원칙에 따라 S4를 D1보다 먼저 둔다. 현행 시드의 검사 규칙과 오류 로그도 앱의 공통 규칙과 다르다.
- 실제 OpenAI 판단 품질·Neon HTTP 연결·Node.js 22·배포는 미검증이다. 다중 탭/재전송의 서버 중복 방지, 이동·새로고침의 작성 복구, 전체 P0는 미완료다. 키 없는 수동 등록과 검사 생략을 실제 의미 검사 통과로 표시하지 않는다. GOAL 체크박스는 변경하지 않았다.
- **다음 반복: S4 — 시드 파일명의 로컬 검사와 추출 원문의 공통 전송 전 검사를 구조화 호출보다 앞에 적용하고, 차단 파일의 외부 전송/새 초안 저장 0회·키 없는 기본 초안·파일별 실패 후 다음 파일 처리 유지·안전한 오류 로그를 검증한다.** 실제 시드 명령을 사용자 DB에 실행하지 않고 격리된 경로/DB/HTTP 전송 경계를 사용한다.
- 재개 환경은 `/tmp/wiki-viki-s3a-20260921/postgres` 격리 클러스터(루프백 55437) 또는 별도 `wiki_viki_test_` 로컬 DB다. 운영 DB·환경 파일·비밀 값·원격 저장소·배포는 이번 반복에서 변경하지 않았다.

## 반복 7 — 시드 파일의 외부 전송 전 검사 (2026-09-21)

- 이전 반복은 구현과 검증 증거를 남긴 진행이다. 목표 파일·GOAL·README·설정·진행 기록·git 상태와 AGENTS 부재를 재확인했다. 시작 검사: 기본 테스트 35개 통과/DB 묶음 4개 생략, check 오류/경고 0, build 통과, lint 기존 migrate/seed 2개 실패. 사용자 변경과 이전 작업을 보존했다.
- 변경 전 CLI를 임시 소스 경로·격리 DB 스키마·SDK HTTP 모의 응답으로 실행했다. `사번`이 포함된 합성 DOCX가 AI에 **1회 전송되고 초안 1개로 저장**되는 것을 재현했다. 기대는 둘 다 0이다. `/tmp/wiki-viki-s4-20260921/baseline.mjs`, `red-result.json`에 절차/결과를 남겼다. 사용자 원본·운영 DB는 사용하지 않았다.

### 변경과 검증

- `scripts/lib/seed-files.js`: 파일명을 파싱 전에, 추출 원문을 AI 호출 전에 공통 로컬 검사한다. 의미 검사 통과 뒤에만 구조화하며 거부·장애·유효하지 않은 응답은 생성/저장을 중단한다. 생성 제목·본문·별칭과 JSON 구조도 검증한다. 지원 형식은 기존 DOCX/PDF로 유지하고 `parseUpload`, `validateDraftDocuments`를 재사용한다.
- `src/lib/server/semantic-governance.js`: 앱과 CLI가 같은 의미 검사 구현을 사용하도록 환경 주입과 검사 본체만 분리했다. `openai.js`의 기존 공개 함수는 유지한다. 기존 로컬 선검사·응답 검증·45초 제한·자동 재시도 0회·저장 비활성화는 그대로다. 시드 구조화에도 지시/자료 분리, JSON 스키마, 90초 제한·자동 재시도 0회·저장 비활성화를 적용했다.
- `scripts/seed.js`: 파일 처리 모듈을 연결하고 임포트 시 실행되지 않도록 진입점을 구분했다. 키 없는 기본 초안에는 실제 `semantic.skipped`를 저장하고 안내한다. 파일별 오류 뒤 다음 파일을 계속 처리하며 파일명·원문·SDK/DB 내부 오류를 로그에 남기지 않는다. 실제 개수와 처리 단계를 보고하고 처리 실패가 있으면 마지막 종료 코드는 1이다. 차단만 있으면 0이며 차단 개수를 별도 출력한다.
- `tests/seed-governance.test.js`, `tests/seed-governance-db.test.js`, `tests/fixtures/seed-source.js`: 실제 DOCX/PDF 파서·OpenAI SDK·PostgreSQL을 실행했다. 민감 파일명/원문 7개 차단 시 AI/초안 저장 0회, 키 없는 두 형식의 기본 초안과 원본 바이트 보존, 의미 거부/잘못된 결과/429의 후속 호출 중단, 생성 결과 검증, 오류 로그 보호, 손상/빈 파일 뒤 정상 파일 처리, DB trigger 실패 뒤 기존 초안 보존을 확인했다.
- 실제 Bun 자식 프로세스에서도 원본 3개 중 안전한 초안 1개·차단 1개·파싱 실패 1개와 종료 코드 1을 확인했다. 복사한 시드 코드, 빈 환경 파일, 테스트용 DB 연결과 HTTP 차단 preload만 사용했다. 생산 CLI의 파일 탐색 경로와 진입점까지 실행한 검사다.
- 최종 `RUN_GOVERNANCE_DB_TESTS=1 bun run test` (격리 연결 지정): **100/100 통과, 생략 0**. 기본 `bun run test`: **44개 통과, DB 묶음 5개 생략**. 공유 의미 검사 분리 후 기존 업로드·검색·초안·편집·되돌리기·토론 회귀 검사도 통과했다.
- `bun run check`: 오류/경고 0. `bun run build`: 앱 컴파일·Vercel 패키징 통과. `bun run lint`: **기존 `scripts/migrate.js` 1개만 실패**. 변경한 seed 포맷은 정리했으며 변경 파일 Prettier·`git diff --check` 통과. 새 검사 실패 없음. README에 검사 순서·로그·종료 코드·검증 조건을 기록했다.
- UI 코드는 변경하지 않아 이번에는 CLI/DB 검증을 수행하고 새 시각 검증은 생략했다. 실제 OpenAI 호출·판단 품질, Neon HTTP 연결, Node.js 22 및 배포는 미검증이다. SDK 전송은 모의 응답, DB와 원본 파싱·Bun 명령은 실제 실행으로 구분한다.

### 문제 조사와 다음 작업

- CLI 테스트 첫 실행은 Bun 옵션을 `run` 앞에 공백 값으로 전달해 도움말만 출력하고 종료 코드 0으로 끝났다. `bun --help`와 argv 진단으로 확인하고 `bun run --preload=... --env-file=... scripts/seed.js`로 고쳤다. 검사나 종료 코드 기준을 낮추지 않고 실제 CLI 실행을 다시 검증했다.
- 파일 처리에서 기존 초안은 보존하지만, 기본 문서 초기화 SQL은 여전히 `ON CONFLICT (slug) DO UPDATE`로 사용자 편집을 덮어쓰고 리비전은 별도로 저장한다. S4는 외부 전송 차단 단위로 완료하며 이 기존 데이터 손실 위험은 D0으로 분리한다. 데이터 손실 방지 우선순위에 따라 D1보다 먼저 처리한다. 원본 재실행 시 새 초안이 중복 생성되는 기존 동작도 구분해 기록한다.
- **다음 반복: D0 — 기본 정책/도움말 시드의 재실행이 기존 문서·별칭·리비전을 덮어쓰지 않도록 보존형 초기화를 구현하고, 문서/첫 리비전의 중간 실패 원자성 및 동시 재실행을 격리 PostgreSQL에서 검증한다.** 이미 제공하는 `seed:prototype`의 보호와 혼동하지 않는다.
- 로그는 `/tmp/wiki-viki-s4-20260921/`에 보관한다. 잔여 테스트 스키마를 확인하고 격리 PostgreSQL을 종료한다. 재개 시 `/tmp/wiki-viki-s3a-20260921/postgres`를 루프백 55437에서 시작할 수 있다. 전체 P0는 미완료이며 GOAL 본문/체크박스, 운영 데이터·실제 환경 파일·원격 저장소·배포는 변경하지 않았다.

## 반복 8 — 기본 시드의 데이터 보존과 원자성 (2026-09-21)

- 목표 파일·GOAL·README·설정·git 상태·적용 AGENTS 부재를 재확인했다. 사용자 GOAL과 이전 반복 변경을 보존했다. D0은 시드 재실행의 실제 덮어쓰기 위험 때문에 기록된 순서대로 D1보다 먼저 처리했다.
- 변경 전: 격리 DB 포함 테스트 **100/100**, 기본 테스트 **44개 통과/DB 묶음 5개 생략**, check 오류/경고 0, 빌드 통과. lint는 기존 `scripts/migrate.js` 포맷 실패 1개. 검사 로그 폴더 생성과 병렬 lint 시작이 경합해 첫 로그 리다이렉션이 실패했으며, 폴더 생성 후 다시 실행해 실제 기존 lint 상태를 확인했다.
- 회귀 검사를 먼저 실행해 기존 코드의 편집 덮어쓰기·이력 중복·제목 충돌 오류·별칭 주소 가림·중간 실패 후 부분 저장을 재현했다. 신규 8개 시나리오와 상위 묶음이 모두 실패했다 (`red-test.log`). 검사 기준은 유지하고 실제 시드 코드를 수정했다.
- `scripts/seed.js`: 기본 5개 문서는 기존 제목·슬러그·별칭이 있으면 건너뛴다. 사용자 행을 갱신하지 않으며 새 문서에만 첫 리비전을 만든다. 기본 문서·리비전·환영 공지 전체를 한 트랜잭션으로 저장한다. 공지 내용과 기존 관계 데이터도 보존한다. 생성/건너뜀 개수는 실제 INSERT 결과에서 계산한다.
- 잠금은 문서·별칭·공지 테이블의 짧은 기본 초기화 구간에만 적용했다. 별칭 검사와 문서 INSERT 사이의 변경 및 고유 제약이 없는 공지의 동시 중복을 막고, 시드 전용 advisory lock에 참여하지 않는 진행 중 편집도 기다린다. 잠금 대기 제한은 10초이며 오류는 롤백 후 기존 CLI의 안전한 오류/종료 코드 1 경로로 전달한다. 파일 읽기·AI·초안 생성은 커밋 뒤 기존 파이프라인에서 수행해 DB 잠금 안에 외부 작업을 넣지 않는다. 스키마·의존성 변경 없음.
- `tests/seed-preservation-db.test.js`: 실제 PostgreSQL 17의 고유 테스트 스키마에서 사용자 본문·작성자·정밀 시각·별칭·전체 이력·토론·초안·공지의 재실행 전후 동일성을 확인했다. 제목/주소/별칭 충돌, 새 문서당 첫 리비전 1개, 실제 생성 개수도 검증했다. 리비전 중간 INSERT 및 마지막 공지 INSERT trigger 실패 시 전체 묶음 롤백과 재실행 성공을 확인했다.
- 동시성 검사는 연결 하나의 큐를 사용하지 않았다. 서로 다른 backend PID의 클라이언트로 두 시드를 실제 겹쳐 실행하고 `pg_stat_activity`의 잠금 대기를 확인한 뒤 해제했다. 문서/리비전 각 5개·공지 1개 유지, 진행 중 사용자 편집 보존, 먼저 커밋한 별칭을 기본 문서가 가리지 않음까지 확인했다. 일반 편집/게시 상호 간 충돌 보호 완료를 뜻하지 않으며 D1/D3에 남긴다.
- 변경 후 `RUN_GOVERNANCE_DB_TESTS=1 bun run test` (격리 연결 지정): **109/109 통과, 생략 0**. 기본 `bun run test`: **44개 통과, DB 묶음 6개 생략**. S4의 실제 Bun CLI 및 나머지 콘텐츠 보호 회귀 검사도 포함해 통과했다. `bun run check`: 오류/경고 0. README에 보존·롤백·재실행 동작과 실제 DB 검증 절차를 갱신했다.
- `bun run build`: 앱 컴파일·Vercel 패키징 통과. `bun run lint`: 기존 `scripts/migrate.js` 1개만 실패. 이번 코드·테스트·README의 Prettier와 `git diff --check` 통과. 새로 남은 검사 실패 없음. 테스트 스키마 0개를 SQL로 확인한 뒤 검증용 PostgreSQL을 종료했다.
- UI 변경이 없는 CLI/DB 단위로 새 브라우저 검증은 생략했다. 실제 OpenAI 호출·Neon 연결·Node.js 22 실행·배포는 미검증이다. 원본 파일 재처리 시 초안이 중복 생성되는 기존 동작은 이번 기본 문서 보존과 구분해 남긴다.
- **다음 반복: D1 — 초안 새 게시에서 문서·첫 리비전·별칭·초안 상태를 원자적으로 반영하고, 같은 초안 재시도/동시 게시 및 제목·주소·별칭 충돌로 기존 문서를 덮어쓰지 않도록 보호한다.** 현재 서버 액션과 데이터 접근 코드를 재확인하고 격리 DB의 중간 실패·독립 연결 동시성 검증을 먼저 준비한다.
- 증거는 `/tmp/wiki-viki-d0-20260921/`에 보관한다. 재개 환경은 `/tmp/wiki-viki-s3a-20260921/postgres`의 격리 PostgreSQL(루프백 55437)이다. 전체 P0는 미완료이며 GOAL 체크박스·운영 데이터·실제 환경 파일·원격 저장소·배포를 변경하지 않았다.

## 반복 9 — 초안 새 게시의 원자성·충돌·검토 버전 보호 (2026-09-21)

- 이전 반복은 검증된 D0 진행으로 이어받았다. 목표 파일·GOAL·README·설정·git 상태·AGENTS 부재와 실제 게시 경로를 재확인했다. 시작 검사: DB 포함 **109/109**, 기본 **44개 통과/DB 묶음 6개 생략**, check 오류/경고 0, 빌드 통과, lint 기존 `scripts/migrate.js` 1개 실패.
- 기존 게시가 `saveDocument`의 덮어쓰기, 별칭 upsert, 초안 상태 UPDATE로 분리되어 있었다. `src/lib/server/draft-publication.js`와 `drafts/+page.server.js`에서 새 게시만 분리해 INSERT로 저장한다. 기존 제목·슬러그·별칭 또는 새 별칭과 기존 주소가 충돌하면 409로 중단하고 모든 기존 행을 보존한다. 같은 주소의 중복 별칭과 자기 주소 별칭은 추가하지 않되 원래 초안 배열은 보존한다.
- Neon의 기존 `sql.transaction(callback, { isolationLevel: 'ReadCommitted' })`로 문서·첫 리비전·별칭·게시 상태를 묶었다. 문서/별칭 테이블 잠금 뒤 별도 SQL의 새 스냅샷으로 충돌을 검사하며, 초안의 조건부 UPDATE로 동일 초안 동시 게시를 막는다. 10초 잠금 대기 제한과 행 개수 검증으로 일반 DB 오류 및 INSERT를 조용히 건너뛰는 trigger에서도 부분 게시를 롤백한다. AI 검사는 트랜잭션 전에 끝낸다. 새 스키마·의존성 없음.
- 화면의 검토 토큰은 PostgreSQL이 직렬화한 초안 전체 상태(ID 제외)의 SHA-256이다. 날짜를 JavaScript Date로 재직렬화하지 않아 마이크로초 변경도 구분한다. 게시 요청 전 토큰 검사와 저장 시 원본 JSONB 비교로 제목·본문·별칭·출처·편집자·검사 정보 변경/삭제를 감지한다. 오래된 검사 결과로 다른 버전을 blocked로 바꾸지도 않는다. 기존 governance 부가 정보는 보존하고 게시 출처 정보를 덧붙인다. 이미 처리한 재시도는 추가 저장 없이 409를 반환한다.
- 초안 선택/게시 ID를 문자열로 처리해 인접 BIGINT ID를 구분했다. 조회 실패의 내부 오류를 페이지 데이터에 싣지 않고, 게시 DB 오류도 안전한 안내로 반환한다. 일반 초안 수정/삭제 액션의 전체 동시성·입력 복구 확장은 E4/D4에 남긴다.
- `drafts/+page.svelte`: 게시 검토 버전 전달, 처리 중 입력·중복 행동 제한, 충돌 안내, 최신 초안 재열기 링크를 연결했다. 오래된 검토 상태는 경고로 표시하고 명시적으로 다시 열기 전 저장·삭제·게시를 비활성화한다. 검토 실패 뒤 기존 화면 값을 유지하며, DB 저장 실패 후 같은 초안을 재시도할 수 있다.
- `tests/draft-publication-db.test.js`의 **32개 시나리오와 상위 묶음(33개)** 통과: 정상 원자적 게시/반복, BIGINT 선택, 5종 주소·제목·별칭 충돌, 4개 테이블의 예외/INSERT 생략 롤백·재시도, 누락/오래된 검토 토큰, 검사 중 9종 변경·삭제, 동일/서로 다른 초안 동시 게시, 진행 중 수정·별칭 작성과의 경합을 검증했다. 실제 독립 PostgreSQL backend PID와 잠금 대기를 확인했다.
- `tests/fixtures/governance-database.js`는 Neon 트랜잭션 콜백을 같은 SQL의 실제 PostgreSQL 트랜잭션으로 실행하며 동시성 검사에만 연결 수를 늘린다. 별도 사례에서는 실제 Neon SDK가 직렬화한 HTTP 요청의 쿼리/매개변수와 격리 수준을 받아 로컬 PostgreSQL에서 실행했다. 외부 Neon 서버 연동 검증과는 구분한다. 기존 `draft-governance-db.test.js`는 실제 load의 검토 토큰을 제출하도록 갱신했고 모든 콘텐츠 보호 기준을 유지했다.
- 최종 DB 포함 `RUN_GOVERNANCE_DB_TESTS=1 bun run test`(격리 연결 지정): **142/142 통과, 생략 0**. 기본 `bun run test`: **44개 통과/DB 묶음 7개 생략**. `bun run check`: 오류/경고 0. `bun run build`: 앱 컴파일·Vercel 패키징 통과. `bun run lint`: 기존 `scripts/migrate.js`만 실패. 변경 파일 Prettier·`git diff --check` 통과.
- 실제 Chrome/SvelteKit/격리 PostgreSQL: `node /tmp/wiki-viki-d1-20260921/browser.mjs`. 360/768/1440px × 라이트/다크에서 제목 충돌→수정 저장→게시, 처리 중 비활성, 키보드 Enter 제출, 빈 목록, 가로 넘침 없음 확인. 오래된 버전의 재검토 제한, 실제 revision trigger 실패 후 입력/DB 보존·재시도 성공, JavaScript 없는 기본 폼의 버전 거부→재열기→게시도 확인했다. 외부 HTTP 시도 0회. 데스크톱/모바일 오류·재검토 스크린샷을 직접 확인했다.

### 조사한 실패와 남은 범위

- Neon 전송 경계 테스트 첫 실행은 PostgreSQL의 SET/LOCK 결과에 `columns`가 없는 것을 테스트 응답 변환기가 가정하지 않아 실패했다. 빈 컬럼 배열로 변환해 실제 SDK/DB 왕복을 다시 통과했다. 제품 SQL 실패가 아니며 검증 기준은 유지했다.
- 기본 테스트와 DB 포함 전체 테스트를 두 프로세스로 동시에 실행하자 공유 `node_modules/.vite/deps` 정리 중 `ENOTEMPTY`가 발생했다. 실패 로그를 `test-cache-race.log`로 보존했다. 기본 테스트의 44개 통과를 확인하고 두 프로세스가 종료된 뒤 DB 포함 검사를 단독으로 다시 실행해 최종 142개를 재확인했다. 후속 반복에서도 전체 테스트 명령끼리 및 브라우저/빌드는 순차 실행한다.
- 최초 화면 확인에서 버전 충돌 후에도 초록색 게시 가능 안내가 남는 것을 발견했다. 경고/재검토 상태와 버튼 제한을 고친 뒤 자동 검사·빌드·브라우저 전체 시나리오를 다시 통과했다.
- 실제 OpenAI 호출·판단 품질, 원격 Neon HTTP 서버, Node.js 22 런타임·배포는 미검증이다. 이번 화면 변경의 반응형/폼 조작은 확인했으며 전체 제품의 200% 확대·키보드 탐색 기준은 U 작업에 남긴다. GOAL의 P0 체크박스는 전체 기준 미완료로 유지한다.
- **다음 반복: D3 — 일반 직접 편집의 제목·주소·별칭 충돌과 별칭 제거를 문서/리비전과 함께 원자적으로 저장하고, 오래된 버전의 동시 저장을 거부하면서 입력을 보존한다.** 실제 편집 코드는 여전히 upsert로 본문/별칭 연결을 덮어쓸 수 있다. 현재 데이터 손실 위험을 먼저 줄이고 AI 통합이 사용할 저장 보호를 마련하기 위해, 아직 실제 경로가 없는 D2의 통합 기능 보완보다 앞당긴다. D2 범위는 축소하거나 완료 처리하지 않는다.
- 증거는 `/tmp/wiki-viki-d1-20260921/`에 보관했다. 테스트 스키마 0개를 SQL로 확인했다. 재개용 PostgreSQL은 `/tmp/wiki-viki-s3a-20260921/postgres`(루프백 55437)이며 종료 후 필요할 때 시작한다. 운영 데이터·환경 파일·원격 저장소·배포·사용자 GOAL 본문은 변경하지 않았다.

## 반복 10 — 직접 편집의 원자성·별칭·동시 저장 보호 (2026-09-21)

- 이전 D1은 구현/검증 진행으로 분류했다. 목표 파일·요구사항·README·설정·git 상태·AGENTS 부재와 현재 편집/되돌리기 호출을 재확인했다. 기록한 순서대로 D3을 시작했고 직접 편집(D3a)과 이력 화면 검토 버전(D3b)을 분리했다. D2의 AI 통합 범위와 나머지 P0는 그대로 남긴다.
- 시작 검사: DB 포함 **142/142**, 기본 **44개 통과/DB 묶음 7개 생략**, check 오류/경고 0, 앱 빌드·Vercel 패키징 통과. lint는 기존 `scripts/migrate.js` 1개 실패.
- 변경 전 소스를 임시 경로로 복사해 실제 격리 DB에서 별도 실행했다. 기존 주소 충돌이 303 성공으로 처리되어 다른 문서의 별칭이 이전됐고, 별칭 INSERT 실패(500) 후에도 본문 변경/새 리비전이 남았다. 증거: `/tmp/wiki-viki-d3a-20260921/baseline-bugs.json`. 실제 작업 파일을 이전 코드로 되돌리지 않았다.
- `src/lib/server/document-write.js`: 문서와 별칭을 같은 조회 스냅샷으로 읽고 정확한 DB 직렬화 값의 SHA-256을 검토 버전으로 사용한다. 저장은 Neon 트랜잭션의 문서/별칭 잠금 뒤 버전을 재확인하고 문서·리비전·별칭 추가/표시 이름 수정/제거를 원자적으로 반영한다. 예외 및 trigger의 조용한 행 생략도 행 개수 검증으로 전체 롤백한다. 새 문서는 INSERT만 허용하고 기존 문서 ID·대표 주소·이력·토론을 보존한다. AI 호출은 잠금 밖에서 끝낸다.
- 유지한 별칭의 ID를 재생성하지 않는다. 쉼표가 든 별칭은 한 줄로 입력하며, 이름을 유지한 기존 별칭은 현재 slug 생성 규칙과 저장 주소가 달라도 원래 URL을 유지한다. 같은 표시 이름을 가진 기존 별칭 여러 개도 입력에 남아 있으면 모두 보존한다. 입력에서 뺀 별칭만 삭제하며 다른 문서의 주소·제목·별칭으로 이전하지 않는다.
- `edit/[slug]/+page.server.js`와 화면: 문서 ID/검토 버전을 함께 제출한다. 이전 버전·검사 중 변경·삭제 후 재생성·진입 별칭의 대상 변경을 감지하면 입력을 그대로 돌려준다. 현재 저장본의 제목·본문·별칭을 읽기 전용으로 보여 주고 명시적인 비교 후 저장에서만 새 검토 버전을 적용한다. 그 사이 또 바뀌면 다시 409로 거부한다. 기본 폼도 원래 입력과 버전을 유지한다. 비교 중 일반 저장/삭제, 요청 중 입력/중복 제출을 제한했다.
- `wiki.js`의 기존 `saveDocument` 이름은 새 공통 저장 함수로 연결하며 검토 상태 없는 무조건 upsert를 제거했다. 같은 함수를 쓰는 `history/[slug]/+page.server.js`도 검사 시작 시 문서/별칭 스냅샷을 넘기도록 최소 변경했다. 검사 중 새 편집을 덮어쓰지 않고 기존 별칭 URL도 유지한다. **이력 화면을 연 시점의 버전 검사는 아직 없으므로 D3b는 미완료다.** 영구 삭제/휴지통은 D4에 남긴다.
- `tests/edit-preservation-db.test.js`: **32개 시나리오+상위 묶음(33개)** 통과. 문서·별칭 ID/URL·과거 리비전/토론 유지, 별칭 전체 제거·쉼표/기존 주소 보존, 5종 충돌, 문서 INSERT/UPDATE·리비전 INSERT·별칭 INSERT/UPDATE/DELETE 각각의 예외/행 생략 롤백·재시도, 마이크로초 버전, 검사 중 변경·삭제/재생성, 독립 backend 동시 저장, 초안 게시와의 별칭 경합, 되돌리기 공유 경로를 실제 PostgreSQL에서 검증했다.
- 기존 `edit-governance-db.test.js`는 실제 load의 문서 ID/버전을 제출하도록 갱신했다. 기존 별칭이 입력에서 빠져도 남아야 한다던 검사 하나는 요구사항의 명시적 별칭 제거에 맞춰 제거 여부와 새 별칭 목록을 확인하도록 변경했다. 콘텐츠 보호·원문 입력 보존·기존 이력 유지 기준은 그대로다.
- 최종 `RUN_GOVERNANCE_DB_TESTS=1 bun run test`(격리 연결 지정): **175/175 통과, 생략 0**. 기본 `bun run test`: **44개 통과/DB 묶음 8개 생략**. `bun run check`: 오류/경고 0. `bun run build`: 앱 컴파일·Vercel 패키징 통과. `bun run lint`: 기존 `scripts/migrate.js`만 실패. 변경 파일 Prettier·`git diff --check` 통과. 스키마·의존성 변경 없음.
- 실제 Chrome/SvelteKit/격리 DB: `node /tmp/wiki-viki-d3a-20260921/browser.mjs`. 360/768/1440px × 라이트/다크에서 동시 수정 거부→내 입력/현재 저장본 비교→Tab·Enter 제출→기존 대표 URL 이동을 확인했다. 쉼표 별칭과 ID 유지·제거 요청 반영, 요청 중 비활성, 가로 넘침 없음도 확인했다. 제목 충돌, 실제 별칭 DELETE trigger 실패 후 완전 롤백·입력 보존·재시도, 새 문서/DB 조회 오류 화면을 검증했다. JavaScript 없는 폼에서 두 번 연속 변경된 버전을 거부한 뒤 비교 후 저장까지 통과했다. 외부 HTTP 시도 0회.

### 조사·보완과 다음 작업

- 변경 전 사본 실행은 임시 폴더에서 `@sveltejs/kit`을 찾지 못해 처음 실패했다. 임시 폴더에 저장소의 기존 node_modules 연결을 추가해 원래 코드의 실제 DB 실패 조건을 재현했다. 원본 환경 파일·운영 DB는 사용하지 않았다.
- 비교 영역을 초점 가능한 pre로 만들었을 때 Svelte 접근성 경고가 발생했다. 검사 예외를 추가하지 않고 읽기 전용 textarea/label로 바꿔 키보드 선택·스크롤과 접근성 검사를 통과했다. 화면 확인에서 기본 브라우저 폭/스타일을 발견해 기존 `.field` 디자인을 적용하고 경고를 비교 영역보다 먼저 배치했다. 최종 화면과 입력란 폭을 다시 검증했다.
- 기존 별칭 주소/표시 이름이 다른 데이터도 보존하도록 마지막으로 보완했다. 회귀 테스트 삽입 스크립트가 포맷 이후 들여쓰기를 잘못 가정해 첫 삽입에 실패한 것은 확인 후 수정했으며, 실제 추가된 두 사례를 포함한 최종 175개 통과로 검증했다.
- 실제 OpenAI·원격 Neon HTTP 서버·Node.js 22·배포는 미검증이다. 전체 200% 확대/탐색 기준은 U 작업, 이동·새로고침 복구는 E3/E4에 남긴다. 이번 직접 편집 단위 완료를 전체 P0 완료로 해석하지 않는다. GOAL 본문/체크박스는 변경하지 않았다.
- **다음 반복: D3b — 이력 화면에서 검토한 문서 ID·버전을 되돌리기 요청에 연결하고, 화면을 연 뒤의 수정·삭제/재생성·같은 되돌리기 재시도를 거부한 뒤 최신 이력을 재검토하도록 구현·검증한다.** 이번에 만든 공통 문서/별칭 스냅샷과 원자적 저장을 사용한다.
- 로그·스크린샷·브라우저 결과는 `/tmp/wiki-viki-d3a-20260921/`. 잔여 테스트 스키마 0개 확인. 검증 후 PostgreSQL을 종료하며 후속 실행은 `/tmp/wiki-viki-s3a-20260921/postgres`(루프백 55437)를 시작하면 된다. 운영 데이터·환경 파일·원격 저장소·배포·사용자 기존 변경은 보존했다.

## 반복 11 — 이력 검토 버전·중복 되돌리기 보호 (2026-09-21)

- 이전 D3a를 검증된 진행으로 이어받고 목표 파일·GOAL·README·설정·git 상태·AGENTS 부재를 재확인했다. 시작 검사: 격리 DB 포함 **175/175**, 기본 **44개 통과/DB 묶음 8개 생략**, check 오류/경고 0, 빌드 통과. lint는 기존 `scripts/migrate.js` 한 파일만 실패했다.
- 변경 전 history 서버 사본을 별도 경로에서 실제 격리 DB로 실행했다. 화면을 연 뒤 저장된 본문을 오래된 되돌리기가 303으로 덮어쓰고, 동일 요청 재전송이 리비전을 또 추가하는 문제를 재현했다. 증거: `/tmp/wiki-viki-d3b-20260921/baseline-bugs.json`. 작업 파일을 이전 코드로 되돌리지 않았다.
- `history/[slug]/+page.server.js`: 화면에서 검토한 문서 ID·문서/별칭 버전·복원 리비전 버전을 제출한다. 실제 DB 직렬화 값의 SHA-256으로 비교하며 누락/잘못된 토큰, 화면 이후 변경·삭제/재생성·별칭 대상 이전·리비전 수정/삭제·재시도를 외부 검사 전에 거부한다. BIGINT 차이 선택을 문자열로 처리하고 존재하지 않는 선택에는 가짜 차이를 만들지 않는다. 같은 시각의 이력도 ID로 순서를 고정한다.
- `document-write.js`: 기존 원자적 저장에 복원 리비전 행의 공유 잠금과 정확한 스냅샷 재확인을 추가했다. AI 검사 중 또는 잠금 대기 중 원본이 바뀌면 문서·새 리비전·별칭 모두 변경하지 않는다. 직접 편집은 기존 저장 보호를 유지하며 스키마·의존성 변경은 없다.
- `history/[slug]/+page.svelte`: 오래된 검토 상태의 모든 되돌리기를 비활성화하고 최신 이력 재열기를 제공한다. 로딩 중 중복 제출 제한과 원래 차이 선택을 유지한다. 기본 POST 후 문서 삭제·조회 장애가 생겨도 안전한 액션 안내를 표시한다. 검사/DB 실패 응답에도 원래 검토 버전을 보존하여, 기본 폼의 후속 load가 새 버전을 가져왔을 때 이를 검토 완료로 자동 승격하지 않는다.
- `tests/history-preservation-db.test.js` **20개 시나리오+상위 묶음(21개)** 통과: 재전송/내용이 같은 복원, 문서·별칭·리비전 변경/삭제, 마이크로초 변경, 같은 URL 재생성, 별칭 대상 이동, 잘못된 토큰, 검사 중 원본 변경/삭제, 독립 DB 연결의 동시 동일 요청, 원본 행 잠금 대기 후 변경 재확인, BIGINT 선택, 기본 POST 장애·실패 후 버전 보존. 기존 history governance와 edit preservation 테스트는 실제 load의 토큰을 제출하도록 갱신하고 기존 보호 기준을 유지했다.
- 최종 `RUN_GOVERNANCE_DB_TESTS=1 bun run test`(격리 로컬 연결 지정): **196/196 통과, 생략 0**. 기본 `bun run test`: **44개 통과/DB 묶음 9개 생략**. `bun run check`: 오류/경고 0. `bun run build`: 앱 컴파일·Vercel 패키징 통과. `bun run lint`: 기존 `scripts/migrate.js`만 실패. 이번 변경 파일 Prettier와 `git diff --check`도 통과했다.
- 실제 Chrome/SvelteKit/격리 PostgreSQL: `node /tmp/wiki-viki-d3b-20260921/browser.mjs`. 360/768/1440px × 라이트/다크에서 차이 표시→동시 변경 거부→Tab·Enter로 최신 이력 재검토→정상 복원, 요청 중 모든 되돌리기 비활성, 대표 URL 유지, 가로 넘침 없음 확인. 실제 리비전 INSERT trigger 실패 후 완전 롤백·재시도, 빈 이력/없는 문서도 확인했다. JavaScript 없는 기본 폼에서 원본 변경·삭제·같은 URL 재생성·조회 장애 및 검사 실패 중 다른 편집이 저장되는 경우를 확인했다. 마지막 사례만 의미 검사 HTTP 경계의 모의 응답 1회이며 실제 외부 전송은 0회다. 모바일/데스크톱 충돌·DB 오류·삭제·실패 후 재검토 스크린샷을 직접 확인했다.

### 조사한 실패·남은 검증·다음 작업

- 첫 관련 테스트 실행의 기존 DB 조회 장애 사례가 500 대신 409를 반환했다. 새 테스트 헬퍼가 장애가 난 후 load를 호출하여 가짜 기본 버전을 제출한 것이 원인이다. 실제 사용자 순서대로 장애 주입 전에 검토 요청을 만들어 제출하도록 고쳤고 500/AI 0회 검증을 유지했다. 최종 전체 검사에 미해결 회귀는 없다.
- 최종 코드 검토에서 기본 폼의 실패 후 load가 최신 버전으로 바뀌는 경계 조건을 발견해 원래 검토 버전/재검토 제한을 보완했다. 관련 실제 DB 사례와 브라우저의 검사 거부 중 동시 변경 사례를 추가하고 테스트·check·빌드·브라우저를 다시 통과했다. 삭제 화면의 중복 제목·불필요한 검사 안내도 정리 후 재확인했다.
- 실제 OpenAI 판단 품질·원격 Neon HTTP 서버·Node.js 22 런타임·배포는 미검증이다. 전체 화면의 200% 확대/탐색은 U 작업에 남긴다. **D3b만 완료했으며 P0 전체는 미완료다.** GOAL 본문과 체크박스는 보존했다.
- **다음 반복: D4의 최소 선행 작업 — 마이그레이션 실행기에 적용 이력·순차 실행·원자적 실패 복구를 추가하고, 기존 데이터가 있는 격리 DB에서 재실행·중간 실패·동시 실행을 검증한다.** 현재 실행기는 `001_initial.sql`만 매번 실행하며 적용 이력이 없다. 관계를 보존하는 휴지통 스키마를 안전하게 도입하기 위해 먼저 필요하다. 기존 영구 삭제의 데이터 손실 위험을 줄이는 D4를 아직 실제 경로가 없는 D2보다 먼저 진행하며, AI 통합 요구를 축소하거나 완료 처리하지 않는다.
- 로그·스크린샷·브라우저/변경 전 재현 결과는 `/tmp/wiki-viki-d3b-20260921/`. 잔여 테스트 스키마 0개를 확인하고 PostgreSQL을 종료했다. 재개 시 `/tmp/wiki-viki-s3a-20260921/postgres`(루프백 55437)를 시작하면 된다. 운영 데이터·환경 파일·원격 저장소·배포·사용자 기존 변경은 보존했다.

## 반복 12 — 보존형 마이그레이션 실행기 (2026-09-21)

- 이전 D3b는 구현·검증을 완료한 진행으로 이어받았다. 목표 파일·GOAL·README·설정·git 상태·AGENTS 부재와 현재 실행기를 재확인했다. D4를 실행기(D4a)와 실제 휴지통/복구(D4b)로 분리했다. 휴지통 스키마의 안전한 순차 도입에 필요한 최소 선행 작업이며, D2와 나머지 P0 범위는 유지한다.
- 시작 검사: 기본 **44개 통과/DB 묶음 9개 생략**, check 오류/경고 0, 빌드 통과. lint는 기존 `scripts/migrate.js`만 실패했다. DB 포함 전체 검사는 Vite 공유 캐시의 `ENOTEMPTY`로 실패했다. 다른 전체 테스트 프로세스와 겹치지 않아도 파일별 병렬 실행으로 재현됐으므로, 새 기능보다 먼저 `package.json`의 테스트 파일 실행에 `--test-concurrency=1`을 적용했다. 개별 테스트 내부의 독립 DB 연결 동시성 검증은 유지하며, 변경 뒤 기존 **196/196**이 통과했다. 실패 로그는 `baseline-db.log`, 복구 결과는 `baseline-db-serial.log`에 남겼다.
- 변경 전 실행기 사본을 실제 격리 DB에서 실행했다. `002_pending.sql`이 있어도 001만 실행하고 종료 코드 0을 반환하며 적용 이력은 없는 것을 재현했다. 증거: `/tmp/wiki-viki-d4a-20260921/baseline-bugs.json`.
- `scripts/lib/migrations.js`: SQL 파일을 숫자순으로 읽고 중복/잘못된 번호·이름·UTF-8·빈 파일·명시적 트랜잭션 제어를 사전 거부한다. 인용 문자열/함수 본문/중첩 주석 안의 단어는 SQL 제어문으로 오인하지 않는다. 파일의 정확한 바이트 SHA-256, 번호, 이름, 적용 시각을 `schema_migrations`에 기록하며 기존 이력은 파일 목록의 정확한 앞부분과 일치해야 한다. 적용한 파일의 삭제·수정·이름 변경·과거 번호 삽입·다른 배포본 불일치를 자동으로 덮어쓰지 않는다.
- 기존 `postgres`의 Read Committed 트랜잭션으로 이번 실행의 미적용 SQL 전체·DDL·데이터 보충·이력을 함께 커밋한다. DB/스키마별 트랜잭션 advisory 잠금은 이력 테이블 생성 전부터 동시 실행을 직렬화하며, 잠금 대기는 10초로 제한한다. 이력 INSERT 반환과 최종 이력 전체를 확인하여 BEFORE/AFTER trigger가 기록을 생략/제거해도 부분 적용을 취소한다. `001_initial.sql` 원본은 수정하지 않았고 기존 스키마 채택 시 모든 기존 행·ID·URL·관계·정확한 시각·AI 통합 메타데이터가 그대로 유지됨을 검증했다. 새 제품 스키마나 데이터 보충 SQL 자체는 D4b에 남긴다.
- `scripts/migrate.js`: 커밋 후에만 실제 적용/기존 적용 개수를 출력하며 파일·연결·DB 실패는 종료 코드 1과 안전한 안내로 처리한다. DB notice·접속 정보·오류 원문은 출력하지 않는다. 연결 정리에는 상한을 두고 CLI 밖으로 튀는 치명 오류도 안전한 안내 후 즉시 비정상 종료한다. 이 경계는 명령 직접 실행에만 적용하며 import한 모듈의 전역 동작을 바꾸지 않는다. 의존성 교체 없음.
- `README.md`: 적용 이력 도입, 기존 DB 채택, 파일 불변 규칙, 데이터 보충, 전체 트랜잭션/잠금 범위, 이력 조회·실패 복구·재실행 절차를 기록했다. 일반 테스트·린트와 격리 DB 검사 실행법 및 파일 실행 순서의 이유도 보완했다. 휴지통이 구현된 것처럼 설명하지 않는다.
- `tests/migrations.test.js` **4개**와 `tests/migrations-db.test.js` **18개 시나리오+상위 묶음(19개)** 통과. 빈 DB/기존 DB, 숫자 1·2·10 순서, 재실행·체크섬·기존 데이터 보존, 추가 컬럼 보충, 초기/후속 중간 실패, 변경/누락/과거 파일·알 수 없는 이력, 이력 trigger 실패, 독립 backend 동시 실행/서로 다른 배포본, 잠금 만료·재시도를 확인했다. 실제 Bun CLI의 성공/실패/출력과 중단 복구까지 검증했으며 모의 DB 응답은 사용하지 않았다.
- 최종 DB 포함 `RUN_GOVERNANCE_DB_TESTS=1 bun run test`(격리 로컬 연결 지정): **219/219 통과, 생략 0**. 기본 `bun run test`: **48개 통과/DB 묶음 10개 생략**. `bun run check`: 오류/경고 0. `bun run lint`: **전체 통과** — 기존 실패 파일을 이번 대상 실행기 수정과 함께 정리했다. `bun run build`: 앱 컴파일·Vercel 패키징 통과. `git diff --check`·문서/변경 파일 포맷 검사 통과.

### 조사한 실패와 검증 범위

- 첫 연결 강제 종료 검사에서 기존 `postgres` 드라이버의 예약된 쓰기가 닫힌 소켓을 참조하는 비동기 TypeError와 연결 정리 대기를 발견했다. 살아 있는 테스트 프로세스와 DB 세션을 확인하고 해당 테스트 프로세스만 종료한 뒤 고유 테스트 스키마를 정리했다. 같은 종료 조건을 실제 Bun 명령에서도 재현했고, CLI 치명 오류 경계와 정리 상한을 추가했다. 최종 검사는 같은 backend 강제 종료를 실제 Bun 및 Node.js 26 CLI에서 수행하고 안전한 비정상 종료·본문/DDL/이력 롤백·재실행을 확인한다. Bun 프로세스 SIGKILL 중단도 추가로 확인했다. 드라이버 자체를 패치하거나 실패 조건을 생략하지 않았다.
- CLI backend 식별 첫 시도는 URL `options`의 application_name이 드라이버 기본값으로 덮여 검사 대상 PID를 찾지 못했다. 실제 연결 옵션 구성 코드를 확인해 URL의 직접 application_name 매개변수로 격리 스키마 이름을 지정했고, 다른 테스트 연결과 구분한 동일 단절 검증을 통과했다. 실패 로그는 `focused-db*.log`에 보존했다.
- UI 변경이 없는 CLI/DB 작업이므로 이번 반복의 브라우저 시각 검증은 해당 없음으로 구분한다. 기존 앱의 자동 검사와 빌드만 새로 수행했다. 실제 OpenAI·원격 Neon 서버·Node.js 22·배포는 이번에 검증하지 않았고 실제 운영 DB를 실행 대상으로 사용하지 않았다. 전체 P0와 GOAL 체크박스는 여전히 미완료이며 변경하지 않았다.
- **다음 반복: D4b — 기존 문서 한 건을 영구 삭제 대신 휴지통으로 이동하고, 일반 읽기·검색에서 제외한 뒤 원래 ID·주소·리비전·별칭·토론을 보존해 복구하는 흐름을 보존형 마이그레이션·서버·화면·실제 DB 검사로 구현한다.** 삭제 시점 버전 검사와 삭제/복구 동시 요청 및 충돌도 이 수직 단위에 포함한다.
- 증거는 `/tmp/wiki-viki-d4a-20260921/`. 종료 전 잔여 테스트 스키마 0개를 SQL로 확인했고 PostgreSQL을 종료했다. 재개는 `/tmp/wiki-viki-s3a-20260921/postgres`(루프백 55437)를 시작하면 된다. 사용자 GOAL 변경·기존 작업·환경 파일·운영 데이터·원격 저장소·배포는 보존했다.

## 반복 13 — 관계를 보존하는 문서 휴지통과 복구 (2026-09-21)

- D4a의 검증된 마이그레이션 실행기를 이어받고 목표 파일·GOAL·README·설정·git 상태·AGENTS 부재를 재확인했다. 시작 검사: 격리 DB **219/219**, 기본 **48개 통과/DB 묶음 10개 생략**, check 오류/경고 0, lint·빌드 통과. D4b를 사용자 삭제→일반 조회 제외→휴지통 확인→원래 주소 복구의 한 수직 단위로 구현했다.
- 변경 전 edit 서버 사본을 별도 경로의 격리 DB에서 실행해, 확인/검토 버전 없이 삭제가 303으로 처리되고 문서·리비전·별칭·토론이 모두 0행이 되는 연쇄 영구 삭제를 재현했다. `baseline-bugs.json`에 결과를 보관했으며 작업 파일을 이전 코드로 되돌리지 않았다.
- `migrations/002_document_trash.sql`: 삭제 시각·익명 삭제자·삭제/복구 버전 카운터와 상태 제약을 추가했다. 기존 001은 그대로다. 삭제/복구는 원래 행의 상태만 바꾸며 ID·제목·대표 URL·본문·작성/수정 시각·편집자·리비전·별칭·토론·게시 초안 메타데이터를 보존한다. 버전 카운터로 삭제→복구 후 오래된 요청도 거부한다. 기존 제목/주소의 유일성은 유지하여 휴지통 중에도 예약하며, 영구 삭제·자동 비우기는 제공하지 않는다.
- `document-trash.js`와 edit/trash 서버: 기존 Neon 트랜잭션에서 문서/별칭 테이블 잠금 뒤 정확한 문서·별칭 스냅샷을 재확인한다. 삭제/복구 중복·동시 편집·오래된 별칭·복구 충돌은 409, DB 실패는 안전한 500 안내로 처리한다. UPDATE 예외뿐 아니라 trigger의 조용한 행 생략도 전체 롤백한다. 잠금 대기는 10초이며 상태 전환 자체에 AI를 호출하지 않는다.
- edit 화면의 명시적 확인 항목과 원래 입력/검토 버전을 같은 폼으로 제출한다. 보관 대상은 **현재 저장본**이고 미저장 입력은 포함되지 않음을 설명한다. 실패 시 제목·본문·별칭·요약을 보존하고 오래된 삭제는 재검토 전 비활성화한다. `/trash`와 Header 진입점을 추가해 50개씩 탐색·보관 본문 확인·원래 URL 복구를 제공한다. 기본 POST에서도 실패 후 load가 가져온 버전을 자동 승인하지 않는다. 정상·빈·로딩·충돌·조회/저장 오류 상태를 구분한다.
- `wiki.js`, `search.js`, 홈/wiki 라우트는 보관 문서를 일반 읽기·편집·이력·토론·검색/자동완성·AI 근거·최근 변경·지도/관련 목록에서 제외한다. 기존 본문의 링크 텍스트는 지우지 않으며 원래 주소/별칭 진입에는 본문 없는 휴지통 안내를 표시한다. 보관된 대표 주소를 다른 문서의 기존 충돌 별칭이 가로채지 않게 한다. `document-write.js`의 활성 상태 검사와 토론 INSERT의 잠금/생명주기 버전 검사로 AI 처리 중 삭제 및 삭제→복구 경합도 거부한다.
- `scripts/seed-demo.js`는 같은 잠금 안에서 휴지통 문서/제목/주소/별칭을 만나면 항목 전체를 건너뛰도록 최소 보완했다. 원래 스크립트의 **활성 문서 upsert와 별칭 대상 교체 위험은 별도로 발견했으며 아직 해결하지 않았다.** 이를 D0b로 추가해 다음 데이터 보존 작업으로 우선한다. 기본 시드 D0의 완료와 혼동하지 않는다.
- `tests/document-trash-db.test.js` **25개 시나리오+상위 묶음(26개)**: 관계/마이크로초 시각·게시 초안 보존, 전체 공개 조회 제외, 확인·BIGINT·검토 토큰 검증, 새 편집/게시의 예약 주소 충돌, 기존 교차 주소 충돌, 오래된 요청·재전송, 삭제/복구 UPDATE 예외·행 생략·재시도, 독립 backend의 동시 요청/편집 경합, AI 처리 중 편집·되돌리기·토론 거부, 페이지 탐색·DB 오류, 실제 Bun 데모 명령의 휴지통 보호를 확인했다. `migrations-db.test.js`에는 기존 001 데이터에 실제 002 적용·재실행·전후 모든 행 비교·상태 제약 검사 1개를 추가했다. DB fixture도 001만 읽는 대신 실제 마이그레이션 전체를 적용한다.
- 최종 격리 DB 포함 `RUN_GOVERNANCE_DB_TESTS=1 bun run test`(로컬 테스트 연결 지정): **246/246 통과, 생략 0**. 기본 `bun run test`: **48개 통과/DB 묶음 11개 생략**. `bun run check`: 오류/경고 0. `bun run build`: 앱 컴파일·Vercel 패키징 통과. 마지막 UI 문구/제목 수정 뒤 check·빌드와 실제 브라우저 흐름을 다시 확인했다. 서버/DB 코드는 246개 통과 이후 바뀌지 않았다.
- 실제 Chrome/SvelteKit/격리 PostgreSQL: `node /tmp/wiki-viki-d4b-20260921/browser.mjs`. 360/768/1440px × 라이트/다크에서 확인 거부→Space/Tab/Enter 삭제→보관 본문/관계 확인→대표 URL 복구, 처리 중 입력/중복 제출 제한, 빈 휴지통, 가로 넘침 없음 확인. 오래된 삭제의 입력 보존·재검토, 실제 UPDATE trigger 오류·재시도, 오래된 복구, JavaScript 없는 기본 POST, 복구 주소 충돌, 51건 페이지 탐색, 실제 DB 조회 장애를 확인했다. 외부 HTTP 시도 0회. 모바일/데스크톱 정상·DB 오류·충돌·보관 중 입력 화면을 직접 확인했다.

### 조사한 실패·남은 범위·재개

- 기존 토론 테스트는 AI 처리 중 문서 물리 삭제 시 500을 기대했으나, 새 원자적 INSERT는 이를 변경 충돌 409로 처리한다. 입력 유지·토론 0행 기준은 유지한 채 기대 상태만 409로 수정했다. 첫 새 회귀 실행에서는 검색 SQL 자동 치환이 내부 점수 subquery까지 바꿔 문법 오류가 났고 해당 내부 쿼리를 원복했다. 이후 빈 PostgreSQL Result의 배열 프로토타입 비교를 행 수 검사로 고쳤으며, 정상 남은 문서가 본문 참조로 검색되는 조건은 휴지통에만 있는 별칭으로 분리해 정확한 제외를 검사했다. 관련 실패는 `existing-after.log`, `focused-trash*.log`에 보존했다.
- 마지막 화면 확인에서 보관 중인 입력 폼의 중복 h1과 저장 불가 상태의 AI 저장 안내를 정리하고 페이지당 50개라는 설명을 명확히 했다. 진행 표 상태 수정으로 발생한 포맷 실패는 문서 포맷을 정리해 해결했고 `bun run lint` 전체와 `git diff --check`가 통과했다. 검사 삭제/비활성화는 없다.
- 작업 중 `src/routes/+layout.svelte`에 별도 사용자 변경(모달 밖 pointer down/up 확인)이 추가된 것을 발견했다. 해당 파일은 수정하지 않고 보존했으며 이번 휴지통 변경으로 기록하지 않는다. GOAL의 기존 본문/체크박스와 환경 파일도 보존했다.
- 실제 OpenAI 판단 품질·원격 Neon 서버·Node.js 22 런타임·배포는 미검증이다. 브라우저의 전체 200% 확대/공통 UI는 U 작업, 이동/새로고침 임시 복구는 E 작업에 남긴다. 과거 영구 삭제 데이터의 복원을 주장하지 않는다. **D4b 단위만 완료이며 전체 P0는 미완료다.** README에 도입·복구·마이그레이션 선행과 구 서버 쓰기 중지 후 교체 절차를 추가했다. 실제 운영 환경에는 적용하지 않았다.
- **다음 반복: D0b — 일반 데모 시드의 활성 문서 upsert/별칭 대상 교체를 보존형 적재로 바꾸고, 실제 CLI의 사용자 편집·주소 충돌·재실행·중간 실패·독립 연결 동시 실행에서 기존 행과 관계가 유지되는지 검증한다.** 이미 확인한 데이터 손실 위험이므로 D2 AI 통합보다 먼저 수행하며 범위를 그 시드 명령으로 제한한다.
- 로그·브라우저 결과·스크린샷·변경 전 재현은 `/tmp/wiki-viki-d4b-20260921/`. 재개 DB는 `/tmp/wiki-viki-s3a-20260921/postgres`(루프백 55437)의 격리 환경이다. 잔여 테스트 스키마 0개를 SQL로 확인하고 브라우저/서버와 검증용 PostgreSQL을 종료했다. 운영 데이터·원격 저장소·배포는 변경하지 않았다.

## 반복 14 — 데모 시드의 활성 문서·별칭 보존 (2026-09-21)

- 이전 D4b는 구현·검증을 완료한 진행으로 이어받았다. 목표 파일·GOAL·README·설정·git 상태·AGENTS 부재와 실제 데모 명령을 재확인했다. 시작 검사: 격리 DB **246/246**, 기본 **48개 통과/DB 묶음 11개 생략**, check 오류/경고 0, lint·빌드 통과. 새 작업 전부터 남아 있던 검사 실패는 없었다.
- 변경 전 `scripts/seed-demo.js` 사본을 별도 폴더에서 실제 Bun CLI/격리 DB로 실행했다. 사용자 제목·본문·편집자를 덮어쓰고 다른 문서가 소유한 별칭을 이전하면서 종료 코드 0을 반환하는 것을 재현했다. `baseline-bugs.json`에 네 가지 손실 조건을 기록했으며 작업 파일을 이전 코드로 되돌리지 않았다.
- `scripts/seed-demo.js`: 활성/휴지통 문서의 제목·대표 주소·별칭 어느 하나와 충돌하면 해당 예시 전체를 건너뛴다. upsert와 별칭 대상 교체를 제거했고, 사용자가 제거한 별칭도 재생성하지 않는다. 기존 ID·내용·URL·편집자·마이크로초 시각·이력·토론·초안·공지를 변경하지 않는다. 예시 5개의 제목·본문·별칭 원본 블록은 변경 전 사본과 정확히 같음을 확인했다.
- 기존 `postgres` 트랜잭션 하나에 이번 실행의 새 문서·첫 리비전·별칭 전체를 묶었다. 문서/별칭 테이블 잠금 후 새 쿼리로 충돌을 검사해 진행 중 편집/주소 선점과 동시 적재를 보호하며 대기는 10초로 제한한다. INSERT 반환 행과 마지막 실제 행 수를 검증해 trigger의 예외·조용한 INSERT 생략·AFTER 삭제에서도 부분 적재를 취소한다. 기존 커밋 데이터는 유지하고 재실행하면 아직 없는 예시만 생성한다.
- 커밋 후에만 실제 생성·전체 건너뜀·그중 휴지통 보존 개수를 출력한다. 연결/설정/DB 실패는 안전한 안내와 종료 코드 1로 처리하고 원문·연결 문자열·DB 오류 원문을 노출하지 않는다. 기존 마이그레이션 CLI와 같은 치명 오류 경계·연결 정리 상한으로 backend 단절도 비정상 종료한다. README에 데모 명령의 보존·원자성·재실행·검증 방법을 추가했다. 새 스키마·의존성·UI 변경은 없다.
- `tests/demo-seed-preservation-db.test.js` **28개 시나리오+상위 묶음(29개)** 통과: 새 적재/반복, 사용자 편집과 제거한 별칭·정확한 시각·모든 관계 유지, 활성/휴지통 각각의 제목·대표 주소·교차 별칭 충돌 5종, 문서/리비전/별칭 각각의 예외·INSERT 생략·AFTER 삭제와 전체 롤백·재시도, 독립 backend의 동시 Bun 명령, 진행 중 편집/별칭 선점, 실제 10초 잠금 만료·재시도, 적재 중 backend 강제 종료·롤백, 누락/잘못된 설정의 안전한 실패, Node.js 26 CLI 호환성을 확인했다. DB 모의 응답 없이 명령의 종료 코드·출력과 실제 저장 행을 함께 검사했다.
- 최종 `RUN_GOVERNANCE_DB_TESTS=1 bun run test`(격리 로컬 연결 지정): **275/275 통과, 생략 0**. 기본 `bun run test`: **48개 통과/DB 묶음 12개 생략**. `bun run check`: 오류/경고 0. `bun run lint`: 전체 통과. `bun run build`: 앱 컴파일·Vercel 패키징 통과. `git diff --check` 통과. 이번 구현·검증에서 발생하거나 미해결로 남은 검사 실패는 없다.
- UI 변경이 없는 CLI/DB 단위이므로 새 브라우저 시각 검증은 해당 없음으로 구분한다. 실제 OpenAI·원격 Neon 서버·Node.js 22·배포는 이번에 검증하지 않았다. 기존 사용자 `GOAL.md`와 `src/routes/+layout.svelte` 변경, 다른 반복의 코드, 환경 파일·운영 데이터·원격 저장소를 보존했다. **D0b만 완료이며 전체 P0는 미완료다.** GOAL 체크박스는 수정하지 않았다.
- 다음 경로를 실제 코드로 확인하니 초안 수정은 검토 버전 없이 UPDATE하고, 삭제는 숫자로 변환한 ID만으로 실행한다. 둘 다 다른 사람이 저장/검토한 작업을 잃을 수 있으며 AI 통합안의 버전 보호에도 선행된다. D2 전체 계획은 유지하고 최소 선행 작업 D2a를 추가했다.
- **다음 반복: D2a — 초안 수정·삭제에 화면에서 검토한 문자열 ID·버전을 연결하고, 동시 변경/게시·검사 중 경합을 거부하면서 작성 입력을 보존한다.** 삭제는 서버에서도 명시적 확인을 검증하고, 실패 뒤 최신 버전을 자동 승인하지 않게 한다. 실제 DB·기본 POST·브라우저 재검토 흐름을 검증한다. AI 통합의 후보→생성→검토→적용 및 상충 이력은 D2에 그대로 남긴다.
- 증거는 `/tmp/wiki-viki-d0b-20260921/`. 재개 환경은 `/tmp/wiki-viki-s3a-20260921/postgres`(루프백 55437)의 격리 PostgreSQL이다. 종료 전 잔여 테스트 스키마 0개를 SQL로 확인했다. 검증용 PostgreSQL을 종료했으며 다음 반복에서 같은 클러스터를 시작하면 된다.

## 반복 15 — 초안 수정·삭제의 검토 버전과 입력 보존 (2026-09-21)

- 이전 D0b는 구현·검증을 완료한 진행으로 이어받았다. 목표 파일·GOAL·README·설정·git 상태·AGENTS 부재를 재확인했다. 시작 검사: 격리 DB **275/275**, 기본 **48개 통과/DB 묶음 12개 생략**, check 오류/경고 0, lint·빌드 통과. 기존 검사 실패는 없었다. D2a는 AI 통합안의 안전한 저장/적용에도 필요한 초안 변경 보호로 한정한다.
- 변경 전 drafts 서버 사본을 별도 경로의 실제 격리 DB에서 실행했다. 오래된 화면의 수정이 더 최근 본문을 303으로 덮어쓰고, 확인 없는 오래된 삭제도 303으로 처리되어 초안이 사라지는 것을 재현했다. `baseline-bugs.json`에 남겼으며 작업 파일을 이전 버전으로 되돌리지 않았다.
- `src/lib/server/draft-write.js`: 기존 게시 검토 버전과 같은 DB 직렬화 스냅샷/SHA-256을 사용한다. ID는 BIGINT 문자열로 검증한다. AI 검사 뒤 기존 Neon 트랜잭션에서 해당 초안 행을 잠그고 새 SQL 스냅샷으로 버전·미게시 상태를 재확인한 뒤 수정/삭제한다. 잠금 대기는 10초다. 변경/게시/삭제·재시도는 반영하지 않으며 UPDATE/DELETE 예외와 trigger의 조용한 행 생략도 실패로 처리한다. AI 호출 중에는 DB 잠금을 유지하지 않는다.
- `drafts/+page.server.js`: 수정/삭제 모두 화면의 ID·검토 버전을 요구하고, 삭제는 명시적 확인 항목도 서버에서 검사한다. 입력 검증·본문 20만 자 제한·검사/DB 실패·충돌 시 원래 제목·본문·별칭·검토자·버전을 반환한다. 현재 저장본과 비교 후 명시적으로 제출한 새 버전에만 재저장을 허용하며 또 변경되면 다시 거부한다. 초안 ID·원본 파일명·생성 시각·기존 부가 정보는 보존하고, 수정 시 오래된 `governance.merge` 통합안만 초기화한다. 정규식/의미 검사 실패 시 수정 내용을 blocked 초안으로 보관하는 기존 흐름은 유지한다.
- 저장·삭제·게시가 같은 입력 폼을 제출하도록 화면을 연결했다. 기본 POST의 게시에서도 미저장 입력을 먼저 저장하도록 서버에서 확인한다. 기존 게시 트랜잭션은 유지하며 게시 실패 응답에도 원래 입력·버전을 남긴다. 기본 폼의 후속 load가 새 버전을 가져와도 자동 승인하지 않는다. 없는 초안/게시·삭제 이후/조회 오류에서 입력이 사라지거나 거짓 빈 목록을 표시하지 않는다.
- `drafts/+page.svelte`: 충돌 시 현재 저장본을 읽기 전용으로 비교하고 입력을 계속 수정·복사할 수 있다. 일반 저장/게시/삭제는 재검토 전 제한하며, 처리 중 입력·중복 제출을 비활성화한다. 초안 영구 삭제의 확인 항목과 복구 불가 안내를 추가하고, 새 버전 저장 시 이전 삭제 확인을 해제한다. 저장된 초안 미리보기와 미저장 구분을 유지했다. 화면 확인 뒤 충돌 상태의 중복 검사 안내와 존재하지 않는 초안의 저장 안내를 정리했다.
- `tests/draft-preservation-db.test.js` **47개 시나리오+상위 묶음(48개)** 통과: 정상 수정/메타데이터 보존·통합안 무효화, 정확한 미게시 초안 삭제·재시도, ID/버전/확인/입력/20만 자 경계, 정규식 차단 전송 0회, 내용·별칭·검사정보·마이크로초·게시/삭제 후 오래된 요청, 연속 충돌 비교 저장, AI 처리 중 변경/게시/삭제, UPDATE/DELETE 예외·행 생략·재시도, 실제 독립 backend의 수정/삭제/게시 경합, BIGINT 인접 ID, 기본 폼의 미저장 게시 차단·정상 게시, DB 장애를 확인했다. 기존 초안 governance 테스트는 실제 load의 토큰을 제출하도록 갱신하고 기존 외부 전송 차단·blocked 보존 기준을 유지했다.
- 최종 DB 포함 `RUN_GOVERNANCE_DB_TESTS=1 bun run test`(격리 로컬 연결 지정): **323/323 통과, 생략 0**. 기본 `bun run test`: **48개 통과/DB 묶음 13개 생략**. `bun run check`: 오류/경고 0. `bun run build`: 앱 컴파일·Vercel 패키징 통과. 이후 서버/DB 코드는 바뀌지 않았으며 마지막 UI 보완 뒤 check·빌드·브라우저를 다시 통과했다. `bun run lint` 전체와 `git diff --check`도 통과했으며 미해결 회귀는 없다.
- 실제 Chrome/SvelteKit/격리 DB: `node /tmp/wiki-viki-d2a-20260921/browser.mjs`. 360/768/1440px × 라이트/다크에서 충돌→원래 입력/현재 저장본 비교→Enter 재저장, 저장 중 제한, 저장된 미리보기, 확인 없는 삭제 거부→Space/Tab/Enter 확인 삭제→빈 목록을 확인했다. 실제 UPDATE/DELETE trigger 오류·입력 보존·재시도, 오래된 삭제 제한, JavaScript 없는 폼의 연속 충돌/명시적 재검토/미저장 게시 차단/정상 게시/초안 삭제 후 입력 유지, 실제 DB 조회 오류도 확인했다. 외부 HTTP 시도 0회이며 AI 응답 품질 검증과 구분한다. 모바일/데스크톱 충돌·DB 오류·입력 보관 스크린샷을 직접 확인했다.

### 조사한 실패와 남은 범위

- 기존 missing/published 테스트는 해당 상태가 된 뒤 load로 토큰을 구해 빈 토큰을 제출했고 새 검증에서 400이 됐다. 실제 사용 순서대로 검토 요청을 먼저 만들고 게시 이후 409를 확인하도록 수정했으며, 외부 호출 0회 조건을 유지했다. 새 테스트의 초기 다수 실패는 FormData 전송이 LF를 CRLF로 바꾸는데 원본 LF와 문자 단위 비교한 것이 원인이었다. 줄바꿈 표현만 정규화해 입력 보존을 확인하며 다른 문자는 정확히 비교한다. 이 조사로 발견한 실제 기본 폼 게시의 여러 줄 본문 비교도 줄바꿈 표현을 정규화해, 변경 없는 본문이 미저장 수정으로 거부되지 않게 보완했다.
- 첫 브라우저 검사는 향상된 폼의 HTTP 상태 200을 액션 실패 상태 409와 비교해 실패했다. JSON의 실제 액션 상태와 화면/DB 결과를 검사하도록 고쳐 통과했다. 기본 HTML POST는 실제 HTTP 상태를 검사한다. 마지막 UI 자동 치환은 포맷 이후 들여쓰기가 달라 첫 시도가 중단되었고, 현재 파일을 읽어 정확한 위치에 적용했다. 실패 증거는 `existing-drafts.log`, `focused-db.log`, `browser.log`에 보존한다. 검사 삭제/비활성화는 없다.
- 실제 OpenAI 판단 품질·원격 Neon 서버·Node.js 22·배포는 미검증이다. 이동/새로고침 임시 복구는 E4, 현재 입력 미리보기는 E2, 전체 200% 확대/공통 UI는 U 작업에 남긴다. **D2a만 완료이며 D2 전체와 P0는 미완료다.** 기존 GOAL 본문/체크박스, 사용자 `+layout.svelte` 변경, 환경 파일·운영 데이터·원격 저장소·배포는 보존했다.
- **다음 반복: D2의 생성·검토 단위 — 실제 활성 문서의 제목/별칭 후보와 직접 대상 선택, 외부 전송 전 공통 검사, AI 통합안 생성·초안에 저장·검토 화면을 구현한다.** 기존 게시 문서는 생성 시 변경하지 않는다. 키 없음에는 생성 불가를 명시하고 임의 이어붙이기를 하지 않는다. 본문/요약/상충 구조·한도와 생성 중 초안/대상 변경을 검증한다. 명시적 원자적 적용·상충 상세 역사는 그다음 단위로 분리하되 D2 전체 완료 기준을 유지한다.
- 로그·변경 전 재현·브라우저 결과·스크린샷은 `/tmp/wiki-viki-d2a-20260921/`. 재개 환경은 `/tmp/wiki-viki-s3a-20260921/postgres`(루프백 55437)의 격리 PostgreSQL이다. 잔여 테스트 스키마 0개를 SQL로 확인하고 검증 서버·브라우저·PostgreSQL을 종료했다. 다음 반복에서 같은 격리 클러스터를 시작하면 된다.

## 반복 16 — AI 통합안 후보·생성·초안 저장·원안 검토 (2026-09-21)

- 이전 D2a의 구현·검증을 이어받고 목표 파일·GOAL §5/§11.2·README·설정·git 상태·AGENTS 부재를 재확인했다. 시작 검사: 격리 DB **323/323**, 기본 **48개 통과/DB 묶음 13개 생략**, check 오류/경고 0, lint·빌드 통과. 기존 실패는 없었다. 실제 코드는 AI 통합 액션이 없어 D2b 생성·검토와 D2c 최종 편집·적용·역사를 분리했다. D2 전체 완료 기준과 GOAL 본문/체크박스는 변경하지 않는다.
- `src/lib/merge.js`, `server/draft-merge.js`: 초안 제목·별칭을 NFKC/기존 slug 규칙으로 정규화해 활성 문서의 제목·주소·별칭과 비교한다. 하나만 일치하면 기본 선택하고 전체 활성 문서에서 직접 선택할 수 있다. 휴지통은 제외하며 BIGINT ID를 문자열로 유지한다. 후보 응답에 전체 게시 본문을 노출하지 않는다. 잘못된 기존 초안 별칭은 데이터 그대로 보존하고 후보 비교에서 제외한다.
- `server/merge-ai.js`: 전송하는 필드는 기존/새 제목·본문 네 개뿐이다. 전부 공통 로컬 검사를 통과한 뒤 의미 검사→AI 통합을 진행한다. 키 없음·의미 차단/오류·불완전 응답·거부·잘못된 JSON/구조/길이·생성 결과의 민감 내용은 저장하지 않으며 임의 이어붙이기를 하지 않는다. 기존 모델 설정과 Responses API, `store:false`, 자동 재시도 없음·90초 생성 제한을 유지한다. 새 초안 우선·고유 정보/출처/주의/위키 링크 보존을 지시하고, 입력 합계 200,000자·본문 200,000자·요약 2,000자·상충 30개·각 필드 3,000자를 자르지 않고 검증한다. 상충 필드는 요구사항의 `topic/previous/incoming`이다.
- 통합안을 `governance.merge`에만 저장한다. 고유 ID, 실제 생성 시각/모델, 초안 SHA-256 지문, 대상 ID/제목/주소, SQL `updated_at::text`의 정확한 `targetVersion`, 기존 본문·AI 원안·요약·상충을 보관한다. 대상 전체 행·별칭의 `targetFingerprint`도 저장해 동일 수정 시각의 내용 변경·별칭 변경·휴지통 왕복까지 보호한다. `document-write.js`에는 기존 스냅샷을 바꾸지 않고 정확한 수정 시각 반환만 추가했다. 기존 JSONB를 활용하므로 마이그레이션이 필요하지 않다.
- AI 호출 동안 DB 잠금을 유지하지 않는다. 저장 시 기존 게시와 같은 순서로 문서/별칭 테이블→초안 행을 잠근 뒤 새 SQL 스냅샷에서 초안·대상·별칭·활성 상태를 재검사한다. 중복 생성 결과는 한 번만 저장하며 UPDATE 예외·행 생략은 실패한다. 미저장 수정은 서버에서도 생성/버리기 전에 거부한다. 버리기는 초안 검토 버전과 통합안 ID를 확인한다. 생성·재생성·버리기 모두 게시 문서/리비전/별칭·초안 본문/출처·기존 부가 정보를 바꾸지 않는다.
- `drafts/+page.server.js`, `+page.svelte`, `components/MergeReview.svelte`: 후보 선택, 생성·재생성·버리기, 요약, 기존/새 상충 내용, AI 원안 기준 추가/삭제 차이와 읽기 전용 원안을 연결했다. 차이 계산에 시간/편집량 상한을 두고 큰 차이는 두 본문을 표시한다. 실패 시 초안 입력과 기존 통합안을 유지하고, 처리 중 입력과 중복 요청을 제한한다. 키 없음·빈 후보·복수 후보·오래된 통합안·조회 실패를 구분한다. **최종 편집·적용은 D2c에 남아 있으므로 화면/README에도 준비 중으로 명시했다.**
- 새 `tests/draft-merge-db.test.js`는 **38개 시나리오+상위 묶음(39개)**: 후보/직접 선택·휴지통 제외·BIGINT, 정확한 버전/지문·메타데이터 및 기존 문서 보존, 잘못된 ID/버전/미저장 입력, 네 입력 각각의 전송 0회, 키 없음, 의미 차단·형식/길이·거부·중간 오류·민감 출력, 초안/대상/별칭/삭제/복구/게시의 생성 중 경합, 재생성/버리기/재전송, UPDATE trigger 예외·행 생략·재시도, 동시 생성 한 번 저장, 독립 backend 잠금 대기 후 최신 상태 재검사를 실제 PostgreSQL로 검증했다. `tests/merge.test.js` 3개는 후보 정규화·기존 잘못된 별칭·모든 구조/한도 경계를 검증한다.
- 최종 서버/DB 코드의 `RUN_GOVERNANCE_DB_TESTS=1 bun run test`(격리 로컬 연결 지정): **365/365 통과, 생략 0**. 기본 `bun run test`: **51개 통과/DB 묶음 14개 생략**. check 오류/경고 0, 프로덕션 앱 컴파일·Vercel 패키징 통과. 최종 화면의 오래된 선택 안내 보완 뒤 check·빌드·브라우저도 다시 통과했다. 직접 AI 함수의 민감 입력 차단을 길이 초과와 분리해 더 정확히 확인하도록 테스트를 보완한 뒤 관련 42/42개를 다시 통과했다. `bun run lint` 전체와 `git diff --check`도 통과했으며 미해결 회귀는 없다. 검사 생략을 통과로 계산하지 않는다.
- 실제 Chrome/SvelteKit/격리 DB: `node /tmp/wiki-viki-d2b-20260921/browser.mjs`. 360/768/1440px × 라이트/다크에서 단일 후보 선택→Enter 생성→요약/상충/차이→재생성→버리기, 처리 중 입력 제한·미저장 제한·가로 넘침 없음을 확인했다. 실제 UPDATE trigger 오류, AI 오류 모의 응답, 오래된 대상·이전 통합안 보존, JavaScript 없는 기본 POST의 미저장 입력 보존·직접 선택·생성/검토/버리기, 키 없음·빈/복수 후보·오래된 통합안·실제 DB 조회 장애도 확인했다. 브라우저 테스트는 외부 HTTP 시도 0회이며 OpenAI 경계는 모의 응답이다. 모바일/데스크톱 정상·로딩·오래된 통합안·DB 오류 화면을 직접 확인했다.
- **실제 외부 호출은 별도 검증했다.** `openai-docs` 스킬과 [공식 Structured Outputs 문서](https://developers.openai.com/api/docs/guides/structured-outputs)를 확인하고 설치된 SDK/기존 모델을 유지했다. 임시 `live-ai.mjs`에서 사용자 자료·운영 DB를 사용하지 않고 공개 검증용 가상 두 문서만 실제 `gpt-5-mini`에 전송했다(의미 검사 1회+생성 1회). 7일→3일, 건조 보관 정보·전원 분리 주의·가상 출처·`[[장비 보관]]` 링크 유지와 상충 반환을 확인했다. 실제 반환 원문/검증 결과는 `live-ai-result.json`에 있으며 키/환경 값은 출력·기록하지 않았다. 이 한 예제의 결과를 모든 입력의 AI 품질 보장으로 확대하지 않는다.

### 조사한 실패와 다음 작업

- 초기 새 DB 테스트는 fixture의 리비전 INSERT에서 필수 익명 편집자를 빠뜨려 실패했다. 편집자를 추가한 뒤 의미 검사의 기존 API 요청에는 `text.format`이 없다는 점을 확인해 HTTP 모의 경계의 분기만 수정했다. 제품 검사나 테스트 기준을 약화하지 않았다. 첫 check의 차이 영역 tabindex 경고는 키보드 접근 가능한 읽기 전용 textbox 의미를 제공해 해결했다.
- 첫 전체 회귀에서는 잘못된 기존 별칭 객체를 후보 함수가 배열로 펼쳐 load가 실패했다. 별칭 원본을 보존하고 유효한 문자열만 추천에 사용하도록 수정했으며 기존 차단/수정 테스트와 추가 회귀가 통과했다. 실제 AI 임시 스크립트 첫 실행은 `$lib` alias 누락으로 호출 전에 실패해 alias를 추가했다. 마지막 화면 확인에서 오래된 대상 버전의 select가 빈칸으로 보이는 것을 발견해 명시적 선택 안내를 표시하도록 보완했다. 실패 로그는 증거 폴더에 남겼고 검사 삭제/비활성화는 없다.
- **D2b 단위만 완료이며 D2 전체와 P0는 미완료다.** 원격 Neon 자체·Node.js 22·배포는 미검증이다. 전체 200% 확대/공통 UI는 U, 임시 입력 복구는 E에 남아 있다. GOAL의 기존 본문/체크박스, 사용자 `+layout.svelte` 변경, 환경 파일·운영 데이터·원격 저장소·배포를 보존했다.
- **다음 반복: D2c — AI 원안에서 최종 본문·검토자 편집과 명시적 확인 후 기존 문서 적용을 구현한다.** 초안/대상 정확한 버전·초안 지문·통합안 ID를 재검사하고 최종 콘텐츠 검사를 거친 뒤 기존 ID/제목/주소와 이전 이력을 유지하면서 본문·리비전·사용 가능한 별칭·초안 published/mergedInto를 한 트랜잭션으로 반영한다. `[AI 통합 · 새 초안 우선]`, 출처/초안 ID/요약/상충 상세/수동 수정 여부를 역사에 보존하고, 동시 적용·별칭 충돌·중간 실패·브라우저를 검증한다. 현재 생성된 저장 계약(`targetVersion` 정확 시각+`targetFingerprint` 전체 행/별칭)을 그대로 이어받는다.
- 증거는 `/tmp/wiki-viki-d2b-20260921/`. 재개 DB는 `/tmp/wiki-viki-s3a-20260921/postgres`(루프백 55437)의 격리 PostgreSQL이다. 잔여 테스트 스키마 0개를 실제 SQL로 확인했고 검증 서버와 브라우저는 종료했다. 검증용 PostgreSQL도 종료했다. 다음 반복은 같은 격리 클러스터를 시작하면 된다.

## 반복 17 — 최종 AI 통합 적용·원자성·상충 역사 (2026-09-21)

- D2b 완료를 이어받고 목표 파일·GOAL §5/§11.2·README·프로젝트 설정·git 상태·AGENTS 부재와 실제 구현을 재확인했다. 시작 검사: 격리 DB **365/365**, 기본 **51개 통과/DB 묶음 14개 생략**, check 오류/경고 0, lint·빌드 통과. 기존 검사 실패는 없었다. 이번 단위는 D2c의 최종 편집→명시적 적용→상충 역사이며 낮은 우선순위 기능으로 확장하지 않았다.
- `server/merge-application.js`, `drafts/+page.server.js`: 최종 본문(20만 자 이하)·익명 통합 검토자·본문/상충 검토 확인을 서버에서 검증한다. 초안의 정확한 검토 버전·내용 지문·미게시 상태, 저장된 통합안 ID/형식, 대상 ID·제목·주소·기존 본문·정확한 시각·전체 행/별칭 지문을 확인한다. 미저장 초안 수정과 생성 이후 바뀐 대상 선택도 거부하며 최종 입력을 반환한다. 화면에서 검토한 통합안이 없거나 바뀌면 최신 데이터를 자동 승인하지 않는다.
- 최종 본문·대상 제목·기존/후보 별칭과 역사에 남길 초안 제목·업로드 파일명·요약·상충 기록 전체를 공통 로컬 검사 후 의미 검사한다. 민감 파일명이나 상충 값도 외부 전송 전에 차단한다. 실패·의미 검사 오류에서 기존 문서/초안/통합안과 최종 편집을 보존한다. API 키가 없으면 새 통합안 생성은 여전히 불가하고, **이미 생성·저장된 통합안**에 한해 직접 검토 후 기본 검사로 적용할 수 있다. 이 경우 의미 검사 생략을 화면과 저장된 governance에 명시한다.
- 검사 후 기존 Neon 트랜잭션에서 문서/별칭 테이블→초안 행 순서로 잠근 뒤 새 SQL 스냅샷으로 상태를 재확인한다. 기존 문서 ID·제목·주소·생성 시각·기존 별칭 ID/주소·이력·토론을 유지하고, 본문/편집자/수정 시각·새 리비전·사용 가능한 초안 제목/별칭·초안 published/`mergedInto`를 한 번에 반영한다. 활성/휴지통 문서의 대표 주소·제목·별칭이 점유한 후보는 건너뛰며 소유권을 바꾸지 않는다. UPDATE/INSERT 예외와 조용한 행 생략을 감지해 전체 취소한다. 동시 적용은 한 번만 반영하고 검사 중에는 잠금을 유지하지 않는다.
- `merge-history.js`, `components/MergeHistory.svelte`, history 라우트: 기존 `revisions.summary`에 `[AI 통합 · 새 초안 우선]`와 형식 버전이 있는 JSON 기록을 남긴다. 초안 ID/제목·원본 파일명·요약·기존/새 상충 내용·추가 수동 편집 여부를 보존한다. 줄바꿈의 LF/CRLF 차이만으로 수동 수정으로 기록하지 않는다. 역사 배지·펼칠 수 있는 상세와 최종 리비전 차이 보기를 제공하고 기존 리비전/본문 되돌리기를 유지한다. 보통 요약이나 잘못된 과거 기록은 AI 배지로 오인하지 않고 원문을 표시한다. 기존 TEXT/JSONB 구조를 활용해 마이그레이션은 필요하지 않다.
- `MergeReview.svelte`, drafts 화면: AI 원안 차이는 고정하고 최종 본문·통합 검토자를 따로 편집한다. 확인 항목과 정확한 적용 버튼을 제공하며 처리 중 입력·중복 제출을 제한한다. 최종 편집 후 초안 저장·새 게시·재생성·버리기로 입력을 잃지 않도록 명시적 버림 확인을 서버/화면에 연결했다. 삭제는 기존 영구 삭제 확인을 유지한다. 통합안이 사라지거나 초안이 처리된 실패에도 최종 입력을 복사할 수 있게 남긴다. 기본 HTML 폼에서도 초기 원안/검토자/대상 값을 서버 렌더링하며 오래된 버전을 교체하지 않는다. 모바일 역사 상세는 행 전체 폭을 사용하고 읽기 전용 AI 본문의 과도한 빈 높이를 줄였다.
- `tests/merge-application-db.test.js` **64개 시나리오+상위 묶음(65개)**: 생성→최종 편집→정확한 대상/별칭/역사→본문 되돌리기, 키 없음, 확인·ID·버전·20만 자·익명 이름·미저장 입력, 모든 전송 필드의 로컬 차단, 의미 차단/오류, 잘못된 통합안·지문·마이크로초·BIGINT, 검사 중 초안/대상/별칭/출처/통합안/삭제/복구/게시 변경, 다섯 점유 형태의 별칭 보존과 검사 중 선점, 네 테이블 각각의 예외/행 생략·재시도, 동시 적용·새 게시 경합·실제 버리기 경합·독립 backend의 잠금 대기 후 재검사, 최종 편집의 다른 작업 보호·실제 DB 오류를 검증했다. `merge-history.test.js` 2개는 이력 구조·정확한 ID·수동 변경·잘못된 과거 기록 처리를 확인한다.
- 최종 `RUN_GOVERNANCE_DB_TESTS=1 bun run test`(격리 로컬 연결 지정): **432/432 통과, 생략 0**. 기본 `bun run test`: **53개 통과/DB 묶음 15개 생략**. `bun run check`: 오류/경고 0. `bun run build`: 앱 컴파일·Vercel 패키징 통과. 이후 제품 코드는 변경하지 않았으며 브라우저의 추가 기본 POST 성공 검증도 통과했다. `bun run lint` 전체와 `git diff --check`도 통과했으며 미해결 회귀는 없다. 기본 명령의 DB 생략은 통과와 구분한다.
- 실제 Chrome/SvelteKit/격리 DB: `node /tmp/wiki-viki-d2c-20260921/browser.mjs`. 360/768/1440px × 라이트/다크에서 확인 없는 적용 거부→최종 편집/확인 초기화→Space/Tab/Enter 적용→동일 문서→역사 상세/최종 차이 보기, 처리 중 제한·가로 넘침 없음을 확인했다. 실제 리비전 INSERT trigger 실패와 재시도, 로컬 차단의 전송 0회, 기본 HTML POST의 초기 값·수동 입력 보존·오래된 대상/사라진 통합안 거부·최종 적용 성공, 키 없는 저장 통합안 적용/생략 표시, 대상 변경 후 재생성도 검증했다. 기본 폼에는 동작 줄이기 설정을 적용해 키보드 흐름도 확인했다. 외부 HTTP 시도 0회이며 OpenAI는 경계에서 모의 처리했다. 모바일/데스크톱 역사와 오류·입력 보관 화면을 직접 확인했다.
- 별도 `live-ai.mjs`에서 사용자 자료·운영 DB를 사용하지 않고 공개 검증용 가상 문서로 **실제 `gpt-5-mini` 생성 입력 검사→생성→최종 적용 검사와 격리 DB 저장/역사 조회**를 확인했다. 기존 ID·제목·주소·이전 리비전 유지, 7일→3일, 건조 보관·전원 주의·가상 출처·위키 링크 유지, 검토자 추가 편집, 사용 가능한 별칭, published/mergedInto, 원본 파일명/상충 상세를 검증했다. 키·환경 값은 출력하거나 저장하지 않았다. 최종 응답은 `live-ai-result.json`에 남겼으며 한 가상 예제를 모든 입력의 AI 품질 보장으로 확대하지 않는다.

### 발견·보완과 재개

- 첫 check의 self-closing textarea 경고 두 건은 명시적인 닫는 태그로 해결했다. 브라우저 확인 준비 중 `$effect`만으로 초기 편집/대상 값을 지정하면 JavaScript 없는 HTML에서는 빈 값이 된다는 점을 발견해 서버 초기값과 후속 동기화를 함께 제공했다. 기본 POST 성공/오류 모두 실제 브라우저로 확인했다.
- 첫 브라우저 실행은 긴 기본 폼의 부드러운 스크롤 중 체크박스 안정성 대기에서 종료됐다. 종료 코드 1을 확인한 뒤 기존 CSS의 `scroll-behavior:smooth`를 확인하고, 기본 폼을 동작 줄이기 설정과 실제 Space 키로 검증했다. 정상 6개 화면 조합은 기본 동작 설정을 유지했다. 역사 화면으로 전체 이동할 때 테스트 초기화 스크립트가 다크 모드를 다시 설정하는 것도 발견해 라이트/다크를 실제 DOM 속성으로 확인하도록 보완했다. 화면 검증을 약화하거나 실패를 숨기지 않았다.
- 첫 실제 AI 결과는 구조/저장 검증은 통과했지만 요약에서 실제로 유지되는 제목을 바꿨다고 설명하고 제목 차이를 상충으로 기록했다. 원문 응답을 `live-ai-first-result.json`에 보존하고 `merge-ai.js` 지시에 기존 제목/주소 유지·제목 차이의 변경/상충 제외를 명시했다. API 경계 회귀에 해당 제약을 추가하고 실제 호출을 다시 실행했다. 재검증은 기존 제목을 유지하면서 7일/3일의 실제 본문 상충 하나만 반환했고 생성→최종 적용→역사 전체가 통과했다. 실제 호출은 첫 확인과 보완 후 확인 각 3회이며, 브라우저의 모의 호출과 구분한다.
- **D2c와 D2의 후보→생성→검토→적용→상충 역사 단위는 완료다. 전체 P0는 미완료다.** 원격 Neon 자체·Node.js 22·배포는 미검증이며 이번에 수행하지 않았다. 공통 UI/글꼴·전체 200% 확대는 U, 현재 입력 미리보기는 E1/E2, 이동/새로고침 임시 복구는 E3/E4에 남긴다. GOAL 본문/체크박스와 사용자 `+layout.svelte` 변경, 환경 파일·운영 데이터·원격 저장소·배포를 보존했다.
- **다음 반복: E3 — 직접 편집의 페이지 이동·새로고침 안내와 임시 입력 복구를 구현한다.** 현재 실패/충돌 입력 보존을 유지하고 제목·본문·별칭·요약·검토 버전을 함께 다룬다. 보관 기간·복구/삭제 방법과 민감 내용 처리 정책을 요구사항/기존 코드에 맞게 정하며, 복구한 입력이 새 DB 버전을 자동 승인하지 않게 한다. 사용자 지정 작업 순위의 입력 보존을 우선해 미리보기 E1/E2보다 먼저 진행한다. 새로고침·탭/경로 전환·저장소 실패·다른 편집자의 변경을 실제 브라우저로 검증한다.
- 로그·검증 스크립트·스크린샷은 `/tmp/wiki-viki-d2c-20260921/`. 재개 환경은 `/tmp/wiki-viki-s3a-20260921/postgres`(루프백 55437)의 격리 PostgreSQL이다. 잔여 테스트 스키마 0개를 실제 SQL로 확인하고 검증 서버·브라우저·PostgreSQL을 종료했다. 다음 반복은 같은 격리 클러스터를 시작하면 된다.

## 반복 18 — E3 직접 편집 임시 입력 복구 (2026-09-21)

- 대상은 P0 §7.5의 직접 편집 입력 보존이다. 사용자 우선순위에 따라 E1/E2 미리보기보다 먼저 진행한다. 시작 시 GOAL/README/설정/진행 기록/적용 AGENTS 부재와 변경 파일을 재확인했고, 사용자 및 앞선 반복의 변경을 보존했다.
- `edit-recovery.js`, `EditRecovery.svelte`, `edit/[slug]/+page.svelte`: 기본 꺼짐/사용자 선택 시 이 브라우저에만 24시간 보관, 제목·본문·별칭·편집자·요약과 원래 ID/검토 버전 복구, SPA/전체 이동·새로고침 경고, 명시적 복구/삭제, 탭·경로별 독립 복구본. 복구로 최신 DB 버전을 자동 승인하지 않으며 원본 복구본도 유지한다. 저장 성공은 현재 복구본만 삭제하고 휴지통 이동은 미저장 복구본을 보존한다. DB 조회 실패 때도 화면 입력/복사가 가능하다.
- `content-guard.js`로 기존 로컬 규칙을 그대로 공유하며 서버 호출 계약을 유지했다. 민감 입력은 브라우저 보관을 금지하고 의미 검사 거부 시 같은 내용의 이전 복구본도 삭제한다. 후속 입력 오류가 차단을 해제하지 않는다. 서버 비공개 임시 저장 정책/인증이 없는 현 구조에서는 보호 입력을 서버 초안으로 자동 전송하지 않고 화면 유지/이동 취소로 보존한다. 승인된 이동·비정상 종료의 복구를 보장하지 않음을 UI/README에 명시했다.
- 저장소 거부·용량 부족은 안전한 오류와 현재 입력을 유지하고 기존 복구본을 부분 덮어쓰지 않는다. 다른 탭의 삭제는 해당 탭의 자동 보관도 중단시켜 복구본이 즉시 재생성되지 않는다. 복구 직전에 만료/존재를 재확인하며, 잘못된 형식의 보관 데이터는 임의 삭제하거나 복구하지 않는다. 앱 외 브라우저 데이터는 변경하지 않는다.
- 회귀: `tests/edit-recovery.test.js`에 보관/만료/범위·탭 분리/원래 버전·BIGINT/보호/실패/의미 거부 7개, `tests/browser/edit-recovery.mjs`에 실제 Chrome+격리 PostgreSQL 시나리오를 추가했다. Playwright는 별도 도구 환경을 사용하며 의존성을 추가하지 않았다. 실행/보관/삭제 방법은 README에 기록했다.
- 변경 전 기준: 기본 테스트 53 통과·DB 15 생략, 격리 DB 전체 432/432 통과, check 0 오류·0 경고, lint/build 통과. 기존 실패 없음.
- 개발 중 발견: Svelte 저장소 참조의 비반응성 경고를 `$state.raw`로 해결했다. 의미 검사 거부 뒤 다른 검증 실패가 보관을 재개할 수 있는 경로와 복구 원본 잔류를 막았다. 브라우저 검사에서는 비동기 이동 확인 대기/없는 main 선택자를 수정했고, 휴지통 상태를 직접 SQL로 바꾸려다 제약에 걸린 준비 코드를 실제 휴지통 폼 제출로 교체했다. 제품 제약이나 검사를 완화하지 않았다.
- 실제 브라우저에서 전체 다시 열기 링크는 `willUnload`여도 `leave`와 달리 native 경고를 띄우지 않는 것을 발견했다. `leave`에만 native 경고를 적용하고 전체/SPA 링크에는 명시적 확인창을 사용하도록 수정했다. 같은 URL의 전체 다시 열기·일반 새로고침을 별도로 검증했다. JavaScript 없는 화면의 안내는 실제 DOM/캡처에서 확인했으며, `noscript`를 생략하는 텍스트 검색 대신 요소 가시성과 본문으로 검사했다. 진행 문서 표 정렬로 발생한 lint 실패도 포맷 적용 후 해결했다.

### 최종 검증과 다음 반복

- `bun run test`: **60 통과, DB 15 생략**, 실패 0. `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test`: **439/439 통과**, 생략 0. 새 보관 정책 회귀 7개와 기존 전송/원자성/동시성/삭제·복구 검사가 함께 통과했다.
- `bun run check`: **오류 0·경고 0**. `bun run lint`와 `bun run build`: **통과**. Vercel 어댑터 패키징까지 확인했으며 실제 배포는 수행하지 않았다.
- `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> PLAYWRIGHT_MODULE=<도구 환경의 Playwright index.mjs> RECOVERY_BROWSER_OUTPUT=/tmp/wiki-viki-e3-20260921 node tests/browser/edit-recovery.mjs`: **전체 통과**. 360/768/1440 × 라이트/다크, 정상/빈/로딩/오류, 키보드·수평 넘침, 기본 꺼짐·옵트인·이동 취소·새로고침·명시적 복구·원래 버전·선택 삭제·탭 분리·삭제 전파·24시간 만료·복구 직전 만료 경합을 확인했다. 실제 DB의 오래된 복구 저장 409/AI 0회→비교 후 저장, 리비전 중간 실패 전체 롤백→입력 보존→재시도, 휴지통 이동 뒤 입력 복구, JavaScript 없는 폼의 충돌/재저장도 통과했다. 화면 캡처를 직접 열어 배치·읽기·오류 안내를 확인했다. 페이지 JS 오류 0, 차단되지 않은 외부 HTTP 0; 의미 검사는 모의 응답이며 실제 외부 AI 호출은 이 반복에서 하지 않았다.
- **E3 완료, 전체 P0 미완료.** 남은 입력 보존 E4, 현재 입력 미리보기 E1/E2, 공통 UI/탐색/읽기 U1~U3와 전체 확대 검증을 계속 진행해야 한다. 원격 Neon 자체·Node.js 22·배포는 미검증이다. 보호 입력의 비공개 서버 임시 저장 기능은 제공하지 않으며 위의 화면 유지/전송 금지 정책과 한계를 유지한다. GOAL 체크박스는 직접 편집/초안 및 전체 조건이 함께 충족되지 않아 변경하지 않았다. 새 미해결 회귀 없음.
- **다음 반복: E4 — 초안 수정의 목록/페이지 이동·새로고침 안내와 임시 입력 복구를 구현한다.** 초안 ID·원래 검토 버전·작성값 및 AI 통합 최종 편집의 입력 보존 범위를 실제 코드에서 재확인하고, 복구가 최신 초안/대상/통합안 버전이나 검토 확인을 자동 승인하지 않게 한다. 현재 `drafts/+page.svelte`는 페이지 검색 조건 변경 시 dirty 상태를 초기화하며 이동/새로고침 복구는 없다. 독립 검증이 필요하면 일반 초안 편집과 통합 최종 편집을 작은 단위로 나누고 완료 범위를 구분한다.
- 로그·검증 결과·캡처: `/tmp/wiki-viki-e3-20260921/` (`browser-verified.log`, `browser-results.json`, `final-test/check/lint/db/build.log`). 재개 환경은 `/tmp/wiki-viki-s3a-20260921/postgres`, 루프백 55437의 `wiki_viki_test_governance` DB다. 실제 SQL로 잔여 `governance_%` 스키마 **0개**를 확인했고 서버/브라우저/검증 PostgreSQL을 종료했다. 운영 데이터·환경 비밀·원격 저장소·배포를 변경하지 않았다.

## 반복 19 — D2d 통합·되돌리기 식별자 오탐 회귀 수정 (2026-09-21)

- 이전 E3 반복은 코드·검증 증거를 남긴 진행이다. 목표 파일/GOAL/README/진행 기록/프로젝트 설정/git status와 적용 AGENTS 부재를 다시 확인했다. 원래 다음 작업 E4를 시작하기 위한 변경 전 검사에서 기존 D2c의 DB 테스트가 실패해, 사용자 지정 “이전 변경의 실패 우선 해결” 원칙에 따라 이번 한 단위를 D2d로 전환했다. E4 요구와 범위는 그대로 대기이며 관련 화면/복구 코드를 변경하지 않았다.
- 변경 전: 기본 `bun run test` 60 통과·DB 15 생략, `bun run check` 오류/경고 0, lint/build 통과. 격리 DB 전체는 439개 중 437 통과·실패 2(문서 갱신 실패/롤백 하위 검사 1개와 그 상위 묶음)였다. 실제 기대 503 대신 400이 반환됐다. 이전 검사 통과를 현재 정상의 근거로 삼지 않았다.
- 원인 확인: 통합 적용이 자유 텍스트와 내부 UUID/초안 ID가 섞인 이력 JSON 전체를 검사했다. 유효 UUID `a0101234-5678-4abc-8abc-abcdefabcdef`와 DB 초안 ID `1012345678`을 사용한 결정적 검사로 안전한 통합의 400 차단과 DB 트리거 도달 전 차단을 재현했다. 원래 실패 실행의 임의 UUID 자체는 정리된 DB에서 다시 조회할 수 없으므로, 동일한 반환/검사 경로를 고정 식별자로 재현한 증거로 구분한다. `identifier-red.log`의 신규 2개 및 기존 중간 실패 8개 검사에서 수정 전 실패를 확인했다.
- `merge-history.js`에 실제 이력의 초안 제목·자료명·요약·상충 주제/기존/새 문구를 추출하는 `mergeHistoryInspection`을 추가했다. `drafts/+page.server.js`의 통합 적용이 이 텍스트와 최종 본문·제목·별칭을 기존 공통 검사에 전달한다. UUID/DB ID는 외부 검사 입력에서 제외하고 저장 이력/초안 관계에 원래 값 그대로 보존한다. 이력 형식 검증 실패는 진행하지 않는다. 정규식/의미 검사 기준·버전/확인/원자성 계약은 완화하지 않았다.
- `merge-history.test.js`는 식별자 제외·표시 텍스트 전부 포함·잘못된 형식 거부·같은 숫자열이 실제 텍스트에 있을 때 차단을 검증한다. `merge-application-db.test.js`는 키 없음/의미 검사 시 안전한 통합, 실제 API 경계의 전송 텍스트, 원래 UUID/초안 ID/상충 이력 보존을 검증한다. 기존 네 테이블의 예외/건너뛰기 트리거 검사는 오탐을 일으키던 고정 UUID로 실행해 롤백·재시도가 검사에서 숨지 않도록 했다. 수정 후 관련 DB 67/67 통과.
- 같은 원인을 사용하는 경로를 확인하니 이력 되돌리기도 자동 생성 요약에 숫자 리비전 ID를 포함하고 있었다. `history-governance-db.test.js`에서 전화번호처럼 보이는 실제 리비전 ID의 키 있음/없음 되돌리기가 수정 전에 차단되는 것을 재현했다(`revision-id-red.log`). `history/[slug]/+page.server.js`는 실제 복원 본문·제목·별칭과 되돌림 설명을 검사하고, 저장 요약에는 원래 리비전 ID를 유지하도록 수정했다. 검사 설명 검증과 원래 리비전/새 이력 보존 검증을 함께 유지했으며 수정 후 해당 DB 12/12 통과. 같은 식별자 오탐 원인의 최소 수정으로 포함했다.

### 최종 검증과 다음 반복

- `bun run test`: **62 통과·DB 15 생략**, 실패 0. `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test`: **445/445 통과**, 생략 0. 공통 보호 규칙·민감 내용 전송 차단·버전/중복/동시성·중간 실패 원자성·이력 보존 회귀가 모두 통과했다. 변경 전 발견된 실패를 해결했으며 새 미해결 회귀 없음.
- `bun run check`: **오류 0·경고 0**. `bun run lint`, `bun run build`: **통과**. 서버 로직/검사 입력의 수정이며 UI 레이아웃 코드는 변경하지 않았다. 전체 색상/크기 매트릭스를 새 UI 변경처럼 반복하지 않고 실제 관련 동작을 추가 확인했다.
- `node /tmp/wiki-viki-e4a-20260921/identifier-browser.mjs`: Chrome·격리 PostgreSQL에서 **통과**. 키 없음/의미 검사 모의 응답 각각 통합 303→원래 식별자/상충 상세→숫자 리비전 되돌리기 303→원래 리비전 ID를 담은 새 이력, 실제 텍스트의 연락처 400·외부 전송 0·DB 불변·최종 입력 보존을 확인했다. 768px 정상 역사/되돌리기와 360px 차단 상태를 캡처하고 직접 열어 확인했다. 페이지 오류 0, 차단되지 않은 외부 HTTP 0. 실제 외부 OpenAI 호출·원격 Neon 자체·Node.js 22·배포 검증은 이 반복에서 하지 않았다.
- **D2d 완료, 전체 P0 미완료.** 요구사항/검증 기준·GOAL 체크박스·사용자 변경·이전 E3 화면/저장소 구현을 보존했다. E4·E1/E2·U1~U3는 계속 대기다.
- **다음 반복: E4 — 초안 화면의 일반 수정과 AI 통합 최종 편집 입력 복구.** 실제 폼은 초안 ID/검토 버전과 `proposalId`/대상 선택/최종 본문/통합 검토자를 함께 사용하므로 함께 보존하고, 복구가 최신 초안·통합안·대상 버전이나 게시/삭제/통합 확인 체크를 자동 승인하지 않게 한다. E3와 같은 기본 꺼짐·24시간·명시적 복구/삭제·보호 입력 전송 금지 정책을 최소 공통화하되 기존 직접 편집 복구본 형식을 유지한다. 기계 ID/버전을 내용 검사 필드로 섞지 않는다. 초안 저장은 같은 URL로 돌아오므로 E3의 완료 후 이동 무시 상태를 그대로 재사용하지 말고, 성공 후 새 작성/경고/복구가 다시 동작하는지 검증한다. 현재 E4 코드 변경은 없다.
- 로그·스크립트·캡처는 최초 준비 경로 `/tmp/wiki-viki-e4a-20260921/`에 보관한다. 주요 증거: `baseline-db.log`, `identifier-red/green.log`, `revision-id-red/green.log`, `identifier-browser-final.log`, `identifier-browser-results.json`, `final-test/check/lint/db/build.log`. 재개 DB는 `/tmp/wiki-viki-s3a-20260921/postgres`의 루프백 55437 격리 클러스터다. 실제 SQL로 잔여 테스트 스키마 **0개**를 확인했고 브라우저/서버/검증 PostgreSQL을 종료했다. 운영 데이터·환경 비밀·원격 저장소·배포를 변경하지 않았다.

## 반복 20 — E4 초안 수정·통합 최종 편집 임시 복구 (2026-09-21)

- 대상은 P0 §7.5의 초안 입력 보존이다. 시작 시 GOAL/README/설정/진행 기록/git status와 적용 AGENTS 부재를 재확인했으며, 사용자 및 이전 반복의 변경을 보존했다. 동일 폼과 초안 버전을 사용하는 일반 수정·최종 통합 입력을 하나의 수직 단위로 다룬다.
- 변경 전: 기본 테스트 62 통과·DB 15 생략, 실제 격리 DB 445/445 통과, check 0 오류·0 경고, lint/build 통과. 기존 실패 없음.
- `recovery-store.js`, `draft-recovery.js`, `EditRecovery.svelte`, `drafts/+page.svelte`: 기본 꺼짐·사용자 선택 시 브라우저에만 24시간 보관, 초안 ID/원래 버전·일반 입력·통합안 ID/대상/최종 본문/검토자 복구. 삭제·통합·최종 편집 버리기 확인은 복구하지 않는다. 오래된 초안은 비교 후 명시적 저장, 바뀐 통합안/대상은 최종 입력 복사만 가능하며 새 버전을 자동 승인하지 않는다. 직접 편집의 기존 저장 키/스키마와 API를 유지하며 초안은 별도 영역을 사용한다.
- 같은 URL로 돌아오는 저장/생성 뒤 경고·보관 선택을 새로 준비한다. 처리 중 다른 초안으로 이동해도 늦은 응답이 새 입력을 덮지 않게 요청의 방문 범위를 확인한다. 삭제·게시·조회 불가인 초안의 복구본은 복사 가능하지만 서버 재생성/게시/삭제는 허용하지 않는다. 서버 영구 삭제와 브라우저 복구본 삭제를 구분한다.
- 로컬 보호 규칙은 원본 파일명·최종 입력까지 검사하되 기계 ID/버전은 제외한다. 의미 검사 거부는 복구 원본과 현재 복구본을 함께 지우고 후속 입력 오류로 해제하지 않는다. `merge-ai.js`, `drafts/+page.server.js`에 생성 단계의 실제 검사 거부 상태도 전달해 보관을 중지한다. 연결/잘못된 검사 응답을 실제 거부로 취급하지 않는다. 인증된 비공개 서버 임시 저장 기능이 없는 정책 및 보호 입력의 화면 유지/이동 취소 한계를 README에 유지했다.
- 회귀: 신규 `tests/draft-recovery.test.js` 7개, 기존 직접 편집 복구 7개 통과. `draft-merge-db.test.js`에 검사 거부/장애 구분 검증을 추가했다. `tests/browser/draft-recovery.mjs`는 실제 Chrome·격리 PostgreSQL로 일반/최종 복구, 원래 버전·확인 초기화, 저장 후 재편집, 경합·DB 중간 실패·삭제/게시·탭/경로 분리·보호·저장소 오류·JavaScript 없는 폼을 검증한다.
- 개발 중: 통합 적용으로 페이지를 떠난 뒤 파괴된 화면의 derived 상태를 읽는 경고를 방문/생명주기 확인으로 해결했다. 브라우저 하니스의 안내 문구/현재 탭을 제외한 복구 목록 기대값, 휴지통 테스트 데이터의 실제 제약/컬럼, 기본 POST 303 기대값을 실제 구현에 맞춰 바로잡았다. 테스트를 제거하거나 필수 검증을 약화하지 않았다. 조회 실패를 삭제·게시로 단정하던 안내를 “현재 상태를 확인할 수 없음”으로 수정하고, 해당 오류 화면/복구를 실제 브라우저에서 추가 확인했다.
- `bun run test`: **69 통과·DB 15 생략**, 실패 0. `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test`: **452/452 통과**, 생략 0. 검사 거부/서비스 장애 구분과 기존 원자성·재시도·동시성·관계 보존을 함께 확인했다. `bun run check`: **오류 0·경고 0**, `bun run build`: **통과**(Vercel 패키징 포함). `bun run lint`, `git diff --check`: **통과**.
- 같은 DB 플래그/연결과 `PLAYWRIGHT_MODULE=<도구 환경 index.mjs> RECOVERY_BROWSER_OUTPUT=<결과 디렉터리>`로 `node tests/browser/draft-recovery.mjs`: **전체 통과**. 일반 편집/통합 최종 편집 각각 360/768/1440 × 라이트/다크, 정상/빈/로딩/오류, 키보드·수평 넘침, 목록/형제 초안/새로고침 복구, 확인 초기화, 이전 버전 비교/명시적 저장, 변경/삭제된 통합안·대상 적용 차단을 확인했다. 실제 DB의 초안 UPDATE/통합 리비전 trigger 실패 롤백·입력 유지·재시도, 처리 중 다른 초안으로 이동/늦은 응답, 조회 실패·삭제/게시 후 복사 전용 복구, 생성/저장/최종 적용 보호 거부·서비스 장애 구분, 탭별 보관/삭제 전파·용량/접근 실패·복구 직전 만료·JavaScript 없는 기본 폼도 통과했다. 마지막 오류 문구 수정은 `/tmp/wiki-viki-e4-20260921/load-copy-check.mjs`로 해당 실제 DB/브라우저 시나리오를 추가 검증했다.
- 공통 구현 변경 회귀로 같은 환경의 `node tests/browser/edit-recovery.mjs`도 **전체 통과**. 기존 직접 편집 저장 키/스키마, 저장/충돌/휴지통/보호/이동/복구 흐름을 유지했다. 두 브라우저 검사 모두 페이지 오류 **0**, 예기치 않은 서버 외부 HTTP **0**; 초안 Svelte 런타임 경고도 0. 스크린샷을 직접 열어 모바일 라이트·데스크톱 다크·조회 오류의 배치/안내를 확인했다. AI 응답은 모의 처리했으며 이번에 실제 외부 AI 호출·원격 Neon 자체·Node.js 22·배포를 검증하지 않았다.
- **E4 완료, 전체 P0 미완료.** 새 미해결 회귀 없음. E1/E2의 현재 입력 미리보기, U1~U3의 공통 UI/탐색/읽기와 전체 확대 검증이 남았다. 민감 내용의 비공개 서버 임시 저장은 제공하지 않으며, 보관 불가 입력의 화면 유지/이동 취소와 승인된 이동·비정상 종료의 한계를 유지한다. GOAL 본문·체크박스, 사용자 변경, 환경 파일·운영 데이터·원격 저장소를 보존했다.
- 주요 증거: `/tmp/wiki-viki-e4-20260921/`의 `test-final.log`, `check-final.log`, `db-final.log`, `build-final.log`, `lint-final.log`, `browser-final.log`, `draft-browser-results.json`, `load-copy-check.log`, `edit-browser-regression.log`, `edit-regression/browser-results.json`. 실제 SQL로 잔여 테스트 스키마 **0개**를 확인하고 브라우저/서버/검증 PostgreSQL을 종료했다. 재개 환경은 `/tmp/wiki-viki-s3a-20260921/postgres`의 루프백 55437 격리 클러스터이며 다음 반복에 시작하면 된다.
- **다음 반복: E1 — 직접 편집의 현재 입력 미리보기.** 현재 직접 편집 화면은 본문 입력만 제공하므로 기존 `renderWiki`의 게시 렌더링 규칙을 공유해 미저장 입력을 미리 보여 준다. 넓은 화면/모바일 전환, 링크·번호·표·각주·안전한 출력의 게시 결과 일치, 오류 시 입력 유지, E3/E4 보관·버전 보호 회귀를 실제 브라우저와 자동 검사로 확인한다. 초안 미리보기 E2는 별도 후속 단위로 유지한다.

## 반복 21 — S5 일반 업무 문서의 과도한 차단 완화 (2026-09-21)

- 사용자의 새 요청 “조금은 민감한 정보가 있어도 대부분의 문서가 올라갈 수 있도록 보안을 최대한 느슨하게”를 우선했다. 예정된 E1 대신 이번 범위를 공통 콘텐츠 허용 기준 변경으로 정하고 GOAL §6에 새 기준을 반영했다. 요구사항을 편의상 낮춘 것이 아니라 명시적 사용자 변경이다. 적용 AGENTS 부재, README/설정/진행 기록/git status를 재확인했고 사용자의 AnswerCard/layout 등 기존 변경을 보존했다.
- 변경 전 기본 69 통과·DB 15 생략, 격리 DB 452/452 통과, check/lint/build 통과. 신규 허용 회귀는 기존 규칙에서 실패함을 `policy-red.log`로 재현했다. 기존 실패는 없었다.
- `content-guard.js`: 기밀·대외비·confidential, 이름/이메일/전화번호, 사번, 인사·급여를 차단 대상에서 제거했다. 주민등록번호 형식과 노출된 인증 키/비밀번호 패턴만 최소 차단하며, 설명·마스킹·환경 변수·플레이스홀더를 허용한다. 공통 함수를 쓰는 업로드/AI/저장/게시/토론/시드/복구에 같은 기준이 적용된다. `semantic-governance.js`는 기본 허용, 위 고위험 값이 명확한 경우에만 거부, 일반 민감성/추측으로 차단하지 않는 지침으로 바꿨다. 검사 장애를 성공으로 숨기지는 않는다. 거부 메시지도 실제 최소 차단 종류를 안내한다.
- 기존 차단 회귀의 자료를 새 기준의 합성 주민번호/인증 값으로 바꾸고, 기존 금지어의 허용 테스트를 별도로 추가했다. 기존 검사 삭제/비활성화 없이 전송 전 차단·원자성·충돌·입력 보존을 계속 검증한다. 내부 식별자 모양이 연락처와 같다는 이유만으로 차단하던 테스트 기대값도 새 허용 기준에 맞췄다.
- `api/wikify/+server.js`는 기존 일괄 INSERT를 다른 서버 경로와 같은 transaction callback/ReadCommitted 형식으로 맞췄다. 실제 격리 DB 어댑터로 파일 업로드부터 게시까지 검증하기 위한 최소 변경이며 일괄 저장의 원자성을 유지한다. 새 `relaxed-upload-db.test.js`는 키 없음/의미 검사 모의 응답 모두 실제 파일 추출→초안 INSERT→수정→게시, 이전 blocked 초안 재검사, 고위험 값의 전송/DB 차단, 두 번째 INSERT 실패 전체 롤백→재시도 6/6을 검증했다. 기존 자료를 자동 변경하거나 게시하지 않는다.
- 새 `content-guard.test.js`, 업로드/문서 검사 허용 테스트, `tests/browser/relaxed-upload.mjs`를 추가했다. 실제 Chrome 360 다크/1440 라이트에서 일반 자료 업로드 201→게시·원문/파일명 보존, 고위험 인증 값 422·AI 호출 0·선택 파일 유지, 빈/로딩/정상/오류와 키보드·가로 넘침을 확인했다. UI 배치는 바꾸지 않았으며 정상/차단 캡처를 직접 확인했다. AI 응답은 모의 처리했고 실제 외부 호출/운영 데이터 변경/배포는 하지 않았다.
- 개발 중 새 비밀번호 패턴이 `password: optional` 같은 설정 설명도 값으로 읽는 것을 `placeholder-red.log`로 재현했다. 일반 설정 단어·변수/예제는 허용하고 명확한 혼합 인증 값만 로컬 차단하도록 좁혀 `policy-green.log`에서 통과했다. AI의 최소 검사 지침은 유지한다. 첫 최종 lint의 변경된 `draft-merge-db.test.js` 포맷 한 건도 수정했다.
- 최종 `bun run test`: **75 통과·DB 16 생략**, 실패 0. `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test`: **464/464 통과**, 생략 0. `bun run check`: **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check`: **통과**. 기존 실패 없음, 새 미해결 회귀 없음.
- `node tests/browser/relaxed-upload.mjs`, `node tests/browser/edit-recovery.mjs`, `node tests/browser/draft-recovery.mjs`: 모두 **통과**. 브라우저 실행에는 같은 DB 설정과 `PLAYWRIGHT_MODULE` 도구 경로를 사용했다. 기존 직접 편집/초안 복구의 360/768/1440 × 라이트/다크, 실패·충돌·보호·삭제/복구·탭/경로·기본 폼 회귀를 새 최소 차단 자료로 확인했다. 페이지 오류/예기치 않은 외부 HTTP 0. 마지막 설명 문구 오탐 조정은 단위·전체 실제 DB 검사로 재검증했다. 실제 외부 AI 응답·원격 Neon·Node.js 22·배포는 미검증이며 AI는 모의 응답임을 구분한다.
- **S5 완료, 전체 P0는 미완료.** 이전 차단 초안은 “검토 내용 저장”으로 명시적으로 재검사할 수 있다. README에 허용 기준과 AI 연결 시 허용 자료도 전송되는 동작을 기록했다. 익명 편집자 형식·파일 형식/용량·원자성·동시성·사람의 검토·삭제/복구는 유지했고 운영 자료·환경 파일·원격 저장소를 변경하지 않았다.
- 증거는 `/tmp/wiki-viki-relaxed-governance-20260921/`의 `baseline-*.log`, `policy-red.log`, `placeholder-red.log`, `policy-green.log`, `final-test/check/lint/db/build.log`, `upload-db-dev.log`, `browser-dev.log`/`browser-results.json`, `edit-browser.log`, `draft-browser.log` 및 하위 복구 결과/캡처다. 실제 SQL로 잔여 테스트 스키마 0개를 확인했고 브라우저/검증 서버를 종료했다. 격리 PostgreSQL도 종료하며 다음 반복에는 기존 `/tmp/wiki-viki-s3a-20260921/postgres` 루프백 55437 클러스터를 시작하면 된다.
- **다음 반복: E1 직접 편집의 현재 입력 미리보기.** 사용자 요청으로 완화한 이번 §6 기준을 유지하면서 기존 `renderWiki` 게시 규칙을 공유하고, E3/E4 입력 복구·버전 보호를 보존한다. 추가 업로드 오탐이 제보되면 새로 남은 최소 차단 사유를 먼저 확인한다.

## 반복 22 — W1 파일 하나를 의미별 여러 위키 초안으로 작성 (2026-09-21)

- 새 사용자 요청을 우선하여 예정된 E1 대신 §4의 업로드 분리를 한 수직 단위로 선택했다. GOAL/README/설정/진행 기록/git status, 적용 AGENTS 부재를 확인했고 기존 사용자 변경과 완화된 콘텐츠 정책을 유지한다.
- 변경 전: 기본 테스트 75 통과·DB 16 생략, 격리 PostgreSQL 464/464 통과, check 오류/경고 0, lint/build 통과. 기존 실패 없음. 기존 단일 AI 요청은 분리를 프롬프트에만 맡겨 여러 의미를 한 문서로 합쳐도 저장했다. 새 3개 주제 회귀는 수정 전 500으로 실패(`split-red.log`)했다.
- 작업: 독립적인 제목·작성 범위를 먼저 계획하고 그 목록과 정확히 일치하는 초안 생성 → 관련 링크/파일명/별칭 보존·원자적 저장·개별 검토/게시 → 단위/실제 DB/브라우저 및 가상 원문의 실제 AI 검증. 한 주제는 한 문서, 최대 8개와 키 없는 원문 기본 초안은 유지한다. 아래 필수 검증을 완료했다.
- 증거 경로: `/tmp/wiki-viki-semantic-split-20260921/`. 격리 DB는 기존 루프백 55437 클러스터를 재사용하며 운영 DB/환경 파일은 변경하지 않는다.
- 구현: `src/lib/server/openai.js`에서 주제 제목·범위를 계획하는 구조화 응답과 본문 생성 응답을 분리했다. 주제의 빈 제목/범위·중복 슬러그·1–8개 제한을 검사하고, 두 번째 스키마의 개수·제목을 해당 목록으로 한정한다. 런타임에서도 누락·추가·중복·합치기를 거부하며 계획 순서대로 반환한다. 두 단계는 기존 90초 생성 제한을 공유한다. 원문의 최소 보호 검사와 `store:false`, 실제 오류에서 기본 초안으로 대체하지 않는 동작을 유지한다. AI가 생성한 주제에 노출된 인증 값이 들어가도 두 번째 전송 전 중단한다.
- UI는 기존 결과 목록·초안 이동을 그대로 활용하고 `WikifyForm.svelte`의 안내를 의미별 분리와 개별 게시에 맞췄다. GOAL §4와 README만 구체화했으며 다른 요구사항/체크박스/기존 사용자 변경은 보존했다. 스키마 마이그레이션은 필요 없고 기존 저장/검토/게시의 원자성·버전 보호를 재사용한다.
- 회귀: `wikify-governance.test.js`에 3개 주제·반환 순서/링크·동일 원문 전달, 누락/합치기/중복/추가/잘못된 본문·계획·JSON·미완료·429, 8개 상한, 생성 주제의 최소 전송 차단을 추가했다. 기존 키 없음/한 주제/완화된 허용 기준도 유지한다. `relaxed-upload-db.test.js`는 실제 추출→3초안 INSERT→개별 수정/게시·다른 초안 불변·별칭/원본/기존 게시문서 보존을 추가하고, 중간 INSERT 실패의 전체 롤백/재시도 회귀를 유지했다.
- 개발 중 신규 DB 테스트의 PostgreSQL Result와 일반 배열 비교 방식, 별칭 테이블명 가정을 실제 `redirects.alias_title` 구조에 맞춰 수정했다. 기능 검사나 테스트를 제거하지 않았고 해당 DB 검사는 7/7 통과했다.
- 실제 OpenAI에는 합성 직원 안내와 합성 압력계 점검 원문만 전송했다. 첫 검사에서 단일 절차의 목적/방법/주의사항까지 3개로 과도하게 분리해, 독립 검색 목적과 수행 절차를 기준으로 나누고 부속 설명은 섹션에 두도록 보정했다. 후속 실제 응답에서 다중 주제 원문은 3개, 단일 점검 원문은 1개로 확인했다. 생성 내용에 원문 밖 보고 지침을 보충하는 문제도 발견해, 주제 범위는 사실 근거가 아니며 원문에 없는 단계/의무/규정/제외범위를 보충하지 말고 해당 섹션을 생략하도록 강화했다. 최종 실제 응답에서 두 사례의 분할 수와 원문 사실 보존을 다시 확인했다.

- 최종 자동 검사: `bun run test` **81 통과·DB 16 생략**, `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test` **471/471 통과**. `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` **통과**. 기존 실패 없음, 이번 변경의 미해결 회귀 없음. 마지막 생성 지침 수정 뒤 기본 검사/check/lint/build를 다시 통과했다.
- 실제 브라우저: 같은 DB 설정과 `PLAYWRIGHT_MODULE=<도구 환경 index.mjs> RELAXED_BROWSER_OUTPUT=<증거 경로> node tests/browser/relaxed-upload.mjs` **통과**. 360/768/1440 × 라이트/다크에서 3초안 목록·실제 DB 저장·각 검토 URL/본문·형제 초안, 생성 결과 누락 500·DB 0·선택 파일 유지→재시도 201, 키보드 제출/검토 진입·가로 넘침 없음을 확인했다. 기존 키 없음/일반 민감 업무 자료의 생성→게시, 고위험 값의 전송 전 차단, 빈/로딩 상태도 통과했다. 모바일 다크 성공·모바일 라이트 오류·데스크톱 라이트 성공 캡처를 직접 확인했다. 페이지 오류/예기치 않은 외부 HTTP **0**. 브라우저의 AI 응답은 모의 처리했으며 실제 PostgreSQL을 사용했다.
- 실제 외부 AI: `/tmp/wiki-viki-semantic-split-20260921/live-ai.mjs`는 DB 연결 없이 합성 원문만 현재 `gpt-5-mini`에 전달했다. 최종 직원 안내 **3개**(VPN 접속/재연결, 휴가 신청/승인, 출장 경비/지급), 압력계 점검 **1개**이며 각각 약 21.6초/15.5초였다. 원문의 계정·긴급 휴가·영수증·25일/5일 조건 및 점검 시점·녹색 구간·기록·유리 파손 주의를 보존했고, 앞서 추가되던 보고 의무/제외범위는 사라졌다. 제목/본문을 직접 대조하고 핵심 원문 사실과 개수를 추가 검사했다. 최종 네 요청 모두 `store:false`. 이 소수 예제 결과가 모든 원문의 의미 분할 정확도를 보장하는 것은 아니며 개별 검토 후 게시 흐름을 유지한다.
- **W1 완료, 전체 P0 미완료.** API 키가 없을 때 의미 분리를 가장하지 않고 원문 기본 초안 하나를 제공한다. 배포·원격 Neon 자체·Node.js 22 실행은 이 반복에서 검증하지 않았다. 원래 문서/URL/리비전/사용자 변경·환경 파일·운영 데이터를 변경하지 않았고 원격 푸시/배포도 하지 않았다.
- 증거: `baseline-*.log`, `split-red.log`, `split-green.log`, `upload-db.log`, `final-test/check/lint/build/db.log`, `browser.log`, `browser-results.json`, `split-*.png`, `live-ai-before-refinement.json`, `live-ai-before-grounding.json`, 최종 `live-ai-grounded.log`/`live-ai-results.json`. 실제 SQL로 잔여 테스트 스키마 **0개**를 확인하고 브라우저/서버/격리 PostgreSQL을 종료했다. 재개 시 기존 `/tmp/wiki-viki-s3a-20260921/postgres` 루프백 55437 클러스터를 시작하면 된다.
- **다음 반복: E1 직접 편집의 현재 입력 미리보기.** 기존 `renderWiki` 규칙으로 미저장 입력을 렌더링하고 E3/E4의 복구·버전 보호를 유지한다. 이번 의미별 업로드와 완화된 콘텐츠 정책은 완료 상태로 보존한다.

## 반복 23 — E1 직접 편집의 현재 입력 미리보기 (2026-09-21)

- 목표 파일을 다시 읽고 GOAL/README/설정/git status/진행 기록 및 적용 AGENTS 부재를 확인했다. 이전 W1 반복은 실제 코드와 자동/DB/브라우저/실제 AI 증거를 남긴 **진행**으로 분류한다. 이번 범위는 P0 §7.5/§12의 직접 편집 미리보기이며 E2 초안 미리보기는 별도 후속 단위다. 기존 의미별 업로드/완화된 검사/복구/버전 보호와 사용자 변경을 유지한다.
- 변경 전: 기본 테스트 81 통과·DB 16 생략, 격리 DB 471/471 통과, check 오류/경고 0, lint/build 통과. 기존 실패 없음. 실제 직접 편집은 textarea만 제공했다. 신규 API 회귀는 미리보기 경로 부재로 실패(`preview-red.log`)했다.
- 진행 중: 기존 서버 `renderWiki`/`buildToc`를 공유하는 읽기 전용 미리보기, 300ms 입력 지연·이전 요청 취소·오래된 응답 무시, 넓은 화면 나란히/모바일 탭·키보드·빈/로딩/오류·재시도, 입력 보존 및 게시 렌더링 일치 검증. API는 앱 서버에서 문법/링크만 확인하며 AI 호출·DB 쓰기·승인을 하지 않고 `no-store`로 응답한다. 미리보기의 서버 처리와 브라우저 복구본 보관 정책을 화면에서 구분한다.
- 작업/증거 경로: `/tmp/wiki-viki-e1-20260921/`. 루프백 55437의 기존 격리 DB를 시작했으며 운영 DB/환경 파일은 사용하지 않는다.

- 구현 파일: 새 `WikiEditor.svelte`, `api/preview/+server.js`, 직접 편집 페이지. 실제 저장 폼과 값/버전/복구 상태는 유지하고 본문 부분만 미리보기 편집기로 교체했다. 빈 본문·10초 시간 초과·연결/렌더 오류는 이전 HTML을 최신 결과로 남기지 않으며 재시도를 제공한다. 모바일의 빈 필수 본문과 저장 차단의 “본문 수정하기”는 편집 탭을 열고 입력에 초점을 맞춘다. 새 문서/미저장/저장 상태 확인 불가를 구분한다.
- `Toc.svelte`의 접기 버튼을 `type=button`으로 지정해 폼 안에서 뜻하지 않은 저장을 막았다. 기존 읽기 색상 토큰을 `.wiki-preview`에도 공유하고 위키 본문 스타일은 그대로 사용한다. 고정 헤더 아래에 목차/각주 대상이 보이도록 미리보기 앵커 여백을 추가했다. `EditRecovery.svelte`의 안내는 임시 복구본의 로컬 보관과 미리보기 서버 처리를 혼동하지 않게 명확히 했다. 저장소 키·보관·복구 동작은 변경하지 않았다.
- 검증 중 별칭 추가/삭제와 새 문서 자기 링크가 저장 후 색상과 달라지는 것을 신규 DB 검사(`alias-preview-red.log`)로 재현했다. 미리보기 API가 `editableAliases`와 기존 문서의 고정 주소를 사용해 해당 편집의 링크 상태를 예측하도록 최소 확장했다. 별칭의 기존 URL을 보존하며, 과거 별칭/대표 URL 충돌에서 다른 활성 문서가 존재하는 주소를 미작성으로 표시하지 않는다. 실제 저장은 여전히 별도의 콘텐츠·버전·충돌 검사를 통과해야 하며 미리보기는 승인으로 취급하지 않는다. 공통 `renderWiki`의 기본 호출 결과와 기존 게시/초안 렌더링은 유지한다.
- 초기 신규 DB 픽스처에서 `deleted_by` 없는 휴지통 행이 실제 제약에 거부되어 픽스처를 올바르게 수정했다. Svelte 진단에서 조건부 모바일 탭과 데스크톱 영역의 역할/초점 경고를 확인해 올바른 탭 패널/이름 있는 영역으로 구분했고, 검사 억제 없이 오류/경고 0으로 해결했다. 목차 버튼 제출·응답 경합·문서 신규 생성·실제 DB 충돌을 브라우저에서 재현해 확인했다.

- 최종 자동 검사: `bun run test` **81 통과·DB 17 생략**, `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test` **477/477 통과**. `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` **통과**. 새 `wiki-preview-db.test.js`는 실제 DB의 렌더 일치/읽기 전용/보호 입력의 AI 호출 0/안전한 HTML·링크/오류/자기 링크·별칭 변경→실제 저장 일치와 과거 URL 충돌을 확인한다.
- `tests/browser/edit-preview.mjs` 최종 **통과**: 360/768/1440 × 라이트/다크의 편집/미리보기·빈/로딩/오류·재시도, 키보드 탭/목차, 고정 헤더 아래 앵커, 가로 넘침 없음, 실제 DB의 변경 충돌 409→입력/버전 유지→명시적 저장과 게시 HTML·제목 글꼴/색상 일치. 취소를 무시하는 이전 응답도 최신 입력을 덮지 않고, 10초 시간 초과 뒤 재시도가 가능하다. 새 문서의 자기 링크/별칭·반응형 전환·생성, 스크립트/원시 이미지/위험 링크의 비실행, 미리보기 뒤 콘텐츠 보호 저장 차단·모바일 “본문 수정하기” 초점, JavaScript 없는 기본 저장도 통과했다. 페이지 오류/예기치 않은 외부 HTTP **0**. 데스크톱 라이트·모바일 다크 정상, 모바일 라이트 오류 캡처를 직접 확인했다.
- 기존 E3 브라우저 검사는 새 미리보기 안내로 `noscript p`가 두 개가 되어 검증 선택자에서 실패했다. 기존 가시성/안내 내용 검사를 유지하면서 `.recovery-panel noscript p`로 한정했고, 저장 대기 검사는 미리보기의 별도 `aria-busy`와 구분해 원래 저장 폼으로 한정했다. 제품의 입력 보존 검사를 제거하거나 비활성화하지 않았다. 수정 후 E3/E4 전체 브라우저 검사도 통과했다.

- 최종 복구 회귀: 같은 격리 DB 설정과 `PLAYWRIGHT_MODULE`을 사용한 `node tests/browser/edit-recovery.mjs`, `node tests/browser/draft-recovery.mjs` **모두 통과**. 기존 360/768/1440 × 라이트/다크의 임시 복구, 저장/충돌/보호/휴지통/탭/경로/실패/기본 폼 및 초안·통합 최종 입력의 버전/확인 보존을 확인했다. 각 검사 페이지 오류/예기치 않은 외부 HTTP **0**. AI 검사는 이 회귀에서 모의 응답을 사용했고, 새 미리보기 자체는 실제 OpenAI를 호출하지 않는다.
- **E1 완료, 전체 P0 미완료.** 남은 항목은 E2 초안 현재 입력 미리보기와 U1~U3 공통 UI/탐색/읽기 및 전체 완료 감사다. 이번 변경의 미해결 회귀 없음. 원격 Neon 자체·Node.js 22 실행·배포는 이 반복에서 검증하지 않았다. 요구사항 체크박스는 직접 편집과 초안 전체 조건을 함께 요구하므로 체크하지 않았다. 사용자/이전 반복 변경·기존 문서/별칭/URL/이력·환경 파일을 보존했고 운영 데이터 변경·푸시·배포는 하지 않았다.
- 최종 증거: `/tmp/wiki-viki-e1-20260921/`의 `baseline-*.log`, `preview-red.log`, `alias-preview-red.log`, `preview-green.log`, `final-test/check/lint/build/db.log`, `browser-final.log`, `preview-browser-results.json`, `normal/error/loading-*.png`, `edit-recovery.log`, `draft-recovery.log` 및 각 복구 결과 하위 디렉터리. 실제 SQL로 잔여 테스트 스키마 **0개**를 확인하고 브라우저/서버/검증 PostgreSQL을 종료했다. 다음 DB 검증은 기존 `/tmp/wiki-viki-s3a-20260921/postgres` 루프백 55437 클러스터를 시작하면 된다.
- **다음 반복: E2 초안 수정의 현재 입력 미리보기.** 현재 초안은 `load`의 저장된 `previewHtml`만 보여 준다. 이번 읽기 전용 렌더링을 재사용하되, 같은 업로드의 초안 연결과 일반/최종 통합 입력을 구분하고 E4의 버전·확인·복구 및 미저장 상태 게시 제한을 보존한다. 필요하면 편집기 필드 ID/name을 매개변수로 만들어 기존 초안 폼 계약과 충돌하지 않게 한다. 현재 입력과 저장된 초안의 차이, 게시 렌더링 일치, 모바일/데스크톱·오류/응답 경합·복구를 실제 브라우저와 DB에서 검증한다.

## 반복 24 — W2 Excel 업로드 오류·모달 상태 보존 (2026-09-21)

- 최신 사용자 요청을 우선해 E2 대신 업로드 한 단위를 처리했다. GOAL/README/설정/기존 기록/git status와 적용 AGENTS 부재를 확인하고 이전/사용자 변경을 보존했다. 변경 전 기본 테스트 **81 통과·DB 17 생략**, 격리 PostgreSQL **477/477**, check 오류/경고 0, lint/build 통과. 기존 실패 없음.
- 재현: 두 시트·빈 열·문자열·날짜·저장된 수식 결과가 있는 XLSX는 변경 전 실제 모달→API→격리 DB 저장에 성공했다. 원본 사용자 파일이나 정확한 오류 문구는 없어 특정 추출/AI 오류의 원인은 확정하지 않았다. 대신 502 HTML 응답이 `Unexpected token ... is not valid JSON`으로 표시되고, Escape→재열기에서 선택 파일/오류가 모두 없어지는 것을 실제 Chrome으로 재현했다(`modal-red.log`). 파서 자체를 근거 없이 교체하거나 검사를 완화하지 않았다.
- `WikifyForm.svelte`, `upload.js`: 비 JSON 오류·불완전 성공 응답을 안전한 안내로 처리하고 결과 확인 링크/선택 파일을 유지한다. 서버 용량 제한과 결과 불확실/120초 시간 초과를 구분하며 자동 재제출하지 않는다. 확정된 저장 뒤 페이지 갱신 실패를 업로드 실패로 바꾸지 않는다. 페이지 컴포넌트 종료 때 브라우저 요청은 정리하지만 서버 취소/롤백을 약속하지 않는다.
- `+layout.svelte`, `app.css`: 모달의 최초 실행 뒤 컴포넌트를 유지해 닫기/재열기에 선택·오류·처리·결과가 사라지지 않게 했다. native dialog, X/배경/Escape 닫기·이동 후 정리는 유지한다. 프레임 내부 본문만 스크롤하고 닫기 버튼은 별도 상단에 유지한다. 배경 스크롤과 내부 스크롤 전달을 막고, 배경 클릭은 패널 좌표 밖에서 누르고 뗀 경우로 한정한다. 상태는 메모리에만 보관하며 새로고침 후 결과 목록 확인을 안내한다. 파일·DB·브라우저 저장소에 새 복구본을 영구 보관하지 않는다.
- 회귀: `tests/upload-response.test.js`의 정상/BIGINT ID/서버 거부/HTML 413·502·504/불완전 결과 3개 검사 통과. `tests/browser/xlsx-upload.mjs`는 실제 XLSX 추출·API·격리 PostgreSQL을 사용하고 AI 응답/전송 장애만 모의 처리한다. 손상 XLSX 422·AI 호출/DB 쓰기 0, 선택 파일 보존→정상 파일 재시도, 키 없음 기본 초안과 키 있음 의미별 2개 초안 저장·개별 검토, 닫기/재열기, 처리 중 중복 제출 제한을 확인했다.
- 360/768/1440px × 라이트/다크 6조합의 빈/오류/로딩/성공, 키보드 Enter/검토 링크/Escape, 고정 닫기 버튼·가로 넘침 없음·배경 스크롤 제한 통과. 모바일 다크 오류·모바일 라이트 로딩·데스크톱 라이트 성공 캡처를 직접 확인했다. 추가 시간 초과 검증에서 브라우저가 이미 취소한 모의 요청에 테스트가 다시 `route.abort()`를 호출해 실패했다. 중복 정리만 제거했으며 제품 검사/회귀 조건을 낮추지 않았다. 실제 사용자 파일·실제 OpenAI 호출·원격 Neon·Node.js 22·배포는 이번 검증과 구분한다.

### 최종 검증과 재개 조건

- `bun run test`: **84 통과·DB 17 생략**, 실패 0. `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test`: **480/480 통과**. `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`, `git diff --check` 통과. 기존 실패 없음·이번 변경의 미해결 자동 회귀 없음.
- `node tests/browser/xlsx-upload.mjs`: 최종 통과. 6개 화면 조합 외에 브라우저 시계로 120초 제한을 실제 발생시켜 파일/목록 링크 보존→재시도 201·DB 1건, 저장 성공 뒤 페이지 데이터 요청 503에도 모달/확정 결과 유지, 빈 시트 422·구형 `.xls` 선택 거부를 확인했다. `node tests/browser/relaxed-upload.mjs`도 통과해 기존 일반 업무 자료 허용·키 없음·최소 보호·3개 의미별 초안·잘못된 생성 결과의 저장 0·재시도·개별 검토/게시를 재확인했다. 두 검사 모두 페이지 오류 0·예기치 않은 외부 HTTP 0. AI 응답은 모의이며 실제 파일 파서/API/DB는 모의가 아니다.
- 확인된 모달 상태 소실과 응답 오류 처리 개선은 구현·검증 완료다. **사용자가 신고한 특정 엑셀 실패의 전체 원인까지 해결됐다고 선언하지 않는다.** 사용한 정상 XLSX는 변경 전에도 저장됐으며 문제 원본/정확한 오류가 없는 조건을 남긴다. 필요한 추가 자료는 파일 확장자와 화면 오류 문구이며, 추출 오류면 동일 조건의 비식별 재현 파일이 필요하다. 실제 외부 AI·운영 배포 검증도 미실행이다. 전체 P0/GOAL 체크박스는 미완료 상태를 유지한다.
- **다음 반복의 구체 작업: 신고한 엑셀의 확장자·오류 문구 또는 비식별 재현 파일로 W2의 남은 서버 실패 경로를 재현하고 해당 회귀를 추가한다.** 이 자료가 계속 없으면 이에 의존하지 않는 원래 P0 E2(초안 현재 입력 미리보기)를 이어갈 수 있다. 구형 `.xls` 지원 확대나 파서/의존성 교체를 추측으로 수행하지 않는다.
- 증거: `/tmp/wiki-viki-w2-20260921/`의 `baseline-*.log`, `modal-red.log`, `response-red/green.log`, `browser-verified.log`, `xlsx-browser-results.json`, 화면 캡처, `final-test/db/check/lint/build.log`, `relaxed-regression.log`와 하위 브라우저 결과. 기존 문서 데이터·DB 모델·환경 파일·URL·원격 저장소·배포는 변경하지 않았다. 테스트 DB는 `/tmp/wiki-viki-s3a-20260921/postgres`, 루프백 55437의 `wiki_viki_test_governance`다.

- 종료 시 실제 SQL로 잔여 `governance_%` 테스트 스키마 **0개**를 확인하고 검증 서버·브라우저·PostgreSQL을 모두 종료했다. 재개 시 위 격리 PostgreSQL 클러스터만 다시 시작하면 된다.

## 반복 25 — R1 목록 검토와 선택 초안 게시 (2026-09-21)

- 최신 사용자 요청을 우선해 기본 검토 목록을 본문 중심으로 바꾸고 여러 초안의 선택 게시를 한 단위로 구현했다. 기존 W2 원본 파일 재현 대기와 E2 요구는 보존한다. GOAL/README/설정/진행 기록/git status/적용 AGENTS 부재를 다시 확인했다. 시작 검사: 기본 테스트 **84 통과·DB 17 생략**, 격리 DB **480/480**, check 오류/경고 0, lint/build 통과. 기존 제품 검사 실패 없음.
- `DraftReviewList.svelte`, `draft-review.js`, `drafts/+page.svelte`: 목록에서 저장된 본문·문단/각주·별칭·원본 파일·검사 상태를 읽고, 긴 본문만 펼치며, 체크→**선택한 초안 게시**로 처리한다. 현재 목록 전체 선택/해제, 상태 필터, 20개 단위 페이지, 최대 8개 선택, 실제 항목별 성공 링크/실패 사유, 키 없음 검사 생략과 DB 조회 오류를 구분한다. 새 검토 상태·일괄 통합·삭제·편집 자동 저장은 추가하지 않는다. 기존 `/drafts?open=ID`의 수정/AI 통합/버전 비교/임시 복구와 모든 서버 액션을 유지했다.
- 선택은 ID+검토 버전에 연결한다. 필터/페이지가 바뀌거나 게시/삭제/수정으로 버전이 바뀐 항목은 다시 선택해야 한다. 게시 중 중복 제출을 막고 이동 시 실제 진행 중임을 안내한다. 처리 결과를 모르면 선택을 유지하고 목록 확인/재시도를 안내한다. 선택 승인은 브라우저 저장소에 자동 보관하지 않는다. 기본 폼도 같은 선택·검토 버전을 전송한다.
- `drafts/+page.server.js`: 단일 게시의 검사를 공통 내부 함수로 옮기고 같은 검사·`publishDraft` 트랜잭션을 일괄 요청에서 항목마다 사용한다. 잘못된/중복/9개 이상/확인 없는 선택은 쓰기 전에 거부한다. AI 동시 검사는 최대 4개, 1회 선택은 기존 한 업로드의 최대 초안 수인 8개로 제한해 무제한 외부 호출을 만들지 않는다. 각 초안의 문서·최초 이력·별칭·게시 상태는 하나의 트랜잭션이며 일부 실패 시 다른 성공 문서를 유지하고 실패 초안을 보존한다. 같은 원래 검토 버전으로 이미 게시된 항목은 기존 게시 메타데이터를 확인해 결과만 돌려준다. 확인 필요/검사 장애/통합안은 기존 개별 검토를 거쳐야 하며 조작한 체크박스 요청도 이를 우회하지 못한다.
- 목록 HTML은 현재 페이지의 최대 20개만 렌더링한다. `wiki.js`는 기존 게시 렌더러에 선택적 문단/각주 접두사와 이미지 링크 표시를 추가했다. 기본 호출의 결과는 유지하고 목록의 여러 초안 사이 ID 충돌 및 외부 이미지 자동 요청을 막는다. 원문·문서·별칭·메타데이터·DB 모델은 그대로 보존한다. GOAL에는 최신 사용자 요청의 목록/선택 게시 조건만 추가하고 기존 본문/체크박스를 낮추거나 완료 표시하지 않았다.
- 실제 PostgreSQL 신규 `draft-batch-db.test.js` **13/13 통과**: 목록 렌더/링크/고유 앵커·페이지/상태, 선택 버전·BIGINT·미선택 보존, 8개 성공/AI 동시 4개 제한, 불량 요청의 쓰기/전송 0, 원래 문서 충돌 보존, 통합안/차단 우회 거부, 민감 내용 전송 0, 의미 검사 장애, 검사 중 수정·삭제, 중간 이력 INSERT 실패의 해당 항목 전체 롤백→재시도, 실제 독립 DB 백엔드 4개 잠금 대기의 동시 일괄 게시→문서/이력 각 1회 생성을 확인했다. 원자성을 모의 DB로 계산하지 않았다.
- 신규 `tests/browser/draft-batch.mjs` 최종 통과: 360/768/1440px × 라이트/다크에서 본문 즉시 확인/펼치기·키보드 선택/게시·처리 중 이동 취소·2개 성공+1개 충돌·최신 버전 자동 선택 해제·상세 수정/통합 진입을 실제 Chrome/API/DB로 검증했다. 20개 목록/8개 선택 제한·필터/페이지 변경 선택 해제·720×475 작은 높이, 커밋 후 응답 유실→원래 선택 재전송→중복 0, JavaScript 없는 2개 게시·긴 본문 전체 표시, DB 장애의 명시적 오류도 통과했다. 페이지 오류 0·서버/브라우저의 예기치 않은 외부 HTTP 0. AI 응답은 모의이며 실제 OpenAI·원격 Neon·Node.js 22·배포 검증과 구분한다.
- 개발 중 검증 문제: 이동 확인 이벤트 도착 전에 검사하던 테스트를 이벤트 완료까지 대기하도록 수정했다. 응답 유실 모의 경로는 액션 쿼리의 `/`를 매칭하지 못하던 glob 대신 URL 조건으로 수정했다. 기본 폼에서 고정 작업바가 체크박스를 덮는 실제 UI 문제는 JavaScript 연결 시에만 고정/본문 접기를 적용하도록 해결했다. 헤더/작업바 높이에 맞춘 스크롤 여백과 작은 높이에서 고정 해제로 키보드 초점이 가려지지 않음을 실제 좌표로 검사했다. 검사를 제거하거나 강제 클릭으로 우회하지 않았다.

### 최종 검증과 다음 반복

- `bun run test`: **84 통과·DB 18 생략**, 실패 0. `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test`: **493/493 통과**, 생략 0. `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징) 통과. 실제 DB의 항목별 원자성·동시 게시·응답 유실 후 재시도를 검증했으며, AI 응답만 모의 처리했다.
- 새 목록의 모바일 라이트·데스크톱 다크 정상, 모바일 다크 부분 실패, 태블릿 로딩, 데스크톱 펼친 본문, 작은 화면 높이의 캡처를 직접 열어 확인했다. 체크박스 기본 여백으로 모바일 제목이 좁아지는 문제를 수정한 뒤 최종 6개 화면 조합과 기본 폼 검사를 통과했다.
- 기존 E4 브라우저 검사는 목록에서 이전 링크 이름 `검증 초안`을 찾다 멈췄다. 실제 변경된 접근성 이름 `수정·통합 검증 초안 →`으로 선택자만 수정했다. 목록 이동→원래 초안 재진입→입력 복구·원래 버전·삭제 확인 초기화 검사는 그대로 유지했다.
- `node tests/browser/draft-recovery.mjs` 최종 **전체 통과**. 기존 360/768/1440px × 라이트/다크의 일반 수정·통합 최종 편집 복구, 목록 이동/새로고침, 원래 검토 버전, 별도 재확인, 실제 DB 실패·충돌·삭제·게시·재시도, 늦은 응답·기본 폼을 재검증했다. 페이지 오류·예기치 않은 외부 HTTP **0**. 최종 문서와 검사 선택자 변경의 Prettier 검사 및 `git diff --check`도 통과했다.
- **R1 완료, 전체 P0 미완료.** 이번 변경으로 남은 회귀는 없다. 실제 외부 OpenAI 호출·원격 Neon·Node.js 22 실행·배포는 이번 검증 범위에서 실행하지 않았으며, 목록/게시 동작은 실제 Chrome/API/격리 PostgreSQL과 AI 모의 응답으로 확인했다. W2의 신고된 특정 엑셀 실패는 원본 조건 부족으로 재현 대기를 유지한다. E2·U1~U3 및 전체 완료 감사가 남아 GOAL 전체 체크박스를 완료로 바꾸지 않았다.
- **다음 반복: E2 — 초안 수정의 현재 입력 미리보기.** 기존 `WikiEditor`/읽기 전용 렌더링을 재사용하고 일반 수정·최종 통합 입력을 구분한다. 목록의 저장된 본문과 편집 중 본문을 혼동하지 않게 하며, E4 버전·재확인·복구와 미저장 상태 게시 제한을 유지한다. W2 재현 자료가 제공되면 해당 오류를 우선 재현한다.
- 증거: `/tmp/wiki-viki-r1-20260921/`의 `baseline-*.log`, `batch-db-final.log`, `browser-final.log`, `batch-browser-results.json`, 화면 캡처, `final-test/db/check/lint/build.log`, `recovery-regression-final.log` 및 `recovery-regression/draft-browser-results.json`. 초기 브라우저 실패 때 남은 스키마 1개는 생성 시각·소유자·합성 초안 5개/합성 문서 1개·활성 연결 없음을 확인한 뒤 해당 테스트 스키마만 정리했다. 실제 SQL로 잔여 테스트 스키마 **0개**를 확인하고 이번 검증의 서버·브라우저·PostgreSQL을 종료했다. 사용자가 실행한 개발 서버는 유지했다. 재개 DB는 `/tmp/wiki-viki-s3a-20260921/postgres`의 루프백 55437 클러스터다. 운영 데이터·환경 파일·원격 저장소·배포는 변경하지 않았다.

## 반복 26 — W3 위키파이어 세부 지식 분할 (2026-09-21)

- 최신 사용자 요청을 우선한 한 단위다. GOAL/README/설정/진행 기록/git status와 적용 AGENTS 부재를 확인했다. 현재 코드는 목적·방법·기준·주의사항을 한 문서에 묶도록 지시하고, 주제가 8개를 넘으면 합치도록 요구했다. 기존 사용자 변경·이전 작업·원문 데이터와 R1의 한 번에 8개 선택 게시는 보존한다.
- 변경 전: 기본 테스트 **84 통과·DB 18 생략**, 실제 격리 DB **493/493**, check 오류/경고 0, lint/build 통과. 기존 제품 검사 실패 없음. 생성 9/32개와 클라이언트 32개 결과 처리의 신규 회귀에서 변경 전 실패를 재현했다(`fine-topics-red.log`).
- `openai.js`: 작은 작업·점검 항목·조작 방법·판단 기준·고장 대응·독립 개념으로 분할하는 지침으로 변경했다. 짧아도 자체 의미가 있으면 분리하고 큰 업무/시트/장 이름으로 다시 합치지 않는다. 빈 제목·문맥 없는 셀/문장 조각·동일 사실의 중복 문서는 만들지 않게 한다. 표의 대상·단위·조건, 가상/교육 자료의 한정, 원문의 조건을 유지하도록 명시했다.
- `knowledge.js`의 공통 최대 생성 수는 32개다. 계획 뒤 8개씩 최대 4개 본문 요청을 병렬 처리하고 기존 합계 90초 한도를 공유한다. 각 묶음에 전체 주제 경계를 제공해 다른 묶음과도 연결할 수 있게 했다. 제목·개수·중복·본문 검사를 전부 통과한 뒤에만 결과를 반환하며 한 요청 실패 시 대기 중인 형제 요청을 취소한다. 기존 API의 전체 초안 INSERT 트랜잭션을 그대로 사용한다. 스키마·의존성·파일 추출기는 변경하지 않는다.
- `upload.js`, `WikifyForm.svelte`: 32개 정상 결과를 업로드 실패로 오인하지 않도록 서버와 같은 상한을 사용하고, 세부 분할·최대 개수·더 큰 자료 나누기 안내를 표시한다. 결과 개수는 실제 저장 결과를 사용한다. 키 없을 때 원문 기본 초안 1개와 생략 안내, 오류/선택 파일/모달 상태 보존을 유지한다. GOAL의 현재 목표 및 미완료 기준만 최신 요청의 1–32개로 갱신했으며 과거 1–8개 구현 이력과 체크박스 상태는 보존했다.
- 신규·관련 자동 회귀 **27/27 통과**: 9/32개 생성, 순서가 바뀐 응답, 서로 다른 묶음 사이 링크, 누락/잘못된 묶음/불완전 응답의 저장 0, 4개 동시 요청·마지막 응답 전 쓰기 0, 실패 시 진행 중 요청 취소, 32개 클라이언트 결과 수신. 기존 원문/키 없음/콘텐츠 차단 흐름도 포함한다. 새 비동기 테스트에서 형식 이름이 없는 의미 검사 요청을 잘못 가정해 대기하던 문제는 실제 요청 구조에 맞춰 수정하고 조기 API 종료도 기다림 실패로 처리했다. 제품 검사를 완화하지 않았다.
- 실제 PostgreSQL의 `relaxed-upload-db.test.js` **8/8 통과**. 32개 중 29번째 INSERT에 트리거 실패를 발생시켜 이전 초안 불변·이번 업로드 0건을 확인하고, 트리거 제거 후 재시도에서 32개/순서/별칭/파일명/관련 링크를 모두 보존했다. 기존 기본 초안·일반 자료 허용·민감 내용 차단·개별 수정/게시·이전 원자성 검사도 통과했다.
- 가상 교육 자료의 실제 OpenAI 검증을 별도로 수행한다. 첫 실행은 원문 12개 항목을 18개로 나눴으나 절차와 기록 방법의 중복 및 교육 한정 누락을 확인했다. 중복 사실만으로 양식 문서를 추가하지 않고 각 문서에 교육/예시 한정을 남기도록 지침을 보완해 다시 확인한다. 실제 호출은 합성 자료만 사용하며 DB 접근 0, 사용자 파일·원격 DB 사용 없음. 첫 결과는 `live-ai-initial-*.json`에 보관했다.

- 보완 후 실제 `gpt-5-mini` 호출은 **12개 원문 항목 → 12개 세부 초안**, 계획 1회+본문 8/4개 두 묶음, 약 55초에 완료했다. 결과 제목/본문을 직접 읽어 압력·온도·진동·유면·보충·누유·차압·청소·기동·정지·경보·인계가 각각 남고 원문의 주요 수치·조건·순서가 보존됨을 확인했다. 첫 실행에서 겹쳤던 기록 양식은 해당 세부 절차에 포함됐다. 이 합성 예제의 확인을 임의 사용자 자료의 완전성 보장으로 확대하지 않으며, 생성물은 계속 검토 대상 초안이다. 결과는 `live-ai-results.json`, `live-ai-documents.json`, 재실행용 격리 스크립트는 `live-ai.mjs`다. 운영 DB 접근은 0회다.
- `node tests/browser/xlsx-upload.mjs` 최종 **전체 통과**: 기존 Excel 추출/오류/빈 시트/HTML 장애/120초 제한/닫기·재열기/기본 초안 회귀와, 새 360/768/1440px × 라이트/다크의 32개 생성·실제 DB 저장·모달 32개 결과·마지막 링크 키보드 진입·목록 20/12개 페이지 이동을 확인했다. 긴 목록 끝에서도 X로 닫고 다시 열 때 결과가 유지된다. 새 클릭 가시성 검사가 버튼 안 SVG를 버튼과 다른 요소로 판정해 한 번 실패했으며, 버튼의 자식도 포함하는 올바른 hit-test와 실제 클릭으로 수정했다. 페이지 오류·예기치 않은 외부 HTTP 0. 해당 브라우저 검사의 AI만 모의이고 XLSX/API/DB는 실제다. 모바일 라이트 성공, 모바일 다크 마지막 항목, 데스크톱 라이트 성공 화면을 직접 열어 확인했다.

### 최종 검증과 다음 반복

- `bun run test`: **92 통과·DB 18 생략**, 실패 0. `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test`: **502/502 통과**, 생략 0. `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 변경 전 실패 없음·이번 변경의 미해결 회귀 없음.
- **W3 완료, 전체 P0 미완료.** 새 업로드의 세부 분할만 변경했으며 기존 초안/게시 문서를 재분할하거나 자동 게시하지 않았다. 실제 AI 검증은 위의 합성 자료 생성이며, 브라우저/원자성 검증은 격리 DB와 모의 AI를 사용했다. 원격 Neon 자체·Node.js 22 실행·배포는 미검증이다. W2의 특정 원본 Excel 오류 재현 대기와 E2·U1~U3는 남아 있다. GOAL의 미완료 체크박스를 완료로 표시하지 않았다.
- **다음 반복: E2 — 초안 수정의 현재 입력 미리보기.** 기존 `WikiEditor`와 읽기 전용 렌더링을 초안 일반 수정·최종 통합 입력에 적용하고 E4의 원래 버전·재확인·임시 복구 및 미저장 게시 제한을 유지한다. W2 재현 자료가 제공되면 해당 실패를 먼저 재현한다.
- 증거: `/tmp/wiki-viki-w3-20260921/`의 `baseline-*.log`, `fine-topics-red/green.log`, `fine-db.log`, `live-ai*.log/json`, `browser-final.log`, `browser/xlsx-browser-results.json`와 화면 캡처, `final-test/db/check/lint/build.log`. 최종 실제 SQL에서 잔여 테스트 스키마 **0개**를 확인한 뒤 검증 서버·브라우저·PostgreSQL을 정리했다. 사용자 개발 서버와 환경 파일·운영 DB·원격 저장소·배포는 변경하지 않았다. DB 재개 환경은 `/tmp/wiki-viki-s3a-20260921/postgres`, 루프백 55437의 `wiki_viki_test_governance`다.

## 반복 27 — L1 문서 자동 연결·전체 검사 (2026-09-21~22)

- 최신 사용자 요청을 우선한 독립 작업이다. 적용 AGENTS 부재, GOAL/README/설정/기록/git status와 실제 렌더링·업로드·그래프 코드를 확인했다. 현재는 업로드 시 AI 제안 제목 일부만 연결하고, 기존 일반 본문과 뒤늦게 생긴 문서는 재연결하지 않는다.
- **L1 완료:** ① 제목·등록 별칭의 공통 자동 연결 규칙과 오탐/중복/코드·기존 링크 보호 ② 읽기·초안 미리보기·역링크·관련 문서·지도 적용 ③ 전체 검사·재계산 UI 및 DB/브라우저 회귀. 같은 사용자 동작의 서버·화면·관계 계산을 한 단위로 구현했다.
- 사람의 개별 링크 승인 없이 최신 활성 문서 목록으로 읽기 시 연결을 계산한다. 원문/리비전/초안 검토 버전을 다시 저장하지 않으므로 동시 편집 덮어쓰기·대량 리비전 생성이 없다. 신규 게시/별칭 변경/휴지통/복구는 다음 조회부터 반영된다. 본문 의미를 외부 AI에 전송하거나 단순 연관 추측으로 링크를 만들지 않고 정확한 제목·별칭을 사용한다. 모호한 이름은 자동 선택하지 않는다. 전체 검사 화면은 동일 규칙으로 전체 연결을 다시 계산하며 영구 본문 변경을 했다고 표시하지 않는다.
- 변경 전: 기본 테스트 **92 통과·DB 18 생략**, 실제 격리 PostgreSQL **502/502**, check 오류/경고 0, lint/build 통과. 기존 검사 실패 없음. 일반 본문·등록 별칭의 연결이 빠지는 신규 회귀를 기존 코드에서 재현했다(`automatic-red.log`).
- `wiki-links.js`는 기존 Marked 토큰을 이용해 제목/별칭의 긴 이름 우선·한국어 조사·영문 대소문자·대상별 1회 연결을 계산한다. 수동 링크를 우선하고 코드·이미지·주소·기존 링크·자기 문서·부분 단어·한 글자/숫자만인 이름·모호한 이름을 제외한다. `wiki.js`의 기존 이스케이프/주소 제한/목차/각주/초안 링크를 유지하며 `knowledge.js`도 같은 토큰 결과로 관계를 계산한다. `document-links.js`는 한 SQL 스냅샷에서 활성 문서와 별칭을 읽고 BIGINT ID를 문자열로 보존한다. 삭제된 직접 URL과 충돌하는 과거 별칭도 실제 라우팅과 일치하게 제외한다.
- 읽기/홈/초안 목록·상세/API 미리보기에 적용했다. `/links`는 실제 전체 문서/연결/자동 연결/고립 문서 수, 미작성 링크·모호한 이름, 문서별 대상과 20개 페이지 목록을 표시한다. **전체 검사·자동 재연결**은 최신 DB로 재계산하고 로딩·중복 제출 제한·오류·재시도·빈 상태·JavaScript 없는 GET 폼을 제공한다. Header와 관련 문서에서 진입한다. 스키마/의존성 변경이나 영구 연결 데이터의 별도 동기화는 없다.
- 신규 순수 회귀 7개와 실제 DB 검사 **7/7**을 통과했다. 실제 저장으로 새 대상 게시→기존 본문 자동 연결, 별칭 제거, 삭제/복구, 과거 별칭/휴지통 URL 충돌, 반복/동시 전체 검사와 실제 편집, 원문·리비전·수정 시각·검토 버전 보존, 23개 문서 전체 통계와 페이지 구분, DB 테이블 장애를 검증했다. 외부 AI/HTTP 시도 **0**. DB 테스트의 첫 실패는 saveDocument 호출에 필수 requestedSlug가 빠진 테스트 작성 오류였으며 실제 계약대로 보완했다.
- 신규 실제 Chrome 검사 **360/768/1440px × 라이트/다크 통과**: 본문/별칭 링크·코드 제외·키보드 진입·역링크, 전체 검사, 응답 대기 중 새 대상 추가→재연결, 로딩/빈/DB 오류·재시도, 원문/이력 불변, 기본 GET 폼·20/3개 페이지 이동. 페이지 오류와 예기치 않은 외부 HTTP **0**. 모바일 다크 검사 목록, 데스크톱 라이트 본문, 태블릿 로딩, 모바일 라이트 오류 캡처를 직접 확인했다. 처음 발견한 모바일 메뉴의 가로 넘침은 작은 화면에서 메뉴 줄바꿈을 허용해 수정하고 6개 조합을 다시 통과했다. 테스트 실행 플래그 누락은 격리 fixture가 DB 접속 전에 거부했고 명시적으로 설정해 재실행했다.
- 공유 헤더·렌더러 변경의 기존 `draft-batch.mjs` 브라우저 회귀도 최종 통과했다. 새 메뉴로 높아진 헤더가 JavaScript 없는 기본 폼의 게시 버튼을 가리는 실제 회귀를 발견했고, `DraftReviewList.svelte`의 작업 버튼에도 기존 스크롤 여백을 적용해 해결했다. 강제 클릭/검사 생략 없이 6개 화면 조합·작은 높이·기본 폼·부분 실패·응답 유실 재시도까지 재검증했다. 페이지 오류/예기치 않은 외부 HTTP 0, AI 응답만 기존대로 모의 처리한다.
- 기존 `edit-preview.mjs`도 전체 통과했다. 360/768/1440px × 라이트/다크의 현재 입력 렌더링·목차·링크·모바일 탭·빈/오류·충돌·늦은 응답·10초 제한·수정 재시도·기본 폼을 확인했다. 페이지 오류·외부 HTTP 0이며 모바일 라이트 미리보기 캡처도 직접 확인했다. 자동 연결이 편집 승인을 대신하거나 원래 검토 버전을 바꾸지 않는다.

### 최종 검증과 다음 반복

- `bun run test`: **100 통과·DB 19 생략**, 실패 0. `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test`: **517/517 통과**, 생략 0. `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 실제 운영 환경 변수 대신 빈 DB/AI 환경을 명시하고 DB 검사는 격리 fixture만 사용했다. 변경 전 실패 없음·이번 변경의 미해결 회귀 없음.
- **L1 완료, 전체 P0 미완료.** 실제 OpenAI 호출은 이 기능에 필요하지 않으며 호출하지 않았다. 원격 Neon·Node.js 22 실행·배포는 미검증이다. W2의 특정 원본 Excel 오류 재현 대기와 E2·U1~U3가 남아 GOAL 전체 체크박스를 완료로 바꾸지 않았다.
- **다음 반복: E2 — 초안 수정의 현재 입력 미리보기.** 기존 `WikiEditor`/읽기 전용 렌더링을 초안 일반 수정·최종 통합 입력에 적용하고 E4의 원래 버전·재확인·임시 복구 및 미저장 게시 제한을 유지한다. W2 재현 자료가 제공되면 해당 실패를 먼저 재현한다.
- 증거: `/tmp/wiki-viki-l1-20260921/`의 `baseline-*.log`, `automatic-red/green.log`, `links-unit.log`, `links-db-final.log`, `links-boundaries.log`, `browser-final.log`, `browser/automatic-links-results.json`와 화면 캡처, `batch-regression-final.log`, `preview-regression.log`, `final-test/db/check/lint/build.log`. 최종 실제 SQL로 잔여 테스트 스키마 **0개**를 확인하고 이번 검증의 서버·브라우저·PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 변경하지 않았다. DB 재개 환경은 `/tmp/wiki-viki-s3a-20260921/postgres`, 루프백 55437의 `wiki_viki_test_governance`다.

## 반복 28 — U3a 읽기 화면의 브랜드 테마 통일 (2026-09-22)

- 최신 사용자 요청을 우선한 스타일 작업이다. 적용 AGENTS 부재, GOAL §7.1/§7.3/§7.8, README, 실제 Vite 설정·스크립트·진행 기록·git status를 확인했다. 기존 사용자 변경과 서버/문서 데이터는 보존한다. `.article-page`/`.wiki-preview`가 공통 브랜드 토큰을 흰색·청회색·파란 링크·녹색 강조로 덮어쓰는 것이 주변과 다른 원인이다.
- **이번 완료 기준:** ① 읽기/미리보기의 공통 라이트·다크 팔레트 공유 ② 목차·관련 문서·편집 안내·본문의 모서리/경계/링크 스타일 정리 ③ 실제 Chrome의 360/768/1440px·두 테마·글자 대비·목차/각주/링크/표·빈 상태와 기존 미리보기 회귀 확인. 별도 탐색/본문 기능이나 U1/U3 전체 개편은 포함하지 않는다.
- 변경 전 `bun run test` **100 통과·DB 19 생략**, `bun run check` 오류 0·경고 0, lint/build 통과. 새 기능·SQL·모델 변경이 없는 스타일 작업이므로 전체 DB 원자성 검사는 재실행하지 않고, 시각 검증에 실제 격리 DB/서버를 사용한다. 기존 검사 실패 없음. 비밀 환경을 읽지 않고 테스트에 빈 DB/AI 변수와 별도 로컬 DB를 지정한다.
- `src/app.css`만 제품 코드 변경: 읽기/미리보기의 하드코딩 팔레트를 제거해 공통 종이·본문·보조 면·경계·링크·그림자 토큰을 상속한다. 작은 메타 글자는 공통 보조 글자색으로 대비를 유지한다. 문서·관련 패널·목차·별칭 안내·편집 안내에 주변 화면과 같은 둥근 모서리를 적용하고, 문단/선택 강조는 브랜드 오렌지·인용은 올리브를 사용한다. 본문 링크에 밑줄을 표시해 색만으로 구별하지 않게 했다. 고딕체·제목 계층·본문 폭·기존 URL/목차/각주/자동 링크는 유지한다.
- 격리 PostgreSQL의 합성 문서로 수정 전 360/1440px × 라이트/다크를 캡처하고, 수정 후 **360/768/1440px × 라이트/다크 6조합**을 실제 Chrome에서 검증했다. 별칭 진입·목차 접기/펼치기와 키보드 문단/각주 이동·표/코드 표시·페이지 가로 넘침 없음·편집 미리보기·빈 본문/미작성/휴지통 상태·테마 즉시 전환을 통과했다. 문서/리비전 불변·페이지 오류 0·외부 HTTP 0. 정상/목차/표/코드/편집 안내의 모바일·태블릿·데스크톱 캡처를 직접 열어 확인했다.
- 실제 계산 스타일로 본문·수정 정보·일반 링크·미작성 링크의 대비를 측정했다. 라이트 **15.17/6.02/6.22/4.79:1**, 다크 **11.05/7.87/7.15/6.91:1**로 네 종류 모두 4.5:1 이상이다. CSS 선언을 복제하는 단위 테스트는 추가하지 않고 재실행 가능한 시각 검증 스크립트와 캡처를 임시 증거 폴더에 보관한다.
- 기존 `node tests/browser/edit-preview.mjs` 전체 통과: 6개 화면 조합의 현재 입력 렌더·모바일 탭·목차, 로딩/빈/오류·재시도·충돌·늦은 응답·시간 제한·키보드·JavaScript 없는 저장을 실제 Chrome/격리 DB로 재검증했다. 페이지 오류·외부 HTTP 0. 데스크톱 다크의 편집/미리보기 캡처도 직접 확인했다.

### 최종 검증과 다음 반복

- `bun run test`: **100 통과·DB 19 생략**, 실패 0. `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 변경 전 실패와 이번 변경의 미해결 회귀 모두 없음. 전체 DB 원자성 검사는 스타일만 변경하여 생략했으며, 브라우저 검사의 실제 격리 DB 확인과 구분한다.
- **U3a 완료, 전체 P0 미완료.** 제품 변경은 `src/app.css`에 한정하고 GOAL의 기존 본문/체크박스는 변경하지 않았다. 실제 외부 서비스 호출·운영 DB·Node.js 22 실행·배포 검증은 수행하지 않았다. 전체 화면의 시각 체계(U1)·탐색(U2)·읽기 기능 확장(U3), E2 및 W2 원본 Excel 오류 재현 대기는 남아 있다.
- **다음 반복: E2 — 초안 수정의 현재 입력 미리보기.** 기존 렌더러와 `WikiEditor`를 재사용하되 일반 수정·최종 통합 입력을 구분하고 원래 버전·명시적 재확인·입력 복구를 보존한다.
- 증거: `/tmp/wiki-viki-theme-20260922/`의 `baseline-*.log`, `browser.mjs`, `browser-before/after.log`, `before/after`의 화면 캡처·`results.json`, `preview-regression.log`와 캡처, `final-test/check/lint/build.log`. 최종 SQL로 잔여 테스트 스키마 **0개**를 확인한 뒤 이번 테스트 서버·브라우저·PostgreSQL을 종료했다. 사용자 개발 서버와 환경 파일은 그대로 유지했다. DB 재개 환경은 이전 기록의 로컬 루프백 55437 클러스터다.

## 반복 29 — E2 초안 일반 수정·최종 통합 입력 미리보기 (2026-09-22)

- 직전 목표 턴은 **진행**이다. U3a의 실제 CSS 수정과 시각/정적 검증 증거가 남았고, 현재 코드에서도 적용을 확인했다. 목표 원본 파일, 적용 AGENTS 부재, GOAL/README/설정/진행 기록/git status를 다시 확인하고 P0의 다음 미완료 E2를 선택했다.
- 현재 초안 화면은 저장된 `previewHtml`로만 전환하며, 일반 수정·최종 통합 입력에는 현재 입력 미리보기가 없다. **이번 단위:** ① 공통 `WikiEditor`를 두 입력에 적용하고 고유 입력/목차/각주 ID로 구분 ② 같은 읽기 전용 API에서 새 게시 별칭·통합 대상의 실제 링크 규칙을 반영 ③ 미저장/저장본/AI 원안 구분, 버전·재확인·복구 유지 및 DB/실제 브라우저 회귀. 저장된 초안 비교는 명시적으로 구분해 유지한다.
- 변경 전 기본 테스트 **100 통과·DB 19 생략**, 실제 격리 PostgreSQL **517/517**, check 오류/경고 0, lint/build 통과. 기존 실패 없음. 데이터/동시성 보호는 기존 액션을 재사용하고, 미리보기는 저장·AI 검사·게시 승인을 수행하지 않는다.
- `WikiEditor.svelte`, `drafts/+page.svelte`, `MergeReview.svelte`: 일반 본문과 최종 통합 본문에 현재 입력 미리보기를 적용했다. 넓은 화면은 나란히, 작은 화면은 키보드 탭으로 전환한다. 현재 저장본·AI 원안·미저장 변경을 구분하고 저장된 초안은 별도 비교 영역으로 유지한다. 두 편집기의 필드 이름과 복구·검토 버전·재확인·미저장 게시 제한은 기존 계약을 유지한다.
- `api/preview/+server.js`: 현재 제목/별칭과 실제 통합 대상·사용 가능한 별칭으로 링크를 계산한다. 통합 대상/별칭/제안이 바뀌면 409로 거부하고 입력을 유지한다. 각 편집기와 저장본의 문단·각주 ID를 분리한다. 초안 이미지는 기존 목록처럼 링크로 표시하며 자동 외부 이미지 요청을 하지 않는다. 본문·이력·초안 상태·DB 모델을 바꾸거나 외부 AI를 호출하지 않는다.
- 신규 `draft-preview-db.test.js`는 실제 PostgreSQL에서 미리보기 전후 상태/버전 불변, 실제 일반 저장→게시·통합 적용 결과와 HTML 비교, 별칭 충돌/휴지통/대상 변경·BIGINT·오류·외부 호출 0을 검사한다. 개발 중 테스트의 함수 닫힘 누락과 기본 폼의 CRLF 정규화 차이를 수정했고, 숫자형 초안 ID가 정규식에서 묵시 변환되던 API 입력은 문자열만 받도록 보완했다.
- 신규 `tests/browser/draft-preview.mjs`는 360/768/1440px × 라이트/다크에서 두 미리보기·저장본 비교·고유 앵커·키보드·실제 통합 적용을 통과했다. 빈/로딩/실제 DB 503·재시도, 취소 뒤 늦은 응답, 변경된 통합 대상 409, 보호 입력의 수정 초점도 통과했다. 페이지 오류·예기치 않은 외부 HTTP 0. 모바일/데스크톱 정상·로딩·오류 캡처를 직접 확인했다.
- 기존 E4 복구 검사에서 새로운 편집기의 `aria-busy`와 `noscript` 안내가 기존 선택자와 겹쳤다. 저장 폼과 복구 안내로 선택자를 한정해 기존 검사를 유지했다. 별도로 변경된 통합 대상의 409 응답 뒤 요청이 끝나지 않는 현상을 실제 브라우저 요청 객체 추적으로 확인했다. 공통 편집기가 오류 응답의 본문도 소비하도록 수정했으며, 신규 브라우저 검사에 실제 409/503 뒤 요청 종료 확인을 추가해 통과했다. 단순 시간 제한 연장이나 검사 삭제로 우회하지 않았다. 기존 E4·E1 및 전체 최종 검증은 진행 중이다.
- 기존 `node tests/browser/draft-recovery.mjs`와 `node tests/browser/edit-preview.mjs` 최종 **모두 통과**했다. 초안/최종 입력 복구·원래 버전·새 확인·저장/통합 실패의 실제 롤백·다른 탭·삭제/게시·기본 폼, 직접 편집의 6개 화면 조합·현재 입력·충돌·늦은 응답·시간 제한·키보드·기본 저장을 재확인했다. 각각 페이지 오류·예기치 않은 외부 HTTP 0. E4의 AI 응답은 기존대로 모의 처리하며, 실제 API/격리 DB·브라우저 검사와 구분한다.

### 최종 검증과 다음 반복

- `bun run test`: **100 통과·DB 20 생략**, 실패 0. `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 DB> bun run test`: **522/522 통과**, 생략 0. 새 DB 검사 5개도 포함하며 실제 별칭 변경 후 오래된 통합 미리보기 거부를 재확인했다. `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 변경 전 실패 없음·이번 변경의 미해결 회귀 없음.
- **E2 완료, 전체 P0 미완료.** 미리보기의 위키 문법·문단·각주·링크는 실제 게시 결과와 비교했다. 초안 이미지는 자동 외부 요청을 막는 링크 표시 정책을 화면과 README에 명시했다. 공통 UI·탐색·읽기 확장과 전체 완료 감사가 남아 GOAL 체크박스는 변경하지 않았다. W2의 특정 Excel 원본 오류 재현은 확장자·오류 문구 또는 비식별 재현 파일을 기다린다. 실제 외부 AI 호출·원격 Neon·Node.js 22·운영 배포 검증은 수행하지 않았다.
- **다음 반복: U1a — 직접 편집·초안 업무 화면의 고딕체와 제목 계층을 통일한다.** 공통 브랜드 로고/색상과 이번 편집·미리보기·복구 흐름을 보존하면서 두 업무 화면의 제목/상태 계층을 정리하고 라이트/다크·360/768/1440px·200% 확대·키보드를 확인한다. U1 전체 7개 화면을 한 번에 개편하지 않는다. W2 재현 조건이 제공되면 해당 실패를 먼저 재현한다.
- 증거: `/tmp/wiki-viki-e2-20260922/`의 `baseline-*.log`, `preview-db-final.log`, `browser-final.log`, `browser/draft-preview-results.json`와 화면 캡처, `debug-stale-requests.log`, `recovery-regression-verified.log`, `recovery-regression/draft-browser-results.json`, `edit-regression.log`와 캡처, `final-test/db/check/lint/build.log`. 환경 파일·운영 데이터·사용자 개발 서버·원격 저장소·배포는 변경하지 않았다. 재개 DB는 기존 `/tmp/wiki-viki-s3a-20260921/postgres`, 루프백 55437의 `wiki_viki_test_governance`다.
- 종료 시 실제 SQL로 잔여 테스트 스키마 **0개**를 확인했다. 이번 검증의 브라우저·서버 정리 후 직접 시작한 격리 PostgreSQL을 종료했다.

## 반복 30 — U1a 편집·초안 화면의 글자 계층 (2026-09-22)

- 직전 목표 턴은 **진행**이다. 현재 코드의 E2 편집기/API와 실제 522개 통과 로그를 다시 확인했다. 목표 원본 파일·GOAL·README·진행 기록·실제 Vite 설정/스크립트·git status와 적용 AGENTS 부재를 확인했다. 이전/사용자 변경을 보존하며 P0의 다음 미완료 U1 중 업무 화면 두 경로를 선택했다.
- 편집·초안의 작업 제목은 아직 Caprasimo/Gowun Batang을 상속하고, 미리보기는 고딕체를 사용한다. 직접 편집에서는 복구 안내 제목이 문서의 주 제목보다 앞선다. **이번 완료 기준:** 고딕체와 주/절/보조 제목 계층 공유, 페이지 주 제목을 먼저 식별, 긴 제목·원본 파일명 줄바꿈과 안내 대비, 두 테마·360/768/1440px·200% 확대·키보드 및 기존 편집 흐름 검증. 본문·브랜드 로고·서버/데이터 계약은 보존한다.
- 변경 전 `bun run test` **100 통과·DB 20 생략**, check 오류/경고 0, lint/build 통과. 기존 자동 검사 실패 없음. 글꼴/배치/마크업만 변경하므로 전체 DB 원자성 검사는 반복하지 않고 실제 격리 DB의 화면과 기존 미리보기·복구 브라우저 검사로 확인한다. 초기 로컬 DB 시작에서 포트 옵션이 빠져 5432로 열려 55437 연결이 거부됐다. 프로세스/로그로 확인하고 직접 시작한 클러스터만 명시적 포트·소켓으로 재시작했다. 운영 DB에는 접속하지 않았다.
- `src/app.css`에 기존 로컬 고딕체 토큰과 두 업무 화면의 공통 글자 계층을 적용했다. 주 제목은 24~30px, 절/하위 제목은 20/17px를 기준으로 하고 기존 복구·목록 컴포넌트의 보조 크기를 유지한다. 작업 제목의 굵기·행간·구분선을 맞추고, 긴 제목/원본 파일명은 화면 안에서 줄바꿈한다. 작은 안내는 공통 보조 글자색으로 대비를 높였다. `.wiki-preview` 내부 본문 제목의 게시 스타일과 로고/짧은 소개의 장식체는 보존한다.
- `edit/[slug]/+page.svelte`는 페이지 주 제목을 복구 안내 앞의 공통 헤더로 옮기고 `main` 영역을 지정했다. 휴지통 상태와 복구한 작성 중 입력 제목도 유지한다. `drafts/+page.svelte`의 목록/상세에 같은 업무 화면 클래스를 적용했다. 이벤트·필드/액션·검토 버전·복구·DB 코드 변경은 없다.
- 실제 Chrome의 변경 전 360/1440px × 라이트/다크 12개 화면(직접 편집·초안 상세·목록)을 기록했고, 변경 후 360/768/1440px × 두 테마 18개 화면을 확인했다. 고딕체/굵기·주 제목 우선·긴 제목/파일명·보조 안내 대비·키보드 초점·모바일 미리보기 탭·현재 입력을 검증했다. 보조 안내의 실제 대비는 직접 편집 **5.53/9.13:1**, 초안 상세/목록 **6.02/7.87:1**(라이트/다크)이다. 정상 화면과 확대 캡처를 직접 열어 확인했다. DB 내용/이력/초안 불변·페이지 오류/외부 HTTP 0.
- 확대 검사의 초기 CSS `zoom: 2`는 브라우저 확대와 달리 1440px 미디어 분기를 유지해 전역 편집자 배지만 넘쳤다. 실제 요소 좌표와 미디어 상태를 기록해 원인을 구분했다. 1440×1000 물리 화면의 200% 확대에 해당하는 **720×500 CSS 화면·픽셀 배율 2**로 두 테마/3개 화면을 재검증해 모두 통과했다. 브라우저 도구 모음의 확대 명령을 직접 조작한 검사는 아니며 이를 확대 대응 레이아웃 검증으로 기록한다. 스타일을 복제하는 단위 테스트 대신 재실행 가능한 임시 브라우저 스크립트/계산 스타일/캡처를 남긴다.
- 신규 시각 스크립트는 입력 확인 뒤 변경을 남긴 채 다음 화면으로 이동하다 기존 미저장 이동 보호에 막혔다. 같은 원문으로 입력을 되돌린 뒤 다음 화면을 확인하도록 검증 절차만 수정했다. 이동 보호를 해제하거나 저장하지 않았다. 기존 직접 편집·초안 미리보기 및 직접 편집 복구 회귀는 진행 중이다.

### 최종 검증과 다음 반복

- `node tests/browser/edit-preview.mjs`, `node tests/browser/draft-preview.mjs`, `node tests/browser/edit-recovery.mjs` **모두 통과**. 현재 입력·목차/각주·모바일 탭·실제 통합 결과, 빈/로딩/실제 DB 오류·재시도·늦은 응답·시간 제한, 복구/충돌/휴지통/새로고침·기본 폼을 재확인했다. 각 검사 페이지 오류·예기치 않은 외부 HTTP 0. 오류 및 휴지통 복구 화면 캡처도 직접 확인했다. 복구 검사에서 기존 AI 응답만 모의 처리했으며 실제 외부 AI 품질 검증과 구분한다.
- `bun run test`: **100 통과·DB 20 생략**, 실패 0. `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 스타일/마크업 변경에 맞춰 전체 DB 원자성 검사는 생략했으며, 위 브라우저의 실제 격리 PostgreSQL 검증과 구분한다. 변경 전 실패 없음·이번 변경의 미해결 회귀 없음.
- **U1a 완료, U1 전체와 P0 미완료.** GOAL의 기존 본문/체크박스를 바꾸지 않았다. 나머지 화면의 공통 UI·상태/버튼 체계, 탐색/홈, 읽기 기능 확장과 전체 감사가 남아 있다. W2의 특정 원본 Excel 오류 재현은 원본 조건을 기다린다. 원격 Neon·실제 외부 AI·Node.js 22 실행·배포는 미검증이다. 확대는 200%에 대응하는 실제 Chrome의 CSS 화면/픽셀 배율 검증이며 브라우저 도구 모음 조작 검증과 구분한다.
- **다음 반복: U1b — 검색·토론·이력 화면의 고딕체와 제목 계층을 통일한다.** 기존 로컬 글꼴/브랜드 토큰을 공유하고 검색 결과·토론 입력·이력/되돌리기 흐름의 정상/빈/오류·모바일·키보드를 보존한다. 저장/검토 서버 로직이나 P1 기능으로 확장하지 않는다.
- 증거: `/tmp/wiki-viki-u1a-20260922/`의 `baseline-*.log`, `browser.mjs`, `browser-before.log`, `browser-final.log`, `before/after/results.json`와 화면 캡처, `zoom-debug.log`, `edit-preview/draft-preview/edit-recovery-regression.log`와 각 결과 폴더, `final-test/check/lint/build.log`. 테스트 스키마 **0개**를 실제 SQL로 확인한 뒤 이번 서버·브라우저·격리 PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 보존했다. DB 재개 시 기존 클러스터에 **`-h 127.0.0.1 -p 55437 -k /tmp/wiki-viki-s3a-20260921/socket`** 옵션을 지정한다.

## 반복 31 — U1b 검색·토론·이력의 글자 계층 (2026-09-22)

- 직전 목표 턴은 **진행**이다. U1a의 현재 CSS/페이지 클래스와 최종 빌드 증거를 다시 확인했다. 목표 원본·GOAL §7.1/§7.9/P0·README·실제 설정/스크립트·git status·진행 기록과 적용 AGENTS 부재를 확인했다. 기존/사용자 변경은 보존한다.
- 현재 검색 설명·토론·이력 제목은 장식체를 상속하고, 토론 주제는 작은 굵은 본문으로 표시된다. **이번 완료 기준:** U1a의 고딕체·주/절/보조 제목·구분선/안내 대비를 세 화면에 공유, 긴 제목/상충/기록 줄바꿈, 360/768/1440px·두 테마·확대 대응·키보드, 검색 설명/출처·토론 등록 실패/재시도·이력 비교/충돌/되돌리기 보존. 검색/저장 서버 로직이나 P1 기능은 확장하지 않는다.
- 시작 검사 `bun run test` **100 통과·DB 20 생략**, check 오류/경고 0, lint/build 통과. 기존 자동 검사 실패 없음. 스타일/제목 마크업 작업으로 전체 DB 원자성 검사는 생략하고 실제 격리 DB·Chrome의 주요 작업 흐름과 시각 검증으로 확인한다. 기존 테스트 클러스터를 명시적 루프백 55437/소켓 옵션으로 시작했으며 원격 DB·환경 파일은 사용하지 않는다.
- `app.css`의 U1a 공통 범위를 편집 전용 이름에서 `work-page`로 바꾸고 기존 편집/초안에도 같은 이름을 적용했다. 검색어가 있을 때만 홈 경로에 적용하며 소개/브랜드 영역은 유지한다. 검색·토론·이력의 주 제목은 24~30px, 주요 절/설명은 20px, 보조 제목은 17px 기준을 공유한다. 검색 제목에 구분선을 추가하고 기존 패널·색상·읽기 본문·코드/차이 스타일을 보존했다.
- `+page.svelte`, `discussion/[slug]/+page.svelte`, `history/[slug]/+page.svelte`: 공통 스타일을 연결하고 토론/이력의 `main` 영역을 지정했다. 토론 주제는 `h2`로 바꿔 제목 탐색이 가능하고 작성자/시각과 구분되게 했다. 이벤트·검색 요청·본문·폼 필드·버전·DB/서버 코드는 변경하지 않았다.
- 실제 Chrome 변경 전 360/1440px × 두 테마의 12개 화면을 기록하고, 변경 후 360/768/1440px × 두 테마의 18개 화면을 검증했다. 고딕체/굵기/주 제목 계층·긴 제목과 기록 줄바꿈·가로 넘침 없음·키보드 필터/출처/상충 펼치기를 확인했다. 보조 안내 대비는 검색 **5.53/9.13:1**, 토론/이력 **6.02/7.87:1**(라이트/다크)이다. 검색에서 홈으로 이동하면 소개 장식체가 유지되고 업무 화면 스타일이 해제된다. 읽기만 한 뒤 DB 문서·리비전·토론이 바뀌지 않음을 확인했다.
- 200%에 해당하는 720×500 CSS 화면·픽셀 배율 2로 세 화면의 두 테마와 키보드를 확인했다. 일반/빈 상태, 검색 설명 로딩·모의 완료 응답/출처·요청 오류/키 없는 실제 API 재시도, 실제 토론 INSERT 실패·입력 유지·명시적 재시도 1회 저장, 이력 충돌 거부·최신 이력 재검토·처리 중 제한·실제 되돌리기와 리비전 추가를 통과했다. 페이지 오류/외부 HTTP 0. AI 완료 응답만 합성 자료로 모의 처리하며 실제 외부 AI 호출과 구분한다. 모바일 검색·데스크톱 토론·모바일 이력/상충/오류 캡처를 직접 확인했다.
- 시각 스크립트의 첫 실패는 키 없음 안내를 실제 화면에 없는 문구로 기다린 것이었다. 현재 API 문구로 검사를 수정했다. 이후 이력 재검토의 전체 페이지 이동이 시작되기 전에 로드 상태 대기가 끝나던 경합은 이동 이벤트 대기를 먼저 등록하고, 새 화면의 되돌리기 활성 상태를 확인하도록 수정했다. 시간 제한을 늘리거나 제품 보호 검사를 제거하지 않았으며 최종 전체 브라우저 검증은 통과했다.

### 최종 검증과 다음 반복

- 공통 클래스 변경의 U1a 브라우저 회귀도 **24개 화면 조합 통과**: 편집·초안 상세/목록의 두 테마·360/768/1440px, 확대 대응, 현재 입력·키보드 탭·고유 제목 계층과 DB 불변을 다시 확인했다. 페이지 오류·외부 HTTP 0. 검증 스크립트는 기존 증거를 덮어쓰지 않고 이번 폴더에 복사했다.
- `bun run test`: **100 통과·DB 20 생략**, 실패 0. `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 스타일/마크업 변경이므로 전체 DB 원자성 검사는 생략했으며, 실제 격리 DB의 등록/실패/충돌/되돌리기 브라우저 검증과 구분한다. 변경 전 실패와 이번 변경의 미해결 회귀 모두 없음.
- **U1b 완료, U1 전체/P0 미완료.** GOAL 본문과 체크박스를 보존했다. 나머지 공통 버튼/상태/홈 UI·탐색·읽기 확장과 전체 감사, W2 원본 Excel 오류 재현이 남아 있다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 미검증이다. 확대는 브라우저 도구 모음 직접 조작이 아닌 200%에 대응하는 화면 크기/픽셀 배율 검증이다.
- **다음 반복: U1c — 공통 버튼의 주/보조/위험 구분과 대비·초점·비활성 상태를 검증하고 통일한다.** 현재 `primary-button`의 브랜드 강조색/배경색 조합부터 실제 대비를 확인하고, 저장·게시·삭제의 구분 및 모바일/키보드 작업과 위키파이어 모달을 보존한다. 새 승인 절차나 서버 액션 변경은 추가하지 않는다.
- 증거: `/tmp/wiki-viki-u1b-20260922/`의 `baseline-*.log`, `browser.mjs`, `browser-before.log`, `browser-verified.log`, `before/after`의 화면 캡처·`results.json`, `u1a-regression.mjs/log`와 하위 결과, `final-test/check/lint/build.log`. 종료 시 SQL로 잔여 테스트 스키마 **0개**를 확인하고 이번 검증의 브라우저·서버·PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 보존했다. DB 재개 옵션은 반복 30 기록과 같다.

## 반복 32 — U1c 공통 버튼 상태와 대비 (2026-09-22)

### 시작 확인과 범위

- 직전 반복은 U1b 완료로 분류한다. 현재 공통 `work-page`와 화면 마크업, 검증 기록을 다시 확인했으며 미해결 회귀가 없었다. `AGENTS.md` 없음, GOAL/README/스크립트·git 상태를 확인하고 기존 변경을 보존했다.
- 변경 전 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build` 통과. Chrome의 360/1440px × 두 테마에서 편집·초안·빈 모달 12개 화면 조합을 확인했다. 읽기만 한 후 실제 격리 PostgreSQL의 문서·리비전·초안·토론이 동일했고 외부 HTTP/페이지 오류 0이었다.
- 기존 라이트 주 버튼의 글자 대비는 약 **3.03:1**이며 비활성 상태에도 호버 배경이 적용됐다. 브랜드 오렌지를 보존하고 글자/테두리/초점/비활성 구분을 개선하는 스타일 단위로 범위를 한정한다. 서버 액션·데이터 모델·승인 절차는 변경하지 않는다.
- 증거 위치: `/tmp/wiki-viki-u1c-20260922/`. CSS만 변경하므로 전체 DB 원자성 검사를 반복하는 대신 실제 격리 DB와 브라우저에서 저장·일괄 검토·업로드 회귀를 실행한다.

### 구현과 시각 검증

- `src/app.css`: 공통 주 버튼은 브랜드 오렌지와 진한 글자, 보조 버튼은 종이색 면/중립 테두리, 위험 버튼은 경고색 글자/테두리로 구분했다. 세 버튼의 44px 최소 높이·600 굵기·긴 이름 줄바꿈과 3px 키보드 초점을 통일했다. 상단 검색 아이콘은 기존 28px 배치를 유지한다.
- 공통 버튼의 호버/눌림은 활성 상태에만 적용한다. 비활성 상태는 투명도 대신 읽을 수 있는 중립 글자/배경과 점선 테두리를 사용하며 기존 native `disabled`를 그대로 유지한다. 원래 모달 열기·폼 제출·링크·확인 조건과 데이터는 변경하지 않았다.
- Chrome **66개 화면 조합 통과**: 홈·검색·읽기·편집·초안·토론·이력·휴지통·자동 연결의 360/768/1440px × 라이트/다크 및 빈 위키파이어 모달, 편집·초안·이력의 200% 대응 레이아웃(720×500 CSS, 픽셀 배율 2). 확대는 브라우저 도구 모음 조작과 구분한다. 일반/호버/눌림/키보드 초점, 최소 높이, 비활성 호버 불변과 실제 클릭 이벤트 0, 가로 넘침 없음, 모달 Escape를 확인했다.
- 텍스트 공통 버튼의 모든 활성 상태 최소 대비는 라이트 **4.60:1**, 다크 **6.30:1**. 비활성 글자는 **5.36/6.78:1**, 초점은 **5.54/5.95:1** 이상이다. 아이콘만 있는 기존 상단 검색은 비텍스트 대비 3:1 이상을 별도로 확인했다. 모바일 편집/빈 모달, 태블릿 다크 이력, 데스크톱 다크 편집/라이트 홈 캡처를 직접 확인했다.
- 시각 검사 전후 실제 격리 PostgreSQL의 문서·리비전·초안·토론은 동일했다. 페이지 오류/외부 HTTP 0. 자동 시각 측정 스크립트는 임시 증거 폴더에 보관하며, CSS 전용 변경에 구현을 복제하는 단위 테스트는 추가하지 않았다.

### 최종 검증과 다음 반복

- 실제 격리 DB의 기존 브라우저 회귀 **모두 통과**: `tests/browser/draft-batch.mjs` 7개 묶음(360/768/1440px × 두 테마, 일부 성공/충돌, 응답 유실 후 중복 방지 재시도, 기본 폼), `tests/browser/xlsx-upload.mjs` 13개 묶음(실제 XLSX→API→저장, 모달 닫기/재열기·고정 X·내부 스크롤·오류/시간 초과, 32개 초안 결과/검토), `tests/browser/edit-preview.mjs` 12개 묶음(현재 입력·저장·미리보기 일치, 빈/로딩/오류·재시도·이동·키보드·기본 폼). 외부 HTTP/페이지 오류 0. AI 응답은 모의 처리했으며 실제 외부 서비스 호출과 구분한다.
- `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징) 통과. 전체 DB 원자성 검사 생략은 CSS 전용 범위에 따른 것이며 실제 DB 브라우저 검증과 구분한다. 초기 추가 기록의 빈 줄 포맷만 Prettier로 정리했고 최종 린트에 통과했다. 변경 전 기능 실패와 이번 변경의 미해결 회귀는 없다.
- **U1c 완료, U1 전체/P0 미완료.** 나머지 공통 상태/홈 UI·탐색·읽기 확장과 전체 감사, W2 원본 Excel 오류 재현이 남아 있다. GOAL 본문·체크박스는 변경하지 않았다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 이번 반복에서 검증하지 않았다.
- **다음 반복: U1d — 저장·검사·게시의 공통 안내/오류/처리 상태를 시각적으로 구분하고 대비·읽기 순서·재시도 동작을 확인한다.** 기존 실제 서버 상태·문구와 검사 생략 표시를 보존하며 근거 없는 진행률이나 새로운 승인 절차를 추가하지 않는다.
- 증거: `/tmp/wiki-viki-u1c-20260922/`의 `baseline-*.log`, `app-before.css`, `browser.mjs`, `browser-before/after.log`, `before/after`의 화면·`results.json`, `draft-batch`, `xlsx-upload`, `edit-preview`의 결과 JSON/화면 및 동명 로그, `final-test/check/lint/build.log`. SQL로 잔여 테스트 스키마 **0개**를 확인하고 이번 검증의 브라우저·서버·PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 보존했다. DB 재개 옵션은 반복 30 기록과 같다.

## 반복 33 — U1d 공통 상태 안내 (2026-09-22)

- 직전 목표 턴은 **진행**: U1c CSS와 실제 검증 로그를 다시 확인했다. 목표 원본·GOAL·README·프로젝트 설정·git 상태와 적용 AGENTS 부재를 확인했다. 기존 사용자 변경·데이터·서버 액션은 보존한다.
- 변경 전 `bun run test` **100 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 기존 검사 실패 없음. 검사 생략/재검토 안내와 저장·전송 오류가 같은 `.notice.warning`을 사용하고, 처리 중·완료 표시가 화면마다 다른 상태다.
- 완료 기준: 재사용하는 `StatusNotice`에 안내·확인 필요·오류·완료·처리 중을 실제 문구와 함께 표시하고 공통 색/테두리/글자 계층을 적용한다. 기존 live role·오류 사유·수정 링크·재시도·입력 보존·모달 동작을 유지한다. 색만으로 상태를 전달하거나 진행률/완료를 추정하지 않는다.
- 구현·검증 진행 중. 공통 컴포넌트/마크업/스타일만 변경하므로 전체 DB 원자성 검사 대신 기존 실제 DB 브라우저 흐름을 재검증한다. 증거 폴더 `/tmp/wiki-viki-u1d-20260922/`에 변경 전 파일과 기준 검사 로그를 보관했다.

### 구현과 현재 검증

- `StatusNotice.svelte`에 상태 제목·본문·기존 live role을 공유하는 컴포넌트를 추가했다. `app.css`는 안내의 중립 면, 확인 필요의 오렌지, 오류의 따뜻한 적색, 실제 완료의 올리브, 처리 중의 점선 테두리를 사용한다. 상태 이름을 실제 텍스트로 표시하며 안내 안의 다음 행동 링크에 밑줄과 충분한 대비를 적용한다.
- 검색 설명·직접 편집·초안/통합·일괄 게시·위키파이어·토론·이력·자동 연결·휴지통·임시 복구의 기존 알림을 연결했다. 기존 요청·액션·본문·검토/저장 조건은 보존하고, 상태를 추론하는 서버 로직을 추가하지 않았다. 일부 게시 실패는 확인 필요, 실제 전부 성공만 완료로 표시한다. 비교 편집 패널과 접는 상세 내용은 해당 구조를 유지하고 공통 notice 스타일만 공유한다.
- 타입 검사 오류/경고 0. 실제 DB 브라우저 검증은 기존 스크립트를 증거 폴더에 복사해 실행하며, 각 화면 캡처 직전에 상태 제목·테두리·글꼴·본문/링크 대비·밑줄을 추가 측정한다. 처음 대비 검사 실패는 검사기의 RGBA 알파를 밝기 성분으로 계산해 NaN이 된 문제였다. RGB 세 성분으로 고쳤고 제품 스타일이나 통과 기준을 낮추지 않았다.
- 일괄 게시/Excel 브라우저 검사는 통과했다. 모바일 처리 중·태블릿 다크 일부 게시 결과·모바일 업로드 오류·다크 업로드 처리 중 화면을 직접 확인했다. 추가 검증 진행 중.

- 임시 복구 회귀에서 JavaScript 비활성 안내의 기존 `noscript p` 문단이 공통 컴포넌트의 `div`로 바뀐 것을 발견했다. 컴포넌트 안에 원래 안내 문단을 보존하도록 수정했으며 검사를 삭제하거나 선택자를 완화하지 않았다. 전체 임시 복구/기본 폼 검증을 재실행한다.

### 최종 검증과 다음 반복

- 실제 격리 PostgreSQL과 Chrome의 **6개 브라우저 검사 모두 통과**: 일괄 게시 7개 묶음, Excel 업로드 13개, 초안/통합 미리보기 7개, 직접 편집 복구 14개, 자동 연결 7개, 검색·토론·이력 25개. 360/768/1440px × 두 테마, 정상/빈/로딩/오류, 키보드, 검사 생략, 실제 저장 실패·입력 복구·충돌·명시적 재시도·기본 폼, 일부 게시/응답 유실, 모달 고정 X/내부 스크롤·닫기/재열기를 확인했다. 검색·토론·이력은 200%에 대응하는 720×500 CSS/픽셀 배율 2도 확인했으며 브라우저 도구 모음 확대 조작과 구분한다.
- 상태 안내의 제목/본문/다음 행동 링크를 실제 계산 스타일로 검사했다. 최소 글자 대비는 라이트 **5.47:1**, 다크 **6.36:1**이며 다섯 상태를 모두 확인했다. 표시는 상태 이름과 테두리를 함께 사용하고, 다음 행동 링크의 밑줄/주소를 유지했다. 페이지 오류와 예기치 않은 외부 HTTP 0. AI 완료/검사 응답은 모의 처리했으며 실제 AI 호출과 구분한다.
- 임시 복구 재검증에서 JavaScript 비활성 안내와 실제 기본 폼 저장까지 통과했다. 자동 연결 스크립트 복사본은 다른 상대 루트 계산 때문에 최초에 Vite 설정을 찾지 못했다. 원본 프로젝트 경로를 명시한 뒤 통과했으며 제품 설정/테스트 기준은 바꾸지 않았다. 기존 12개 화면/컴포넌트의 script 본문이 새 컴포넌트 import 외에는 동일한 것도 확인했다.
- 최종 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징) 통과. 마크업·스타일 변경이므로 전체 DB 원자성 검사를 생략하고 위의 실제 DB 브라우저 검증과 구분한다. 기준 검사 실패 없음, 이번 변경의 미해결 회귀 없음. GOAL 본문/체크박스는 보존했다.
- **U1d 완료, U1 전체/P0 미완료.** 홈 업무 영역·운영 화면의 남은 글자 계층, 탐색·읽기 확장과 전체 완료 감사, W2 특정 원본 Excel 오류 재현이 남았다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 이번 반복에서 검증하지 않았다.
- **다음 반복: U1e — 홈의 업무 패널과 휴지통·자동 연결 화면에 기존 공통 고딕체/제목 계층을 적용한다.** 홈의 로고/짧은 소개는 유지하고 실제 지표·목록·업무 안내의 구분과 두 테마/모바일/키보드를 확인한다. 홈 정보 배치/탐색 개편 U2로 임의 확장하지 않는다.
- 증거: `/tmp/wiki-viki-u1d-20260922/`의 `baseline-*.log`, `before/`, `notice-audit.mjs`와 `notice-audit.jsonl`, 각 브라우저 복사본/동명 로그/결과 폴더, `work-pages/after`, `implementation-check.log`, `final-test/check/lint/build.log`. SQL로 잔여 테스트 스키마 **0개**를 확인하고 이번 브라우저·서버·격리 PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 변경하지 않았다. DB 재개 옵션은 반복 30 기록과 같다.

## 반복 34 — U1e 홈 업무 영역과 운영 화면의 글자 계층 (2026-09-22)

- 직전 목표 턴은 **진행**: 공통 상태 컴포넌트와 최종 브라우저/빌드 증거를 다시 확인했다. 목표 원본·GOAL·README·설정·진행 기록·git status를 읽었고 적용 AGENTS는 없었다. 기존 변경과 데이터·서버 액션을 보존한다.
- 기준 검사 `bun run test` **100 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 홈 지표/업무 패널의 장식체와 휴지통의 별도 제목 크기, 자동 연결의 별도 제목/보조 안내 스타일을 확인했다.
- 완료 기준: 홈 주 소개/로고는 유지하고 업무 패널·실제 수치·보조 정보에 공통 글자 계층을 적용한다. 휴지통·자동 연결은 기존 `work-page`와 제목 헤더를 공유한다. 두 테마·360/768/1440px·확대 대응·긴 이름·키보드·실제 지표/복구/관계 보존을 검증한다. U2의 정보 재배치/탐색 개편과 서버 기능으로 범위를 확장하지 않는다.
- 구현/검증 진행 중. 스타일/마크업만 변경하므로 전체 DB 원자성 검사는 반복하지 않고 실제 격리 DB에서 복구 실패·충돌·재시도와 홈/연결 화면을 확인한다. 증거 위치 `/tmp/wiki-viki-u1e-20260922/`, 변경 전 파일과 기준 검사 로그를 보관했다.

### 구현과 실제 화면 검증

- `src/routes/+page.svelte`: 주 소개 뒤의 업무 영역에 `work-page`를 적용했다. 최근 변경·초안·연결·공지·토론 패널은 같은 수준의 `h2`로 정리했다. 로고·주 소개의 장식체와 보기 URL·지표·목록·링크·서버 script는 보존했다.
- `src/routes/trash/+page.svelte`, `src/routes/links/+page.svelte`: 공통 업무 화면/헤더와 24~30px 주 제목·20px 절 제목·구분선을 공유한다. 휴지통은 `main` 영역을 지정했고 자동 연결의 중복 로컬 글꼴/제목 크기는 제거했다. 복구 버전·액션·관계 데이터는 변경하지 않았다.
- `src/app.css`: 지표를 고딕체·700 굵기·고정 숫자 폭으로 바꾸고 보조 안내를 공통 보조색으로 맞췄다. 선택된 보기·단계 번호·지도 중심 문서의 오렌지/올리브 글자 대비를 높이며 기존 브랜드 색을 유지했다. 업무 패널 제목은 20px로 통일하고 패널 헤더 줄바꿈을 허용했다.
- 빈 목록을 “모든 검토 완료/모든 문서 연결 완료”로 해석하던 홈 문구 두 곳을 현재 표시할 초안/미작성 링크가 없다는 안내로 바로잡았다. 빈 배열만으로 실제 검토 완료나 그래프 전체 연결을 증명할 수 없기 때문이다. 새로운 완료 상태·숫자·AI 결과를 만들지 않았다.
- 변경 전 20개 화면, 변경 후 **60개 화면/흐름 묶음 통과**: 홈 3가지 보기·자동 연결·휴지통, 360/768/1440px × 두 테마, 지도 호버, 200% 대응 레이아웃(720×500 CSS·픽셀 배율 2), 키보드 공지/초안/지도 링크, 긴 제목/파일명·가로 넘침 없음. 주 소개는 Caprasimo, 업무 제목은 Figtree 고딕체임을 실제 스타일로 확인했다. 확대는 브라우저 도구 모음 직접 조작과 구분한다.
- 글자 최소 대비는 홈 각 보기 **4.60/6.30:1**, 자동 연결 **5.53/7.87:1**, 휴지통 **6.02/7.87:1**(라이트/다크). 모바일 활동/휴지통, 태블릿 다크 지도, 데스크톱 다크 자동 연결 캡처를 직접 확인했다. 홈의 실제 DB 지표 3문서·1편집·2연결·2초안과 읽기 전후 데이터 동일성을 검증했다.
- 실제 PostgreSQL 트리거로 복구 실패를 일으켜 문서/관계 보존을 확인하고, 검토 뒤 버전 변경의 409·재검토 링크·처리 중 제한·명시적 재시도·별칭 주소 복구를 확인했다. DB 조회 실패에서는 지표 `—`, 실제 빈 DB에서는 `0`임을 구분했다. 최초 재시도 검증의 실패는 PostgreSQL BIGINT 문자열과 JSONB 숫자 ID의 엄격 비교에서 발생했다. 문자열로 정규화하여 동일 ID를 조회한 뒤 전체 검증을 통과했다. 제품 복구 로직/제약을 수정하지 않았다.
- 빈 상태 문구 수정 후 **4개 추가 화면 검사 통과**: 라이트/다크 활동·지도, 실제 빈 DB, 홈 위키파이어 키보드 열기·비활성 생성·Escape 닫기. 현재까지 페이지 오류/외부 HTTP 0. 자동 연결 기존 브라우저 회귀와 최종 검사 진행 중.

### 최종 검증과 다음 반복

- 기존 `node tests/browser/automatic-links.mjs` **7개 묶음 통과**: 두 테마·360/768/1440px의 연결/역링크·전체 재검사·새 문서 반영·빈/로딩/오류·기본 폼. 페이지 오류/외부 HTTP 0. 새 시각/복구 검사 60개, 문구/모달 추가 검사 4개와 구분한다.
- 최종 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 전체 DB 원자성 검사는 스타일/마크업 범위에 따라 생략했으며 실제 DB의 복구 실패/충돌/재시도 검증과 구분한다. 기존 검사 실패와 새 미해결 회귀 없음.
- **U1e 완료, U1 전체/P0 미완료.** 공통 패널/입력 토큰과 전체 UI 감사, U2 탐색·홈 정보 배치, U3 읽기 확장, W2 특정 원본 Excel 오류 재현이 남았다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 이번 반복에서 검증하지 않았다. GOAL 본문/체크박스·기존 사용자 변경은 보존했다.
- **다음 반복: U1f — 공통 패널·입력 영역의 크기/간격/테두리 토큰을 정리하고 U1의 남은 적용을 확인한다.** 현재 반복적인 패널/폼 스타일과 입력 면의 대비를 실제 화면 기준으로 점검하며 읽기·미리보기·모달과 모바일 작업을 보존한다. 홈 탐색 개편은 U2에 남긴다.
- 증거: `/tmp/wiki-viki-u1e-20260922/`의 `baseline-*.log`, `before/` 변경 전 파일, `browser.mjs`, `browser-before/after.log`, `before/after` 화면/결과 JSON, `empty.mjs/log`, `empty-verified`, `automatic-links.log`와 결과 폴더, `final-test/check/lint/build.log`. SQL로 잔여 테스트 스키마 **0개**를 확인하고 이번 브라우저·서버·PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 변경하지 않았다. DB 재개 옵션은 반복 30 기록과 같다.

## 반복 35 — U1f 공통 패널과 입력 토큰 (2026-09-22)

- 직전 목표 턴은 **진행**: U1e 코드와 최종 실제 DB/빌드 로그를 다시 확인했다. 목표 원본·GOAL·README·설정·진행 기록·git status와 적용 AGENTS 부재를 확인했다. 기존 사용자 변경과 서버·데이터 계약을 보존한다.
- 변경 전 `bun run test` **100 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 기존 자동 검사 실패 없음. 패널마다 28/24/18/14px 등의 곡률·여백이 별도로 있고, 입력은 낮은 대비의 투명 테두리/플레이스홀더와 검색/폼의 다른 초점 스타일을 사용한다.
- 완료 기준: 공통 폭·여백·패널/내부 영역/입력 곡률 토큰을 실제 기존 컴포넌트에 적용하고 큰 작업 패널의 여백을 줄인다. 기존 베이지·오렌지·올리브 테마 안에서 입력 면과 3:1 이상의 테두리/초점, 4.5:1 이상의 글자/플레이스홀더를 확인한다. 읽기 전용·비활성 값도 읽을 수 있게 유지하고 실제 네이티브 입력/검증·키보드·처리 중 제한·모달을 검증한다.
- 스타일만 변경하므로 전체 DB 원자성 검사는 반복하지 않고 격리 PostgreSQL의 기존 브라우저 회귀와 시각 측정을 실행한다. 증거 폴더 `/tmp/wiki-viki-u1f-20260922/`에 변경 전 파일·기준 검사 로그와 재실행 가능한 임시 스크립트를 보관한다.

### 구현과 시각 검증

- `src/app.css`: 공통 화면 폭·가로 여백·패널/내부 영역의 여백·간격·모서리·입력 크기를 토큰으로 정리했다. 주요 패널은 데스크톱 24px/모바일 18px 여백과 20px/16px 모서리를 공유하며, 검색·폼 입력은 따뜻한 중립 면과 공통 테두리/3px 초점을 사용한다. 읽기 전용은 보조 면, 비활성은 읽을 수 있는 글자와 점선 테두리로 구분한다.
- `WikiEditor`, `EditRecovery`, `MergeReview`, `DraftReviewList`, 자동 연결 화면의 해당 패널에도 토큰을 적용했다. 기존 script·마크업·폼 계약이 그대로임을 변경 전 복사본과 비교했다. 검색의 둥근 모양·상단 검색의 작은 높이, 읽기 본문의 스타일과 모달의 내부 스크롤/고정 X는 유지했다.
- 변경 전 Chrome 36개, 변경 후 **60개 화면 조합 통과**: 홈·검색·읽기·편집·초안 상세·토론·이력·휴지통·위키파이어 모달의 360/768/1440px × 두 테마, 편집·초안·홈의 200% 대응 레이아웃(720×500 CSS·픽셀 배율 2). 브라우저 도구 모음의 확대 조작과 구분한다. 키보드 초점·입력 크기·플레이스홀더·가로 넘침 없음·모달 Escape를 확인했다.
- 입력 최소 대비는 글자 **5.36/6.78:1**, 플레이스홀더 **6.48/9.13:1**, 테두리 **3.49/5.03:1**, 초점 **5.72/7.55:1**(라이트/다크). 변경 전 플레이스홀더는 3.87/3.34:1, 테두리는 1.37/1.58:1이었다. 모바일 라이트 편집/다크 모달과 데스크톱 다크 편집 캡처를 직접 확인했다.
- 화면 검사 전후 실제 격리 PostgreSQL의 문서·리비전·초안·토론은 동일했고 페이지 오류/외부 HTTP는 0이었다. 기존 브라우저 회귀 복사본의 화면 캡처 전에 입력 대비·읽기 전용/비활성·넘침을 추가 측정 중이다. CSS 전용 변경에 구현을 복제하는 단위 테스트는 추가하지 않았다.

### 최종 검증과 다음 반복

- 실제 격리 PostgreSQL + Chrome의 기존 회귀 **5종 모두 통과**: 일괄 게시 7개 묶음, Excel 업로드 13개, 직접 편집 미리보기 12개, 초안/통합 미리보기 7개, 직접 편집 임시 복구 14개. 360/768/1440px × 두 테마에서 정상/빈/로딩/오류, 키보드, 네이티브 폼, 저장 실패·충돌·복구·명시적 재시도, 일부 게시/응답 유실과 중복 방지, 실제 XLSX 추출/저장·모달 닫기/재열기·고정 X/내부 스크롤을 확인했다. 페이지 오류/외부 HTTP 0. AI 응답은 모의 처리했다.
- 회귀 중 실제 읽기 전용/비활성 입력의 글자 대비는 **5.36/6.78:1** 이상이다. JavaScript 없는 기본 화면까지 포함한 모든 측정에서 테두리 최소 **3.46:1**, 플레이스홀더 최소 **5.36:1**을 통과했다. 모바일 업로드 처리 중·다크 업로드 오류·통합 편집·실제 저장 충돌 후 복구 화면을 직접 확인했다. 최종 지표 집계에서 JS 비활성 화면의 없는 테마 속성 때문에 집계 스크립트만 실패했고, 해당 화면을 SSR 기본값으로 별도 분류해 집계했다. 브라우저 검증 자체와 제품 코드는 수정하지 않았다.
- 최종 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 스타일 전용 범위에 따라 전체 DB 원자성 검사는 생략했으며 위의 실제 DB 브라우저 검사와 구분한다. 기존 검사 실패와 새 미해결 회귀 없음.
- **U1f 완료, U1 전체/P0 미완료.** 공통 UI 최종 적용 감사, U2 탐색·홈 배치, U3 읽기 확장, W2 특정 원본 Excel 오류 재현이 남아 있다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 이번 반복에서 검증하지 않았다. GOAL 본문/체크박스와 기존 사용자 변경은 보존했다.
- **다음 반복: U1 최종 적용 감사 — 주요 7개 화면의 공통 버튼·상태·글자 계층과 빈/로딩/오류 표시를 현재 코드 및 실제 화면으로 대조하고 누락만 보완한다.** U1 전체 완료 근거를 정리한 뒤 U2로 이동한다. 이미 검증된 스타일을 근거 없이 다시 구현하지 않는다.
- 증거: `/tmp/wiki-viki-u1f-20260922/`의 `baseline-*.log`, `before/` 파일, `browser.mjs`·`control-audit.mjs`, 변경 전후 36/60개 화면·결과 JSON, 기존 5종 브라우저 복사본·동명 로그/결과 폴더, `control-audit.jsonl`, `implementation-check.log`, `final-test/check/lint/build.log`. SQL로 잔여 테스트 스키마 **0개**를 확인했다. 이번 브라우저·서버·격리 PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 보존했다. DB 재개 옵션은 반복 30 기록과 같다.

## 반복 36 — U1 공통 UI 최종 적용 감사 (2026-09-22)

- 직전 목표 턴은 **진행**: 공통 패널/입력 변경과 5종 브라우저·최종 검사 로그를 확인했다. 목표 원본·GOAL·README·설정·진행 기록·git status와 적용 AGENTS 부재를 다시 확인했다. 이전 사용자 변경을 보존한다.
- 기준 `bun run test` **100 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 기존 자동 검사 실패 없음. 토론/이력의 `document-actions`는 공통 버튼을 우회해 작은 높이와 낮은 대비를 사용하고, 빈 화면 마크업/제목/여백은 경로마다 별도다. 라우트 로드 오류에는 공통 오류 화면이 없어 기본 프레임워크 화면이 표시된다.
- 이번 완료 기준: 중복된 빈 화면의 안내·다음 행동을 공통 컴포넌트로 표시하고, 토론/이력 이동 버튼에 공통 보조 버튼을 적용한다. 라우트 404/조회 실패에서도 실제 오류와 다시 불러오기/홈 이동을 제공한다. 기존 URL·목록/데이터 판정·검토/게시·입력 보존 계약은 유지한다.
- 증거 폴더 `/tmp/wiki-viki-u1-audit-20260922/`에 변경 전 코드와 기준 검사 로그를 보관했다. 스타일/표시 범위이므로 전체 DB 원자성 검사는 반복하지 않고 실제 격리 DB의 조회 실패/복구 및 기존 핵심 브라우저 흐름을 확인한다. 브라우저 검증 중 구현을 변경하지 않아 변경 전후 증거를 분리한다.

### 발견한 누락과 보완

- 변경 전 Chrome 60개 화면의 실제 측정에서 토론/이력 “읽기” 버튼이 **라이트 3.03:1 대비·38.8px 높이**였다. 미작성 문서의 주 제목은 38px·400 굵기로 공통 업무 제목과 달랐다. 서버/목록 판정은 그대로 두고 해당 표시만 보완한다.
- `EmptyState.svelte`를 추가하고 검색 결과·미작성/초안 대기/휴지통 문서·초안 목록·홈 최근 변경/지도·자동 연결의 기존 빈 표시를 연결했다. 기존 안내/대상 URL을 유지하며 고딕 제목·보조색·토큰 여백·줄바꿈·행동 간격을 공유한다. 일반 게시 문서의 렌더링과 목차는 변경하지 않았다.
- 토론/이력의 기존 이동 링크에 `secondary-button`을 적용하고 별도 저대비 버튼 스타일을 제거했다. `+error.svelte`는 실제 HTTP 상태·서버 오류 메시지와 다시 불러오기/홈 이동을 공통 제목·상태·버튼으로 표시한다. 다시 불러오기는 기존 주소/쿼리의 명시적 전체 재요청이며 자동 재시도나 저장 액션은 추가하지 않았다.
- `tests/browser/ui-error.mjs`에 실제 격리 DB 조회 실패/복구와 404·503·500, 기본 링크/키보드, 내부 상세 비노출, 기존 데이터 보존 회귀를 추가했다. README에 실행 방법을 기록했다. 타입 검사 오류 0·경고 0. 현재 화면 감사와 신규/기존 브라우저 회귀 진행 중.

### U1 최종 완료 근거

- **공통 토큰/글자/버튼:** 현재 `app.css`의 브랜드·본문색/중립 면·업무 제목 크기·간격·폭·패널/입력·주/보조/위험 버튼 토큰을 확인했다. 홈/검색/읽기/편집/초안/토론/이력의 실제 제목이 고딕체·600 이상 굵기를 사용하고, 주/보조/위험 버튼·입력·키보드 초점의 대비/높이를 Chrome에서 측정했다. 로고와 짧은 소개만 장식체를 유지한다. 토론/이력 이동 버튼은 **15.17/11.05:1 대비·44px 높이**로 바뀌었다.
- **공통 상태/다음 행동:** `EmptyState`와 `StatusNotice`의 빈 상태·안내·처리 중·오류·성공·확인 필요, native 비활성 표시와 실제 검사 생략/검토 대기 텍스트를 확인했다. 정상/빈 검색·초안·문서, 저장/게시/조회 실패의 안내와 재시도는 현재 실제 DB 브라우저로 검증했다. 가상 검토 완료·진행률·숫자를 추가하지 않았다.
- **추가 누락:** 업로드 완료 제목에 `work-page`의 20px·700 고딕체/보조색을 적용하고, 읽기 화면의 편집 링크도 공통 보조 버튼을 사용하도록 별도 스타일을 제거했다. 짧은 업로드 소개, 생성 개수·대상 URL·저장/검토 로직은 보존했다. 기존 8개 컴포넌트/화면의 script가 새 컴포넌트 import 외에는 동일한 것을 비교했다.
- 변경 후 **118개 화면 검사 통과**: 주요 7개 화면과 홈 보기·초안 상세·미작성/대기/보관 문서·실제 빈 DB·자동 연결, 360/768/1440px × 두 테마, 긴 제목·가로 넘침·버튼 일반/호버/초점·입력 대비·상태 제목·키보드 이동. 미작성 문서/이력은 200% 대응 720×500 CSS·픽셀 배율 2를 별도로 확인했다. 읽기 버튼의 마지막 수정 후 해당 화면 **6개**를 추가 통과했다. 읽기 전후 실제 문서/리비전/초안/토론 동일, 페이지 오류/외부 HTTP 0.
- 신규 `node tests/browser/ui-error.mjs` **8개 묶음 통과**: 360/768/1440px × 두 테마의 실제 404/503·홈 이동·키보드 전체 재요청·쿼리 보존, JavaScript 없는 서버 렌더링/재시도, 실제 500의 내부 SQL/오류 상세 비노출·복구. 문서/리비전/별칭/토론이 검사 전후 동일했다. 모바일 다크 503·데스크톱 라이트 404를 직접 확인했다.
- 기존 회귀 **4종 통과**: 일괄 게시 7개 묶음, 자동 연결 7개, 검색·토론·이력 25개, Excel 업로드 13개. 실제 저장 실패/충돌/재시도·기본 폼·일부 성공/응답 유실·관계 재검사, 정상/빈/로딩/오류·200% 대응·키보드, 실제 XLSX 추출/저장·32개 결과·모달 고정 X/내부 스크롤/닫기·재열기를 확인했다. Excel 결과 제목의 고딕체/20px/700과 소개의 장식체를 별도 측정했다. AI 응답은 모의 처리했으며 실제 외부 호출과 구분한다.

### 종료 상태와 다음 반복

- 최종 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징) 통과. 최초 최종 lint는 신규 브라우저 파일의 메서드 체인 줄바꿈만 실패했다. Prettier로 해당 파일만 다시 정리하고 lint/build를 통과했으며 테스트 조건은 변경하지 않았다. 기존 실패 없음, 이번 변경의 미해결 회귀 없음. 전체 DB 원자성 검사는 UI 전용 범위에 따라 생략했고 실제 DB 조회 실패/데이터 보존/기존 브라우저 회귀와 구분한다.
- **U1/U1g 완료, 전체 P0 미완료.** GOAL §12 P0의 공통 버튼·상태·글자 계층 체크박스 하나만 완료로 갱신했다. U2 탐색/홈 배치, U3 읽기 확장, W2 특정 원본 Excel 오류 재현은 남아 있다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 이번에 검증하지 않았다.
- **다음 반복: U2a — 전역 메뉴의 모바일 접기/펼치기와 검색·현재 작업 우선 배치를 구현한다.** 기존 주요 경로·테마·위키파이어 모달을 유지하며 키보드 열기/닫기·초점·화면 이동과 좁은 화면을 검증한다. 홈 정보 재배치는 그다음 단위로 남긴다.
- 증거: `/tmp/wiki-viki-u1-audit-20260922/`의 기준/최종 검사 로그, `before/` 변경 전 파일과 60개 화면, `after/` 118개 화면/측정 JSON, `reading-final/` 6개 결과, 신규 `ui-error`와 기존 4종 결과/로그, `final-lint-first.log`. SQL로 잔여 테스트 스키마 **0개**를 확인했다. 이번 브라우저·서버·격리 PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 보존했다. DB 재개 옵션은 반복 30 기록과 같다.

## 반복 37 — U2a 모바일 메뉴와 검색 우선 배치 (2026-09-22)

- 직전 목표 턴은 **진행**: U1 완료 코드·실제 DB/브라우저·최종 검사 증거를 확인했다. 목표 원본·GOAL·README·설정·진행 기록·git 상태와 적용 AGENTS 부재를 확인했다. 기존 사용자 변경과 운영 데이터는 보존한다.
- 기준 `bun run test` **100 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 기존 검사 실패 없음. 800px 이하에서도 주요 메뉴를 항상 모두 표시해 헤더가 여러 줄을 차지하고 있다. `mobile-navigation.mjs`로 변경 전 모바일 접기 기능 부재를 재현한다.
- 완료 기준: 검색·테마를 메뉴 밖에 유지하고 모바일 메뉴는 기본 HTML `details/summary`로 제공한다. JavaScript 없는 메뉴/링크, Enter/Space·Tab·Escape, 화면 이동 완료 후 닫기, 미저장 입력으로 이동을 취소할 때 보존, 모달 닫기 후 초점, 크기 변경·좁은 높이·두 테마·확대를 검증한다. 기존 메뉴 경로/제목·파일 전환·본문/데이터 계약은 변경하지 않는다.
- 이번 단위는 모바일 메뉴에 한정하며 U2의 메뉴 항목 재편·새 작성 진입·홈 정보 배치는 후속 단위다. DB 로직 변경이 없어 전체 원자성 검사는 반복하지 않고 격리 DB의 실제 화면 이동·데이터 동일성과 기존 작성/검토/업로드 브라우저 회귀를 확인한다. 증거 위치 `/tmp/wiki-viki-u2a-20260922/`.

### 구현과 현재 검증

- `Header.svelte`는 기존 메뉴 항목을 한 Svelte snippet으로 유지하고 데스크톱 메뉴와 모바일 `details/summary`에서 공유한다. 모바일의 기본 닫힘·키보드 열기/닫기·메뉴 밖 초점/클릭·이동 완료 후 닫기·화면 크기 변경 시 초점 이동을 구현했다. 위키파이어 모달이 열려 있을 때 메뉴를 유지해 닫은 뒤 원래 링크로 돌아오게 한다. 기존 익명 배지/테마 저장·실패 대체·링크 대상은 보존한다.
- `app.css`: 800px 이하에서 브랜드·테마·메뉴를 첫 줄, 검색을 별도 전체 폭으로 배치한다. 열린 메뉴는 헤더 아래에서 실제 헤더 높이와 화면 높이에 맞춰 내부 스크롤한다. 540px 이하는 한 열, 그 위는 두 열이며 기존 브랜드/공통 버튼 토큰을 공유한다.
- 신규 브라우저 검사의 변경 전 실패는 `mobile menu must exist`였다. 첫 구현에서 Playwright ARIA 역할 조회가 native summary를 `button`으로 찾지 못해 검사기가 실패했다. Chrome 접근성 트리의 `DisclosureTriangle`·이름·`expanded`·`focusable`을 직접 확인하도록 검사를 바꿨다. 역할을 임의로 덮어쓰거나 기준을 낮추지 않았다.
- 이후 메뉴를 연 직후 검색창으로 초점을 옮기는 검사에서 실제 닫힘 경합을 찾았다. native `toggle` 이벤트보다 늦게 갱신되는 별도 Svelte 상태 대신 `details.open`을 유일한 상태로 사용해 즉시 닫도록 수정했다. 같은 실패 조건을 유지한 전체 회귀를 재실행 중이다.
- 기존 Excel 업로드와 자동 연결 검사의 메뉴 진입에 `tests/browser/helpers/navigation.js`를 추가했다. 접힌 메뉴를 키보드로 연 뒤 원래 링크/동작을 검증하며 기존 저장·충돌·모달·관계 검사는 그대로 유지한다. README에 새 메뉴와 검사 명령을 기록했다.

- 화면 폭 전환 검사에서는 CSS가 먼저 링크를 숨기면서 초점이 `body`로 이동하고, 그 뒤 media change 이벤트가 발생하는 실제 순서를 확인했다. 메뉴 안의 마지막 초점을 기억하고 다른 곳의 클릭/초점·화면 이동 때 지워, 크기 변경 때만 보이는 메뉴 버튼 또는 같은 대상의 데스크톱 링크로 옮기도록 수정했다. 취소한 이동의 입력 보존 검사도 유지했다.
- 새 메뉴 브라우저 **12개 묶음 통과**: 360/768/1440px × 두 테마, native 열림 접근성 상태, Tab/Escape·메뉴 밖 검색·모달 복귀·현재 경로·검색/뒤로 가기, 양방향 크기 변경 초점, 미저장 이동 취소/허용, 지연된 이동 완료/실제 DB 조회 실패, 200% 대응 720×500 CSS/픽셀 배율 2, 360×320px 메뉴 내부 스크롤, JavaScript 없음·저장소 차단. 실제 문서/리비전/초안 동일, 페이지 오류/외부 HTTP 0. 360px 닫힌 읽기 헤더는 113px이고 검색은 전체 폭으로 표시된다.
- 모바일 라이트/태블릿 다크의 펼친 메뉴와 짧은 화면의 마지막 링크 초점을 직접 확인했다. 기존 Excel 회귀 13개 묶음도 통과했으며 나머지 작성/검토/연결 회귀와 최종 검사 진행 중이다.

- 모달을 연 채 360↔1440px로 바꾼 추가 검사에서 닫은 뒤 초점이 숨겨진 모달 버튼에 남는 실제 회귀를 확인했다. 헤더에서 모달 진입 링크를 기억하고, 닫을 때 그 링크가 숨겨져 있으면 현재 보이는 메뉴 버튼/동일 데스크톱 링크로 초점을 돌린다. 이동 완료 시 기억을 지워 새 화면의 초점을 빼앗지 않도록 했다. 양방향 크기 변경 중 모달 초점 유지와 닫은 뒤 복귀를 영구 회귀 검사에 추가했으며 새 메뉴 **13개 묶음 통과**. 실제 데이터 동일·페이지 오류/외부 HTTP 0.
- 기존 회귀는 Excel 13개, 자동 연결 7개, 일괄 게시 7개, 직접 편집 미리보기 12개, 직접 편집 복구 14개 묶음을 통과했다. 마지막 모달 초점 수정 뒤 메뉴 전체 검사와 Excel 모달 회귀를 다시 실행하고 있다. DB 로직·원문·리비전·초안은 변경하지 않았다.

### 최종 검증과 다음 반복

- 마지막 모달 초점 수정 후 신규 `node tests/browser/mobile-navigation.mjs` **13개 묶음**, 기존 `node tests/browser/xlsx-upload.mjs` **13개 묶음**을 다시 통과했다. 앞서 실행한 자동 연결 7개·일괄 게시 7개·직접 편집 미리보기 12개·직접 편집 복구 14개와 구분한다. 모든 검사에서 페이지 오류와 외부 HTTP 0. Excel의 실제 XLSX 추출/DB 저장·다중 결과·실패/재시도·닫기/재열기·모달 내부 스크롤, 편집의 입력 보존·충돌·기본 폼과 검토의 중복 보호를 유지했다. 외부 AI 응답은 모의 처리했다.
- 최종 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 전체 DB 원자성 검사는 헤더 UI 범위에 따라 생략했으며 실제 격리 PostgreSQL의 위 브라우저 검증/조회 실패/데이터 동일성과 구분한다. 기준 자동 검사 실패 없음. 구현 중 발견한 메뉴 상태 경합·폭 전환/모달 초점 회귀는 수정 후 같은 조건으로 통과했으며 미해결 회귀 없음.
- **U2a 완료, U2/P0 전체 미완료.** U2의 전역 진입·홈 배치, U3 읽기 확장, W2 특정 원본 Excel 재현이 남아 있다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 검증하지 않았다. GOAL 본문/체크박스와 기존 사용자 변경은 보존했다.
- **다음 반복: U2b — 검색·문서 탐색·새 문서 작성 진입점을 주요 화면의 전역 메뉴에 정리한다.** 기존 라우트/검색/작성 및 파일 전환을 사용하며 제목 입력·이동 취소·키보드·메뉴 길이/좁은 화면을 확인한다. 홈 정보 재배치는 이후 단위로 남긴다.
- 증거: `/tmp/wiki-viki-u2a-20260922/`의 `baseline-*.log`, `before/` 파일, `browser-before.log`, 신규 `navigation-final`/로그와 기존 5종 브라우저 결과/로그, 마지막 `xlsx-upload-final`/로그, `debug-ax.log`, `debug-resize.log`, `navigation-focus-race.log`, `modal-resize-before.log`, `final-test/check/lint/build.log`, `final-schemas.log`. SQL로 잔여 테스트 스키마 **0개**를 확인하고 이번 브라우저·서버·격리 PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 보존했다. DB 재개 옵션은 반복 30 기록과 같다.

## 반복 38 — U2b 전역 탐색과 새 문서 진입 (2026-09-22)

- 직전 목표 턴은 **진행**: U2a 코드와 실제 메뉴/모달·기존 5종 브라우저 및 최종 검사 로그를 다시 확인했다. 목표 원본·GOAL·README·실제 Vite/패키지 설정·git 상태·진행 기록과 적용 AGENTS 부재를 확인했다. 기존 사용자 변경은 보존한다.
- 기준 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, lint/build 통과. 기존 자동 검사 실패 없음. 전역 메뉴에 검색·문서 탐색·새 문서 작성 진입이 없고 홈 빈 상태는 고정 `/edit/새-문서`로 이동한다.
- 이번 완료 기준: 검색·문서 탐색·최근 변경·초안 검토를 먼저 제공하고 기존 파일 전환/휴지통/자동 연결을 유지한다. 모든 주요 화면의 새 문서 진입에서 제목을 입력하고 같은 제목/주소/별칭·휴지통 문서·미게시 초안은 실제 대상을 안내한다. 새 제목은 기존 편집·저장·입력 보존 흐름으로 연결하며 제목의 대소문자·기호를 보존한다.
- 새 진입은 읽기 전용 제목 확인과 기존 편집 화면을 연결하는 최소 범위로 구현한다. 새 저장 API·DB 모델·게시 우회는 만들지 않는다. 기존 검색/지도 URL과 기본 HTML, 메뉴 접기/초점/모달, 작성 중 이동 취소를 검증한다. 홈 전체 배치는 후속 단위이며 분류/즐겨찾기 등 P1 기능을 추가하지 않는다. 증거 위치 `/tmp/wiki-viki-u2b-20260922/`.

### 구현과 검증 중 발견한 문제

- `Header.svelte`의 전역 메뉴를 검색·문서 탐색·최근 변경·초안 검토 순으로 정리하고 새 문서 작성/기존 파일 전환·휴지통·자동 연결을 제공한다. 홈 보기/검색어를 실제 라우트와 함께 판단해 현재 메뉴를 표시하며 기존 검색창·모달·모바일 접기/초점 로직을 유지했다. `app.css`는 중간 폭 801~1280px에서 메뉴를 둘째 줄로 배치해 항목 증가에 대응한다.
- `/new`의 기본 GET 폼은 제목을 확인하고 실제 기존 문서/등록 별칭·휴지통·미게시 초안을 안내한다. 새 제목만 기존 `/edit/[slug]`로 연결한다. 편집 서버/초기 값은 주소와 일치하는 새 제목의 원문을 받아 대소문자·기호를 보존하며 기존 문서/실패 응답의 제목을 우선한다. 저장·충돌 보호는 원래 액션을 그대로 사용한다. 홈 빈 화면과 검색 결과의 새 작성 진입도 연결했다.
- `tests/browser/navigation-entry.mjs`의 변경 전 실패로 전역 진입 부재를 확인했다. 첫 구현의 실제 DB 조회 실패/복구 검사에서는 같은 제목으로 재제출할 때 SvelteKit이 같은 URL의 기존 서버 로드 결과를 재사용해 오류 화면에 남았다. 현재 설치된 라우터 코드를 확인하고 명시적 제출의 `goto(..., { invalidateAll: true })`로 재조회하며, 기본 GET은 보존했다. 제목 확인 중 안내·읽기 전용 입력·중복 클릭 제한을 추가했고 지연 응답 검사를 포함해 재실행 중이다.
- 최초 구현 `bun run check`는 오류 0·경고 0이었다. 360px 라이트 새 작성 화면에서 공통 글자·입력·버튼·테마를 직접 확인했다. 최종 브라우저/DB 회귀와 자동 검사는 진행 중이며 GOAL 체크박스를 변경하지 않았다.

- 지연 조회 검사에서 처리 중 상태 갱신이 `value={data.title}`를 다시 적용해 사용자가 입력한 제목이 잠시 비워지는 실제 회귀도 확인했다. 새 진입 폼을 입력 상태와 양방향으로 연결하고 서버 응답 때만 초기 값을 갱신해 조회 중·실패 시 제목을 보존했다. 동일 지연 조건을 유지한 최종 신규 브라우저 **12개 묶음 통과**.
- 새 검사는 홈·검색·읽기·편집·초안·토론·이력의 360/768/1440px × 두 테마(주요 화면 42개 방문)에서 전역 진입·기존 지도/검색 URL·현재 메뉴·작성 중 이동 취소를 확인했다. 제목 대소문자/기호, 기존/별칭/휴지통/초안 안내, 빈/잘못된/200자 초과 제목, 실제 DB 실패/복구 후 같은 제목 재시도, 지연 조회·입력 보존·비활성 버튼, 801/1024/1200px·200% 대응 720×500 CSS/픽셀 배율 2·가로 넘침 없음을 확인했다. 확대는 브라우저 도구 모음 직접 조작과 구분한다.
- 읽기 전용 진입/검사 전후 문서·리비전·초안 동일성을 확인했다. JavaScript 없는 기본 GET→기존 저장 액션으로 명시적으로 저장한 경우에만 실제 문서와 첫 리비전이 한 개씩 생겼다. 페이지 오류/외부 HTTP 0. 모바일 라이트 새 진입, 801px 헤더와 데스크톱 다크 화면 캡처를 직접 확인했다.
- `RUN_GOVERNANCE_DB_TESTS=1 node --test --test-concurrency=1 tests/edit-governance-db.test.js tests/edit-preservation-db.test.js` **42 통과·생략 0**. 실제 격리 PostgreSQL의 전송 차단·입력 보존·별칭/제목 충돌·중간 실패 롤백·동시 저장·초안 게시와 잠금 호환을 확인했다. 신규 저장 경로를 추가하지 않았으므로 나머지 전체 DB 스위트는 반복하지 않는다. 기존 모바일 메뉴 13개 묶음도 통과했고 업로드·검토·연결·편집 브라우저 회귀 진행 중이다.

- 추가 점검에서 801px의 두 줄 헤더 높이는 **104.59px**인데 기존 문서 보조 패널의 고정 위치는 90px여서 약 15px가 가려지는 실제 회귀를 재현했다. 헤더가 이미 측정하는 높이 변수를 루트에 공유하고 보조 패널의 상단/최대 높이가 이를 따르도록 수정했다. JavaScript 없는 중간 폭에는 CSS 기본 높이를 제공한다. 새 기능 확장 없이 이번 탐색 배치가 읽기 흐름을 가리지 않도록 필요한 최소 보완이다.
- 긴 문서의 801/1024/1200px 스크롤·목차 이동과 JavaScript 없는 보조 패널을 신규 영구 브라우저 검사에 추가했다. 기존 모바일 메뉴·Excel·일괄 게시·자동 연결·직접 편집 미리보기·복구 6종은 모두 통과했으며 마지막 높이 보완 후 새 진입/읽기와 모바일 메뉴만 다시 확인한다.

### 최종 검증과 다음 반복

- 마지막 헤더 높이 수정 후 `node tests/browser/navigation-entry.mjs` **12개 묶음**, `node tests/browser/mobile-navigation.mjs` **13개 묶음**을 다시 통과했다. 새 진입의 전체 42개 주요 화면 방문·모든 제목/실패/재시도/저장 조건과 긴 문서 스크롤·목차·기본 CSS, 메뉴 열림 접근성·Tab/Escape·모달 복귀·폭 전환·지연/실패 이동·200% 대응·짧은 높이·저장소 차단을 확인했다. 수정 후 801px 읽기 보조 패널 캡처도 직접 확인했다.
- 기존 Excel 13개·일괄 게시 7개·자동 연결 7개·직접 편집 미리보기 12개·복구 14개 묶음 통과. 실제 XLSX 추출/다중 결과/DB 저장·오류/재시도·모달 스크롤/닫기·응답 유실·중복 방지·기본 폼·미저장 입력·저장 충돌/복구와 명시적 재시도를 유지했다. 위의 실제 DB 통합 **42개**와 구분한다. 모든 브라우저에서 페이지 오류/외부 HTTP 0이며 AI 응답은 모의 처리했다.
- 최종 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 기준 자동 검사 실패 없음. 새로 발견한 동일 제목 재시도·처리 중 입력·고정 패널 가림 회귀는 같은 실패 조건으로 수정/재검증했고 미해결 회귀 없음. 전체 DB 스위트는 이번 읽기 전용 진입/편집 초기 값 범위에 맞춰 생략했으며 관련 실제 DB 42개 및 브라우저 검증과 구분한다.
- **U2b 완료, U2/P0 전체 미완료.** 홈 정보 배치·U3 읽기 확장·W2 특정 원본 Excel 재현이 남았다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 검증하지 않았다. GOAL 본문/체크박스와 기존 사용자 변경은 보존했다.
- **다음 반복: U2c — 홈 첫 화면을 검색·실제 문서 진입·사용 안내 중심으로 재배치한다.** 통계/서비스 소개를 아래로 옮기고 기존 활동·지도 URL, 실제 목록/지표와 빈/오류 상태를 유지한다. 분류·개인화를 가상 데이터로 만들거나 P1 기능으로 확장하지 않는다.
- 증거: `/tmp/wiki-viki-u2b-20260922/`의 `before/` 파일, `baseline-*.log`, `browser-before.log`, `entry-first.log`·`entry-pending-input.log`·`reading-scroll-before.log`, 신규 최종 `entry-final`/로그, `mobile-final`/로그, 기존 5종 결과/로그, `edit-db.log`, `final-test/check/lint/build.log`, `final-schemas.log`. SQL로 잔여 테스트 스키마 **0개**를 확인하고 이번 브라우저·서버·격리 PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 보존했다. DB 재개 옵션은 반복 30 기록과 같다.

## 반복 39 — U2c 홈의 검색과 실제 문서 진입 우선 배치 (2026-09-22)

- 직전 목표 턴은 **진행**: U2b의 전역 메뉴·새 작성/편집 초기 값 코드와 최종 브라우저/DB·자동 검사 증거를 다시 확인했다. 목표 원본·GOAL·README·프로젝트 설정·git 상태·진행 기록과 적용 AGENTS 부재를 확인했다. 기존 사용자 변경을 보존한다.
- 이번 단위는 §7.2의 홈 첫 화면에 검색·분야 진입·사용 안내를 먼저 배치하고 통계/소개를 뒤로 옮기는 작업이다. 최근 본 문서나 즐겨찾기를 만들지 않고 실제 게시 문서·현재 제목 기반 분류를 활용한다. 활동/지도 보기와 기존 데이터·URL·지표·업로드/검토 진입은 유지한다.
- 분야별 진입에 필요한 최소 읽기 데이터로 실제 분야별 개수와 최근 수정된 문서 최대 12개를 제공한다. 선택한 분야는 홈 URL에 보존하고 같은 `categoryOf` 규칙을 검색과 공유한다. 전체 검색 필터/페이지 처리·새 분류 모델·개인화는 P1에 남긴다. DB 추가 쓰기·외부 전송·모델 변경은 없다.
- 검증 기준: 정상/빈/실제 DB 조회 실패·복구, 분야 선택→문서 도달·새로고침/뒤로 가기, 검색·기본 HTML·새 작성/업로드 모달, 실제 통계/문서 보존, 360/768/1440px × 두 테마·200% 대응·키보드·가로 넘침. 증거 위치 `/tmp/wiki-viki-u2c-20260922/`.

### 구현과 현재 검증

- 기준 `bun run test` **100 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 기존 자동 검사 실패 없음. 새 브라우저 검사에서 변경 전 분야별 진입 부재를 확인했다.
- `+page.server.js`: 기존 활성 문서 스냅샷과 `categoryOf`로 분야별 실제 개수와 선택 분야의 최근 수정 문서 최대 12개를 반환한다. 전체 개수/표시 개수를 구분하고 게시 문서가 없는 분야는 빈 목록으로 안내한다. 추가 DB 조회/쓰기·새 분류 모델은 없다. 홈 검색 보기에서만 이 데이터를 직렬화한다.
- `+page.svelte`/`app.css`: 짧은 고딕 제목·검색창 뒤에 분야/사용 안내를 배치하고 실제 문서 목록→연결 용어→통계→기존 브랜드 소개/업로드 설명 순으로 구성했다. 활동/지도는 짧은 작업 제목·검색·기존 업무 패널을 유지한다. 분야 링크는 실제 URL/기본 HTML로 동작하며 현재 선택·로딩·빈/오류·새 작성/업로드 진입을 제공한다. 기존 큰 소개 문구와 베이지/오렌지/올리브·장식 글꼴은 아래 소개에서 유지했다.
- 첫 통계 검사 실패는 테스트 자료의 수동 위키 링크가 저장된 문서 주소와 불일치했기 때문이다. 실제 연결이 없는 상태를 2개라고 가정한 검사 자료를 정확한 대표 주소 링크로 수정했으며 제품의 그래프 규칙/실제 개수는 변경하지 않았다.
- 실제 DB 장애/복구 후 재시도에서는 같은 URL/문단 해시의 기본 링크가 문단 이동으로 처리되어 새 조회가 없음을 확인했다. 다시 불러오기는 같은 분야 쿼리를 유지하고 해시 없이 전체 요청하도록 수정했다. 원래 실패 조건을 유지하고 Tab으로 첫 문서 접근·지연된 분야 이동 검사를 포함해 재실행 중이다.
- 최초 구현 check는 오류 0·경고 0. 모바일 라이트 캡처에서 검색·분야·사용 안내가 첫 화면에 있고 통계/소개가 뒤로 이동한 것을 직접 확인했다. 신규 브라우저 및 기존 탐색/업로드·DB 회귀와 최종 검사는 진행 중이다.

- 신규 `node tests/browser/home-entry.mjs` **11개 묶음 통과**: 360/768/1440px × 두 테마의 검색→분야/안내→문서→통계/소개 순서, 실제 16문서·14개 업무 절차 중 최신 12개·1개 설비·휴지통 제외, 지표 16/1/2/2, 분야 URL·현재 선택·새로고침/뒤로 가기·Tab 첫 문서/Enter 원문 이동, 검색·활동/지도·새 작성/모달을 확인했다. 실제 지연 탐색 중 기존 목록과 로딩 안내, 존재하지 않는 분야, DB 오류의 `—`·분야 비노출/재시도, 진짜 빈 DB의 `0`, JavaScript 없는 링크, 200% 대응 720×500 CSS/픽셀 배율 2·가로 넘침 없음을 검증했다. 확대는 도구 모음 직접 조작과 구분한다.
- 읽기 검사 전후 실제 문서·리비전·초안·토론·공지 데이터가 동일했다. 빈 DB 검사는 이후 별도 fixture 초기화로 수행했다. 페이지 오류/외부 HTTP 0. 모바일 라이트 첫 화면과 데스크톱 다크 홈·실제 오류 캡처를 직접 확인했다.
- `RUN_GOVERNANCE_DB_TESTS=1 node --test --test-concurrency=1 tests/automatic-links-db.test.js tests/document-trash-db.test.js` **33 통과·생략 0**. 실제 DB 연결/삭제 제외·복구·이력/관계/주소 보존·중간 실패·경합·독립 연결을 확인했다. DB 쓰기를 추가하지 않았으므로 전체 원자성 스위트는 반복하지 않는다. 기존 모바일 메뉴 13개도 통과했고 나머지 브라우저 회귀 진행 중이다.

### 최종 검증과 다음 반복

- 기존 브라우저 회귀 **5종 통과**: 모바일 메뉴 13개·전역/새 작성 진입 12개·Excel 업로드 13개·자동 연결 7개·페이지 오류 8개 묶음. 메뉴/모달 키보드·폭 전환 초점·작성 중 이동 취소·새 제목/실패/실제 저장, XLSX 추출/다중 결과/DB 저장·모달 스크롤·오류/재시도, 관계 재검사와 실제 404/503/500·내부 상세 비노출·기본 폼/재요청을 유지했다. 페이지 오류/외부 HTTP 0. AI 응답은 모의 처리했으며 신규 홈 11개 및 실제 DB 통합 33개와 구분한다.
- 최종 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 기준 자동 검사 실패 없음. 구현 중 발견한 해시 때문에 재조회되지 않던 문제를 같은 조건으로 수정/재검증했고 이번 변경의 미해결 회귀 없음. 전체 DB 원자성 스위트는 읽기/UI 범위에 맞춰 생략했으며 관련 DB 33개·브라우저의 실제 조회/데이터 동일성 검증과 구분한다.
- **U2c 완료, U2/P0 전체 미완료.** 코드 대조에서 기존 활동 보기는 조회 실패 때 `data.drafts.length`로 0건을 표시하고, 지도는 미작성 링크 0곳/연결 문서 없음으로 표시하는 분기가 남아 있음을 확인했다. 이번 홈 검색 보기의 오류 처리는 완료했으며 이 기존 활동/지도 상태는 실제 재현·보완 대상으로 남긴다. U3 읽기 확장·W2 특정 원본 Excel 재현도 미완료다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 검증하지 않았다. GOAL 본문/체크박스와 기존 사용자 변경은 보존했다.
- **다음 반복: U2d — 활동·지도에서 실제 DB 조회 실패를 재현하고, 확인하지 못한 개수/목록을 빈 데이터로 표시하지 않도록 보완한다.** 명시적 재조회와 보기 URL·현재 작업 진입을 검증한 뒤 U2의 P0 탐색/홈 완료 기준을 현재 증거와 대조한다. 검색 분야 필터/페이지 처리 등의 P1 확장으로 범위를 넓히지 않는다.
- 증거: `/tmp/wiki-viki-u2c-20260922/`의 `before/` 파일, `baseline-*.log`, `browser-before.log`와 첫 화면, `home-fixture-links.log`, `home-retry-hash.log`, 최종 `home-final`/로그, 기존 5종 브라우저 결과/로그, `read-db.log`, `final-test/check/lint/build.log`, `final-schemas.log`. SQL로 잔여 테스트 스키마 **0개**를 확인하고 이번 브라우저·서버·격리 PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 보존했다. DB 재개 옵션은 반복 30 기록과 같다.

## 반복 40 — U2d 조회 실패와 빈 결과 구분 (2026-09-22)

- 직전 목표 턴은 **진행**: 홈 분야/목록·URL·재조회 코드와 실제 브라우저/DB·최종 검사 증거를 확인했다. 목표 원본·GOAL·README·실제 설정/스크립트·git 상태·진행 기록과 적용 AGENTS 부재를 다시 확인했다. 기존 사용자 변경을 보존한다.
- 이번 단위는 활동·지도에서 `databaseReady: false`를 빈 목록/0건으로 표시하는 기존 분기를 재현하고 명시적 재조회를 제공하는 작업이다. 같은 서버 로드 실패를 사용하는 검색 결과도 `문서 0건`·`전체 0`·미작성 안내로 표시하므로 동일한 오류/빈 결과 구분에 포함한다. 홈 검색 보기의 기존 오류/지표 `—`는 유지한다.
- 완료 기준: 실제 전체/부분 DB 조회 실패에서 확인하지 못한 목록/개수·빈 안내·AI 설명을 표시하지 않고, 같은 검색어/보기/분야 쿼리로 재요청한다. 정상 및 진짜 빈 DB, 기본 HTML/키보드·두 테마·주요 크기·지연 재조회·기존 데이터 동일성을 확인한다. 서버 조회/저장·스키마·외부 서비스 호출은 변경하지 않는다. 증거 위치 `/tmp/wiki-viki-u2d-20260922/`.

### 구현과 재현

- 기준 `bun run test` **100 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 기존 자동 검사 실패 없음. 최초 브라우저 실행은 테스트 DB 환경 변수 이름의 오타로 준비 단계에서 실패했고, fixture가 요구하는 `GOVERNANCE_TEST_DATABASE_URL`로 수정했다. 사용자 환경 파일/운영 DB는 읽거나 연결하지 않았다.
- 변경 전 격리 PostgreSQL의 문서 테이블을 일시적으로 다른 이름으로 바꾸어 실제 조회 오류를 만들자 활동 화면의 `함께 완성할 초안 0건`을 재현했다. 테이블 이름은 `finally`에서 복원하고 fixture를 정리한다. 제품 서버의 빈 배열 실패 반환을 믿고 정상 목록을 렌더링하던 원인이다.
- `+page.svelte`: 실제 조회 성공 때만 활동/지도/검색 결과·개수·AI 설명을 표시한다. 실패는 공통 상태/빈 화면 컴포넌트의 오류와 재조회로 안내하고 검색 개수는 확인 불가로 표시한다. 다시 불러오기에서 원래 URL 쿼리를 유지하고 문단 해시를 빼 실제 전체 요청을 보장한다. 정상 목록·쓰기·서버 쿼리·관계 규칙은 변경하지 않았다.
- 신규 `tests/browser/home-load-error.mjs`는 실제 전체/부분 DB 실패, 키보드 재시도, 검색어/보기/분야 보존, 기본 HTML·지연·진짜 빈 데이터와 DB 동일성을 검증한다. 첫 수정 후 검색 1건 예상은 두 fixture 문서가 같은 검색 단어를 포함해서 실패했다. 기존 검색이 단어별 OR 조건임을 확인하고 두 번째 fixture에서 겹치는 단어를 제거했다. 검색 로직/검증 기준은 변경하지 않았다. 모바일 라이트 검색 오류 캡처에서 공통 브랜드 배경·패널·오류·보조 버튼이 넘침 없이 보이는 것을 직접 확인했다.

- 두 테마·세 화면 크기의 전체 실패/재시도와 공지 쿼리만 실패하는 부분 장애는 수정 후 통과했다. JavaScript 없는 검색 오류에서는 기존 `SearchBox`가 초기 검색어를 클라이언트 effect에서만 적용해 입력이 비어 있음을 새로 재현했다. `SearchBox.svelte`의 상태 초기화에 전달된 검색어를 사용하고 이후 검색어 갱신 effect는 유지했다. 기본 HTML에서도 오류/재시도 때 검색어가 남도록 필요한 최소 보완이다. 동일 실패 조건으로 재검증 중이다.

- 지연된 전체 페이지 재요청에서는 Playwright 링크 클릭/locator가 탐색 완료를 기다려, 응답을 보류한 테스트 자체가 30초 뒤 실패했다. `noWaitAfter`로 클릭 대기는 해결했지만 DOM 평가도 다음 페이지를 기다렸다. 멈춘 소유 테스트 프로세스만 종료하고, Chrome 합성 화면을 직접 캡처한 후 보류 응답을 해제하도록 검사를 수정했다(캡처 5초 제한). 두 중단 실행의 잔여 fixture 스키마는 격리 DB에서 각각 정리했다. 제품 재시도는 앞선 정상/기본 HTML 검증에서 이미 동작했으며 지연 조건은 별도로 재실행한다.

### U2 범위 대조

- 현재 `Header.svelte`의 공통 메뉴/작성/파일 전환과 기존 진입·모바일 검사를 대조했다. 홈은 실제 분야/최근 수정 목록과 사용 안내를 먼저 제공하고 통계/소개는 뒤에 있다. 활동의 초안·최근 문서·토론은 각 원래 작업 주소로 연결하고 공지는 실제 본문을 펼친다.
- 홈·검색·지도는 활성 문서와 같은 `documentSummary`/`categoryOf`를 사용하고, 지도·통계·역링크는 같은 연결 스냅샷/규칙을 사용한다. 보기 URL과 홈 분야 URL은 새로고침/뒤로 가기를 지원하며, 이번 오류 재시도에서도 쿼리를 유지한다. 개인화 수치/목록을 만들지 않는다.
- §7.2의 P0 탐색/홈 범위에 해당하는 위 항목을 기존 홈·전역/새 작성·모바일 브라우저 검사로 다시 확인한다. 전체 검색 필터/정렬/페이지 URL과 문서 중심 지도 확장은 §12의 명시적 P1 범위로 남긴다. U2 완료는 이 P0 범위를 뜻하며 P1까지 완료했다는 뜻이 아니다.

### 검증 결과

- 신규 `node tests/browser/home-load-error.mjs` **23개 묶음 통과**: 360/768/1440px × 라이트/다크 × 활동/지도/검색, 실제 문서 조회 실패와 공지만 실패하는 부분 장애, 같은 쿼리의 키보드 재조회, JavaScript 없는 검색어/보기/재시도, 응답을 보류한 전체 재요청의 화면 캡처와 복구, 200% 대응 720×500 CSS/픽셀 배율 2, 실제 빈 DB의 0건을 확인했다. 도구 모음 확대 직접 조작과 구분한다.
- 읽기 검사 전후 실제 문서·리비전·초안·토론·공지 데이터 동일. 빈 DB 검사는 그 이후 별도 fixture 초기화로 수행했다. 페이지 오류/외부 HTTP **0**. 모바일 검색/지도 오류·데스크톱 다크 활동 오류·지연 재요청·진짜 빈 검색 캡처를 직접 확인했다. 지연 요청 중 기존 오류 안내와 재시도 링크가 남으며 응답이 도착하면 실제 목록으로 바뀐다. 공통 브랜드 토큰/상태/버튼을 재사용했고 이번 반복에서 CSS·DB 쓰기·외부 AI 로직은 수정하지 않았다.
- 기존 홈 11개·모바일 메뉴 13개 묶음도 통과했다. 실제 분야/통계·빈/지연/오류·URL/키보드·새 작성/모달과 모바일 초점/폭 전환·작성 중 이동 취소·기본 HTML을 유지했다. 이후 전역 진입/페이지 오류/Excel 브라우저와 최종 자동 검사도 아래와 같이 통과했다.

### 최종 검증과 다음 반복

- 기존 브라우저 회귀 **5종·57개 묶음 통과**: 홈 11개·모바일 메뉴 13개·전역/새 작성 12개·페이지 오류 8개·Excel 업로드 13개. 주요 화면 진입, 실제 제목 조회/기본 폼 저장, 기존 문서/관계 보존, 지연/오류/재시도, 키보드/메뉴/모달/반응형·200% 대응, 실제 XLSX 추출→다중 초안→DB 저장을 유지했다. 실제 외부 AI 호출과는 구분한다.
- 최종 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check` 통과. 기준 자동 검사 실패 없음. 기존 제품의 실패→0건/기본 HTML 검색어 소실을 재현·수정했고 동일 조건을 통과했다. fixture 검색어 중복·지연 탐색 검사 도구 대기는 위 근거로 수정했다. 이번 변경으로 발생한 미해결 회귀 없음.
- 이번 단위는 읽기 UI/검색 초기 값만 바꾸므로 전체 DB 원자성 스위트는 반복하지 않았다. 기본 테스트의 DB 생략 20개와 별도로 신규·기존 브라우저에서 격리 PostgreSQL의 실제 실패/복구·조회/저장·데이터 동일성을 검증했다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 검증하지 않았다.
- **U2d와 U2의 P0 탐색/홈 범위 완료, P0 전체 미완료.** U3 읽기 확장·W2 특정 원본 Excel 실패 재현이 남았다. GOAL 본문/체크박스와 기존 사용자 변경은 보존한다. README에 실패/재조회 동작과 새 검사 실행 방법을 추가했다.
- **다음 반복: U3b — 긴 문서의 현재 문단을 목차에 표시하고 모바일 목차에서 본문으로 복귀하는 동작을 구현·검증한다.** 현재 `Toc.svelte`는 계층 들여쓰기·번호·접기만 있고 읽는 위치/본문 복귀가 없다. 기존 `section-N` 주소·문단 번호·미리보기 목차를 보존하며 실제 긴 문서/키보드/기본 HTML·두 테마/주요 크기에서 확인한다. 안정 문단 식별자·읽기 설정 저장 등 P1 기능으로 확장하지 않는다.
- 증거: `/tmp/wiki-viki-u2d-20260922/`의 `before/`, `baseline-*.log`, `browser-environment.log`, `browser-before.log`, `browser-first.log`, `browser-query.log`, `browser-delay-harness.log`, `browser-pending-dom.log`, `harness-cleanup.log`, 신규 최종 `browser-final`/로그(23개 결과·지연 캡처), 기존 5종 브라우저 결과/로그, `verification-results.json`, `final-test/check/lint/build.log`, `final-schemas.log`. SQL로 잔여 테스트 스키마 **0개**를 확인하고 이번 브라우저·서버·격리 PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 보존했다. 재개 DB 조건은 반복 30과 같다.

## 반복 41 — U3b 긴 문서의 목차 접근과 본문 복귀 (2026-09-22)

- 직전 목표 턴은 **진행**: U2d의 오류/재조회·검색 초기 값과 최종 실제 브라우저·자동 검사 증거를 다시 확인했다. 목표 원본·GOAL·README·설정/스크립트·git 상태·진행 기록과 적용 AGENTS 부재를 확인하고 기존 사용자 변경을 보존한다.
- 현재 읽기 목차는 본문 위에만 있어 스크롤하면 사라지며 현재 문단 표시/모바일 본문 복귀가 없다. 이번 단위는 기존 고정 보조 영역에 데스크톱 목차를 추가하고, 모바일 접이식 목차·읽던 문단 복귀를 제공하는 것이다. 공유 `Toc`의 미리보기 동작·계층/번호·기존 `section-N` 주소·본문/이력/관계는 보존한다. 새 문단 식별자·읽기 설정 저장 등 P1은 포함하지 않는다.
- 완료 기준: 실제 긴 문서의 스크롤/직접 해시/뒤로 가기·목차 선택에서 현재 위치·문단 이동·키보드 초점을 확인한다. 좁은/넓은 화면 전환, JavaScript 없는 기본 목차, 모바일 접기/펼치기/본문 복귀, 두 테마·360/768/1440px·200% 대응과 제목 없는/미작성/조회 실패 화면을 검증한다. DB 쓰기·외부 호출은 추가하지 않는다.
- 기준 `bun run test` **100 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 기존 자동 검사 실패 없음. 증거 위치 `/tmp/wiki-viki-u3b-20260922/`.

### 구현과 검증 중 확인한 내용

- 변경 전 실제 긴 문서의 현재 위치 표시가 없는 것을 신규 브라우저 검사로 재현했다. `Toc.svelte`에 선택 문단/선택 이벤트·제목/접기 표시 옵션을 추가하되 기본값으로 편집 미리보기의 기존 목차를 유지한다. 읽기 페이지는 데스크톱 보조 영역에 목차를 두고, 모바일은 기본 HTML `details`와 본문 복귀 링크를 사용한다. JavaScript 사용 시에만 모바일 목차를 고정한다.
- 읽기 페이지에서 실제 제목 위치로 현재 문단을 선택한다. 스크롤은 주소/이력/저장 데이터를 쓰지 않으며, 선택 문단은 색상 외에 굵기·왼쪽 선·`aria-current=location`으로 구별한다. 직접 선택하면 기존 해시 링크로 이동하고 해당 제목에 키보드 초점을 옮긴다. 화면 폭 전환/해제 때 이벤트·관찰자와 초점을 정리한다. 공통 테마 토큰과 기존 고정 헤더 높이를 사용한다.
- 최초 구현 check **오류 0·경고 0**. 모바일의 직접 `#section-3` 진입에서 제목 상단 **154.81px**, 새 목차 바 하단 **170px**로 제목이 약 15px 가려짐을 실제 측정했다. 초기 HTML의 해시 이동 뒤 클라이언트 고정 목차가 나타나는 원인이다. 초기화 때 현재 화면에 있고 가려진 해시 제목만 다시 정렬해 해결했다. 화면 밖의 이전 해시 때문에 복원된 읽기 위치를 덮어쓰지 않는다.
- 수정 후 360/768/1440px × 두 테마의 직접 해시·1/1.1/1.1.1 번호·18개 목차·키보드 선택/본문 초점·모바일 복귀/Escape·스크롤 현재 문단과 URL 보존 **6개 묶음 통과**. 실제 데이터 동일·페이지 오류/외부 HTTP 0. 계획된 기본 HTML·화면 폭/짧은 높이·뒤로 가기·지연/빈/실제 DB 오류 검사와 기존 미리보기/탐색 회귀를 이어서 확인한다.

- 신규 최종 `node tests/browser/reading-toc.mjs` **13개 묶음 통과**. 위 6개에 숨겨지는 목차의 초점 전환, 360/801px JavaScript 없는 펼침/기존 링크, 제목 없는 문서 이동 시 관찰자 정리와 `document-toc` 호환, 이전 해시와 다른 실제 읽기 위치의 뒤로 가기 복원, 두 테마 200% 대응(720×500 CSS/픽셀 배율 2), 400px 높이 데스크톱에서 현재 항목 자동 노출, 실제 지연 탐색의 이전 본문 유지, 미작성/실제 DB 조회 503와 재시도를 포함했다. 도구 모음 확대 직접 조작과 구분한다.
- 모바일에서 Escape뿐 아니라 Tab/클릭으로 목차 밖에 나가면 목록을 닫아 초점 대상이 뒤에 가려지지 않도록 보완했고, Tab→본문 조건을 최종 6개 화면 검사에 포함했다. 전체 검사 전후 문서·리비전·별칭·토론 동일, 페이지 오류/외부 HTTP 0. 모바일 라이트 펼친 목차와 데스크톱 다크 현재 위치 캡처를 직접 확인했다. 기존 편집/초안 미리보기·탐색/메뉴·자동 연결 회귀와 최종 자동 검사 진행 중이다.

- 기존 직접 편집 미리보기 **12개**, 초안/통합 미리보기 **7개**, 전역/새 작성 **12개**, 모바일 메뉴 **13개**, 자동 연결 **7개** 묶음과 test/check/lint/build가 통과했다. 새 화면 검토에서 문단에 옮긴 초점의 기본 파란 테두리가 공통 스타일에 빠진 것을 확인해 공통 초점 색상에 포함했다.
- 고정 목차가 기존 각주 이동을 가릴 가능성을 검사에 추가했고, 실제 모바일 `#fn-review`의 약 155px 위치와 목차 하단 170px의 겹침을 재현했다. 이는 이번 고정 목차 변경의 회귀이므로 먼저 보완한다. 본문의 모든 기존 ID 대상에 같은 이동 여백을 적용하고, 초기 보정도 실제 본문 안의 각주 등 해시 대상으로 확장했다. 잘못 인코딩된 해시는 무시하며, 기존 각주 주소/파서/원문은 변경하지 않는다. 직접 각주 진입·클릭 이동을 신규 회귀에 추가했고 최종 재검증한다. 각주→원문 복귀 신기능은 다음 독립 단위로 남긴다.

- 각주/초점 보완 후 신규 브라우저 **14개 묶음 전부 통과**. 원래 문단 조건을 유지하면서 각주의 직접 주소/클릭 이동·잘못 인코딩된 해시를 추가로 확인했다. 수정 후 모바일 다크 각주와 200% 대응 문단 초점 캡처를 직접 확인했다. 문서·이력·별칭·토론 보존 및 페이지 오류/외부 HTTP 0도 유지했다. 마지막 보완에 관련된 직접 편집 미리보기·전역 진입/읽기를 다시 검증하고 test/check/lint/build를 재실행한다.

- 각주/초점 수정 후 직접 편집 미리보기 12개·전역 진입 12개와 자동 검사도 다시 통과했다. 마지막 키보드 읽기 순서 대조에서 목차를 보조 영역으로 옮겨 Tab 순서가 본문 뒤로 밀린 회귀를 확인했다. 실제 문서 작업의 마지막 `역사` 링크에서 Tab하면 첫 본문 문단 링크로 넘어갔다. 본문 앞 `목차로 이동`을 추가해 현재 목차 항목에 바로 초점을 옮기며, JavaScript 없는 기본 링크도 보조 영역에 초점을 옮긴다. 문서 작업→목차→본문의 Tab/Enter 검사를 새 묶음으로 추가해 검증한다.

- 목차 진입 보완 후 신규 최종 `reading-toc.mjs` **15개 묶음 통과**. 실제 문서 작업→목차→본문의 Tab/Enter와 JavaScript 없는 목차 진입·첫 항목 초점도 확인했다. 이전 14개 문단/각주·반응형·복원·지연/오류 조건과 DB 동일성, 페이지 오류/외부 HTTP 0을 모두 유지했다. 마지막 전역 진입/읽기 회귀 및 자동 검사 진행 중이다.

### 최종 검증과 다음 반복

- 마지막 키보드 진입 수정 후 신규 읽기 **15개 묶음**, 기존 전역/새 작성/읽기 **12개 묶음**과 `bun run test` **100 통과·DB 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build`(Vercel 패키징), `git diff --check`를 통과했다. 직접 편집 미리보기 12개는 각주/초점 수정 후에도 재통과했고, 공유 목차의 초안/통합 미리보기 7개·모바일 메뉴 13개·자동 연결 7개도 이번 반복에서 통과했다. 중복 실행 수를 새 검증 개수로 합산하지 않는다.
- 기준 검사 실패 없음. 구현 중 재현한 초기 문단 가림·각주 가림·Tab 목차 진입 회귀를 원래 조건으로 수정/재검증했으며 미해결 회귀 없음. 모바일 펼친 목차·데스크톱 다크 현재 위치·짧은 데스크톱·200% 대응 초점·각주 캡처를 직접 확인했다. 실제 브라우저의 조회/저장·문서/이력/별칭/토론 동일성 검증과 기본 테스트에서 생략한 DB 원자성 스위트 20개를 구분한다. DB 쓰기/서버 렌더러/스키마를 수정하지 않았으므로 전체 원자성 검사를 반복하지 않았다.
- 주요 파일: `src/routes/wiki/[slug]/+page.svelte`, `src/lib/components/Toc.svelte`, `src/app.css`, `tests/browser/reading-toc.mjs`, README/진행 기록. **U3b 완료, U3/P0 전체 미완료.** U3a의 주변 테마·공통 토큰과 기존 문단 번호/주소·미리보기/문서 연결을 유지했다. GOAL 본문/체크박스와 기존 사용자 변경은 보존했다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 검증하지 않았고 W2 원본 Excel 특정 실패는 재현 대기다.
- **다음 반복: U3c — 각주에서 원문 참조 위치로 돌아가는 링크를 구현하고 반복 참조·키보드·미리보기 일치를 검증한다.** 현재 서버 렌더러는 `fn-ID`로 가는 링크와 각주 목록만 만들고 참조 위치 ID/복귀 링크를 만들지 않는다. 기존 각주 주소/본문/번호를 보존하며 실제 반환 위치를 추가한다. 표·관련 문서 등 나머지 P0 읽기 완료 대조는 이후 진행하고 P1 읽기 설정/안정 문단 식별자로 확대하지 않는다.
- 증거: `/tmp/wiki-viki-u3b-20260922/`의 `before/`, `baseline-*.log`/결과, `browser-before`/로그, `browser-first`/`browser-alignment`와 위치 측정, `browser-core`/`browser-complete`/`browser-final`, `browser-footnote-before`·`browser-anchor-final`, `browser-tab-before`·최종 `browser-keyboard-final`의 15개 결과/로그·캡처, 기존 5종 브라우저 결과/로그와 직접 편집/전역 진입의 최종 재검사, `before-anchor-*`/`anchor-final-*`/최종 `final-test/check/lint/build.log`, `keyboard-verification-results.json`, `final-schemas.log`. SQL로 잔여 fixture 스키마 **0개**를 확인하고 이번 브라우저·서버·격리 PostgreSQL을 종료했다. 사용자 개발 서버·환경 파일·운영 데이터·원격 저장소·배포는 보존했다. 재개 DB 조건은 반복 30과 같다.

## 반복 42 — U3c 각주의 원문 참조로 복귀 (2026-09-22)

- 직전 목표 턴은 **진행**: U3b의 읽기/목차·앵커·초점 코드와 최종 15개 브라우저 묶음 및 자동 검사 증거를 다시 확인했다. 목표 원본·GOAL·README·설정/스크립트·git 상태·진행 기록과 적용 AGENTS 부재를 확인했다. 기존 사용자 변경을 보존한다.
- 현재 각주 렌더러는 `fn-ID`로 가는 링크/목록만 만들고 원문 참조 ID나 복귀 링크를 만들지 않는다. 이번 단위는 한 각주의 반복 참조마다 실제 원문 위치로 돌아가기, 키보드 초점, 게시/직접 편집/초안·통합 미리보기의 같은 동작이다. 기존 한 줄 각주 정의 문법·주소·표시 번호와 원문/이력/관계는 보존하며 데이터 모델을 바꾸지 않는다.
- 참조 위치는 실제 일반 텍스트 토큰에서만 만들고 코드·기존 링크·이미지·HTML 속성의 표기를 참조로 만들지 않는다. 정의가 없는 표기/참조하지 않은 각주에는 없는 복귀 위치를 만들어 내지 않는다. 정규화된 이름이 충돌해도 ID를 중복시키지 않고 기존 첫 각주 주소를 유지한다. 미리보기의 기존 접두사를 양쪽 링크/위치에 적용한다.
- 완료 기준: 단일/반복/미참조/미정의·이름 충돌·안전한 HTML·실제 미리보기/게시 일치, JavaScript 없는 기본 링크, 키보드·직접 주소·두 테마/주요 크기/확대·모바일 목차와의 가림, 실제 조회 실패/재시도·입력/DB 보존. 기준 test **100 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 증거 `/tmp/wiki-viki-u3c-20260922/`.
- `wiki-links.js`에서 일반 텍스트 참조를 파서 토큰으로 표시하고, `server/wiki.js`에서 실제 렌더링한 참조마다 `fnref-ID-N`과 키보드로 복귀할 링크를 만든다. 반복 참조·정규화 이름 충돌·초안별 접두사를 구분한다. 기존 전체 HTML 문자열 치환을 제거해 코드/링크/HTML 속성의 표기와 원문에 쓴 내부 마커가 가짜 참조를 만들지 않는다. 각주 정의의 기존 한 줄 추출 문법은 그대로이며 전체 Markdown 각주 문법 확장으로 주장하지 않는다.
- `app.css`의 각주 식별자·본문 복귀 링크는 기존 브랜드/초점 토큰을 사용한다. 미사용 각주는 내용만 보이고 복귀 링크를 만들지 않는다. 직접 편집/초안·통합 브라우저 검사에 왕복 이동과 초점을 추가했다. 원문·리비전·관계·스키마/저장 API는 변경하지 않는다.
- 신규 `wiki-footnotes.test.js`는 변경 전 실패(`unit-before.log`)를 재현한 뒤, 수정 후 기존 자동 연결 검사와 함께 **14 통과**했다. `RUN_GOVERNANCE_DB_TESTS=1 node --test --test-concurrency=1 tests/wiki-preview-db.test.js tests/draft-preview-db.test.js tests/automatic-links-db.test.js`는 격리 PostgreSQL에서 **18 통과·생략 0**. 실제 게시/통합 결과와 미리보기 일치·읽기 전용 보존·연결 계산을 확인했다.
- 신규 읽기 브라우저 첫 실행은 **10개 묶음 통과**: 360/768/1440px × 두 테마, 실제 반복 참조 왕복/초점, 기존 각주·한글 직접 주소, JavaScript 없는 링크, 두 테마 200% 대응(720×500 CSS/픽셀 배율 2), 각주 없음, 실제 DB 503 후 재조회. 문서/이력/별칭/초안/토론 불변·페이지 오류/외부 HTTP 0. 모바일 라이트 각주·데스크톱 다크 원문 복귀 캡처를 직접 확인했다.
- 초안 목록의 기존 `[id]` 이동 여백이 고정 검토 도구 모음을 보호하는지 실제 왕복 검사를 추가했다. 첫 추가 실행의 실패는 펼치기 후 버튼 이름이 바뀌어 검사 locator의 두 번째 인덱스가 사라진 원인이었다. 초기 개수만큼 현재 첫 펼치기 버튼을 누르도록 검사만 수정했으며 원래 각주/초점 조건을 유지해 재실행한다(`browser-final.log`, 이후 `browser-list/`).
- 목록 검사를 추가한 최종 각주 브라우저도 **10개 묶음 통과**. 같은 이름의 각주가 있는 두 초안에서 각각 두 번째 원문 위치로 복귀하고 고정 검토 도구 모음 아래에 초점을 표시했다. 6개 화면/테마 조합과 JavaScript 없는 목록도 확인했으며 DB 불변·오류/외부 HTTP 0을 유지했다. 목록 모바일 라이트/데스크톱 다크와 200% 대응 다크 캡처를 직접 확인했다. 기존 직접 편집 미리보기 **12개 묶음 통과**; 나머지 공유 렌더러/목차 브라우저와 최종 자동 검사 진행 중이다.

### 최종 검증과 다음 작업

- `node tests/browser/edit-preview.mjs` **12개**, `draft-preview.mjs` **7개**, `reading-toc.mjs` **15개**, `automatic-links.mjs` **7개 묶음 통과**. 새 각주 왕복/초점과 기존 실제 게시·통합, 빈/지연/오류/재시도·입력 보존, 목차/기존 주소, 문서 연결과 전체 재검사를 확인했다. 각 검사 페이지 오류·예기치 않은 외부 HTTP 0. 신규 각주 최종 10개 묶음과 별도로 기록하며 반복 실행을 합산하지 않는다.
- 최종 `bun run test` **107 통과·DB 스위트 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build` **통과**(Vercel 패키징 포함). 기록 갱신 후 `bunx prettier --check README.md RALPH_PROGRESS.md`와 `git diff --check`도 통과했다. DB 통합 18개는 별도 실제 로컬 PostgreSQL에서 실행했다. 이번 변경은 읽기 전용 렌더러/CSS라 전체 쓰기 원자성 스위트를 반복하지 않았다. 검사 종료 후 남은 `governance_%` 스키마 **0개**를 확인하고 이번 실행에서 시작한 로컬 PG를 종료했다.
- 기준 자동 검사 실패 없음. 변경 전 신규 각주 검사로 재현한 복귀 없음·가짜 참조 문제는 수정 후 통과했다. 목록 검사 locator 실패도 원인을 확인해 수정했으며 미해결 회귀 없음. 정상/빈/로딩/실제 조회 오류 및 시각 검증과 단위/DB 통합 검증을 구분한다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포 검증은 수행하지 않았다.
- 주요 파일: `src/lib/server/wiki.js`, `src/lib/wiki-links.js`, `src/app.css`, 신규 `tests/wiki-footnotes.test.js`/`tests/browser/reading-footnotes.mjs`, 기존 편집·초안 미리보기 브라우저 검사, README/진행 기록. **U3c 완료, U3/P0 전체 미완료.** GOAL 본문/체크박스와 기존 사용자 변경을 보존했다. W2 특정 원본 Excel 오류의 재현 조건은 여전히 대기다.
- **다음 반복: U3d — 표의 가로 스크롤 안내와 키보드 접근을 구현·검증한다.** 현재 공통 표 CSS는 `display: block; overflow: auto`지만 가로 탐색 안내는 없다. 좁은 화면의 실제 긴 표를 먼저 확인하고, 기존 표 내용·의미 구조·미리보기 일치를 유지하며 안내/초점/키보드 이동을 보완한다. P1의 읽기 설정·문단 식별자·코드 복사로 확장하지 않는다.

## 반복 43 — U3d 표의 가로 스크롤 안내와 키보드 접근 (2026-09-22)

- 직전 목표 턴은 **진행**: U3c의 실제 공통 렌더러/스타일과 107개 기본 검사·DB 18개·브라우저 결과를 현재 코드/증거 파일로 확인했다. 목표 원본·GOAL·README·설정·git 상태·진행 기록과 적용 AGENTS 부재를 확인했다. 이번 기준 test **107 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 기존 사용자 변경을 보존한다.
- 현재 표는 공통 CSS의 `display: block; overflow: auto`만 있고 안내/명시적인 키보드 진입점이 없다. 이번 완료 기준은 ① 게시·편집·초안/통합 미리보기의 동일한 표 구조/내용 ② 표 전용 스크롤 영역과 가로 탐색 안내·이름/초점 ③ 열/행·정렬·표 안의 링크/각주 보존 ④ 실제 Chrome의 세 화면 크기/두 테마·넓고 좁은 표·Tab/방향키/탈출·터치·JavaScript 없는 기본 흐름·200% 대응 ⑤ 빈/로딩/실제 DB 오류 복구 및 데이터 불변이다.
- 공통 서버 렌더러와 CSS를 우선 사용해 기본 HTML에서도 작동하게 한다. 실제 폭을 모르면서 넘친다고 단정하지 않도록 안내는 조건형으로 표시한다. 저장/스키마·사용자 원문·기존 주소·P1 읽기 설정/코드 복사 기능은 변경하지 않는다. 증거 경로 `/tmp/wiki-viki-u3d-20260922/`.
- 변경 전 실제 Chrome 360px에서 첫 표는 표시 폭 298px/내용 폭 1201px였지만 안내·이름 있는 초점 영역이 없었다. 신규 단위/브라우저 검사는 이 누락으로 실패했다(`unit-before.log`, `browser-before/`). 합성 문서의 명시적 링크는 실제 저장된 영문 slug를 대상으로 수정해 존재하는 링크를 검증한다.
- `server/wiki.js`는 기존 marked 표 렌더링을 그대로 감싸 표 순서별 이름·고유 설명 ID·기본 Tab 진입점을 제공한다. 초안별 기존 접두사를 적용하며 별도 JavaScript나 표 데이터 재구성은 없다. `app.css`는 표 자체의 `display:block`을 없애 기본 table 의미 구조를 유지하고 바깥 영역만 스크롤하게 한다. 안내/초점은 기존 브랜드/보조 글자 토큰을 공유한다. 짧은 표에도 조건형 안내를 유지해 서버가 실제 넘침 여부를 만들어 내지 않고 기본 HTML에서 동일하게 접근하게 한다.
- 신규 `wiki-tables.test.js`와 기존 각주/자동 연결 검사는 **18 통과**: 이름/설명 연결, 열/행/정렬·셀 내용·링크·각주·접두사, 코드/HTML 안전 처리. 신규 브라우저 첫 수정 후에는 실제 HTML 전체가 같은데 SSR의 Svelte 주석만 달라 비교가 실패했으므로 주석만 제외하고 나머지 마크업·속성을 모두 비교한다. 두 번째 실행은 40회 연속 오른쪽 키 직후 왼쪽 키 검사에서 실패했다. 브라우저의 진행 중 스크롤이 안정되는 것을 확인한 뒤 반대 방향을 누르도록 보완했으며 마지막 열 도달·왼쪽 실제 이동 기준은 유지한다. 각 실패 로그를 남기고 같은 조건으로 재검증한다.
- 표 마지막 셀의 좌표도 진행 중 애니메이션 뒤에 측정하도록 정리했다(세 번째 실행의 768px 마지막 열 좌표 검사). 네 번째 실행에서 일반 6개 화면/테마·게시/편집/두 초안은 통과했지만 JavaScript 없는 페이지의 내부 대기에서 시간 초과됐다. 별도 실제 브라우저 진단은 360px에서 `scrollLeft=903`(1201−298), 1440px에서 `399`(1201−802)로 정상 이동한 것을 확인했다. 페이지 내부 타이머/프레임 대기 대신 Node 측 반복 조회로 실제 위치를 확인하도록 검사만 수정했다(`native-probe.log`, `native-timer-probe.log`). 마지막 열 노출·양방향 이동·Tab 진입/탈출·표 안 각주 왕복 조건은 유지한다.
- 신규 최종 `node tests/browser/reading-tables.mjs` **12개 묶음 통과**: 360/768/1440px × 라이트/다크에서 게시/직접 미리보기/두 초안의 넓은·작은·행 없는 표, 키보드 진입·양방향 끝 열·Tab 탈출·표 안 각주 복귀와 동일 HTML/독립 ID, 360/1440px JavaScript 없는 기본 링크/키보드, 두 테마 200% 대응(720×500 CSS/픽셀 배율 2), 실제 Chrome 터치 제스처, 표 없는 문서, 미리보기 지연/입력 보존/빈 상태, 실제 DB 503와 명시적 재시도. 오류/외부 HTTP 0, 문서/리비전/별칭/초안/토론 불변. 모바일 라이트·데스크톱 다크·초안 목록 다크·200% 대응·터치·로딩 캡처를 직접 확인했다. 브라우저 도구 모음의 확대 조작/실기기 검증과 구분한다.
- `RUN_GOVERNANCE_DB_TESTS=1 node --test --test-concurrency=1 tests/wiki-preview-db.test.js tests/draft-preview-db.test.js tests/automatic-links-db.test.js` **실제 격리 PostgreSQL 18 통과·생략 0**. 실제 게시/통합 이후 미리보기 일치·읽기 전용 보존·자동 연결을 재확인했다. 기존 초안 브라우저에도 일반 편집·통합 표의 고유 설명/초점·열/셀 검사를 추가해 실행 중이다.

### 최종 검증과 다음 작업

- 기존 `node tests/browser/edit-preview.mjs` **12개**, 수정한 `node tests/browser/draft-preview.mjs` **7개 묶음 통과**. 초안 편집/최종 통합의 표 설명 ID·초점·열/셀, 실제 통합 이후 HTML 일치와 기존 입력/버전/빈/지연/실제 오류·재시도를 확인했다. 페이지 오류·예기치 않은 외부 HTTP 0. 신규 표 최종 12개 묶음과 구분하며 실패/재실행 개수를 합산하지 않는다.
- 최종 `bun run test` **111 통과·DB 스위트 20 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`, `bun run build` **통과**(Vercel 패키징 포함). 실제 DB 통합 18개는 별도로 실행했으며 기본 생략분을 통과로 계산하지 않는다. 읽기 전용 표 렌더러/CSS 변경이므로 전체 쓰기 원자성 스위트를 반복하지 않았다. 남은 `governance_%` 스키마 **0개** 확인 후 이번 반복에서 시작한 로컬 PG를 종료했다.
- 기준 자동 검사 실패 없음. 신규 검사에서 확인한 표 탐색 누락은 구현 후 통과했다. 비교 주석·좌표 측정 시점·JavaScript 없는 페이지 대기 문제는 별도 진단/수정 후 원래 셀·키보드·보존 조건으로 통과했고 미해결 회귀 없음. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포 및 실기기/스크린리더 직접 검증은 수행하지 않았다. 실제 Chrome의 역할/레이블·키보드/초점·반응형 시각 검증과 구분한다.
- 주요 파일: `src/lib/server/wiki.js`, `src/app.css`, 신규 `tests/wiki-tables.test.js`/`tests/browser/reading-tables.mjs`/합성 표 fixture, `tests/browser/draft-preview.mjs`, README/진행 기록. **U3d 완료, U3/P0 전체 미완료.** 기존 사용자 변경과 GOAL 본문/체크박스를 보존했다. W2 특정 원본 Excel 오류 재현은 여전히 원본 또는 확장자·오류 문구 대기다.
- **다음 반복: U3e — 관련 문서·역링크에 실제 연결 방향과 이유를 표시한다.** 현재 읽기 서버는 그래프의 입·출력 집합을 문서 요약으로만 전달하고 관련 문서 UI는 제목/분류만 표시한다. 기존 토큰에는 자동/명시적 연결과 실제 표시 문구가 있으므로 이를 재사용해 근거 있는 연결 이유를 표시하고 모바일/키보드·별칭/휴지통·빈/오류 상태를 검증한다. 연결 규칙·문서 원문·주소·관계를 바꾸거나 의미 유사도를 추측하지 않는다.
- 기록 갱신 후 `bunx prettier --check README.md RALPH_PROGRESS.md`와 `git diff --check`도 통과했다.

## 반복 44 — U3e 관련 문서·역링크의 실제 연결 이유 (2026-09-22)

- 직전 목표 턴은 **진행**: U3d의 현재 공통 표 렌더러/스타일과 최종 자동 검사·브라우저 증거를 다시 확인했다. 목표 원본·GOAL·README·설정/스크립트·git 상태·진행 기록 및 적용 AGENTS 부재를 확인했다. 이번 기준 test **111 통과·DB 20 생략**, check **오류 0·경고 0**, lint/build 통과. 기존 사용자 변경을 보존한다.
- 이번 단위는 §7.3/§12의 관련 문서·역링크 연결 이유와 방향 표시다. 현재 읽기 서버는 공통 그래프에서 방향별 문서 집합만 만들고, 화면은 제목/분류 또는 역링크 칩만 표시한다. 기존 파서의 `automatic`과 원문 링크 표기를 사용해 근거 있는 이유를 전달한다. 의미 유사도 추측·연결 규칙 변경·원문/URL/관계 저장·새 외부 호출은 포함하지 않는다.
- 완료 기준: ① 제목/별칭 자동 연결과 본문에 저장된 링크 구분·원문 표기·입/출력 방향 ② 자기 연결/중복/모호한 이름/코드·미작성/휴지통 제외와 기존 문서 순서/대표 주소 유지 ③ 현재 표시 본문과 출발 연결의 일치, 실제 DB 읽기 보존·별칭/삭제/복구 재계산 ④ 360/768/1440px·두 테마·키보드/기본 HTML·200% 대응과 긴 이름 ⑤ 빈/지연/실제 DB 오류/재시도, 기존 자동 연결/목차 회귀. 증거 `/tmp/wiki-viki-u3e-20260922/`.
- `knowledge.js`의 읽기 전용 `documentConnections`는 기존 파서의 방향별 대상과 `automatic`/원문 표기를 문서 요약에 붙인다. 기존 문서 순서를 유지하고 자기 연결/휴지통을 제외한다. 출발 연결은 실제 렌더링한 본문을 사용해 후속 목록 조회 사이에 편집이 있어도 표시한 본문과 이유가 어긋나지 않게 한다. 지도/전체 검사 그래프의 데이터 형태·계산 규칙과 SQL/저장 모델은 변경하지 않는다.
- 읽기 서버와 신규 `DocumentConnections.svelte`에서 양방향 목록·연결 종류·원문 표기를 표시한다. 이유를 링크의 접근성 설명에 연결하며 Svelte 텍스트로 출력해 원문의 HTML을 실행하지 않는다. 기존 대표 주소와 역링크 이름을 유지한다. 문서 정보는 들어오는/나가는 연결 개수를 분리한다. 공통 토큰·목록 구분선·초점/44px 링크 영역을 적용하고 두 목록의 개수 표시는 같은 중립 스타일로 맞췄다.
- 신규 단위 검사는 변경 전 실패 후 기존 자동 연결 검사와 함께 **11 통과**: 방향/순서/중복·별칭·삭제/모호함·자기/코드 제외·표시 본문 기준·원문 표기 보존. 격리 DB에서는 신규 이유 검사 **5 통과**, 기존 자동 연결 검사 **7 통과**. 초기 신규 DB 검사에서 기존 `saveDocument`에 별칭 객체 대신 문자열 배열을 전달한 테스트 오류가 있었고, 실제 API 규격대로 검토한 별칭 객체를 전달해 수정했다. 저장/보존 검사를 줄이지 않았으며 최종 재실행 5개 모두 통과했다.
- 신규 브라우저 첫 실행 **12개 묶음 통과**: 360/768/1440px × 두 테마, 별칭 진입·방향/원문 표기·긴 제목/표기·키보드 왕복·고유 설명 ID·가로 넘침 없음, JavaScript 없는 기본 링크, 두 테마 200% 대응(720×500 CSS/픽셀 배율 2), 실제 빈/지연/DB 오류·재시도, 실제 양방향 문서 휴지통/복구. 읽기 전후 데이터 동일, 복구 후 의도한 생명주기 카운터 외 내용/관계 동일, 페이지 오류/외부 HTTP 0. 모바일·데스크톱·200% 대응 캡처를 직접 확인했다. check **오류 0·경고 0**.
- 마지막 U3 단위의 완료 대조를 위해 최종 이유 화면과 기존 자동 연결·목차·각주·표 브라우저를 현재 코드로 순차 재검증한다. 공통 지식 모듈·읽기 서버/보조 영역 변경이 있으므로 P0의 읽기 체크박스 전체를 과거 증거만으로 완료 처리하지 않는다.

- 최종 현재 코드 브라우저 재검증: `reading-connections.mjs` **12개**, `automatic-links.mjs` **7개**, `reading-toc.mjs` **15개**, `reading-footnotes.mjs` **10개**, `reading-tables.mjs` **12개 묶음 모두 통과**. 세 화면 크기·두 테마, 키보드/JavaScript 없는 기본 탐색, 200% 대응 화면, 실제 빈/지연/DB 실패·재시도와 삭제·복구를 확인했다. 각 검사 페이지 오류·예기치 않은 외부 HTTP 0. 읽기 데이터 보존 및 의도한 삭제/복구 카운터 외 원문·관계 보존 확인. 중립 개수 태그를 적용한 최종 모바일 다크/데스크톱 라이트 캡처도 직접 확인했다.
- P0 §12 읽기 완료 기준 대조: U3a 공통 테마, U3b 목차 이동·현재 문단·계층/번호·기존 주소, U3c 각주 원문 복귀, U3d 표 안내·키보드/터치, U3e 실제 관련 문서/역링크 이유·방향을 현재 실행 증거로 확인했다. U3 완료는 이 P0 범위이며, 읽기 설정 저장·안정 문단 식별자·메타데이터 확장 등 §7.3의 P1까지 완료했다는 뜻이 아니다.
- 최종 기본 `bun run test` **115 통과·DB 21 생략**, `bun run check` **오류 0·경고 0**. 이번 기능의 실제 DB 검증 12개는 위 별도 실행 결과이며 생략된 전체 DB 검사를 통과로 계산하지 않는다. 최초 최종 lint는 진행 기록과 신규 DB 테스트 두 파일의 포맷으로 실패해 해당 파일만 정리했다. 빌드/포맷 최종 결과는 아래에 기록한다.

- 포맷 수정 후 `bun run lint`와 `bun run build` **통과**. 신규 코드로 발생한 미해결 검사 실패는 없다. 테스트·타입 검사 통과 뒤 변경은 두 파일의 포맷과 문서 완료 기록뿐이다. 기존 사용자 변경과 요구사항 본문을 보존하고 GOAL §12의 P0 읽기 체크박스 하나만 완료로 갱신했다.
- 증거: `/tmp/wiki-viki-u3e-20260922/`의 기준/최종 자동 검사 로그, `db-reasons.log`·`db-first.log`(기존 자동 연결의 통과 및 수정 전 신규 테스트 오류 구분), `browser-regression-results.json`과 각 브라우저의 결과 JSON/캡처. 잔여 `governance_%` 테스트 스키마 **0개** 확인 후 이번에 가동한 로컬 PostgreSQL만 종료했다.
- 주요 파일: `src/lib/knowledge.js`, `src/lib/components/DocumentConnections.svelte`, 읽기 `+page.server.js`/`+page.svelte`, `src/app.css`, 신규 이유 단위/DB/브라우저 검사와 합성 fixture, README/GOAL/진행 기록. **U3e 및 U3의 P0 읽기 범위 완료, 전체 P0 미완료.** 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 이번 검증 범위에서 수행하지 않았다. 확대 검증은 200%에 대응하는 CSS 화면/픽셀 배율이며 브라우저 도구 모음 직접 조작과 구분한다.
- 남은 검증: 미리보기·입력 보존·게시/별칭/삭제 보호·검사 일관성의 P0 전체 완료 기준을 현재 코드/실행 결과로 감사해야 한다. W2 신고된 특정 Excel 실패는 확장자·오류 문구 또는 비식별 재현 파일이 없어 재현 대기다. 원격 푸시·배포·운영 데이터 변경은 하지 않았다.
- **다음 반복: E1/E2 — 직접 편집·초안 수정의 현재 입력 미리보기와 실제 게시 화면 일치를 현재 코드로 완료 대조한다.** 기존 회귀를 실행해 누락이 발견되면 가장 작은 실패 단위를 수정·검증하고, 두 경로의 전체 조건이 입증될 때만 해당 P0 체크박스를 갱신한다. 이미 완료한 편집기나 읽기 기능을 다시 구현하지 않는다.

## 반복 45 — E1/E2 미리보기 완료 대조와 반응형 초점 보존 (2026-09-22)

- 직전 목표 턴은 **진행**: U3의 현재 읽기/연결 코드와 최종 결과를 확인했다. 목표 원본·GOAL·README·진행 기록·실제 Vite 설정/스크립트·git 상태와 적용 AGENTS 부재를 재확인했다. 설정은 별도 svelte.config.js 없이 vite.config.js에 있다. 기존 사용자 변경을 보존한다.
- 이번 기준: `bun run test` **115 통과·DB 21 생략**, check **오류 0·경고 0**, lint/build **통과**. 실제 로컬 격리 PostgreSQL의 기존 `wiki-preview-db.test.js`와 `draft-preview-db.test.js` **11 통과·생략 0**, 외부 HTTP 0. 코드 대조에서 현재 입력·대표 URL/별칭·각주/표의 공통 렌더러, 읽기 전용·재시도·오래된 응답 제외를 확인했다.
- 실제 Chrome 재현에서 데스크톱 미리보기의 문단 링크에 초점을 둔 뒤 768px로 전환하면 초점이 BODY로 사라진다. WikiEditor가 넓은 section과 좁은 div를 별도 분기로 다시 만드는 것이 원인이다. 입력/미리보기 탭에 초점을 둔 채 넓히는 경우도 같은 전환 경계를 검사한다. 임시 재현 파일의 Vite 모듈 경로 문제는 실제 설치 경로로 수정했고, 이후 제품 초점 실패를 확인했다.
- **이번 작은 작업:** 공통 미리보기 DOM을 유지하고 제거되는 모바일 탭의 초점을 해당 편집 영역으로 옮긴다. 직접 편집·초안 수정·최종 통합에서 양방향 폭 변경/키보드·현재 입력/HTML·실제 게시 일치를 검증한다. 이미 완료한 편집기·검사/저장 정책을 재구현하거나 P1 작성 도구로 확대하지 않는다. 완료 대조 후에만 GOAL의 미리보기 체크박스를 갱신한다.
- 증거 경로: `/tmp/wiki-viki-e-preview-audit-20260922/`. 운영 DB 대신 기존 소유 로컬 테스트 DB를 가동했고 실제 문서/초안·이력·별칭 스냅샷으로 보존을 확인한다.

- 구현: `WikiEditor.svelte`의 반응형 분기에서 미리보기 DOM을 공유해 링크/표의 초점을 보존한다. 모바일 탭이 제거되면 `tick()` 뒤 해당 입력란/미리보기 영역으로만 초점을 넘기며, 다른 곳으로 이동한 초점은 빼앗지 않는다. 숨김·역할·레이블은 화면 폭에 맞게 유지하고 미리보기 영역의 초점도 공통 브랜드 색으로 표시한다.
- 신규 공통 브라우저 회귀 `tests/fixtures/preview-focus.js`를 기존 직접 편집·초안 검사에 적용했다. 실제 링크/표와 입력란의 양방향 폭 변경, 사라지는 두 탭의 초점 이동, 제목 입력 초점 유지, 원래 입력/렌더링 HTML 보존을 검사한다. 720px·픽셀 배율 2의 양쪽 테마를 추가해 200% 대응 화면도 확인한다.
- **완료 감사에서 남은 실제 차이:** 초안/최종 통합은 `api/preview`의 `imageLinks: !!draft`에 따라 Markdown 이미지를 링크로 렌더링하지만 게시 읽기는 기본 `<img>`를 사용한다. 기존 E2 검사는 각주/표/링크의 게시 일치를 검증하면서 이미지에는 별도 링크 정책만 확인했으므로 이미지까지 포함한 P0 전체 일치의 증거가 아니다. E2를 진행 중으로 되돌리고 GOAL 미리보기 체크박스는 미완료로 유지한다. 기존 외부 이미지 자동 요청 방지 의도와 URL/원문을 보존하면서 실제 이미지 표시를 확인할 수 있는 흐름을 별도 작은 작업으로 해결해야 한다.

- 첫 반응형 검사에서는 직접 편집 **12개**, 초안/통합 **7개 묶음 통과**. 표 초점·다른 필드 초점·확대 화면을 추가한 재검증도 직접 편집 **14개**, 초안/통합 **9개 묶음 통과**, 페이지 오류/외부 HTTP 0이었다. 직접 편집의 모바일 다크 확대와 데스크톱 라이트 캡처를 직접 확인했다.
- Svelte 정적 검사가 동적 role을 판별하지 못해 단일 공유 노드에 `a11y_no_noninteractive_tabindex` 경고를 냈다. 실제 데스크톱은 이름 있는 `region`/tabindex -1, 모바일은 `tabpanel`/tabindex 0이며 올바른 탭/제목 레이블을 가진다. 이 노드에만 이유를 적은 정적 경고 예외를 두고, 두 분기의 실제 role/tabindex/aria-labelledby를 브라우저 회귀에서 명시적으로 검사한다. 다른 접근성 검사나 실패 테스트는 제거하지 않았다. 예외 적용 후 check **오류 0·경고 0**.

- 최종 검증: `bun run test` **115 통과·DB 21 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`/`bun run build` **통과**. 직접 편집 브라우저 **14개**, 초안/통합 브라우저 **9개 묶음 통과**. 새 role/tabindex/레이블 검사와 기존 실제 저장·통합/충돌, 빈/지연/실패/재시도·오래된 응답 제외·입력 보존을 포함한다. 각 페이지 오류·예기치 않은 외부 HTTP **0**. 재실행은 새 접근성 속성 검사와 컴파일러 경고 해소 확인 때문이며 반복 결과를 합산하지 않는다.
- 실제 DB 11개 검사는 위의 별도 실행 증거로 구분한다. 이번 제품 변경은 공통 프런트 편집기에 한정되며 데이터/서버 렌더러/저장 경로를 바꾸지 않았다. 신규 브라우저도 미리보기 중 스냅샷 보존과 실제 저장/통합 후 결과를 확인했다. 최종 정상·오류·로딩 캡처와 초안 모바일 라이트/최종 통합 다크 확대 화면을 확인했다. 확대는 200% 대응 CSS 화면/픽셀 배율이며 도구 모음 직접 조작 검증과 구분한다.
- 최종 증거: 위 경로의 `verified-results.json`, `verified-{test,check,lint,build}.log`, `edit-preview-verified/`·`draft-preview-verified/` 결과/캡처. 시작 시 기본 검사 실패는 없었고 새로 확인된 초점 소실은 해결했다. 동적 role 정적 경고의 처리 근거는 위에 남겼다. 잔여 테스트 스키마 **0개** 확인 후 이번에 가동한 소유 로컬 PostgreSQL을 종료했다. 운영 DB·실제 외부 AI·Node.js 22·배포는 이번 검증에서 실행하지 않았다.
- 주요 파일: `src/lib/components/WikiEditor.svelte`, `tests/fixtures/preview-focus.js`, 기존 `tests/browser/edit-preview.mjs`·`draft-preview.mjs`, README/진행 기록. **E2a 초점 보존 완료, E2 전체와 P0 미완료.** GOAL 본문/체크박스와 기존 사용자 변경은 보존했다. W2 특정 Excel 오류는 원래 확장자·오류 문구 또는 비식별 재현 파일이 필요하다.
- **다음 반복: E2b — 초안/최종 통합의 Markdown 이미지 표시를 실제 게시 화면과 대조할 수 있게 한다.** 외부 이미지를 자동 요청하지 않는 현재 원칙을 유지하면서 이미지 결과를 확인할 수 있는 명시적 흐름을 구현하고, 합성 로컬 이미지로 정상/실패·입력 변경·게시 일치·외부 호출 여부를 검증한다. 확인되지 않은 상태를 전체 미리보기 완료로 표시하지 않는다.

## 반복 46 — E2b 초안 이미지 미리보기와 게시 표시 일치 (2026-09-22)

- 직전 목표 턴은 **진행**: 현재 공통 미리보기 초점 수정과 검증 로그, 이미지 링크/게시 이미지의 실제 코드 차이를 다시 확인했다. 목표 원본·GOAL·README·설정/스크립트·git 상태·진행 기록과 적용 AGENTS 부재를 확인했다. 기존 사용자 변경을 보존한다.
- 기준 검사: test **115 통과·DB 21 생략**, check **오류 0·경고 0**, lint/build **통과**. 신규 이미지 게시/통합 일치와 명시적 boolean 입력 검사에서 변경 전 실패를 확인했다. 테스트 DB는 소유 localhost 전용 DB이며 운영 환경 파일/DB를 사용하지 않는다.
- 이번 단위: 초안/최종 통합의 현재 미리보기에서 사용자가 이미지 표시를 선택하면 게시와 같은 렌더러로 실제 이미지를 표시한다. 기본은 기존 링크 표시를 유지한다. 선택은 현재 본문·문서/초안 정보·대상·편집 범위에만 유효하고 입력 변경 시 해제한다. 선택 해제·늦은 응답·두 편집기 분리·실제 이미지 로딩/실패/재시도·DB 보존과 실제 게시 일치를 검증한다.
- 구현은 공통 편집기와 기존 preview API에 한정한다. 이미지 주소로의 브라우저 요청을 선택 UI에 설명하며 서버는 이미지를 대신 요청하거나 AI/저장을 실행하지 않는다. 기존 게시 이미지·원문·URL·앵커/별칭 규칙은 변경하지 않는다. 증거 경로는 `/tmp/wiki-viki-e2b-20260922/`이다.

- 구현 후 DB 회귀: `wiki-preview-db.test.js`와 `draft-preview-db.test.js` **11 통과·생략 0**. 명시적 표시 시 이미지 URL/대체 텍스트/제목과 전체 HTML이 실제 초안 저장·게시 및 최종 통합 후 결과와 일치한다. 기본 링크 표시/외부 요청 없음·잘못된 표시 값 거부·버전/원문/관계 보존을 유지했다. check **오류 0·경고 0**.
- 새 `preview-images.mjs` 첫 실행 **12개 묶음 통과**. 360/768/1440px와 200% 대응 720px/픽셀 배율 2 × 두 테마에서 Space로 표시 선택·두 편집기 분리·실제 새 게시/통합 후 이미지 HTML 일치·가로 넘침 없음을 확인했다. 별도 localhost 이미지 서버에서 기본 요청 0, 입력/제목/별칭 변경과 되돌리기 후 선택 해제, 다른 초안 이동, 취소 불가능한 늦은 API 응답 제외, 실제 이미지 지연/404·재시도 성공·빈 입력과 DB 보존을 검증했다. 페이지 오류/예기치 않은 외부 HTTP 0. 통제한 로컬 이미지 요청 20회는 실제 외부 서비스 호출과 구분한다.
- 모바일 라이트·데스크톱 다크의 선택/이미지와 실제 지연·실패 안내 캡처를 직접 확인했다. 공통 토큰/상태 컴포넌트를 사용하고 실제 이미지의 대체 텍스트·제목과 게시 스타일을 유지했다. 기존 직접 편집·초안 브라우저 회귀와 최종 자동 검사를 이어간다.

- 기존 `edit-preview.mjs` **14개**, `draft-preview.mjs` **9개 묶음 통과**: 현재 입력·렌더링/스타일의 실제 게시 일치, 반응형 초점/탭·목차·각주/표, 실제 저장 충돌·통합과 빈/지연/API 실패/재시도·오래된 응답 제외·입력 보존을 현재 코드로 확인했다. 페이지 오류/예기치 않은 외부 HTTP 0. 새 이미지 검사 12개와 구분하며 반복 실행을 합산하지 않는다.
- 최종 `bun run test` **115 통과·DB 21 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`/`bun run build` **통과**. 실제 DB 11개는 위 별도 실행 결과다. 기존 검사 실패는 없었으며 신규 실패 조건은 이미지 표시 차이와 잘못된 표시 값 허용으로 재현한 뒤 해결했다. 이번 변경의 미해결 회귀 없음.
- 완료 대조: 직접 편집/새 문서·일반 초안·최종 통합 모두 현재 입력을 읽기 공통 렌더러로 표시하고 실제 저장·게시 결과와 비교했다. 이미지도 명시적 표시 선택 뒤 게시와 같은 URL/대체 텍스트/제목 및 HTML을 확인할 수 있다. 반응형·키보드·200% 대응, 빈/실패/재시도, 읽기 전용·외부 AI 없음, 늦은 응답·이미지 선택 해제까지 증거가 있어 **E2b/E2 및 P0 미리보기 일치 항목 완료**로 갱신했다. P1 작성 도구까지 완료한 것으로 해석하지 않는다.
- 주요 파일: `src/lib/components/WikiEditor.svelte`, `src/routes/api/preview/+server.js`, `tests/draft-preview-db.test.js`, 신규 `tests/browser/preview-images.mjs`, README/GOAL/진행 기록. 공통 게시 렌더러·DB 모델·저장 로직·전역 스타일은 이번에 변경하지 않았다. 요구사항 본문·사용자 변경은 보존하고 GOAL의 P0 미리보기 체크박스 하나만 갱신했다.
- 증거: 위 경로의 `baseline-*.log`, 실패 재현 `db-before.log`, `db-first.log`, `browser-first/image-preview-results.json`과 캡처, 기존 회귀 결과/캡처, `regression-results.json`, `final-*.log`. 잔여 테스트 스키마 **0개** 확인 후 이번에 가동한 로컬 PostgreSQL만 종료했다. 이미지 서버/Vite/Chrome도 종료했다. 실제 외부 이미지·OpenAI·원격 Neon·Node.js 22 실행·배포는 이번 검증에서 수행하지 않았다.
- **전체 P0는 미완료. 다음 반복: E3/E4 입력 보존 완료 대조 — 직접 편집·초안·최종 통합의 저장 실패/충돌·이동 취소·새로고침 복구와 보관/삭제 정책을 현재 코드와 격리 DB/브라우저로 검증한다.** 새로 생긴 이미지 표시 선택이 복구 데이터나 게시 승인으로 저장되지 않는지도 확인한다. 누락이 발견되면 가장 작은 실패 단위를 수정하며, 기존 완료 기능을 근거 없이 다시 구현하지 않는다. W2 특정 Excel 오류는 확장자·오류 문구 또는 비식별 재현 파일 대기를 유지한다.

## 반복 47 — E3/E4 입력 보존 감사와 직접 편집의 늦은 응답 보호 (2026-09-22)

- 직전 목표 턴은 **진행**. 목표 원본·GOAL·README·설정/스크립트·git 상태·진행 기록과 적용 AGENTS 부재를 확인했다. 기존 사용자 변경을 보존한다. P0 입력 보존 체크박스는 전체 완료 대조 전까지 유지한다.
- 기준 검사: `bun run test` **115 통과·DB 21 생략**, check **오류 0·경고 0**, lint/build **통과**. 소유 localhost PostgreSQL에서 `edit-preservation-db.test.js`, `draft-preservation-db.test.js`, `merge-application-db.test.js` **148 통과·생략 0**. 기존 브라우저 `edit-recovery.mjs` **14개**, `draft-recovery.mjs` **27개 묶음 통과**, 페이지 오류/외부 HTTP 0. AI 응답 모의와 실제 외부 호출을 구분한다.
- 완료 대조에서 직접 편집은 초안과 달리 이전 방문의 비동기 응답을 막지 않는 것을 발견했다. 실제 서버 저장 응답만 지연한 신규 Chrome 검사에서 다른 편집 화면으로 이동해도 입력란이 비활성 상태인 오류를 재현했다. 기존 검사에는 이 경계가 없었다. 초안 구현을 다시 만들지 않고 직접 편집의 방문 구분·화면 종료·저장 상태 초기화만 수정한다.
- 이번 작은 작업 E3a의 완료 기준: ① 이전 저장 대기 중 다른 문서 작성/저장 ② 같은 문서로 돌아온 새 입력 ③ 실제 DB 중간 실패의 늦은 응답 ④ 편집기 종료 뒤 이전 리다이렉트 ⑤ 늦은 휴지통 응답에서 현재 입력/버전·복구본/저장 상태 보존. 사용자가 이미 요청한 원래 서버 작업은 처리되며 응답을 무시한다고 취소되었다고 표시하지 않는다.
- 새 검사 첫 시도는 요청 패턴의 `*`가 `?/save`의 슬래시를 포함하지 않아 지연을 걸지 못한 검사 코드 오류였다. 해당 프로세스와 소유 테스트 스키마를 정리하고 URL 경로/액션으로 정확히 매칭하도록 수정해 위 제품 오류를 재현했다. 대기에 제한 시간도 추가했다. 실패를 우회하거나 저장 검증을 모의 응답으로 바꾸지 않는다.
- 증거: `/tmp/wiki-viki-recovery-audit-20260922/`의 기준 검사/DB/기존 브라우저 결과, `late-before-reproduced.log`. 새로운 이미지 표시 선택의 복구 제외까지 포함한 E3/E4 전체 완료 대조는 이번 작은 수정 이후 계속한다. 원격 DB·운영 자료를 사용하지 않는다.

- 구현: 직접 편집 `+page.svelte`에서 방문 번호와 화면 생존 여부를 기록한다. 문서가 바뀌면 이전 저장/삭제/통신 오류 상태를 초기화하고, 오래된 성공·실패 응답의 복구본 정리·폼 갱신·리다이렉트·저장 해제를 생략한다. 같은 주소로 돌아온 새 방문도 구분한다. 서버 액션·DB 모델·보관 기간·버전 검사는 변경하지 않았다.
- 신규 `edit-late-response.mjs` 첫 수정 후 **5개 상황 통과**. 이후 불필요한 모의 AI/헬퍼를 제거하고 브라우저 외부 요청 차단·실제 응답 전달·기존 리비전 보존 검사를 명시적으로 추가한 최종 검사도 **5개 모두 통과**했다. 실제 DB trigger의 중간 실패는 원래 내용/리비전을 유지하며, 승인한 저장·휴지통 작업은 정확히 반영된다. 화면 오류/서버 및 브라우저 외부 HTTP **0**. 현재 문서 저장 중 화면과 이전 DB 실패 후 새 입력 화면 캡처를 직접 확인했다.
- README에 지연 응답 회귀 실행법과 화면 이동이 이미 요청한 서버 작업을 취소하지 않는다는 동작을 기록했다. 기존 직접 편집 복구 브라우저와 최종 자동 검사를 진행한다.

- 최종 기존 직접 편집 브라우저 **14개 묶음 통과**: 360/768/1440px × 라이트/다크, 새로고침·이동 취소·명시적 복구, 빈/저장 대기/DB 오류/충돌/저장소 실패, 보호 입력·다른 탭·기본 HTML을 현재 코드로 확인했다. 페이지 오류/예기치 않은 외부 HTTP 0. 모바일 라이트 복구·다크 저장 오류 캡처를 직접 확인했다. 기존 초안 27개 묶음의 이번 기준 검사도 통과했으며 초안 제품 코드는 변경하지 않았다.
- 최종 자동 검사: 실제 격리 DB 보존/통합 **148 통과·생략 0**, 기본 `bun run test` **115 통과·DB 21 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`와 `bun run build` **통과**. 생략된 전체 DB 검사는 통과로 계산하지 않는다. 시작 시 기존 검사 실패는 없었으며 새 지연 응답 경계의 제품 실패와 검사 경로 오류를 구분해 재현·수정했다. 이번 변경의 미해결 회귀는 없다.
- 주요 파일: `src/routes/edit/[slug]/+page.svelte`, 신규 `tests/browser/edit-late-response.mjs`, README/진행 기록. 제품 변화는 이전 방문의 프런트 응답 처리에 한정하며 기존 URL·서버 트랜잭션·문서·별칭·리비전·초안과 사용자 변경을 보존했다. GOAL 본문/체크박스는 변경하지 않았다. **E3a 개별 작업 완료, E3/E4 전체 완료 대조 및 P0 미완료.**
- 최종 증거: 위 경로의 `late-verified/late-edit-results.json`과 캡처, `edit-recovery-verified/browser-results.json`과 캡처, `browser-verified-results.json`, `final-results.json`·`final-{db,test,check,lint,build}.log`. 잔여 테스트 스키마 **0개** 확인 후 이번에 가동한 소유 로컬 PostgreSQL을 종료했다. Vite/Chrome도 종료했다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 수행하지 않았다.
- **다음 반복: E3/E4 입력 보존 완료 대조의 남은 이미지 선택 복구 제외를 실제 브라우저로 확인한다.** 일반 초안/최종 통합에서 이미지 표시와 임시 보관을 선택한 뒤 새로고침·명시적 복구를 실행해, 작성값·원래 검토 버전은 유지하고 이미지 표시/게시·통합 승인은 다시 선택해야 하며 자동 이미지 요청이 없음을 검증한다. 이미 통과한 복구 기능을 근거 없이 재구현하지 않고 누락된 가장 작은 조건만 처리한다. 전체 조건이 입증될 때만 GOAL 입력 보존 체크박스를 갱신한다. W2 특정 Excel 오류는 원래 확장자·오류 문구 또는 비식별 재현 파일 대기다.

## 반복 48 — E3/E4 입력 복구의 이미지 선택·승인 제외 완료 대조 (2026-09-22)

- 직전 목표 턴은 **진행**. 직접 편집의 방문별 응답 보호 코드와 실제 회귀 실행 결과를 확인했다. 목표 원본·GOAL·README·진행 기록·실제 설정/스크립트·git 상태와 적용 AGENTS 부재를 재확인했다. 기존 사용자 변경을 보존한다.
- 기준 검사: `bun run test` **115 통과·DB 21 생략**, check **오류 0·경고 0**, lint/build **통과**. 현재 복구 필드 목록·명시적 복구 함수·초안 확인 초기화·미리보기 이미지 선택의 범위를 읽어, 이미지 표시 선택이 저장 필드에 없고 새 화면에서 기본 꺼짐임을 확인했다. 코드만으로 전체 완료 처리하지 않는다.
- 이번 작은 작업은 누락된 실제 복구 회귀를 추가하는 것이다. 일반 초안/최종 통합의 현재 입력·원래 버전/통합안/대상 보존, 이미지 선택·삭제/통합/편집 폐기 승인 제외, 새로고침과 복구 시 자동 이미지 요청 없음, DB 불변을 실제 Chrome·격리 PostgreSQL·localhost 합성 이미지로 검증한다. 기존 구현이 기준을 만족하면 근거 없이 제품 동작을 다시 바꾸지 않는다.
- 증거 경로: `/tmp/wiki-viki-recovery-consent-20260922/`. 관련 입력 보존/통합 DB 및 기존 직접 편집/초안 복구도 현재 코드로 완료 대조한다. 전체 조건이 입증될 때만 P0 입력 보존 체크박스를 갱신한다. 원격 DB·실제 외부 AI는 사용하지 않는다.

- 신규 이미지 복구 첫 실행에서 일반/최종 입력·원래 버전·이미지 선택/확인 초기화는 통과했으나, 최종 통합 버튼이 확인 미선택 시 비활성이라는 검사 가정이 실패했다. 실제 `MergeReview`는 입력 충돌/저장/오래된 통합안일 때만 버튼을 막고, 확인이 없으면 서버 액션이 400으로 거부해 안내한다. 제품 실패로 오인해 UI 정책을 바꾸지 않고, 새 검사를 실제 무승인 요청의 400·입력/DB 보존까지 검증하도록 수정했다. 기존 이미지 게시/통합 검사는 유지한다.

- `tests/browser/preview-images.mjs`에 360/768/1440px 및 200% 대응 720px·픽셀 배율 2 × 두 테마의 복구 8개 묶음을 추가했다. 일반/최종 본문·제목·별칭·두 검토자를 바꾸고 이미지 표시·삭제/편집 폐기 확인을 선택한 뒤, 새로고침·키보드 복구로 모든 작성값과 원래 초안/통합안/대상 버전을 확인한다. 일반 입력을 저장본과 같게 한 후 실제 통합 확인을 선택·새로고침해 다시 확인해야 함도 검증한다. 무확인 통합 요청은 실제 400이고 입력·DB는 유지된다.
- 최종 이미지 검사 **20개 묶음 통과**(신규 복구 8 + 기존 미리보기 12). 복구 전후 이미지 선택 기본 꺼짐·자동 요청 0, 보관값에 이미지/승인 없음, 초안/문서/리비전/별칭 스냅샷 불변을 확인했다. 페이지 오류/예기치 않은 외부 HTTP/서버 외부 호출 0. 통제한 localhost 이미지 GET **52회**는 사용자가 표시를 선택하거나 게시 결과를 조회한 요청이며 실제 외부 서비스 호출과 구분한다. 모바일 라이트/태블릿 다크의 복구 미리보기 캡처를 직접 확인했다.
- `tests/draft-recovery.test.js`의 기존 허용 필드 검사에도 이미지 표시/권한을 추가했고, README에 복구 후 이미지 표시 동작과 검증 범위를 기록했다. 제품 구현은 이미 해당 조건을 충족하여 변경하지 않았다. 관련 기존 DB/브라우저 전체와 최종 자동 검사는 계속 진행한다.

- 현재 코드 기존 회귀: `edit-recovery.mjs` **14개**, `draft-recovery.mjs` **27개**, `edit-late-response.mjs` **5개 묶음 통과**, 각 페이지 오류/예기치 않은 외부 HTTP 0. 격리 DB의 `edit-preservation-db.test.js`·`draft-preservation-db.test.js`·`merge-application-db.test.js` **148 통과·생략 0**. 독립 DB 연결과 실제 중간 실패를 사용하며 AI 응답만 모의 처리한다.
- P0 입력 보존의 완료 대조: ① 저장 실패/충돌에서 입력·원래 버전 반환은 DB 148개와 두 복구 브라우저 ② 이동 취소·새로고침·명시적 복구와 늦은 응답 격리는 14/27/5개 브라우저 ③ 기본 보관 꺼짐·24시간·개별 삭제·탭/문서 분리·보호 입력 제외·저장소 오류는 복구 단위/브라우저 ④ 일반/최종 작성값·통합안/대상 원래 버전 보존과 승인/이미지 제외는 초안 27개 및 이미지 복구 8개 ⑤ 세 화면 크기·두 테마·키보드/기본 HTML은 기존 복구, 200% 대응은 새 이미지 복구로 확인했다. 민감 입력은 인증 없는 서버에 자동 보관하지 않는 명시된 정책과 화면/이동 취소로 보존한다. 사용자가 경고를 승인하고 떠난 보호 입력이나 브라우저 비정상 종료까지 복구한다고 주장하지 않는다.

- 최종 `bun run test` **115 통과·DB 21 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`/`bun run build` **통과**. 실제 DB 148개는 위 별도 실행 결과이며 전체 DB 생략분을 통과로 계산하지 않는다. 이번 신규 검사에서 발견한 버튼 상태 가정 오류는 실제 서버 거부 검증으로 바로잡았고, 제품 회귀나 미해결 검사 실패는 없다.
- 위 현재 코드·DB·브라우저·자동 검사 증거에 따라 **E3/E4 및 GOAL §12 P0 입력 보존 항목 완료**. GOAL 해당 체크박스 하나만 갱신하고 요구사항 본문·기존 사용자 변경을 보존했다. 이번 변경은 복구 회귀 코드 두 파일과 README/GOAL/진행 기록이며, 이미 기준을 만족하는 제품 구현·DB 모델을 중복 수정하지 않았다. **전체 P0는 미완료**다.
- 증거: 위 경로의 `baseline-*.log`, 가정 오류 `images.log`, 최종 `images-verified/image-preview-results.json`과 캡처, `db.log`, 각 복구 브라우저 결과/캡처와 `regression-results.json`, `final-results.json`·`final-*.log`. 모바일/태블릿 및 데스크톱 복구 화면을 직접 확인했다. 확대는 200% 대응 CSS 화면/픽셀 배율 검증이며 브라우저 도구 모음의 직접 조작 검증과 구분한다. 잔여 테스트 스키마 **0개** 확인 후 이번에 가동한 소유 PostgreSQL을 종료했고 Vite/Chrome/합성 이미지 서버도 종료했다.
- 남은 검증: P0의 직접 편집·별칭/동시 저장 보호, 게시/통합 및 휴지통 관계 복구, 외부 전송 차단/검사 생략의 전체 완료 대조. 이번에 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 하지 않았다. W2 특정 Excel 오류는 원래 확장자·오류 문구 또는 비식별 재현 파일 대기를 유지한다.
- **다음 반복: D3 — 직접 편집의 제목/별칭 충돌·별칭 제거·동시 저장과 기존 초안 중복 게시/통합 보호의 P0 완료 기준을 대조한다.** 관련 실제 DB 검사와 사용자 수정/충돌 화면을 현재 코드로 확인하고, 누락이 발견되면 가장 작은 실패 단위를 수정한다. 이미 완료한 보호를 근거 없이 다시 구현하지 않으며 전체 조건이 입증될 때만 해당 GOAL 체크박스를 갱신한다.

## 반복 49 — D3 기존 주소와 다른 제목·별칭의 이름 충돌 보호 (2026-09-22)

- 직전 목표 턴은 **진행**. 목표 원본·GOAL·README·설정/스크립트·git 상태·진행 기록과 적용 AGENTS 부재를 확인했다. P0 다음 미완료 항목은 직접 편집 및 게시/통합 충돌 보호의 완료 대조다. 기존 사용자 변경을 보존한다.
- 기준 자동 검사: test **115 통과·DB 21 생략**, check **오류 0·경고 0**, lint/build **통과**. 실제 저장 SQL은 주소 충돌과 제목끼리의 충돌을 검사하지만, 주소를 유지한 문서의 제목을 다른 문서의 별칭으로 추가하거나 기존 별칭의 표시 이름을 새 제목/별칭으로 입력하는 경우를 누락했다.
- 실제 격리 DB에 주소와 표시 이름이 다른 문서를 만들고 신규 회귀 3개에서 예상 409 대신 **303 저장 성공**을 재현했다. 기존 DB 검사는 통과하고 신규 실패 3개와 그 상위 묶음만 실패했다. 증거 `/tmp/wiki-viki-name-conflict-20260922/db-before.log`.
- 이번 작은 작업은 **기존 저장 표기와 정확히 같은 새 제목/별칭의 충돌**을 기존 트랜잭션 잠금 안에서 확인하는 것이다. 기존 문서의 같은 이름·주소와 이미 있던 중복 표기는 그대로 유지하며 새로 추가/변경하는 이름을 검사한다. 동일한 이름을 만드는 반대 순서의 게시/통합 경로도 최소 수정해 경합으로 우회하지 않게 한다. 전면적인 이름 정규화 정책 변경·데이터 정리는 포함하지 않는다. 원래 사용자 입력·기존 URL·관련 데이터와 충돌 후 수정·재시도를 검증한다.
- 새 단위 완료까지 D3 전체 체크박스는 유지한다. 이후 정확히 남은 대조 결과를 기록한다. 운영 자료 대신 소유 localhost 테스트 DB만 사용한다.

- 구현: `document-write.js`는 검토한 기존 제목/별칭에 없던 새 이름을 계산하고, 기존 잠금/스냅샷 검증 안에서 다른 문서의 제목 및 별칭 표기를 확인한다. 보존하는 이름에 새 제약을 소급하지 않으므로 기존 중복 표기/옛 별칭 URL의 일반 편집은 유지된다. `draft-publication.js`는 새 문서의 모든 이름을 같은 잠금 안에서 검사하고, `merge-application.js`는 다른 별칭의 실제 표기와 겹치는 새 별칭을 추가하지 않는다. 기존 제목/주소·원자성·충돌 메시지·입력 반환 경로를 재사용한다.
- 관련 DB 신규 회귀는 활성/휴지통 소유권, 생성/수정, 기존 중복 표기의 편집, 직접 저장과 새 게시의 동시 이름 선점, 게시의 세 충돌 방향, 통합의 기존 표기 소유권을 다룬다. 확대 재현의 휴지통 두 fixture는 삭제자를 함께 넣지 않아 실제 `documents_deletion_state` 제약에 실패한 검사 오류였다. 삭제 시각/삭제자 쌍을 맞춰 고쳤으며 제약조건은 유지했다.
- 수정 후 `edit-preservation-db.test.js`·`draft-publication-db.test.js`·`merge-application-db.test.js`·`draft-batch-db.test.js` **160 통과·생략 0**. 독립 PostgreSQL 연결의 실제 잠금 대기와 중간 실패/롤백·재시도·기존 데이터 스냅샷 보존을 확인했다. 공통 저장 경로 변경이므로 이력/삭제/자동 연결 등 기존 관련 DB 회귀도 확인한다.
- `edit-recovery.mjs`에 세 이름 충돌 방향의 409 안내·모든 입력/검토 버전·DB 불변 및 이름 수정 후 한 번 저장·기존 URL/별칭/이력 보존 검사를 추가했다. 360/768/1440px × 두 테마 및 JavaScript 없는 기본 폼을 포함하며 기존 복구 검사를 유지한다. README에 실제 표시 이름 충돌과 기존 중복 표기 보존 정책을 기록했다.

- 새 브라우저 검사 첫 실행은 일곱 화면 모두 충돌·수정·저장을 거친 뒤, JavaScript 없는 폼의 저장 본문 줄바꿈 비교에서 실패했다. 기본 HTML 폼은 CRLF로 제출하고 서버가 이를 그대로 보존하지만 검사는 LF를 기대했다. JavaScript 경로의 LF와 기본 폼의 실제 CRLF를 각각 정확히 비교하도록 수정했다. 입력/원문을 바꾸어 검사를 맞추지 않는다.

- 공통 저장 경로의 영향을 확인하려고 격리 DB를 활성화한 전체 `bun run test`를 실행해 **556 통과·생략 0**을 확인했다. 이 수는 기본 단위 검사와 DB 통합 검사를 포함하며 앞선 160개와 합산하지 않는다. 기존 이력 되돌리기·휴지통·별칭/자동 연결·시드·마이그레이션의 실제 DB 회귀도 통과했다. 신규 충돌 화면의 모바일 라이트/데스크톱 다크 캡처를 직접 확인했다. 현재 기존 직접 복구·선택 게시·최종 통합 미리보기 브라우저를 순차 검증 중이다.

- 최종 브라우저: 직접 편집/복구 **21개 묶음**(기존 14 + 새 이름 충돌 7), 선택 게시 **7개**, 초안/최종 통합 미리보기 **9개 묶음 통과**. 직접 편집의 오류·키보드 수정·JavaScript 없는 기본 폼, 기존 복구/충돌/빈/지연/실패 흐름과 실제 선택 게시·통합 결과를 현재 코드로 확인했다. 각 페이지 오류·예기치 않은 외부 HTTP 0. 세 화면 크기·두 테마, 기존 통합 미리보기의 200% 대응과 초점 보존도 유지된다.
- 남은 완료 대조에서 초기화 코드 `scripts/seed.js` 및 `scripts/seed-demo.js`가 여전히 별칭 주소만 확인하며, `document-trash.js`의 복구 충돌 역시 같은 종류의 표시 이름 검사를 포함하지 않는 것을 확인했다. 새 이름 충돌 규칙을 우회해 생성하거나 예전 중복을 복원할 수 있는지 다음 작은 단위에서 실제로 재현해야 한다. 따라서 이번 직접 편집·게시·통합 수정만으로 D3 전체/GOAL 체크박스를 완료 처리하지 않는다.

- 마지막 호환성 대조에서 새 검사로 인한 공백 경계 회귀를 재현했다. 기존 별칭 `원래 이름`은 원래 저장 경로가 양끝 공백을 정리하면서 같은 URL/행을 유지하는데, 새 이름 비교가 이를 신규 이름으로 판단해 이미 있던 중복 표기 때문에 409를 냈다. 별칭 준비 함수와 동일하게 기존 검토 이름의 양끝 공백을 정리해 비교하도록 보완했다. 새 DB 회귀는 기존 URL/행 ID와 다른 소유자의 행을 그대로 유지하고 기존 저장 규칙대로 표기 공백만 정리되는지 확인한다. 이전 556개 통과 이후의 변경이므로 최종 전체 DB/브라우저·자동 검사를 다시 실행한다.

- 공통 저장 함수의 이력 경로도 확인해, 양끝 공백이 있는 기존 제목을 그대로 되돌릴 때 같은 과거 별칭 표기 때문에 막히는 경계도 재현했다. 기존 이름과 제출 이름 모두 같은 양끝 공백 기준으로 비교하되 실제 저장 제목/URL은 바꾸지 않도록 수정했다. 신규 되돌리기 DB 회귀에서 원래 제목의 공백·주소·별칭/상대 문서·기존 이력을 그대로 유지하고 되돌림 이력만 한 개 추가되는지 확인한다. 최종 코드의 전체 DB/자동 검사를 다시 실행하며, 브라우저 검사는 마지막 비교식 보완 전 공백이 없는 표준 입력에 대해 21개 모두 통과한 결과와 구분한다.

- 최종 코드 검증: 격리 DB를 활성화한 전체 `bun run test` **558 통과·생략 0**, 기본 `bun run test` **115 통과·DB 21 생략**, check **오류 0·경고 0**, lint/build **통과**. 기존 기본 검사 실패는 없었고 실제 이름 충돌 누락을 재현해 수정했다. 추가 중 발견한 두 공백 호환성 회귀도 실패 재현 후 해결했으며 최종 검사에 미해결 실패는 없다. 전체 검사 수에는 단위/DB 검사가 함께 포함되고 반복/부분 검사 결과를 합산하지 않는다.
- 브라우저 검증은 직접 편집 21·선택 게시 7·통합 미리보기 9개 묶음이며, 기존 UI·표준 입력의 마지막 비교식 보완 전 실행이라는 구분은 위에 기록했다. 마지막 변경은 양끝 공백이 있는 기존 이름의 신규 여부 판단만 보완했고 UI는 변경하지 않았다. 그 경계는 최종 실제 DB 저장·되돌리기 회귀에서 직접 검증했다. 최종 정상/충돌/입력 보존·원래 주소 저장 화면을 확인했으며 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 수행하지 않았다.
- 주요 파일: `src/lib/server/document-write.js`, `draft-publication.js`, `merge-application.js`, 직접 편집/게시/통합 DB 회귀 세 파일, `tests/browser/edit-recovery.mjs`, README/진행 기록. 기존 라우트/오류 UI·DB 스키마·사용자 자료를 유지했다. **D3c 개별 작업 완료, D3 전체 및 P0 미완료.** GOAL 본문/체크박스는 변경하지 않았다.
- 증거는 위 경로의 재현 로그 `db-before.log`, `db-expanded-before.log`, `whitespace-before.log`, `rollback-name-before.log`, 최종 `complete-results.json`·`complete-*.log`, `final-regression-results.json`·`final-compatibility-results.json`과 각 브라우저 JSON/캡처다. 두 fixture 오류(삭제자 누락·기본 폼 줄바꿈)는 제품 오류와 구분했다. 잔여 테스트 스키마 **0개** 확인 후 소유 PostgreSQL을 종료했고 모든 테스트 Vite/Chrome도 종료했다.
- **다음 반복: D3d — 기본/데모 시드의 기존 제목·별칭 표기 충돌을 실제 CLI/격리 DB에서 재현하고 보호한다.** `scripts/seed.js`의 별칭 주소 검사와 `scripts/seed-demo.js`의 기존 문서/별칭 검사에서 주소와 다른 표시 이름을 놓치는지 확인한다. 이번 보호가 초기화로 우회되지 않게 하는 최소 후속 범위이며, 기존 자료/ID/이력을 유지하고 충돌한 예시만 건너뛰어야 한다. 복구 경로의 이름 충돌 의미와 기존 중복 데이터 보존도 후속 완료 대조에 남긴다. W2 특정 Excel 오류는 원래 확장자·오류 문구 또는 비식별 재현 파일 대기를 유지한다.

## 반복 50 — D3d 기본/데모 초기화의 표시 이름 충돌 보호 (2026-09-22)

- 목표 원본·GOAL·README·설정/스크립트·git 상태·진행 기록과 적용 AGENTS 부재를 확인했다. 직전의 D3c는 개별 완료이며 P0 직접 편집 보호의 완료 대조를 이어간다. 기존 사용자 변경과 GOAL 본문/체크박스를 유지한다.
- 기준 검사: `bun run test` **115 통과·DB 21 생략**, check **오류 0·경고 0**, lint/build **통과**. 증거 `/tmp/wiki-viki-seed-names-20260922/baseline-*.log` 및 `baseline-results.json`.
- 실제 코드에서 기본 시드의 `redirects.alias_title`, 데모 시드의 기존 제목 대 새 별칭 및 기존 별칭 표기 대 새 제목/별칭 충돌 검사가 빠져 있다. URL이 표시 이름에서 만들어지지 않은 기존 데이터에 새 이름을 중복 생성할 수 있으므로 같은 트랜잭션/잠금 안에서 최소 보완한다. 활성·휴지통, 재실행 및 진행 중 이름 선점은 소유 localhost PostgreSQL과 실제 CLI로 확인한다. 기존 중복 이름 정리·전체 정규화·복구 정책 변경은 포함하지 않는다.
- 수정 전 새 실패 조건 **11개**(기본 활성/휴지통 별칭 표기 2 + 동시 별칭 표기 1, 데모 세 충돌 방향 × 활성/휴지통 6 + 동시 제목/별칭 표기 2)를 재현했다. 기존 세부 검사는 통과했다. 테스트 출력의 실패 13은 실패한 상위 묶음 2개를 포함한 수이며 별도 제품 실패가 아니다. 기본/데모 모두 예상 생성 4·건너뜀 1 대신 생성 5·건너뜀 0을 반환하거나 같은 이름의 추가 행을 저장했다. 증거 `db-before.log`.
- `scripts/seed.js`는 기존 별칭의 실제 표기를 새 기본 제목과 비교한다. `scripts/seed-demo.js`는 새 제목과 모든 별칭의 이름을 기존 문서 제목/별칭 표기 모두와 비교한다. 기존 테이블 잠금 안에서 실행하며 활성/휴지통 구분 없이 이름을 보존한다. URL 비교·원자성·기존 데이터·건너뜀/생성 출력은 그대로 유지한다. 스키마/데이터 마이그레이션은 필요하지 않다.
- 두 보존 DB 검사에 실제 Bun CLI의 옛 URL/같은 이름·휴지통·반복 실행 및 독립 backend의 실제 잠금 대기 회귀를 추가했다. 기본 CLI는 빈 임시 `.env`·빈 원본 폴더·명시적 로컬 연결·외부 HTTP 차단으로 실행한다. 기존 데모의 전체 예시 생략·중간 trigger 실패/연결 종료 롤백·재시도·제거한 별칭 보존 검사는 유지했다. README에 이름 충돌 및 검증 범위를 명시했다.
- 수정 후 `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<소유 로컬 테스트 DB> node --test --test-concurrency=1 tests/seed-preservation-db.test.js tests/demo-seed-preservation-db.test.js tests/seed-governance-db.test.js` **64 통과·생략 0** (`db-after.log`). 이 수는 세 검사 묶음과 하위 검사를 포함한다. 실제 DB 제약/잠금/커밋/롤백을 확인했으며 모의 SQL 결과가 아니다. 신규 회귀는 모두 통과했고 검사 작성 오류는 없었다.
- 최종 자동 검사: `bun run test` **115 통과·DB 21 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`·`bun run build` 및 `git diff --check` **통과** (`final-results.json`, `final-*.log`). 별도로 활성화한 관련 실제 DB 검사 64개 결과와 구분하며 중복 합산하지 않는다. 변경 전 기본 검사 실패는 없었고 신규 11개 회귀가 드러낸 기존 누락을 해결했다. 이번 변경으로 남은 실패는 없다.
- CLI/SQL 동작만 수정했으므로 브라우저 시각 검증은 이번 단위에 해당하지 않으며 새 UI 검증을 수행했다고 간주하지 않는다. 실제 외부 AI·원격 Neon·Node.js 22 런타임·운영 배포 검증은 수행하지 않았다. 테스트 스키마 잔여 **0개** 확인 후 소유 PostgreSQL을 종료했다. 증거/수정 전 사본은 `/tmp/wiki-viki-seed-names-20260922/`에 보존한다. 운영 자료·외부 서비스·사용자 `.env`는 사용하지 않았다.
- 주요 변경 파일은 `scripts/seed.js`, `scripts/seed-demo.js`, 두 `*-seed-preservation-db.test.js` 보존 검사(기본 검사는 `tests/seed-preservation-db.test.js`), README와 이 진행 기록이다. **D3d만 완료, D3 전체 및 P0 미완료.** GOAL 본문/체크박스는 수정 전 사본과 동일하다.
- **다음 반복: 복구 경로의 기존 이름·관계 보존과 충돌 규칙을 대조한다.** `document-trash.js`와 실제 복구 회귀를 확인해, 휴지통에서 이미 예약한 제목/별칭을 복구할 때 옛 중복 표시 이름·기존 URL/관계를 보존하는지 검증한다. 기존 자료에 신규 이름 규칙을 소급 적용해 복구를 막지 않으며, 별도 문서의 URL/관계를 덮어쓰는 실제 누락이 있다면 가장 작은 단위로 수정한다. 검증을 마친 범위에 한해 D3/D4의 P0 체크 조건을 판단한다. W2 특정 Excel 오류의 비식별 재현 자료 대기는 그대로 남긴다.

## 반복 51 — 휴지통 복구의 기존 이름·실제 주소 보존 (2026-09-22)

- 직전 목표 턴은 **진행**으로 분류한다. 실제 기본/데모 시드 충돌 누락을 수정·검증했고 이번 복구 대조로 이어진다. 목표 원본·GOAL·README·설정/스크립트·git 상태·적용 AGENTS 부재와 실제 복구 SQL을 확인했다. 시작 자동 검사는 test **115 통과·DB 21 생략**, check **오류 0·경고 0**, lint/build **통과** (`/tmp/wiki-viki-restore-audit-20260922/baseline-*.log`).
- 복구는 이름/별칭 행을 추가하지 않고 보관 상태만 변경하므로 이미 존재한 표시 이름 중복을 새로운 이름 선점으로 간주하지 않는다. 반면 복구 SQL이 `slugify(document.title)`까지 경로 집합에 포함하는 것을 발견했다. 옛 문서의 실제 주소가 제목과 다를 때 다른 문서가 소유한 미사용 제목 URL 때문에 복구를 거부할 수 있다. 기존 이름의 세 중복 방향과 활성/휴지통 소유권, 미사용 제목 URL, 실제 대표/별칭 주소 충돌을 실제 DB 회귀로 대조한다. 새 이름 정규화/기존 데이터 정리는 하지 않는다.
- 신규 검사 첫 실행에서 읽기 함수 반환값을 문서 행으로 잘못 가정한 검사 오류가 있었다. 실제 `getDocument`는 `{ document, redirectedFrom }`을 반환하므로 ID 접근을 `.document.id`로 수정했다. 제품 코드는 바꾸지 않은 채 다시 실행해 기존 중복 이름 여섯 경우와 실제 주소 충돌을 통과하고, **미사용 제목 URL 네 경우만 409 대신 303 기대에 실패**함을 확인했다. `db-before.log`(검사 오류 포함)와 `db-reproduced.log`(실제 누락 4개 + 상위 실패 1개)를 구분한다.
- `src/lib/server/document-trash.js`에서 복구 충돌의 경로 집합을 저장된 대표 주소와 별칭 주소로 제한했다. 제목에서 새 URL을 만들지 않는 상태 전환에 불필요한 `slugify(title)` 선점 검사를 제거했다. 제목 고유성·실제 교차 주소 충돌·검토 스냅샷·생명주기 버전·트랜잭션 잠금은 유지한다. 새 이름 등록 정책과 기존 URL/표기/본문/메타데이터/관계를 변경하지 않는다.
- 실제 DB에서 `document-trash-db.test.js`, `edit-preservation-db.test.js`, `history-preservation-db.test.js`, `automatic-links-db.test.js`를 순차 실행해 **109 통과·생략 0**을 확인했다 (`db-after.log`). 새로운 복구 회귀와 기존 원자성·독립 backend의 동시 요청·실패/롤백·정확한 마이크로초 시각·별칭 ID·리비전/토론/게시 초안 관계·일반 조회 제외 및 복구 후 연결을 포함한다.
- `tests/browser/trash-recovery.mjs`를 추가해 실제 Chrome/SvelteKit/격리 DB로 **12개 묶음 통과**했다. 360/768/1440px × 라이트/다크, 720 CSS px·DPR 2 × 두 테마, 768px JavaScript 없는 폼의 9개 왕복과 실제 주소 충돌·오래된 검토·UPDATE trigger 오류의 3개 재시도를 확인했다. Space/Enter 삭제·복구, 원래 대표/별칭/다른 문서 URL, 모든 관계의 DB 전후 비교, 로딩 버튼 제한·빈 휴지통·입력된 검토 버전 보호를 확인했으며 페이지 오류/예기치 않은 외부 HTTP **0**이었다. 모바일 라이트 보관 화면, 데스크톱 다크 복구 문서, 모바일 다크 충돌 캡처를 직접 확인했다. 결과 `browser/results.json`·PNG 및 `browser.log`.
- README에 복구 시 실제 주소의 충돌과 기존 표시 이름 보존을 구분하고 새 브라우저 회귀 실행법을 기록했다. 제품 UI/스키마/마이그레이션은 변경하지 않았다.
- 최종 자동 검사: `bun run test` **115 통과·DB 21 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`·`bun run build`·`git diff --check` **통과** (`final-results.json`, `final-*.log`). 별도 실제 DB 109개 및 브라우저 12개 묶음 결과와 구분한다. 시작 검사 실패는 없었으며 기존 복구 거부 문제를 재현해 수정했다. 반환값 구조를 잘못 가정한 검사 오류도 바로잡았고 최종 미해결 회귀는 없다.
- 테스트 스키마 잔여 **0개** 확인 후 소유 PostgreSQL을 종료했다. 테스트 Vite/Chrome은 정상 종료했다. 실제 외부 AI·원격 Neon·Node.js 22 런타임·배포는 검증하지 않았고 운영 데이터와 `.env`는 사용하지 않았다. 증거/수정 전 사본은 `/tmp/wiki-viki-restore-audit-20260922/`에 있다. 주요 파일은 `document-trash.js`, `document-trash-db.test.js`, 새 `tests/browser/trash-recovery.mjs`, README/진행 기록이다.
- **D4c 개별 작업 완료, D3/D4 전체 및 P0는 미완료.** 복구의 실제 이름/주소 보존은 이번 증거로 확인했지만 상위 완료 기준은 다른 생성 경로까지 대조한다. GOAL 본문/체크박스와 복구 액션/화면 코드는 수정 전 사본과 동일하다.
- **다음 반복: D3e — `scripts/seed-prototype.js`의 기존 이름/주소 충돌과 동시 적재를 실제 CLI/격리 DB에서 재현하고 보호한다.** GOAL §8은 일곱 예시의 제목·슬러그·별칭 충돌 확인과 기존 문서/초안 보존을 명시한다. 실제 코드는 새 대표 제목/주소만 먼저 확인하고, 별칭 INSERT는 `documents.slug`만 확인하며, 일반 편집/게시와 공유하지 않는 시드 전용 advisory lock만 사용한다. 기존 표시 이름·휴지통 예약 또는 진행 중 별칭 등록을 우회할 가능성을 다음 작은 단위에서 확인한다. 현재 코드의 증거에 따른 남은 완료 대조이며 기존 초기화 자료/사용자 편집·초안을 유지해야 한다. W2 특정 Excel 재현 자료 대기는 유지한다.

## 반복 52 — 프로토타입 예시 적재의 충돌·동시 실행 및 기존 초안 보존 (2026-09-22)

- 직전 턴은 **진행**: 복구 거부를 수정·검증했고 예시 초기화 경로의 실제 누락 근거를 확보했다. 이번에도 목표 원본·GOAL·README·설정·실제 적재 스크립트/일곱 예시·진행 기록·git 상태·적용 AGENTS 부재를 확인했다. 기존 사용자 변경을 보존한다.
- 시작 검사 test **115 통과·DB 21 생략**, check **오류 0·경고 0**, lint/build **통과**. 증거 `/tmp/wiki-viki-prototype-preservation-20260922/baseline-*.log`. GOAL §8은 반복 적재 시 사용자 문서·초안 및 별칭을 보존하도록 요구한다.
- 작은 작업은 일곱 예시 적재를 사용자 이름/주소·초안과 충돌 없이 원자적으로 처리하는 것이다. 현재 대표 제목/주소만 먼저 검사하고 별칭 충돌을 일부 생략하며, 전용 advisory lock은 일반 편집·게시·초안 작성과 공유하지 않는다. 초안의 제목/주소가 바뀌면 기존 `governance.seed.referenceDraft/source`가 남아 있어도 재생성할 수 있다. 실제 Bun CLI/격리 DB의 신규 회귀로 충돌·진행 중 쓰기·기존 초안 변경·중간 실패를 대조한다. 기존 예시의 본문/출처/별칭과 사용자 자료는 유지한다.
- 수정 전 실제 CLI 검사 **40개 중 11 통과·29 실패**를 재현했다 (`db-before.log`). 상위 묶음 1개를 제외한 28개 실패는 기존 문서/초안 이름 충돌·변경한 참조 초안 재생성 13개, INSERT 행 생략/사후 제거에도 성공 종료 5개, DB/드라이버 세부 오류를 출력하는 실패 경계 7개, 진행 중 쓰기와 잠금 대기를 공유하지 않는 경우 3개다. 새 테스트 fixture 오류는 없었다. 기존 새 적재·일반 제목/주소 충돌·두 시드 간 직렬화·Node 실행은 통과했다.
- `seed-prototype.js`에 문서·별칭·초안 공통 테이블 잠금과 10초 대기 제한을 추가했다. 기존 advisory lock은 이전 실행기와의 시드 직렬화를 위해 유지한다. 잠금 획득 후 새 SQL 문장에서 새 제목/별칭의 이름·주소를 기존 활성/휴지통 문서 및 초안과 확인해 충돌 항목 전체를 건너뛴다. 기존 참조 초안은 이미 저장한 `governance.seed.referenceDraft/source`로 인식하므로 제목/주소 변경·게시 상태에서도 재생성하지 않는다. 기존 JSON 메타데이터와 스키마를 변경하지 않는다.
- 별칭을 일부만 만들고 성공시키던 분기를 제거하고 문서·첫 리비전·별칭·초안 INSERT 결과 및 마지막 실제 행 수를 확인한다. trigger가 행을 생략/사후 삭제하면 전체 트랜잭션이 취소된다. 결과 JSON은 커밋 후 출력한다. 연결·잠금·SQL 실패 및 지연된 드라이버 오류의 CLI 경계를 기존 실행기 방식으로 보호해 내부 오류/연결 값을 출력하지 않고 종료 코드 1을 반환한다. 실제 저장 예시의 본문·출처·별칭·검사 생략 표기는 유지한다.
- 첫 수정 후 신규 검사 **40 통과** (`db-after.log`). 추가로 잠금 시간 초과·backend 강제 종료·설정 오류·진행 중 참조 초안 이름 변경 회귀를 보완했다. 최종 관련 `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<소유 localhost 테스트 연결> node --test --test-concurrency=1 tests/prototype-seed-preservation-db.test.js tests/seed-preservation-db.test.js tests/demo-seed-preservation-db.test.js` **93 통과·생략 0** (`db-final.log`). 이 수는 프로토타입 44개와 기존 기본/데모 회귀를 포함하며 앞선 결과와 합산하지 않는다. 독립 PostgreSQL backend의 실제 잠금 대기·커밋·제약·trigger·롤백·재시도를 사용했다. 임시 빈 `.env` 및 명시적 로컬 연결로 실제 Bun/Node 프로세스를 실행하고 외부 HTTP는 차단했다.
- README에 전체 항목 생략·기존 참조 초안 보존·원자성·실패 후 재실행과 실제 검증 명령/범위를 기록했다. 실행 데이터 원본 `seed-data/prototype-documents.json`과 UI는 변경하지 않았다.
- 최종 자동 검사 `bun run test` **115 통과·DB 22 생략**(새 프로토타입 DB 묶음 1개 추가), check **오류 0·경고 0**, lint/build 및 `git diff --check` **통과** (`final-results.json`, `final-*.log`). 관련 실제 DB/CLI 93개는 별도 실행해 생략 없이 통과했고 기본 테스트 수와 중복 합산하지 않는다. 최종 미해결 회귀는 없다.
- CLI/SQL만 바뀌었으므로 이번 단위에 브라우저 시각 검증은 해당하지 않는다. 실제 외부 AI·원격 Neon·Node.js 22 런타임·운영 배포는 검증하지 않았다. 잔여 테스트 스키마 **0개** 확인 후 소유 PostgreSQL을 종료했고 자식 CLI/연결/임시 파일은 정리했다. 로그와 수정 전 사본은 `/tmp/wiki-viki-prototype-preservation-20260922/`에 남긴다.
- 주요 파일: `scripts/seed-prototype.js`, 새 `tests/prototype-seed-preservation-db.test.js`, README/진행 기록. **D3e만 완료이며 P0 상위 완료 체크는 아직 보류한다.** GOAL 본문/체크박스와 원본 예시 JSON은 수정하지 않았다. 문서/별칭 쓰기 경로 검색 결과는 기본/데모/프로토타입 시드, 공통 직접 편집, 초안 게시, 통합, 휴지통 전환으로 한정됨을 확인했다.
- **다음 반복: 직접 편집/이력 되돌리기의 기존 제목과 실제 주소가 다른 경우를 최종 대조한다.** `document-write.js:71`은 복구에서 제거한 것과 같은 `slugify(title)` 경로를 기존 제목 그대로 저장할 때도 검사한다. 옛 실제 주소와 이름을 그대로 두고 본문만 편집하거나 되돌릴 때 미사용 제목 URL의 다른 소유자 때문에 막히는지 재현한다. 기존 데이터 보존과 새 제목/실제 별칭 주소 충돌 보호를 함께 유지하는 최소 수정 또는 검증을 완료하고 D3/D4 완료 기준을 대조한다. 이미 검증한 시드/복구를 다시 구현하지 않는다. W2 특정 Excel 재현 자료 대기는 유지한다.

## 반복 53 — 기존 제목의 미사용 URL 때문에 막히는 본문 저장·되돌리기 보존 (2026-09-22)

- 직전 목표 턴은 **진행**으로 분류한다. 프로토타입 적재의 실제 충돌·부분 저장·동시 쓰기를 수정·검증했고, 공통 직접 저장의 남은 경계를 코드에서 확인했다. 목표 원본·GOAL·README·설정/스크립트·git 상태·진행 기록·AGENTS 부재를 다시 확인한다. 기존 사용자 변경을 보존하고 이번 단위와 관련된 공통 저장 및 실제 DB/브라우저 회귀만 수정한다.
- `document-write.js`는 기존 제목을 유지하는 저장/되돌리기에도 `slugify(title)`을 경로 집합에 넣는다. 실제 URL을 만드는 작업인지 기존 이름을 보존하는 작업인지 구분하지 않아, 옛 문서의 미사용 제목 URL을 다른 문서가 쓰는 경우 보존 편집을 막을 수 있다. 본문 저장/이력 되돌리기 × 활성/휴지통 소유자 × 대표/별칭 주소의 8개 회귀를 추가한다. 새 제목과 실제 새 별칭 URL은 계속 충돌을 차단해야 한다.
- 시작 자동 검사: test **115 통과·DB 22 생략**, check **오류 0·경고 0**, lint/build **통과** (`/tmp/wiki-viki-legacy-title-routes-20260922/baseline-*.log`). 수정 전 실제 DB의 신규 8개 사례에서 예상 303 대신 **409 거부**를 확인했다. 원래 43개는 통과했고 실패 9는 새 8개와 상위 묶음 1개를 포함한다 (`db-before.log`).
- 공통 저장에서 제목의 양끝 공백을 제외한 값이 기존 제목과 같으면 실제 대표/별칭 주소만 검사하도록 수정했다. 제목을 새로 만들거나 바꿀 때는 계산 주소도 계속 검사한다. 스냅샷·제목/별칭 표기·실제 URL 충돌·원자성·별칭 제거·이력/버전 보호는 유지한다. 기존 제목/주소를 바꾸거나 중복 이름을 정리하지 않는다.
- 첫 시도는 제목이 기존 별칭 이름인 경우도 계산 주소 검사에서 제외했다. 이를 별도 회귀로 대조해 별칭을 새 제목으로 올릴 때 409 대신 303을 허용하는 새 회귀를 재현했다 (`rename-guard-before.log`). 제외 조건을 **현재 제목 유지**로 좁혀 해결했다. 기존 본문만 편집하는 경우와 실제 제목을 바꾸는 경우를 구분한다.
- 새 검사에서 FormData가 줄바꿈을 CRLF로 직렬화하는데 LF를 기대한 입력 비교 오류도 발견했다. 실제 제출값과 같은 CRLF로 fixture를 작성했고 제품의 입력 보존/정규화 동작은 바꾸지 않았다. `db-after.log`의 비교 실패는 초기 문제의 재발과 구분한다. 수정 후 직접 편집/이력/휴지통 실제 DB **111 통과** (`db-fixed.log`).
- 공통 저장의 모든 호출 경로와 P0 저장 보호 완료 기준을 대조하려고 격리 DB를 활성화한 전체 `bun run test`를 실행했다. **633 통과·생략 0** (`full-db.log`). 신규 9개 회귀, 직접 편집·별칭 제거·동시 저장, 이전 리비전 되돌리기, 초안 게시·통합/일괄 게시의 중복 및 중간 실패, 휴지통/관계 복구·마이그레이션 보존, 세 초기화 명령의 실제 CLI 검사가 포함된다. 기본 단위와 DB 검사를 포함한 전체 수이며 앞선 부분 검사와 합산하지 않는다.
- `tests/browser/edit-recovery.mjs`에 360/768/1440px × 두 테마와 JavaScript 없는 폼의 7개 왕복을 추가했다. 실제 주소가 제목과 다른 문서를 별칭 URL에서 본문 저장하고 이전 리비전으로 되돌리며, 기존 대표/별칭·다른 주소 소유자·모든 이전 이력·토론·게시 초안의 DB 전후 보존을 확인한다. 기존 21개 입력 복구/충돌 검사는 유지한다. 같은 최종 코드에서 직접 편집·휴지통·선택 게시·최종 통합 미리보기 브라우저를 순차 검증 중이다.
- 최종 브라우저 네 스크립트가 모두 종료 코드 0이다 (`browser-results.json`). 직접 편집/되돌리기/입력 복구 **28개 묶음**(기존 21 + 신규 7), 휴지통 **12개**, 선택 게시 **7개**, 초안/최종 통합 미리보기 **9개**를 현재 코드로 확인했다. 각 JSON에 페이지 오류 0이 기록됐고, 외부 AI 응답을 사용하는 경우는 모의 처리했다. 실제 외부 서비스 호출과 구분한다. 신규 모바일 라이트 저장 화면·데스크톱 다크 되돌림 화면을 직접 확인했다.
- P0 저장 보호 완료 대조: `document-write.js`·편집/이력 액션과 실제 DB 검사에서 제목/별칭의 이름·주소 충돌, 빈 목록/일부 별칭 제거, 옛 URL·ID·이력 보존, 스냅샷/리비전 검토 버전·동시 저장 단 한 번 반영, AI 검사 도중 변경·실패/재시도·입력 보존을 확인했다. 새 제목/실제 별칭 충돌은 계속 409이며 기존 제목을 유지한 본문 저장·되돌리기만 불필요한 URL 선점에서 제외된다. 실제 UI의 충돌 후 수정/재검토·키보드·기본 폼·현재 입력 보존은 28개 브라우저 묶음으로 연결된다.
- 기존 게시/통합 보호 유지 대조: `draft-publication.js`·`merge-application.js`·관련 실제 DB 회귀에서 문서·리비전·별칭·초안 상태의 원자성, trigger 실패/행 생략 롤백·재시도, 같은 초안의 중복/동시 게시·통합 단 한 번 반영, 검토 도중 초안/대상 변경 거부, 외부 문서/휴지통 별칭 보존을 확인했다. 최종 입력/상충 상세/출처/이전 이력은 생성→수정→적용→되돌리기 DB 사례 및 선택 게시 7·통합 미리보기 9개 브라우저 묶음에서 보존된다.
- P0 삭제/복구 완료 대조: `002_document_trash.sql`·마이그레이션 검사·`document-trash.js`·휴지통 액션의 실제 DB 검사에서 기존 행을 보존한 전환, 정확한 수정 시각·ID·리비전·별칭·토론·게시 초안 관계 유지, 일반 조회 제외, 기존/별칭 URL의 휴지통 안내, 실제 주소 충돌·오래된 검토·동시 삭제/복구·삭제/복구 왕복 버전·중간 실패를 확인했다. 실제 브라우저 12개 묶음은 삭제→보관→원래 URL 복구, 빈/로딩/오류 상태·기본 폼·키보드와 충돌/DB 오류 재시도를 확인한다. 세 초기화 명령이 보관 자료를 덮어쓰지 않는 기존 CLI 회귀도 전체 633개 검사에 포함돼 있다.
- 최종 자동 검사: 기본 `bun run test` **115 통과·DB 22 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`·`bun run build`·`git diff --check` **통과** (`final-results.json`, `final-*.log`). 실제 DB를 활성화한 전체 **633 통과·생략 0**과 구분한다. 신규 8개 기존 거부 문제, 첫 시도의 제목 변경 검사 누락 1개, CRLF fixture 비교 오류를 모두 해결했고 최종 미해결 회귀는 없다.
- 구현과 필수 검증이 완료되어 GOAL §12의 **직접 편집 충돌/별칭 제거/동시 저장 및 기존 게시·통합 보호**, **원자성 유지와 휴지통/관계 복구** 두 체크박스만 체크했다. 위 완료 대조를 근거로 하며 다른 요구 본문·범위·기준을 낮추지 않았다. **D3f 및 D3/D4 상위 두 기준 완료, P0 전체는 미완료**다.
- 주요 파일: `src/lib/server/document-write.js`, 직접 편집 DB 검사와 `tests/browser/edit-recovery.mjs`, README/GOAL/진행 기록. DB 스키마·기존 URL/자료·UI 코드는 유지했다. 테스트 스키마 잔여 **0개** 확인 후 소유 PostgreSQL을 종료했고 테스트 Vite/Chrome도 정상 종료했다. 실제 외부 AI·원격 Neon·Node.js 22 실행·배포는 이번에 수행하지 않았다. 증거/수정 전 사본은 `/tmp/wiki-viki-legacy-title-routes-20260922/`에 있다.
- **다음 반복: P0의 마지막 미완료 기준인 차단 원문의 외부 AI 전송 방지와 검사 생략 표시를 완료 대조한다.** 업로드/초안/직접 편집/이력/토론/검색 설명/시드의 실제 검사 진입점과 전송 경계, 키 없는 대체 흐름·검사 실패/생략 UI를 현재 코드·테스트·브라우저 근거로 확인한다. 누락이 있으면 작은 재현 단위를 먼저 수정한다. 이미 완료한 저장/휴지통을 근거 없이 재구현하지 않으며 P1로 확장하지 않는다. W2 특정 Excel 재현 자료 대기는 기존 기록대로 남긴다.

## 반복 54 — 문서 읽기 화면의 공통 테마 마감 (2026-09-22)

- 최신 사용자 요청에 따라 U3a의 공통 팔레트 적용을 유지하면서 문서 작업 탭·분류·보조 패널의 형태를 주변 UI와 맞추는 작은 시각 작업을 선택했다 (§7.1/§7.3). 기존 완료 기능을 재구현하지 않고, 고정값으로 남은 패널/내부 곡률과 간격을 공통 토큰으로 연결한다. 전체 P0 보안 완료 대조는 다음 반복으로 남긴다.
- **U3f 완료**: 따뜻한 본문 바탕과 고딕체·제목 계층은 유지하고, 작업 탭은 둥근 그룹/오렌지 선택 표시, 분류는 올리브 배지, 인용·표·기여 안내는 공통 브랜드 면으로 맞춘다. 미리보기의 본문 스타일도 같은 규칙을 사용한다. 제품 수정은 `src/app.css`에 한정한다.
- 착수 확인: GOAL/README/package.json/진행 기록/git status 및 적용 AGENTS 부재 확인. 기존 수정·미추적 파일을 보존한다. 변경 전 `bun run test` **115 통과·DB 22 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`·`bun run build` **통과**. 실제 격리 PostgreSQL/Chrome의 기존 `reading-tables.mjs`도 통과했고, 외부 요청 0·DB 내용 동일을 확인했다. 기존 실패는 없다.
- 검증 계획: 변경 후 같은 자동 검사와 읽기 표/목차 브라우저 회귀, 360/768/1440px·라이트/다크·키보드·모바일·미리보기·빈/로딩/오류 상태를 확인한다. CSS만 바꾸므로 새 단위 테스트나 DB 모델 변경은 추가하지 않는다. 증거와 수정 전 사본: `/tmp/wiki-viki-reader-theme-20260922/`.

- 변경 후 자동 검사: `bun run test` **115 통과·DB 22 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`·`bun run build`·`git diff --check` **통과**. 변경 전과 같은 검사 결과이며 새 실패는 없다. CSS만 수정했으므로 전체 633개 DB 동작 검사는 반복하지 않고, 아래 실제 DB를 사용하는 읽기/미리보기 브라우저 검증으로 변경 범위를 확인했다.
- 실제 격리 PostgreSQL/Chrome: `node tests/browser/reading-tables.mjs` **12개**, `node tests/browser/reading-toc.mjs` **15개** 묶음 통과. 라이트/다크·360/768/1440px·200% 상당 화면·키보드/터치·JavaScript 없는 기본 링크, 표/각주/목차·폭 변경 초점, 미리보기·빈/지연/실제 조회 오류와 재시도를 확인했다. 데이터 스냅샷 동일, 페이지 오류·외부 HTTP **0회**. 결과는 `reading-tables/table-results.json`, `reading-toc/toc-results.json`에 있다.
- 스타일 추가 확인: 증거 폴더의 임시 `theme-gallery.mjs`로 가상 자료를 실제 격리 DB에 적재해 **320/360/768/1440px × 두 테마 8개** 묶음을 확인했다. 제목·별칭·인용·목록·코드·표·각주·보조 패널을 캡처하고 모바일 라이트/데스크톱 다크를 직접 시각 검토했다. 새 강조 면 5종의 일반 글자 대비 최솟값 **5.49:1**, 문서 작업 링크 최소 44px·키보드 초점/편집 진입, 미리보기 인용 색 일치, 가로 화면 넘침 없음·문서 데이터 보존을 확인했다. 시각 점검용 스크립트는 저장소에 새 단위 테스트로 추가하지 않았다.
- 제품 변경은 `src/app.css`, 작업 기록은 `RALPH_PROGRESS.md`뿐이다. GOAL의 기존 체크박스/본문과 기존 사용자 수정을 유지했다. 테스트 스키마 잔여 **0개** 확인 후 소유 PostgreSQL 및 테스트 Vite/Chrome을 종료했다. 실제 외부 AI·원격 DB·배포는 수행하지 않았다. **이번 테마 마감 완료, 전체 P0는 미완료**이며 W2 특정 Excel 재현 자료 대기는 기존 기록대로 남는다.
- **다음 반복: 기존 P0 마지막 기준인 차단 원문의 외부 AI 전송 방지와 검사 생략 표시를 완료 대조한다.** 현재 검사 진입점/전송 경계와 키 없는 대체 흐름·검사 장애 UI의 검증 증거를 확인하고 실제 누락이 있을 때만 작은 재현 단위를 수정한다.

## 반복 55 — 생성 주제의 JSON 이스케이프 검사 우회 차단 (2026-09-22)

- 직전 반복은 **진행**이다. 문서 테마의 실제 CSS 수정과 자동/브라우저 증거가 남았다. 이번에는 목표 원본 파일·GOAL/README/package.json/git status/진행 기록 및 적용 AGENTS 부재를 다시 확인하고, 마지막 P0 전송 전 검사 기준을 대조했다. 기존 변경은 보존한다.
- **S6 완료** (§11.3/§12 P0): 원문을 검사한 뒤 AI가 생성한 주제의 제목/범위를 본문 생성 요청으로 재전송하는 경계에서 누락을 확인했다. `JSON.stringify(topics)`에 정규식을 적용하면 값의 따옴표·줄바꿈·탭·PEM 줄바꿈이 이스케이프되어, 같은 실제 문자열에 대한 공통 검사는 차단하는 값이 통과했다. 다음 요청은 이를 실제 텍스트로 다시 전달하므로 전송 전 검사 보완이 필요하다. 일반 연락처·급여·대외비와 플레이스홀더 허용 기준 자체는 바꾸지 않는다.
- 변경 전 기본 `bun run test` **115 통과·DB 22 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`·`bun run build` **통과**. 새 단위 회귀는 **20개 모두 실패**(201 대신 422 기대): 제목/범위 × 5가지 이스케이프 사례 × 1개/32개 주제의 마지막 항목을 검증했다. 정상 업무 정보와 줄바꿈/따옴표 플레이스홀더 허용 사례는 통과했다.
- 실제 DB 재현: 격리 PostgreSQL에 기존 문서·이력·별칭·토론·초안을 넣고 같은 실패를 확인했다. 첫 실행은 새 fixture의 리비전/토론에 필수 익명 편집자를 빠뜨려 23502가 났다(`database-before.log`); fixture만 수정한 다음 5가지 사례가 모두 **201 대신 422 기대**로 실패했다(`database-before-fixed-fixture.log`, 하위 5개+상위 1개 실패). 이는 기존 검사 실패와 구분한 새 회귀 재현이다.
- 구현: `openai.js`가 검증한 주제의 실제 제목/범위를 합쳐 공통 로컬 검사를 수행한다. 전체 계획을 먼저 검사하므로 32번째 주제에 차단 값이 있어도 첫 번째 본문 생성 묶음부터 중단된다. 주제 차단 안내는 원문에서 발견한 값이라고 오인하지 않도록 생성 주제 단계와 저장하지 않았다는 결과·재시도를 알린다. `ContentBlockedError`의 기본 안내와 나머지 호출 경로는 유지한다.
- 회귀: `tests/fixtures/governance-topics.js`의 가상 값, `wikify-governance.test.js`, `relaxed-upload-db.test.js`, `tests/browser/relaxed-upload.mjs`. 기존 파일 재업로드·선택 파일/모달·키보드·기존 데이터 보존과 정상 주제 재시도, 키 없는 업로드/검토의 의미 검사 생략 표시를 확인한다. 첫 수정 후 관련 단위/DB **57 통과·생략 0**이며, 단계별 안내까지 반영한 최종 전체 검사와 브라우저 결과를 아래에 남긴다.
- 증거 및 수정 전 사본: `/tmp/wiki-viki-security-audit-20260922/`. 실제 AI 응답은 전송 경계에서 모의 처리하고 DB는 기존 격리 환경만 사용한다. GOAL 마지막 체크박스는 아직 체크하지 않는다.

- 최종 실제 DB 활성화 전체 `RUN_GOVERNANCE_DB_TESTS=1 GOVERNANCE_TEST_DATABASE_URL=<격리 로컬 URL> bun run test`: **659 통과·실패 0·생략 0** (`final-full-db.log`). 생성 주제의 따옴표/LF/CRLF/탭/PEM 검사, 마지막 주제 차단 시 본문 생성 요청 0·DB 전체 스냅샷 동일, 정상 재시도 후 32개 초안과 기존 문서/관계/초안 보존을 포함한다. 기존 원문·검색·직접 편집·초안·통합·되돌리기·토론·시드의 회귀도 함께 통과했다.
- 기본 `bun run test` **136 통과·DB 22 생략**, `bun run check` **오류 0·경고 0**, `bun run lint`·`bun run build`·`git diff --check` **통과** (`final-results.json`, `final-*.log`). 새 회귀 20개와 실제 DB 5개가 재현한 제품 결함은 해결됐고 최종 미해결 회귀는 없다.
- 실제 Chrome `node tests/browser/relaxed-upload.mjs` **14개 묶음 통과** (`browser/browser-results.json`). 기존 업로드/게시/분할 8개 및 새 주제 차단 모달 6개(360/768/1440px × 라이트/다크)를 확인했다. 처리 중 중복 제출 차단, 차단 후 선택 파일·모달·기존 초안 유지, 키보드 재시도 후 3개 초안 생성, 키 없는 업로드 결과와 상세 검토의 의미 검사 생략 표시를 검증했다. 모바일 라이트·데스크톱 다크 차단 화면과 재시도 성공 화면을 직접 확인했다. 페이지 오류·실제 외부 HTTP **0회**, AI 응답은 모의 처리했다.
- 첫 브라우저 시도는 실제 DB 행 값이 같지만 PostgreSQL의 `Result` 배열과 일반 배열의 프로토타입 차이 때문에 새 검증에서 실패했다(`browser-fixture-failure.log`). 행 목록만 일반 배열로 펼쳐 같은 값을 그대로 비교하도록 고쳤고, 기존 검증을 포함해 재실행하여 통과했다. 제품 로직이나 데이터 보존 기준은 낮추지 않았다.
- 주요 파일: `src/lib/server/openai.js`, `governance.js`, 주제 fixture 및 위키파이어 단위/DB/브라우저 검사, README/진행 기록. DB 스키마·기존 URL/내용·허용 정책은 유지했다. 테스트 스키마 잔여 **0개** 확인 후 소유 PostgreSQL과 테스트 Vite/Chrome을 종료했다. 실제 외부 AI·원격 DB·Node.js 22·배포 검증은 수행하지 않았다. GOAL 본문과 체크박스는 변경하지 않았다. **S6 완료, 전체 P0 미완료**이며 기존 W2 특정 Excel 재현 자료 대기는 별도다.
- **다음 반복: P0 전송 차단·검사 생략 기준의 경로별 최종 대조를 마친다.** 원문/생성 주제/검색 근거/직접 편집/초안 게시/통합/되돌리기/토론/시드의 실제 전송 필드, 실패 시 후속 요청 중단, 키 없는 대체 흐름과 생략 UI를 이번 전체 검사 및 해당 브라우저 근거에 연결한다. 이번 S6를 반복 구현하지 않고, 모든 필수 증거가 확보된 때만 마지막 체크박스를 변경한다.

## 반복 56 — P0 전송 차단·검사 생략의 경로별 완료 대조 (2026-09-22)

- 직전 반복은 **진행**이다. S6의 실제 전송 전 검사 수정과 회귀 증거를 현재 코드에서 확인했다. 목표 원본 파일·GOAL/README/package.json/git status/진행 기록과 적용 AGENTS 부재를 다시 확인하고, 마지막 P0 기준만 대조했다. 기존 사용자/이전 변경을 보존하며 P1로 확장하지 않는다.
- **S7 완료** (§6/§11.3/§12 P0): 원문/생성 주제/검색 근거/직접 편집/되돌리기/토론/초안 수정·개별/선택 게시/통합 생성·적용/파일 시드의 실제 전송 필드와 검사 순서, 거부/장애 이후 호출·저장 중단, 키 없는 대체 흐름·생략 표시를 코드와 회귀에 연결했다. 상세 근거는 `docs/p0-content-governance-audit-2026-09-22.md`에 남겼다. 이미 구현·검증된 제품 로직을 근거 없이 다시 바꾸지 않았다.
- 변경 전 현재 제품 코드의 실제 격리 DB 전체 **659 통과·생략 0**, 기본 검사 **136 통과·DB 22 생략**, check **오류 0·경고 0**, lint/build **통과**. 기존 실패 없음 (`/tmp/wiki-viki-p0-governance-final-20260922/baseline-*.log`). 이번에는 제품 동작 변경 없이 실제 브라우저 회귀와 완료 문서를 추가했다. 제품 코드가 그대로인 상태에서 전체 DB/단위·타입/빌드 검사를 불필요하게 반복하지 않고 새 브라우저 검사와 최종 문서/형식 검사를 실행한다.
- `tests/browser/governance-status.mjs`를 추가했다. 실제 서버/격리 DB/Chrome에서 생략 안내 5개 화면 × 주요 폭/테마·기본 HTML 7개 묶음, 키 없는 선택 게시/저장 통합안 수동 적용 4개 묶음, 로컬 거부/의미 거부/서비스 장애/정상 재시도 2개 묶음으로 총 **13개**를 확인한다. 조회는 AI 전송·DB 변경 0, 키 설정만으로 저장된 초안을 검사 완료로 바꾸지 않으며 명시적 저장 뒤 실제 완료 결과만 기록한다. 토론의 거부/장애 시 입력·DB를 보존하고 재시도 후 한 번만 저장한다.
- 초기 9개 묶음과 선택 게시/통합을 추가한 13개 묶음 모두 통과했다. 초안 상세와 목록의 캡처 이름이 겹치는 것을 발견해 파일명을 구분했고 최종 캡처를 다시 생성한다. 제품/검증 기대를 바꾸거나 실패한 검사를 없애지 않았다. 실제 외부 AI는 모의 응답과 구분하며 원격 DB와 운영 데이터는 사용하지 않았다.
- 경로 전체의 구현과 필수 자동/실제 DB/브라우저 증거를 확인하여 GOAL §12의 마지막 **전송 차단·검사 생략** 체크박스만 체크했다. 다른 본문/체크박스는 보존했다. 제품 P0 기준과 사용자 신고의 특정 원본 Excel 재현 여부는 별도다.
- **남은 사용자 요청 W2: 구현 완료·검증 대기.** 일반 XLSX 추출/저장 및 확인된 오류 응답/모달 상태 소실은 이전에 재현·수정·검증했다. 당시 문제 파일의 확장자·오류 문구·동일 조건 재현 자료가 없어 특정 실패 해결 여부는 아직 확인할 수 없다. 이번에 확장자와 오류 문구를 요청했으며, 답변 없이 임의의 파일 문제를 추측해 파서·정책을 바꾸지 않는다. 같은 외부 조건은 반복 54·55·56에 연속으로 남아 있다. 현재 독립적으로 진행할 P0 기능 구현/필수 검증은 완료됐으므로 P1을 시작하지 않는다.
- **다음 작업 하나: 사용자가 제공한 Excel 확장자·오류 문구(추출 문제면 같은 조건의 비식별 재현 자료)로 W2의 남은 사례를 재현하고, 해당 실패 경로만 수정·검증한다.** 이 조건이 해소되기 전에는 전체 요청 완료 문자열을 출력하지 않는다.
- 최종 `governance-status.mjs` 재실행도 **13개 묶음 통과**했다(`browser-final.log`, `browser/results.json`). 상세/목록 캡처는 이제 별개 파일이며 모바일 라이트 편집·데스크톱 다크 이력의 실제 생략 안내를 직접 시각 확인했다. 페이지 오류·실제 외부 HTTP **0회**. 새 브라우저 검증과 형식 검사에 실패한 항목은 없다.
- 최종 `bun run lint`·`git diff --check` **통과**. GOAL 수정 전 사본과 비교해 마지막 P0 체크박스 한 줄만 바뀐 것을 확인했다. 새 주요 파일은 `tests/browser/governance-status.mjs`, `docs/p0-content-governance-audit-2026-09-22.md`이며 README에 재실행 방법을 추가했다. DB 스키마 잔여 **0개**, 브라우저/Vite 종료 확인 후 소유 PostgreSQL을 종료했다.
- W2의 재개 정보 부재는 반복 54·55·56에서 연속 유지됐고, 이 조건과 독립적인 P0 작업/검증은 모두 마쳤다. 현재 추가로 실행할 근거 있는 수정은 없어 **W2 차단(구현 완료·원래 실패 사례 검증 대기)**으로 기록한다. 사용자에게 확장자·오류 문구를 요청한 상태이며, 전체 목표도 같은 조건의 입력을 기다리는 차단 상태로 전환한다. P0의 체크박스 완료를 특정 Excel 사례까지 해결했다는 주장으로 확대하지 않는다.
