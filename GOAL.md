# Wiki Viki — 프로젝트 목표

> 현재 README와 구현을 바탕으로 정리한 개발 방향 문서입니다. 아래 완료 기준은 검증할 항목이며, 모두 검증 완료되었다는 의미는 아닙니다.

## 목표

흩어진 업무 문서와 사내 지식을 검색하고 연결하며 함께 개선할 수 있는 한국어 사내 위키를 만든다. DOCX·PDF·XLSX·PPTX 문서를 AI로 구조화해 초안 작성 부담을 줄이고, 사람이 내용을 검토한 뒤 게시하도록 한다.

핵심 사용자 흐름은 **문서 업로드 → 텍스트 추출 → 콘텐츠 검사 및 초안 생성 → 담당자 검토·수정 → 게시 → 공동 편집·토론**이다.

## 사용자와 제공 가치

- 구성원: 정책, 시스템, 용어, 업무 방식을 검색하고 관련 문서를 탐색한다.
- 편집자: 기존 문서를 위키로 전환하거나 직접 작성하고, 변경 이유와 토론을 남긴다.
- 운영 담당자: 초안의 정확성과 게시 적합성을 확인하고, 변경 이력을 확인하거나 이전 내용으로 되돌린다.

이 구분은 사용 목적을 설명한다. 현재 코드에 인증 기반 역할이나 접근 권한 구분이 구현되어 있지는 않다.

## 핵심 범위

### 1. 문서 탐색과 읽기

- 제목, 본문, 별칭으로 문서를 검색한다.
- Markdown 본문, 자동 목차, 표, 각주, 위키 링크를 제공한다.
- `[[문서]]`와 `[[문서|표시 이름]]`으로 지식을 연결한다.
- 별칭은 원래 문서로 연결하고, 존재하지 않는 문서 링크는 구분해 새 문서 작성으로 이어지게 한다.
- 대문과 사이드바에서 공지, 최근 토론, 최근 변경을 확인한다.

### 2. 공동 작성과 변경 관리

- 문서를 직접 생성·편집하고 별칭을 등록한다.
- 저장할 때 본문 리비전과 편집자, 변경 요약을 기록한다.
- 문서별 이력, 변경 비교, 이전 본문으로 되돌리기를 제공한다.
- 문서별 토론으로 수정 제안과 의견을 기록한다.
- 편집과 토론에는 `Editor-01`, `Operator-A` 형식의 익명 식별자를 사용한다.

### 3. AI 문서 전환과 초안 검토

- 최대 10MB의 DOCX, 텍스트 기반 PDF, Excel(.xlsx), PowerPoint(.pptx)를 업로드한다.
- 추출한 원문을 최대 8개의 독립적인 용어별 초안으로 나누고, 한국어 제목·섹션·관련 링크·별칭을 제안한다.
- 원문에 없는 사실을 추가하지 않으며, 불확실한 내용은 `[검토 필요]`로 표시하도록 AI에 지시한다.
- 생성 결과는 초안으로 저장한다. 사용자가 검토·수정하고 명시적으로 게시한다.
- 초안 상태는 검토 대기(`review`), 게시 차단(`blocked`), 게시 완료(`published`)로 관리한다.
- API 키가 없어도 추출한 텍스트로 기본 초안을 만들 수 있도록 한다. 이때 의미 기반 검사는 생략된다.

### 4. 콘텐츠 보호

- 개인정보, 개인 연락처, 인사·급여 정보, 기밀 자료의 게시를 줄이는 것을 목표로 한다.
- 정규식·키워드 검사와 AI 의미 기반 검사를 사용하고, 차단 이유를 검토할 수 있게 한다.
- 업로드에서 생성한 초안은 게시 시 다시 검사하며, 검사에 실패하면 게시를 차단한다.
- 자동 검사만으로 정확성이나 게시 적합성을 보장하지 않는다. AI 초안은 사람의 검토를 전제로 한다.

## 구현 원칙

- 문서를 읽고 찾는 경험을 중심에 두고, 한국어 안내와 일관된 위키 탐색 구조를 유지한다.
- AI는 원문 구조화와 검토 보조에 사용한다. 생성 결과를 자동 게시하지 않는다.
- 문서 저장과 리비전 기록의 일관성을 유지한다.
- API 키와 DB 연결 정보는 서버 환경 변수로 관리한다.
- 기존 SvelteKit·JavaScript 구조를 유지하고, 기능에 필요한 범위에서 변경한다.
- 현재 구현, 검증된 동작, 향후 개선 목표를 구분해 기록한다.

## 기술 및 운영 기준

- UI와 서버: SvelteKit, Svelte 5, JavaScript
- 개발 및 스크립트 실행: Bun
- 데이터 저장: PostgreSQL / Neon
- AI 연동: OpenAI Responses API, 환경 변수로 모델 선택
- 문서 처리: DOCX는 Mammoth, PDF는 pdf-parse, XLSX는 read-excel-file, PPTX는 JSZip 및 XML 파서
- 배포: Vercel, 서버 런타임 Node.js 22
- 초기화: 마이그레이션과 시드 스크립트로 정책·도움말 및 검토용 초안 준비

실행 명령과 환경 설정의 상세 절차는 [README.md](./README.md)를 따른다.

## MVP 완료 기준

- [ ] DB 설정 후 마이그레이션과 시드를 실행하고 기본 정책·도움말 문서를 열 수 있다.
- [ ] 문서를 생성·편집하고 제목·본문·별칭 검색으로 다시 찾을 수 있다.
- [ ] 목차, 위키 링크, 별칭 연결, 미작성 문서 링크가 의도대로 동작한다.
- [ ] DOCX와 텍스트 PDF를 업로드해 검토용 초안을 생성할 수 있다.
- [ ] API 키가 없는 환경에서도 기본 초안을 만들고 검토·게시 흐름을 진행할 수 있다.
- [ ] 초안 수정 후 검사 결과를 확인하고, 검사에 실패한 초안은 게시할 수 없다.
- [ ] 검토한 초안을 게시하면 위키 문서와 최초 리비전이 생성된다.
- [ ] 문서 편집 이력과 변경 비교를 확인하고 이전 본문으로 되돌릴 수 있다.
- [ ] 익명 식별자 검증과 문서별 토론 등록이 동작한다.
- [ ] 지원하지 않는 파일, 용량 초과, 텍스트 추출 실패를 사용자에게 안내한다.
- [ ] `bun run check`와 `bun run build`가 통과한다.

## 현재 한계와 후속 검토 사항

- 익명 식별자는 입력 형식 검사이며 로그인이나 권한 검증이 아니다. 실제 사내 운영에 필요한 인증과 접근 통제는 별도 설계가 필요하다.
- 검사 적용 범위가 경로마다 다르다. 초안에는 정규식·의미 기반 검사를 사용하지만, 직접 편집과 토론은 정규식 검사만 수행하고, 되돌리기는 콘텐츠를 재검사하지 않는다. 시드도 별도의 간소화된 검사를 사용한다.
- API 키가 없을 때 의미 기반 검사는 생략되므로, 두 검사를 모두 통과한 상태와 구분해야 한다.
- 현재 콘텐츠 검사는 외부 AI 전송 전에 민감 정보를 완전히 차단하는 구조가 아니다. 외부 전송 정책이 필요한 운영 환경에서는 전송 전 검사 범위를 별도로 정해야 한다.
- 리비전과 되돌리기는 본문 중심이다. 제목·별칭을 포함한 문서 전체 상태 복원은 현재 범위에 포함되지 않는다.

## 현재 범위 밖

- 스캔 PDF나 이미지 OCR, 원본 문서의 시각적 레이아웃 복원
- 실시간 동시 편집과 자동 충돌 해결
- 벡터 검색, 대화형 챗봇
- SSO, 조직별 권한, 다중 조직 지원
- 사람의 검토 없는 AI 자동 게시

새 기능은 먼저 문서 탐색, 초안 검토, 변경 추적이라는 핵심 흐름에 기여하는지 판단하고 추가한다.

## 프로토타입 반영 추가 범위

- 검색 결과의 여러 게시 문서를 바탕으로 AI 설명과 문단별 원문 출처를 표시한다. 기존 키워드 검색과 OpenAI Responses API를 사용하며 벡터 저장소를 추가하지 않는다.
- 검색 우선·활동 대시보드·지식 지도 보기를 제공한다. 문서·리비전·초안 수와 위키 링크는 실제 저장된 데이터에서 계산한다.
- AI가 응답하지 못하거나 근거가 부족한 경우 해당 상태를 명시하고 원문 검색 결과를 유지한다.
